terrain-architect-workspace/
__pycache__/
*.py[cod]

# Agent worktrees. The harness checks a full copy of the repo out under here
# when a subagent runs with worktree isolation; committing it would nest the
# repository inside itself.
.claude/worktrees/
