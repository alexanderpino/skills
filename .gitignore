terrain-architect-workspace/
__pycache__/
*.py[cod]
gauntlet/.gates.lock
gauntlet-loop-workspace/

# Renderer outputs. `water-physics/reference-impl/render.py` writes its frames
# beside itself and `beach_render.py` writes into `evidence/`; both are
# regenerated by running the file and neither is source. A `git add -A` once
# committed 9 MB of them into the wrong skill.
water-physics/reference-impl/pool_*.png
