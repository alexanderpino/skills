{
  "name": "@skills/terrain-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Terrain Studio — node-graph WebGL terrain generator. Source for the installable PWA; the skill ships a generated single-file build.",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "vite build",
    "bridge:surface": "node tests/legacy/_verify_bridge.js",
    "bridge:check": "node tests/legacy/_verify_bridge.js --check",
    "bridge:gen": "node scripts/generate-bridge.mjs",
    "verify": "node scripts/run-legacy-verify.mjs",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "vite": "^7.1.5"
  }
}
