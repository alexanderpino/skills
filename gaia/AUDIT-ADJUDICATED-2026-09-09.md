# Adjudicated audit findings — 2026-09-09

Supersedes the unverified state of `AUDIT-UNREFUTED-2026-09-09.md` for the 62 findings below.
Every one was attacked by an independent refuter told to KILL it and to default to refuted when
uncertain; every refutation was then checked by a second independent adjudicator.

| verdict | count |
|---|---|
| DEFECT_CONFIRMED | 25 |
| DEFECT_PARTIAL (substance holds, the finder overreached) | 35 |
| DEFECT_REFUTED | 2 |
| blocked / COULD_NOT_ASSESS | 0 |

**Read the refutation rate honestly.** Only 2 of 62 were killed outright — far below this corpus's
1-in-3 base rate. But 35 came back PARTIAL, meaning the substance held while the finder's wording,
figure or scope did not. Counting those, **37 of 62 (60%) needed correction before they could be
used**, which is consistent with the base rate in kind if not in verdict. A reader who takes the
25 clean confirmations as the whole story will overstate what was found.

Refuters also reported `blastRadius` — every other site in the corpus repeating the same claim —
so a confirmed defect arrives knowing all its ends.

---

## Refuted — the finding was wrong

### impact-craters.md · `IC-2`

IC-2 claims the sentence at impact-craters.md:94-97 is unsupported on two grounds: (a) the verbatim quotation "visually the inflection seems closer to 20 km" is attributed to Pike but carried by no locator, and (b) "the paper says so three separate times" overcounts because the 30.4 km rim-flank-width crossing is "this corpus's own numerical solve ... not a figure Pike prints". Both grounds are fa

### virtual-texturing.md · `VT-3`

VT-3 is a provenance finding: it asserts the bias rule at virtual-texturing.md:116-120 is unsourced, that its [barrett2008] citation cannot carry it, and that the owner should therefore carry shader-craft.md:228's "Derived here, not sourced" flag.

Routes I tried, and what happened to each:

(1) "The finder cut off a qualification." CLOSED — held for the finding. I read :113-125 whole and grepped

---

## Survivors — confirmed or narrowed

### coastal-erosion.md · `CE-1` · `wrong` · gaia/references/coastal-erosion.md:302

**Narrowest true statement.** At coastal-erosion.md:300-302 the document names wave-models.md's shore-band machinery — "the travel-time/eikonal field whose iso-lines are refracted wavefronts, and the `H ≈ 0.78·h` breaking criterion" — and then says "sample the shore-normal and the local wave direction out of it and feed `ψ`". `ψ` in eq. (7)/(8) is the DEEPWATER angle (:202-203 "the useful version is the deepwater recast"; :252 "Differentiate the deepwater transport"; :264-265; :388), and the 42.392° threshold exists only for that deepwater cos^1.2·sin form (the breaking-angle CERC form peaks at 45°, :201). A direction sampled out of that eikonal field is by construction refracted: with wave-models.md:278's own speed c(h)=sqrt(g·h) and the document's T = 10 s, the sampled angle obeys Snell, sin ψ = sin ψ0 · c(h)/c0 with c0 = 15.613 m/s, so it is capped at arcsin(sqrt(g·h)/c0) — 39.374° at the document's own 10 m shoreface (:225) for ANY deepwater angle up to 90°, and → 0 at the sea-level contour that :352 says the shore normal comes from. The cap first reaches 42.392° only at h = 11.295 m. So a pipeline built as :302 prescribes returns μ > 0 at every shoreline cell for every wave climate: a genuinely unstable deepwater ψ0 = 60° reads 33.325° at h = 10 m (μ angle factor +0.324), 25.186° at h = 6 m (+0.590), 11.344° at the H0=1 m breaking depth (+0.911). That is the document's own failure rows :386 and :388 produced by its own constructive instruction, and it contradicts :388's fix line. The proposed fix (state at :302, and in the :352 row, that ψ is built from the deepwater direction differenced against the local shore normal, not from the refracted direction sampled out of the travel-time field) stands. WHAT FAILS IN THE FINDING AS WRITTEN, all minor: (1) its 28.13° at h* = 6 m, 13.00° at breaking depth and "ψ0 = 60° arrives as 24.1°" are full-linear-dispersion values, not values of the c(h)=sqrt(g·h) the finding itself cites — under the document's prescribed speed they are 29.432°, 13.129° and 25.186°; (2) "μ ∝ cos²−1.2sin² = +0.632, near-maximum smoothing" — the bracket is 0.633 and the full angle factor cos^0.2ψ·(cos²ψ−1.2sin²ψ) is 0.622, i.e. 62% of its ψ=0 maximum, not near-maximum; (3) "pins μ > 0 at every cell forever" needs the depth qualification the wording of :302/:352 supplies but the finding leaves implicit — it is true for samples at or inside the document's own shoreface (h ≤ 11.295 m at T = 10 s; h_crit ∝ T²), and false for a sample taken at Bruun's 18 m closure depth, where a 60° deepwater wave still reads 47.483° and μ stays negative.

**Blast radius.** One site only. The instruction to source `ψ` from the travel-time/eikonal field exists at gaia/references/coastal-erosion.md:300-302 and nowhere else in the corpus. Its companion row gaia/references/coastal-erosion.md:352 ("Wave angle `ψ` | per-shoreline-cell | shore normal from the contour, wave direction from `wave-models.md`") is in the same file and is neutral rather than wrong — "wave direction from wave-models.md" can be read as the deep-water directional field — so a correction landing only at :302 would leave :352 ambiguous but not false; the narrowest complete fix touches both. grep across the tree: `grep -rn "42.392\|deepwater angle" --include=*.md .` returns only coastal-erosion.md (:4, :11, :23, :34, :250, :268, :271, :273, :277, :282, :369, :387, :388), papers-coastal-and-ice.md:67-70,:135 (bibliography, states the 42° deepwater / 45° breaking split correctly), registers/pseudocode-execution.tsv:190,:204 (closed-form re-derivation only, no ψ-sourcing claim), and index.md:24 (description string). `grep -rn "eikonal\|travel-time" .` hits gaia/references/wave-models.md:22,:277-279,:295 — the source of the field, which makes no claim about `ψ` or about coastal transport — plus obsolete/terrain-renderer/references/12-water-rendering.md (:408, :432-434, :464, :550) and 14-auxiliary-maps-runtime.md:63, which are rendering-only phase fields with no transport formula. The only other CERC site, obsolete/terrain-architect/references/12-glacial-coastal.md:785, uses the BREAKING-angle form `Q_long ∝ sin(2·(waveAngle − shorelineNormal))` "peaks near 45° approach", for which a refracted direction is the correct input, so it does not repeat the defect. `grep -rn "shore-normal\|shore normal"` adds no further site.

### coastal-erosion.md · `CE-2` · `unsupported` · gaia/references/coastal-erosion.md

**Narrowest true statement.** gaia/references/coastal-erosion.md states no timestep stability condition anywhere (zero matches for Δt / timestep / CFL / Courant / von Neumann / stab* / explicit / implicit across all 392 lines), while every other time-stepped-solve document in the corpus states its own: stream-power.md:149-152 (`D·dt/Δx² ≤ 0.25`, `Δt ≤ Δx²/(4D)`, 0.9 safety factor `c = 0.225` at :157), shallow-water.md:124-135 (`dt <= C*dx/(max|u|+sqrt(g*h_max))`, "Positivity is not stability" at :108), hydraulic-erosion.md:264 (Mei's `Δt·|v| < cellSize`), and thermal-and-aeolian-erosion.md:82 (the `c` margin, explicitly "a stability margin, not an oscillation cure"). Consequence, which is the defect: the :387 failure row "Grid-scale noise on the shoreline explodes" assigns the symptom to one mechanism only — "Negative `μ` with no regularisation" — but the identical symptom is produced at STRICTLY POSITIVE μ by an over-long explicit step, and the row's two prescribed fixes ("wave shadowing", "clamp `ψ` below 42.392°") bound nothing about Δt. Under the document's own numbers — μ = 0.053886 m²/s at ψ = 0 (:226) and its own finest listed cell Δx = 2.441 m (:130) — the 1-D FTCS bound is Δt ≤ Δx²/(2μ) = 55.31 s (1382.6 s ≈ 23 min at the 50 km row's 12.207 m). The clamp is worse than inert here: the document's worked μ is taken at ψ = 0, already far below 42.392°, so clamping ψ is a no-op in exactly the case the document computes.

Overreach that does NOT survive, and is why this is PARTIAL rather than CONFIRMED: (1) the finding's premise "The document prescribes running `dy/dt = μ·d²y/dx²` explicitly on a grid" is not what the page says — it prescribes "run one-line diffusion along it" (:33-34) and writes the PDE (:190), and its :387 row presupposes a grid, but it prescribes no discretisation at all; an implicit or spectral implementation (which the document's own mode solution τ = L²/(4π²·μ) at :223 would directly support) is unconditionally stable and never meets this bound. The defect is therefore conditional on the explicit scheme being taken as the default — which the corpus's sibling does take, but the page does not say. (2) "The document's own e-folding table invites steps far larger" is the finder's inference about reader behaviour, not a claim on the page; the table at :228-233 lists decay times, never a timestep.

**Blast radius.** One site only. `grep -rniE "0\.0539|Grid-scale noise|42\.392|backward diffusion|clamp .psi|wave shadow"` over the corpus: μ = 0.0539 appears only at coastal-erosion.md:226 and, as a reproduced value with no timestep claim, at registers/pseudocode-execution.tsv:190; the ":387" row text appears only at coastal-erosion.md:387; 42.392° at coastal-erosion.md:271, :369, :387 and papers-coastal-and-ice.md:70 (a source note, no timestep claim). The cell sizes 2.44 m / 12.2 m / "4096 cells" appear only in coastal-erosion.md:128-133 (hydraulic-erosion.md:73-77 and noise-and-warping.md:138 use 4096 cells for unrelated quantities). No other document repeats the mechanism attribution or would need the same correction. Corroborating context, not a repeat: registers/pseudocode-execution.tsv:190 closes with "n/a -- coastal-erosion.md's five fences are closed forms", i.e. no coastal operator was ever executed as a time-stepped solve anywhere in the registers, which is consistent with the bound never having been derived. A correction must land in two places in the one file — a stability statement in §"Plan view" (near :190) and a split of the :387 row into over-long explicit step at positive μ / unregularised backward diffusion at negative μ; landing it in only one of the two is the failure mode to avoid.

### coastal-erosion.md · `CE-3` · `wrong` · gaia/references/coastal-erosion.md:164

**Narrowest true statement.** coastal-erosion.md:164's bolded "**Nearly all of the answer is `h*`**" is false under the document's own equation. With R = S·(h*/A)^(3/2)/(h*+B), ∂lnR/∂lnA = −3/2 exactly, while ∂lnR/∂lnh* = (h*+3B)/(2(h*+B)), which is 0.833 at h*=4 m and 0.600 at h*=18 m (B=2 m) — so R is 1.8×–2.5× more sensitive to A than to h* everywhere in the document's own published h* range, and 3/2 − ∂lnR/∂lnh* = h*/(h*+B) > 0 for every h*>0, B>0, i.e. there is NO admissible input under which h* is the more influential parameter. Over ranges: h* 4→18 m gives 2.864×, A 0.10→0.20 (Dean's anchors, :96-99) gives 2.828×, and A 0.05→0.25 (the slider range the same document prescribes at :103) gives 11.180× — to match that with h* alone you would need h* to run 4 m→226 m. The defect is one sentence's attribution of the retreat number's spread, echoed one-sidedly in the failure row at :385 ("Sea-level retreat numbers feel arbitrary | They are — `h*` is a convention"). It is NOT a claim that causes A to be hard-coded: the same document orders A exposed at :103 and lists "`A` not exposed, or exposed and ignored" as a failure at :380, and concedes the point in its own next paragraph at :174-175.

**Blast radius.** The dominance claim itself ("Nearly all of the answer is `h*`") is ONE SITE ONLY: gaia/references/coastal-erosion.md:164. `grep -rn "Nearly all of the answer" /home/user/skills` returns that line and the audit file's quotation of it; `grep -n "dominat|most of the|[Nn]early all|carries"` over the whole document returns no other dominance phrasing (:235 is an unrelated wave-angle sentence).

Two related sites carry the same one-sided attribution in weaker form and would need touching if :164 is corrected:
- coastal-erosion.md:385, failure row "| Sea-level retreat numbers feel arbitrary | They are — `h*` is a convention | 4 m to 18 m of assumed closure depth spans 2.86× in retreat [cooper2004] |" — numerically TRUE (I reproduce 2.8638×) but it names h* as the sole reason the retreat number is arbitrary, while the A slider the same document prescribes at :103 spans 11.18×. A correction landing at :164 only would leave this end saying the same thing.
- gaia/references/coastal-erosion.md:173 "A **2.86× spread** from one unmeasured parameter" — correct as arithmetic, but "one unmeasured parameter" is the same framing.

Sites that repeat the MAGNITUDES and are correct, needing no change:
- gaia/registers/pseudocode-execution.tsv:190 — "Bruun retreat spanning 12.65-36.22 m (2.86x) across Cooper & Pilkey's own 4-18 m closure-depth span", "W* ratio for halving A exactly 2^(3/2) = 2.8284", "Bruun amplification R/S = 71.55 at h* = 8 m". All three reproduce (12.6491/36.2243, 2.8638×, 2.8284, 71.554). No dominance claim.
- gaia/references/papers-coastal-and-ice.md:130 — records the 18 m / 4 m closure depths from cooper2004 p. 159. Source-side only, no sensitivity claim.
- gaia/references/coastal-erosion.md:10 (frontmatter locator) — same 4 m / 18 m record.
- obsolete/terrain-architect/references/12-glacial-coastal.md:351-352 cites Bruun 1962 for the retreat rule but makes no h*-sensitivity or closure-depth claim (grep for "closure|h\*|nearly all|convention" there returns only unrelated Glen-law/CERC-closure hits).
Unrelated coincidental hits on the magnitude 2.86: driver-fields.md:384 (2.86° horizon angle) and stratigraphy-and-lithology.md:161 (2.86 cells) — different quantities, not this claim.

### coastal-erosion.md · `CE-4` · `unsupported` · gaia/references/coastal-erosion.md

**Narrowest true statement.** coastal-erosion.md:213-214 instructs the reader to resolve the H_rms/H_s ambiguity at `wave-models.md` ("owns which height your spectrum produces — go and check before wiring this up"), but wave-models.md states no elevation-height convention anywhere: it never names H_s, H_rms or H_1/3, never gives H_s = 4√m₀, and its spectrum section (:173-177) names Pierson-Moskowitz/JONSWAP/Phillips and stops at "drive them from wind speed and fetch". Its only heights are the breaking criterion H ≈ 0.78h (:288, itself unqualified) and Green's-law amplitude (:282); all 11 "variance" mentions are SLOPE variance / mss, never surface-elevation variance m₀. So the imperative half of the sentence cannot be executed — the check it orders does not exist at its target, a gap the corpus records independently at coverage.md:77 ("`wave-models.md` names the family and stops... no document states the check") and schedules at AUDIT-2026-09-05.md:1795 item 22. The ownership half is NOT defective: wave-models.md:29-32 declares itself owner of "the spectrum and slope statistics", so the locator names the right document, not the wrong one.

**Blast radius.** The defective pointer itself: ONE SITE ONLY — gaia/references/coastal-erosion.md:213-214. No other document tells a reader to get a wave-height convention from wave-models.md (grep -rn "wave-models.md" gaia/references/ returns 20+ cross-references; the other coastal ones, :300 and :352, ask it for the eikonal/travel-time machinery and wave DIRECTION, both of which it genuinely carries at :277-282). Same magnitudes repeated, all CORRECT and needing no change: coastal-erosion.md:11 (frontmatter locator, "K_2 = 0.34 ... for r.m.s. wave height and 0.15 for significant height"), :207-208, :211-212, :383; papers-coastal-and-ice.md:135 (bibliography entry, same pair). Counterpart sites that MUST move if the fix lands in wave-models.md rather than in coastal-erosion.md — this corpus's recorded one-end-only failure mode: gaia/references/coverage.md:77 (the `wave-spectra` `planned` row, whose whole text is the record of this absence), gaia/PLAN.md:221 and :235, and gaia/AUDIT-2026-09-05.md:707, :928, :1126, :1795 (item 22, "add the spectrum block ... and H_s = 4√m₀" against references/wave-models.md:161-167,36). Outside the gaia corpus, the predecessor skill does define the term — obsolete/terrain-renderer/references/12-water-rendering.md:96 and :1550, "Significant wave height `H_s` (the mean of the highest third...)" — so the definition existed in the corpus's own lineage and was lost in the rewrite; it is not reachable from a gaia reader and does not close the gap.

### coastal-erosion.md · `CE-5` · `unsupported` · gaia/references/coastal-erosion.md

**Narrowest true statement.** The exclusivity half of coastal-erosion.md:16-20 is false, and only that half. "Everything else in this skill's erosion axis **roughens**" and "The coast is the corpus's **one** process whose governing equation is the **heat equation**" hedge only thermal relaxation (correctly excluded: it is slope-limited, not linear diffusion). They omit `D·∇²h` [culling1960], which sits inside the corpus's fluvial governing equation `∂h/∂t = U − K·A^m·S^n + D·∇²h` (stream-power.md:35), is "always on" / "not optional" (stream-power.md:139-148, :263), and exists purely to smooth (stream-power.md:275 "Knife-edge interfluves ... The diffusion term omitted"; thermal-and-aeolian-erosion.md:161-166 "it drives faces to planar repose where `D·∇²h` makes hilltops convex"). The corpus writes that term standalone as a governing equation — sketch-based-authoring.md:111 "Under `∂h/∂t = D∇²h`, a Gaussian ridge of half-width σ ..." and :145 — i.e. the heat equation, verbatim, in the erosion axis, obeying the same Fourier-mode decay the coastal page uses. So "one" should be "one of two", and "everything else roughens" is untrue of the fluvial solve as this corpus prescribes it.

What does NOT fail, and where the finder overreached: (a) the sentence's own em-dash gloss — "the one-line shoreline model is a diffusion equation, literally" — is correct, and under the strictest reading of "governing equation IS the heat equation" stream power's full equation (uplift + incision + diffusion) is not the heat equation, so the claim is loose framing plus a wrong exclusivity count, not a false statement about the coastal model; (b) the finder's failure scenario ("a reader who accepts the frame builds the fluvial solve as pure stream power ... and gets stream-power.md:281's symptom") is not constructible — nobody takes the fluvial governing equation from this page, and the page that owns it prints `D·∇²h` in its one-line "Use this" summary, gives it a section titled "The diffusion term is not optional", repeats "not optional" at :263, and lists its omission twice in its own failure table (:275, :283) with exactly the symptom cited. The correct remedy is the finder's own proposed fix (narrow "one process whose governing equation is the heat equation" to "the only process whose ENTIRE governing equation is a diffusion equation", and name `D·∇²h` as the other heat-equation term in the erosion axis, which smooths too) — but as a wording/completeness repair, not as a guard against a buildable failure.

**Blast radius.** The exclusivity sentence itself is one site only: `grep -rn "one process\|only process\|corpus's one" --include=*.md gaia/` returns coastal-erosion.md:18 and nothing else in the technique corpus (the other two hits are gaia/PLAN.md:63 and :257, unrelated subjects). But the SAME FRAME — fluvial = purely roughening/advective, coastal = the smoothing one — recurs twice more inside the same file and must be corrected with it, or the fix lands at one end only: coastal-erosion.md:242-244 "**This is the exact opposite of the fluvial axis, and the contrast is the point.** `stream-power.md` describes an *advective* process: knickpoints propagate upstream, a signal travels and is preserved, and small features are created rather than destroyed" — flatly contradicted by sketch-based-authoring.md:139 ("why erosion eats drawn creases and spares drawn mountains"), which is derived from and measured against the always-on `D∇²h`; and coastal-erosion.md:373 (failure/decision table row) "A river mouth | ... | Advective process meeting a diffusive one; see `stream-power.md`". The description in the frontmatter (:4) and the index row (index.md:24) say only "the one-line shoreline model as a literal diffusion equation" — true as written, needs no change. Counter-evidence sites that the correction must stay consistent with (none of them wrong, all of them repeating that `D·∇²h` is present and smoothing): stream-power.md:35, :139-148, :169-175, :263, :275, :283; sketch-based-authoring.md:111, :145; papers-generation.md:323; resolution-independence.md:304, :440; stratigraphy-and-lithology.md:234; thermal-and-aeolian-erosion.md:161-166, :291.

### coastal-erosion.md · `CE-6` · `incomplete` · gaia/references/coastal-erosion.md

**Narrowest true statement.** Narrowest true defect: the magnitude the ⚠️ quotes is off-scale for every grid the document targets, and the heightfield consequence it draws from it is not produced on any of them. At :119-120 the gradient 0.31 ("one in 3.2 — which is a cliff, not a beach face") is the continuum dh/dx at x = 1 cm, i.e. 244× finer than the finest cell the document's own resolution table admits (:128-133, 10 km / 4096 = 2.4414 m). What a node-sampled heightfield actually draws in the first cell is the mean slope A·Δx^(−1/3) = 0.0743, 0.0434, 0.0274, 0.0202 (1 in 13.5, 23.0, 36.6, 49.6; steepest 4.25°) at the four listed cell sizes, and the first cell is only 1.70× the second at ANY Δx. Gradient 0.31 needs Δx = 3.36 cm — a 137 m domain across 4096 cells — which is outside the 10-500 km regime of :128-133 and the "domain under ~50 km" rule at :364. So the final sentence of the ⚠️ ("The pure power law drawn straight into a heightfield puts a step at the shoreline, and it is the first thing anyone notices") and the :379 failure row ("A step or wall at the waterline | ... drawn to x = 0, where its slope is infinite") name a symptom the document's own grids do not produce; the row is better restated as the land/sea slope break, which is what does make a wall in practice.

Three parts of the finder's wording fail and must not be carried into a fix:
(1) "only 1.65× the profile's own mean slope of 1 in 22 quoted at :114" — mis-paired profiles. 1 in 22.36 is the mean of the A = 0.20, h* = 4 m case; the profile actually gridded in the resolution table is A = 0.10, h* = 6 m, whose mean is 1 in 77.46, so the first cell is 5.75× its own profile's mean, not 1.65×. The corroboration the finder draws from that number points the other way.
(2) "gentler than a real coarse-sand beach face" — 1 in 13.5 (4.25°) sits inside the usual coarse-sand beach-face range and is steeper than the document's own coarse-sand shoreface mean of 1 in 22; nothing in the document establishes it as gentler.
(3) "the ⚠️ and the :379 failure row are inert" over-reaches onto the analytic half of the ⚠️, which is correct and independently corroborated (papers-coastal-and-ice.md:129: "Eq. (8) p. 58 is the modified profile that adds gravity and so has a finite, planar beach face near the waterline instead of eq. (1)'s infinite slope"). The prescribed remedy is also close to harmless at the document's scales: a linear splice over the first three cells at Δx = 2.44 m costs ≤ 5.6 cm of depth error across 7.3 m of a 465 m shoreface, so "replacing a correct profile with an arbitrary one" is true but consequence-free. The defect is a mis-scaled illustrative magnitude plus a failure row that mis-attributes its symptom — not bad advice.

**Blast radius.** Effectively one site, but it has two ends inside the same file that must move together. The defective magnitude-plus-consequence appears at gaia/references/coastal-erosion.md:119-124 (the ⚠️: "the local gradient is 0.31 — one in 3.2 — which is a cliff" and "puts a step at the shoreline") and is restated as a symptom at gaia/references/coastal-erosion.md:379 ("A step or wall at the waterline | ... drawn to x = 0, where its slope is infinite"). Correcting only the ⚠️ and leaving :379 is exactly the one-end correction this corpus records most often.

No other file repeats it. I grepped the whole checkout for `x^(2/3)`, "infinite slope", "0.31", "one in 3.2", "splice", "planar face", "step at the waterline", "step or wall", and for every file mentioning dean1991 or the equilibrium profile:
- gaia/references/papers-coastal-and-ice.md:129 carries only the true analytic half ("eq. (8) ... has a finite, planar beach face near the waterline instead of eq. (1)'s infinite slope") — no magnitude, no heightfield claim, so it needs no correction and in fact corroborates the part of the ⚠️ that should be kept.
- gaia/references/index.md:24 carries the equation in the one-line description only.
- gaia/references/water-closed-vs-open.md:146 mentions "the waterline is a hard edge on a vertical wall", but about degenerate closed-body shorelines, not the Dean profile — unrelated.
- obsolete/terrain-architect/references/12-glacial-coastal.md:979-1032 uses the same Dean ramp (depth ∝ distance^2/3) but makes no infinite-slope or step claim; at :1020 it explicitly calls its own nearshore artifact "a slope discontinuity, not a step". Unaffected, and out of the active corpus anyway.
- gaia/registers carry no numeric-claim row for the 0.31 gradient (the only 0.31 hits in gaia are hillaire2020's 0.31 ms sky-LUT figure in atmosphere-and-aerial-perspective.md / papers-rendering.md — a different quantity entirely).

### impact-craters.md · `IC-1` · `wrong` · gaia/references/impact-craters.md:30-34

**Narrowest true statement.** At gaia/references/impact-craters.md:30-34 and :272 the document selects WHICH Pike regression to evaluate by a gravity-scaled `D*`, with no floor at the fits' own crossings and no mention anywhere in the body of the complex fit's sample range (">15 km, N=33", recorded in the document's own frontmatter locator at :9). The three fit pairs cross at 10.583 / 21.273 / 30.352 km — fixed properties of lunar regressions that do not move with g — so a single gravity-scaled selector cannot coincide with them. Concretely: on any body whose blend-band top 1.58·D* falls below 10.583 km (g > 4.598 m/s² at the document's own D*·g = 30.8), i.e. Earth (D* = 3.14 km, band 1.66-4.96 km, printed on the page as "1.7-5.0 km") and Venus, every crater above the band top is placed ENTIRELY on the complex depth fit below that fit's crossing, where it returns MORE depth than the simple fit. At D = 5 km on Earth the prescription yields 1.695 km of depth against the simple law's 0.996 km (1.702x), d/D = 1:2.95 — deeper relative to diameter than the document's own simple-bowl constant of 1:5 (:83-87) — and 1.70x deeper than the same-diameter lunar crater the same prescription produces, inverting "a low-gravity moon is a world of deep bowls where Earth would have terraced, flat-floored, central-peaked basins" at :121-124. Under the blended rule the Earth over-deepening peaks at 1.717x (D = 4.55 km, d/D 1:2.93); under the bare threshold the same page also licenses ("if the scene shows craters near it" makes blending conditional) it is a 2.37x upward step at D = 3.14 km. The narrowest fix is a floor: branch on each fit's own crossing (equivalently min of the two branches), and state that Pike's coefficients are lunar and their crossings do not scale with gravity.

**Blast radius.** One site only — gaia/references/impact-craters.md. `grep -rn "1\.044\|0\.53.1\.58\|30\.8\|10\.583\|10\.6 km" --include=*.md --include=*.tsv --include=*.py .` over the whole tree returns hits in exactly three places: gaia/references/impact-craters.md (:9, :33, :64, :65, :77, :90, :95, :109, :111, :272, :282), the audit documents that quote it (gaia/AUDIT-UNREFUTED-2026-09-09.md:119-123, gaia/AUDIT-2026-09-05.md:844 raising the related X46 about the 0.18 rim ratio), and obsolete/terrain-architect (VALIDATION.md:304, registers/numeric-claims.tsv:5123-5125) where "~10.6 km / 10-30 km" is only the transition band, not a branch rule. The one other crater site in the corpus, obsolete/terrain-architect/references/11-geological.md:697 and :768, carries the same 1/g idea but in a self-guarding formulation — `Dc = 3200·(9.81/g)` metres with `depth = 0.20·D` simple and `depth < 0.20·D` complex — so it never evaluates Pike's complex regression and the inversion cannot arise there; obsolete/.../references/20-archetypes.md:1045,1120 and 25-planetary-spherical.md:236 and 00-index.md:48 mention the ~1/g transition qualitatively with no fit. No register or corrections row propagates the branch rule: gaia/registers/pseudocode-execution.tsv:188 is the only impact-craters row and records the transcription check, not the branching. Within the affected file the same root cause recurs at :89-91 and :281-282 (the complex rim law evaluated and printed at D = 3 km, 7x below its own 21.273 km crossing, where it gives 3.34x the simple rim) — so a correction landing only on the depth branch at :30-34/:272-273 would leave the rim end uncorrected.

### mesh-extraction.md · `ME-2` · `wrong` · gaia/references/mesh-extraction.md:48

**Narrowest true statement.** gaia/references/mesh-extraction.md:47-48 states of vertex clustering "the error is unbounded". That is false for the method the page describes ("snap to a grid and merge"), and it is contradicted by the page's own P-tier source: garland1997 §2.1, Vertex Clustering paragraph, reads verbatim "However, while the size of the grid cells does provide a geometric error bound, the quality of the output is often quite low. In addition, it is difficult to construct an approximation with a specific face count, since the number of faces is only indirectly determined by the specified grid dimensions." When the cell representative lies in its cell (member vertex or weighted average — both are what "snap to a grid and merge" means), every vertex moves at most one cell diagonal and every surface point, being an affine combination of three such vertices, moves at most as far, so the Hausdorff error is bounded by h*sqrt(3) and a 0.5 m cell bounds it at 0.866 m. The correct charge against clustering is the source's: low output quality and no direct control of face count — the reason is wrong, the verdict ("unusable at the low end") is right, so the fix is to swap the reason, exactly as the finding proposes. The part of ME-2 that fails is one clause in its harm narrative: "rejects the one method on the page whose geometric error is bounded by construction". Clustering is not the only such method on the page — greedy insertion [garland1995] is presented at :43-45 and :205-207 as delivering "the fewest triangles for a stated vertical error — because its importance measure *is* that error, exactly", i.e. a vertical-error bound by construction. Drop the uniqueness clause; the defect and the fix stand unchanged.

**Blast radius.** One site only. `grep -rn -i "vertex clustering|rossignac|clustering"` across /home/user/skills returns just gaia/references/mesh-extraction.md:47 for this subject (the other three hits are principal-architect/reverse-engineering.md and its SKILL.md, about dependency clustering of code graphs - unrelated). `grep -rn "unbounded"` across the whole corpus returns ~30 hits, none of which repeats this claim: gaia/references/driver-fields.md:55-90 (unbounded horizon sweep), heightfield-raymarching.md:100 (unbounded search interval), sky-and-weather-state.md:309, gauntlet-loop/decomposition.md:245, fan-out and terrain-architect/terrain-renderer occurrences - all different quantities. No sibling document restates a clustering error bound in the other direction either: `grep -rn -i "error bound|bounded by construction|Hausdorff" gaia/references/*.md` gives only heightfield-raymarching.md:135 and water-rendering.md:201, both unrelated. The magnitudes involved (0.87 m, h*sqrt(3), 0.5 m cell) appear nowhere else in the corpus. Registers carry no row for this line: registers/corrections.tsv's two mesh-extraction rows are both about loop-termination erratа, registers/pseudocode-execution.tsv's four are the QEM figures, and source-findings.tsv and guard-proofs.tsv have none - so a fix at :47-48 needs no register update and has no second end to land at. One in-audit dependency worth flagging to the orchestrator: ME-5 (AUDIT-UNREFUTED-2026-09-09.md:177) uses ME-2's substance as one of its three counterexamples, so if ME-2 is edited away, ME-5's supporting text must be re-read - that is in the audit file, not the corpus.

### mesh-extraction.md · `ME-3` · `wrong` · gaia/references/mesh-extraction.md:121

**Narrowest true statement.** The page's parenthetical at mesh-extraction.md:125 — "The direction holds everywhere (the ratio never drops below 1)" — is false, and false already inside the document's own measured sweep. Narrowest statement: (a) at the document's own steepest measured configuration (max slope 10), on the *per-contraction* reading the document itself uses one paragraph earlier (":118, ranging 4.17 to 6.94 across the contractions"), 21%–31% of contractions have ratio < 1, minimum 0.575–0.590, in every rig variant I ran (41² and 61² grids × keep fractions 0.5/0.25/0.1); (b) the ratio-of-means crosses 1 at max slope ≈13.5–14 (0.967 at slope 14, 0.687 at 20 for the keep fraction that best matches the document's own 5.59/5.60 at slope 1.0); and (c) rig-independently, the document's *own* verified identity at :108-110 (Δ = n·d²·cos²θ) gives ratio = √N·cos θ on a uniformly sloped patch, crossing 1 at slope √(N−1) — slope 4.2 for the N≈19 actually accumulated, i.e. between the document's slope-4 and slope-10 samples. What fails in the finding as written is its whole quantitative apparatus: the accumulated plane counts backed out from the printed ratios (48/63/84/110/138) are 3–7× the real mean accumulated plane count (19.1 at keep 0.25, 13–14 at keep 0.5) and, unlike the back-out, the real count is essentially flat in slope (19.1 → 19.8 from slope 0.1 to 100); the √N/√(1+s_max²) model correlates 0.098 with the measured per-contraction ratio at slope 1.0; and the proposed fix's bound "roughly 8–12" is not derivable — with the real N it is 4.2, and the crossing is not a function of slope alone (still 1.41 at slope 20 at keep 0.1). The finding also understates the defect: it places the crossing just past the measured range, when on the cliff case the sentence is actually about it lies at slope ≈4, inside it. All line citations in the finding are 4 low (:121→:125, :118-119→:122-123, :114→:118, :266→:270), consistent with registers/pseudocode-execution.tsv:188's note that the document is under repair and its line numbers are moving.

**Blast radius.** The sentence itself is ONE SITE ONLY: gaia/references/mesh-extraction.md:125 ("The direction holds everywhere (the ratio never drops below 1)"). `grep -rn "never drops below 1\|direction holds everywhere\|nearly honest on cliffs" --include=*.md --include=*.tsv .` returns that line and the audit file's quotation of it, nothing else.

The same ONE-DIRECTIONAL framing it licenses repeats at TWO further sites, both in the same file, and both would need the mirror case added if the sentence is corrected:
- gaia/references/mesh-extraction.md:216 — "separately measures overstating true deviation by 1.2× to 7×" (asserts overstatement only).
- gaia/references/mesh-extraction.md:270 — failure-table row "Runtime tessellates 1.2× to 7× more than the budget implies, worst on gentle ground | `sqrt(Δ)` exported as the geometric error `e`". This is the row the finding correctly says admits no under-tessellation case. Note this row is a correction landing at one end only: it names the gentle-ground end and omits the cliff end.

The underlying magnitudes 0.0175 / 0.0031 / 5.60 / (4.17–6.94) are also recorded in gaia/registers/pseudocode-execution.tsv:188 ("over 3453 contractions mean sqrt(Delta) 0.0175 against true vertical miss 0.0031, ratio 5.60 (4.17-6.94)"), marked ALL REPRODUCE against scripts w3/qem.py and w3/qem2.py that are absent from the tree. That register row does NOT carry the slope sweep (6.91/5.59/4.09/2.54/1.17) nor the "never drops below 1" sign claim, so it needs no edit for this finding — but it does carry the 3,453 contraction count that finding ME-1 attacks, and it is the only other file that would go stale if the QEM figures are re-run.

Outside gaia/, matches for the bare magnitudes 1.17 / 2.54 / 4.09 fall only in obsolete/terrain-architect/ (crater diameter coefficient 1.17·D_tc^1.13 and a glacial-coastal table) and are unrelated to this claim.

### mesh-extraction.md · `ME-4` · `wrong` · gaia/references/mesh-extraction.md:252

**Narrowest true statement.** mesh-extraction.md:255-256 asserts "every symptom in that document's failure table starts with a guessed `e`" about heightfield-lod.md. That universal is false: heightfield-lod.md's failure table (:261 header, :263-275 body) has 13 rows, and 11 of them contain no `e` anywhere in symptom, mechanism or fix — their mechanisms are morph constants, T-junctions, un-morphed height sampling, mip-level mismatch, un-morphed normals, frame-dependent morph factor, per-pass LOD selection, light-distance LOD selection, skirt curtains, normal-mip aliasing and fp32 world-space jitter, none of which involve `e`. At most 2 rows depend on `e` at all (:273 and :274), and exactly 1 (:274, "Halving `tau` does not roughly quadruple triangles") is diagnosed as a guessed `e`. Where ME-4 itself overreaches: it says "Exactly one of them involves a guessed `e` ... The other twelve have no dependence on `e` at all", then enumerates only ten rows (263,264,265,266,267,269,270,271,272,275), silently dropping :268 and :273. :273 ("Objects float or sink on distant terrain | Physics samples the authoritative field; render shows error `e`") names `e` in its mechanism and is genuinely aggravated by an under-measured `e` — since `e` enters only through `rho = e·K/d` (heightfield-lod.md:73-76), a too-small `e` holds a coarser level than `tau` allows and widens the render-versus-authoritative gap. So "no dependence on `e` at all" is false for that row, and ME-4's proposed replacement ("the ONE symptom ... that is not a runtime bug") is itself one row too narrow. Also: the audit locator :252 is the paragraph's first line; the offending sentence is at :255-256.

**Blast radius.** One site only. `grep -rn "starts with a guessed"` over the whole corpus returns just gaia/references/mesh-extraction.md:256 (plus the audit's own quotation at AUDIT-UNREFUTED-2026-09-09.md:167,171). `grep -rn "every symptom"` returns only mesh-extraction.md:255 and the audit. `grep -rn "guessed \`e\`"` outside the audit returns only mesh-extraction.md:256. The underlying measure-don't-guess rule is restated in three other places WITHOUT the false universal, so none of them needs the correction: gaia/references/heightfield-lod.md:70-71 ("computed over the *actual* vertex removals, never guessed from a mip level"), obsolete/terrain-renderer/references/01-heightfield-lod.md:30 ("not as a guess from mip level" — and that file has a "Pitfalls" section at :349, no failure table, so it cannot carry the claim), and mesh-extraction.md:254 itself. No register row repeats it: `grep -rn "mesh-extraction" gaia/registers/*.tsv | grep -iE "guess|failure|symptom"` returns nothing. The magnitudes to fix at the one site are the quantifier itself (13 rows, not "every"), and if a count is printed it must be 11 rows with no `e` at all, 2 rows touching `e`, 1 row diagnosed as a guessed `e`.

### mesh-extraction.md · `ME-5` · `unsupported` · gaia/references/mesh-extraction.md

**Narrowest true statement.** At gaia/references/mesh-extraction.md:212-213 the sentence "it is the one load-bearing comparison in this document with no number behind it. Everything else here was run" is false under every available scoping of "here". (a) "What it beats" (:47-56) is a four-way comparison section — vertex clustering, uniform decimation, marching cubes over an SDF, exporting the full grid — containing zero numbers, zero measurements and zero citations, carrying no ⚠️ or "argued" marker, and it is load-bearing beyond this page: coverage.md:96 takes the skill's `isosurface-extraction` `out-of-scope` decision from it and cites back here. (b) The cluster-DAG floor claim at :246-248 ("those vertices survive to the coarsest level, so the chain floors out at the cluster-boundary vertex count") is a second argued-only quantitative comparison between two build strategies, and the magnitude the earlier finding carried for it ("~50% of source vertices", AUDIT-2026-09-05.md:692 / X16) was dropped rather than measured. (c) Even scoped as narrowly as possible — to the ⚠️'s own section (:169-219) or its own paragraph (:203-210) — nothing there was run: registers/pseudocode-execution.tsv:188 records only the QEM-metric measurements that live at :58-153, and :233 records the greedy-insertion fence as "NOT RUN". The operative harm is that the vouching sentence covers, unmarked, the empirical and false claim at :47-48 that vertex clustering's "error is unbounded" — the standard construction bounds surface displacement by the cell diagonal (0.866 m for a 0.5 m cell). Narrower than the finder in one respect only: of the four "What it beats" entries, the uniform-decimation verdict is true by construction of uniform decimation and the full-grid entry is not a rejection ("defensible"), so the count of comparisons that a number could actually settle and that the ⚠️ wrongly vouches for is at least two (vertex clustering, greedy-vs-QEM), and at least three counting the cluster-DAG floor.

**Blast radius.** One site only for the claim itself. `grep -rn "load-bearing" --include=*.md .` and `grep -rn "Everything else\|was run" gaia/references/*.md gaia/*.md` return no other occurrence of this vouching construction anywhere in the corpus — mesh-extraction.md:212-213 is unique. The vertex-clustering "the error is unbounded" claim the ⚠️ shields is also one site only (mesh-extraction.md:47-48; `grep -rni "vertex clustering"` hits only that line plus the audit's own quotations). One propagation site worth naming: coverage.md:96 restates the marching-cubes half of the same unmeasured comparison verbatim in substance ("convert an exact 2.5-D function into an approximated 3-D one that then needs simplifying anyway") and attributes it back with "(`mesh-extraction.md` says so where it lists what its method beats)" — so a correction landing only at :212 leaves coverage.md:96 still sourced to an unmarked argued comparison; that is the one-end-only risk here. For contrast, the corpus does carry the correct provenance marker elsewhere and those sites are NOT affected: atmosphere-and-aerial-perspective.md:106 ("Bounded here, not measured anywhere"), sketch-based-authoring.md:146 ("not measured here"), node-graph-runtime.md:300 ("derived from that line, not measured"), resolution-independence.md:325 ("computed, not measured").

### planetary-precision.md · `PP-1` · `unsupported` · gaia/references/planetary-precision.md

**Narrowest true statement.** Locator-coverage gap, one line: the `cozzi2011` locator in planetary-precision.md's frontmatter (:9) names only "ch. 5 Vertex Transform Precision — §5.3 … §5.4 … §5.5 — and ch. 6 Depth Buffer Precision, §6.3 … §6.4" (pp. 157-199 of Part II Precision), while the file's third [cozzi2011] citation, the horizon-culling test at :146 with the pseudocode at :149-154, draws on material outside that range — the culling treatment the corpus itself places at ch. 12 §12.4 (tiled-streaming.md:9, papers-rendering.md:131). The file's other two cozzi2011 citations (:44 camera-relative CPU = §5.3; :82 GPU double-single = §5.4) do resolve inside the declared range, and no cross-reference near :146 redirects the reader elsewhere, so the horizon citation is the only one a reader cannot check from the declared coordinates. Nothing about the source choice, the horizon prose, or the pseudocode is wrong — the defect is confined to the locator string at :9 (fix: extend it to name ch. 12 §12.4 Culling). Caveat I could not close, same as the finder's own: I confirmed §12.4 "Culling" p. 390 from the publisher TOC and that §12.4 has sub-subsections (a §12.1 sample chapter cross-references "Section 12.4.4" for hardware occlusion queries), but I did not open §12.4, so the exact sub-subsection and page of the horizon test are unverified; that does not affect the defect, which turns on the declared range excluding it.

**Blast radius.** The defect site — a structured `locator:` that fails to reach a horizon-culling citation — is one site only: gaia/references/planetary-precision.md:9 against :146. The surrounding claim and its coordinates appear at four other places, all clean: (1) gaia/references/tiled-streaming.md:9 carries the correct cozzi2011 range "ch. 12 Massive-Terrain Rendering — §12.1 … §12.4 culling", and that file's only other cozzi2011 mention (:29) is meta-prose about the book's tier, not a claim; (2) gaia/references/papers-rendering.md:131 is the bibliography entry, which lists "the horizon-culling test" at topic level where no chapter locator is expected; (3) gaia/references/gpu-driven-culling.md:62 states the horizon stage ("one dot product against the horizon plane/cone") with no [cozzi2011] citation at all — grep shows cozzi2011 occurs in exactly three gaia files (planetary-precision.md, tiled-streaming.md, papers-rendering.md), so there is no locator there to be wrong (the separate dot-product-count discrepancy against :149-154 is a different, already-noted issue); (4) obsolete/terrain-renderer/references/09-planetary-precision.md:171,178-179,291 reprints the identical pseudocode and credits "Cozzi & Ring" in prose with tier P and no chapter locator — an older, pre-structured-locator format in the obsolete tree, so the same omission cannot be expressed there. Every magnitude the finding uses (pp. 157-180, 181-199, §5.3 p.169, §5.4 p.171, §5.5 p.177, §6.3 p.189, §6.4 p.191, §12.4 p.390) occurs only in the audit file and, for the chapter/section names, in the two gaia locators above; none is repeated with a competing value anywhere in the corpus, and grep of gaia/registers/*.tsv returns no cozzi rows at all, so no correction register needs a matching fix.

### planetary-precision.md · `PP-2` · `unsupported` · gaia/references/planetary-precision.md

**Narrowest true statement.** In /home/user/skills/gaia/references/planetary-precision.md the horizon block binds the occluder radius to the ellipsoid and never provides a way to change it: :146-147 scales space so the *ellipsoid* is the unit sphere, and the constant `1` in both comparisons at :152-153 is that occluder radius squared — the only place any radius enters the test (replacing both `1` with `r*r` reproduces ray-sphere ground truth exactly). The prescription at :158-159 and the fix column at :175 name a *different* occluder radius (ellipsoid minus the deepest depression) but the document nowhere says to rescale by it or to replace the constant, so a reader who implements :146-153 literally has hard-coded the very failure :175 claims to fix. Reproduced under the finding's own inputs (a = 6 378 137 m, camera at 10 km, target 430 m below the ellipsoid): the printed test culls that target from 349.165 km to 364.521 km of ground distance — a 15.356 km annulus — where the prescribed radius r = (a−430)/a culls nothing. One non-load-bearing parenthetical in the finding is wrong: at its own 10 km camera altitude a −2 555 m trench widens the band 5.99×, not "~2.4×" (2.4376 = sqrt(2555/430), the deep-depression asymptote that only applies for camera altitudes of order 100-200 m). The error understates the effect, so the finding stands unchanged in substance.

**Blast radius.** Two sites carry the identical construction; the gaia file is the descendant. (1) /home/user/skills/gaia/references/planetary-precision.md:146-147, :152-153, :158-159, :175 — the finding's site. (2) /home/user/skills/obsolete/terrain-renderer/references/09-planetary-precision.md:170-187 — the same defect verbatim in the obsolete ancestor: :172 "scale space so the ellipsoid is the unit sphere, let `cv = camera` in that space", :178-179 the same two `> dot(vc, vc) - 1` comparisons, :182 "using the patch's *maximum* height", :187 "Use the ellipsoid *minus* max terrain depression as the occluder radius or valleys get culled while still visible" — again with no r and no rescaling instruction. A fix applied to the gaia file alone would leave the obsolete copy uncorrected. Non-repeats checked and cleared: /home/user/skills/gaia/registers/pseudocode-execution.tsv:161 stamps the test SOUND but only for a sphere occluder, so it does not repeat the radius claim (it could, however, be misread as covering it); /home/user/skills/gaia/references/papers-rendering.md:131 only attributes the horizon test to cozzi2011; /home/user/skills/gaia/references/gpu-driven-culling.md:62 budgets the horizon stage's cost with no radius claim; /home/user/skills/obsolete/terrain-architect/references/25-planetary-spherical.md:183's "unit sphere" is noise sampling, unrelated. No other site in the corpus states the 15.4 km / 349.2 / 364.5 km magnitudes or the r = 1 − maxDepression/a relation.

### planetary-precision.md · `PP-3` · `incomplete` · gaia/references/planetary-precision.md

**Narrowest true statement.** At planetary-precision.md:69-70 the word "only" is the document's own exclusivity strengthening and is false as printed against Reed's table. Reed writes only "Reversed-Z with a float depth buffer gives a zero error rate in this test"; the word "only" appears nowhere in that sense on his page. His table has SEVEN data rows, and THREE of them are 0% indist / 0% swap in both float32 columns: "Unaltered Z values (control test)", "Reversed Z", and "Infinite + reversed-Z". So "reversed-Z with a float depth buffer" is TWO rows, not "the only ... row", and the same sentence's second clause ("erases the finite-versus-infinite far plane distinction entirely") is true precisely BECAUSE the second of those two rows is equally zero — the singular "the only ... row" and that clause cannot both hold. The identical false uniqueness rides in the frontmatter locator at :11 ("whose reversed-Z float32 row is the zero-error one"), where the definite article asserts a uniqueness the table does not have.

The narrowing: the locator at :11 does NOT misdirect and does not leave a reader unable to verify. It names the row by its label — "the reversed-Z float32 row" — and both rows carrying that label ("Reversed Z", "Infinite + reversed-Z") are 0/0 in both float32 columns, so whichever the reader lands on, the cited fact checks out. The control row is not a candidate for a row labelled reversed-Z. The minimal repair is one word at :69-70 ("the only zero-error depth mapping — both its finite and infinite far-plane rows") and the same de-uniquing at :11 and its two duplicate sites.

**Blast radius.** FOUR sites, of which the finding names only two.

1. gaia/references/planetary-precision.md:70 — "the only zero-error row" (named by the finding).
2. gaia/references/planetary-precision.md:11 — locator "whose reversed-Z float32 row is the zero-error one" (named by the finding).
3. gaia/references/shader-craft.md:15 — BYTE-IDENTICAL locator string, verified by python string compare (BYTE-IDENTICAL: True). Not named by the finding. shader-craft.md:247 leans on it ("on the storage evidence in [reed2015]"), so the same overstated uniqueness is load-bearing in a second document.
4. gaia/registers/source-findings.tsv:28 — the same wording in the register that recorded the original upchurch2012 mis-attribution repair: "Reed's simulation, where reversed-Z with float32 is the only zero-error row". Not named by the finding. This is the row that authorised the :63-71 warning paragraph, so a fix at :69-70 alone leaves the register still asserting the false exclusivity.

grep -rn "zero-error|zero error|only zero" over the corpus returns no other site (mesh-extraction.md:275 and volumetric-clouds.md:251 are unrelated uses). A correction applied at planetary-precision.md only would land at one of three prose ends and leave shader-craft.md:15 and source-findings.tsv:28 carrying it.

### river-networks.md · `RN-1` · `wrong` · references/river-networks.md:42

**Narrowest true statement.** `river-networks.md:42`, inside the operative `## Use this` list ("The three numbers that fill that graph", :40), prescribes `φ = 0.42·A^0.69` with `A` in m² and `φ` in m³/s — repeated as a unit claim in the genevaux2013 locator at :13 — and at :43 points the reader straight at `flow-routing.md`'s accumulation, which is m² (`flow-routing.md:378`, `A[:] = cellArea  # m²`). It does not flag that this unit, though faithfully taken from the source, is physically impossible: read in m² the relation inflates φ by 10^(6·0.69) = 1.380×10⁴ over the km² reading, so a 10 km² headwater yields φ = 2.8395×10⁴ m³/s — an areal runoff of 8.96×10⁴ m/yr, and 12.3× the mean discharge of the Rhine. The error reaches the page's own two ABSOLUTE consumers of φ: at :339-341 `a = φ/‖u‖` turns it into a 2.84×10⁴ m² water cross-section on that basin (≈3 km wide under a braided template, ≈920 m at w/d = 30), and the planform line at :46-48/:156 gives `S_crit = 0.0125·Q^−0.44` = 1.372×10⁻⁴, so every reach steeper than 0.0137 % is authored braided and a 0.2 %-slope creek comes out a braid. It is specifically the MISSING FLAG that is the defect, not a misquotation: the paper reads verbatim "Let A [m2] be the watershed area. The mean flow φ of the river [m3s−1] is given by φ = 0.42·A 0.69" (verified here from the PDF), and this document flags exactly this class of unit trap for three other constants — candel's D50 at :190-199 ("⚠️ **That unit is METRES, and an earlier version of this line said millimetres**"), `λ = 6.5·w^1.1` at :429, and Strahler's 0.541-vs-3.52 — while leaving this one unmarked. Two narrowings the finder did not make: (i) the relation's other appearances at :132 and :422 are RATIO-only (`w ∝ A^0.345` normalised at 1 km²; 1.4289× and 2.9174× reproduce exactly), and the unit scale cancels identically in a ratio, so those two sites and the anchored width chain at :127-129 are untouched by the error — only the two absolute consumers break; (ii) the finder's own proposed km² fix is itself only defensible above roughly 10³ km² (it gives 13.25 m/yr of runoff at 1 km² and 6.49 m/yr at 10 km², above the wettest catchments on Earth), and Génevaux's φ is the paper's *mean* flow while `leopoldwolman1957`'s line at :46 and :152 takes *bankfull* Q — a second silent equation the finder raises in one closing sentence but then contradicts by calling the km² values "the right order for bankfull".

**Blast radius.** The UNIT CLAIM is two sites, both in `river-networks.md`, and the finding names both: `:42` ("`A` in m², `φ` in m³/s") and the genevaux2013 frontmatter locator `:13` ("with A in m² and φ in m³/s"). A repair landing only on `:42` leaves `:13` — this corpus's most-recorded defect class. The BARE FORMULA without units appears at four further sites, none of which is damaged: `river-networks.md:14` (peytavie2019 locator, `a = φ/‖u‖ with φ = 0.42A^0.69` — the consumer, unitless as written), `river-networks.md:132` and `:422` (both ratio-only, unit-cancelling), and `papers-flow.md:184` ("**Dunne & Leopold (1978)** … is the source both graphics papers cite for `φ = 0.42 A^0.69`. It is a textbook, it **was not opened**…" — provenance note, no units, no prescription). Sweep run: `grep -rn "0\.42" --include=*.md gaia/` (excluding D50/unrelated decimals) and `grep -rn "A\^0\.69\|0\.42 A\|φ = 0\.42\|discharge from drainage\|Q ∝ A"` over `gaia/` return exactly those six sites plus the audit file itself. No magnitude of the failure (2.84×10⁴, 5,798, 1.372×10⁻⁴, 1.38×10⁴) appears anywhere in the corpus; `registers/corrections.tsv` has no row for this relation (its four `river-networks` hits are `check_paths` scope and unrelated files), and `registers/pseudocode-execution.tsv:239` covers only the `h(p)` carve fence. No sibling document states a competing discharge-from-area rule.

### river-networks.md · `RN-2` · `wrong` · references/river-networks.md:322

**Narrowest true statement.** `gaia/references/river-networks.md`:328-331 asserts of the expression it itself prints — `w(p) = (1 − d(p)²) ² / r⁴` — that it is "a quartic bump with no flat top, reaching 1 only at the single point `d = 0`", and concludes that the carved bed "does come out shallower than the profile asks for except directly on the centreline". Both statements are true of that expression only at r = 1 exactly (sympy: solve(1/r**4 == 1, r) → [1]), and the page never states r anywhere, while the same paragraph (:334) makes r explicitly variable ("the same operator with a wider support"). For any r > 1 the printed expression gives w(0) = r⁻⁴ and rises toward the support edge, so the carve is shallowest on the centreline rather than exact there: at r = 10 m, w(0) = 1×10⁻⁴ (a requested 4 m carve lands 0.4 mm) against w(9.9) = 0.94109401 (a 3.76 m cut at the bank), inverting the clause "except directly on the centreline". It is also discontinuous at the support boundary the same section states (d→r⁻ gives (1−r²)²/r⁴ = 0.9801 at r = 10, where `sketch-based-authoring.md`:292-293 states §7 sets w = 0), and for r < 1 it exceeds 1 (w(0) = 16 at r = 0.5 m), so `h_C = (1−w_B)h_A + w_B h_B` is the extrapolation −15·h_A + 16·h_B, failing the paragraph's own opening premise "That expression is a convex blend". The one part of the finding that fails is its shape description: "The weight is a bathtub with its *minimum* on the centreline, not a bump with its maximum there" is not correct. d = 0 is a strict LOCAL MAXIMUM of the printed expression (w′ = −4d(1−d²)/r⁴ < 0 for 0 < d < 1; numerically w(0) = 0.00010000 > w(0.01) = 0.00009998), and the minimum over the support is exactly 0 at d = 1 m, not on the centreline; worse, in the r < 1 regime the finding invokes two sentences later, the function is strictly decreasing on [0, r) — i.e. precisely "a bump with its maximum there", just with peak r⁻⁴ > 1 — so the "bathtub" claim contradicts the finding's own next case. The defect stands on the two centreline values and the boundary discontinuity, not on the "minimum on the centreline" characterisation.

**Blast radius.** One site only. `grep -rn "1 − d(p)\|1 - d(p)\|1−d(p)"` over the whole repo returns `gaia/references/river-networks.md:328` and nothing else outside the audit file itself; `grep -rn "reaching 1 only"` and `grep -rn "flat top"` likewise return :329 and :328 as the sole hits in gaia/ (the other "flat top" hits are mesa/caprock prose in obsolete/ and gaia/mask-operators.md:351, unrelated). The corpus's other statements of the same quartic are the CORRECT normalised form and are the contrast, not repeats: `sketch-based-authoring.md`:45 (`w(a) = (a² − 1)²`, "`a` = distance over support radius"), :269 (`W(a) = (a^2 - 1)^2, a = distance from the feature / distance to the boundary`), :280 (table row), :382 (failure row), and `papers-generation.md`:378 ("a C1 weight `(a² − 1)²` on the ratio of distance-from-feature to distance-from-boundary"). `sketch-based-authoring.md`:12 and :292-293 carry genevaux2013 §7's compact-support rule (`w(p) = 0` wherever `d(p)² ≥ r²`) without printing the broken expression. The magnitudes: 0.0625 appears at `sketch-based-authoring.md`:287 and is correct there (I verified (1/2²−1)² − (1 − (3·¼ − 2·⅛)) = 9/16 − 1/2 = 1/16 exactly), an identity that holds only for the normalised form; 0.941 / 3.76 / 0.9801 / 1e-4 appear nowhere else in gaia/ in this context. `river-networks.md`'s "Use this" block (:33-45) and its 20-row failure table (:417-436) contain no row about the carve weight, plateau or centreline depth, so no correction is owed at a second end within this document. `registers/pseudocode-execution.tsv`:239 explicitly records the weight as un-fenced prose ("The convex-blend `replace` operator and the quartic weight discussed beside it are prose and are fenced nowhere"), so no register row repeats it either.

### river-networks.md · `RN-5` · `unsupported` · references/river-networks.md

**Narrowest true statement.** river-networks.md:99-100 presents "thresholding an MFD field to get a network produces a smeared, braided mask" as a verbatim quotation of flow-routing.md — introduced by "`flow-routing.md` says exactly what you get if you try:" and enclosed in double quotes — and that 75-character string appears nowhere in flow-routing.md, nowhere else in gaia/references/, and nowhere in the repository except this line. The defect is exactly the quotation marks plus "says exactly": the substance is genuinely flow-routing.md's, at its failure-table row :436 "| River mask smeared and braided | MFD thresholded to make a network | Route D8 for the network |", so the sentence is an accurate prose compression of that row presented as verbatim text, with no line locator. Two of the finder's ancillary assertions do not hold: (a) "*Smeared* is the operative word" does NOT rest on invented wording — "smeared" is flow-routing.md's own word at :436 (the only occurrence of the stem in that file); (b) "the locator does not resolve" is too strong — the named file exists and carries the claim, only the quoted string does not. The finder's line anchor (:94) is also 5-6 lines off the claim it attacks (:99-100); line 94 reads "costs nothing in a tree; it is *splitting* that is impossible…".

**Blast radius.** The fabricated string is ONE SITE ONLY. `grep -rn "thresholding an MFD" /home/user/skills` → river-networks.md:100 and the two audit lines quoting it; nothing else. Programmatic corpus check (requote.normalise, all 48 references/*.md): the normalised quotation occurs in river-networks.md alone. The same substance appears correctly and UNQUOTED at two sites that need no change: flow-routing.md:436 (the failure row) and river-networks.md:420 ("| Marking \"river\" from MFD paints the whole hillslope | MFD splits at 99.2% of interior cells — it is a wetness field | Use MFD for masks (`flow-routing.md`) … |"). Nearest analogue of the same PATTERN (paraphrase-in-quotes of a sibling document), found by sweeping every sibling-attributed quotation in the corpus: driver-fields.md:374 quotes `coastal-erosion.md` as calling exposure "the input that matters", where coastal-erosion.md:44 actually reads "The exposure field is the input you will be tempted to skip, and it is the one that matters." — a compression, below requote's 40-char MIN_QUOTE, meaning preserved. Every other sibling-document quotation at or above the 40-char threshold resolves verbatim in the document it names: papers-coastal-and-ice.md:147→sea-ice.md, papers-flow.md:146→stream-power.md, papers-rendering.md:413→mask-to-material.md, papers-simulation.md:94 and :97→water-optics.md, shader-craft.md:166 and :425→heightfield-raymarching.md/water-rendering.md, simulation-time-budget.md:143→shallow-water.md, volumetric-clouds.md:182→gpu-driven-culling.md (9 of 9). Short sibling quotes spot-checked and resolving: mask-operators.md:25 "is speckle" (terrain-analysis-masks.md:134,274), sky-and-weather-state.md:283 "**not** a significant predictor of snow depth (p = 0.38)" and "Where wind moves snow, wind wins." (driver-fields.md:217,223), shader-craft.md:248 (heightfield-raymarching.md:205), papers-generation.md:362 "an amount slider" (hydraulic-erosion.md). So the corpus convention is verbatim, and river-networks.md:100 is the outlier at full quotation length.

### sketch-based-authoring.md · `F1` · `wrong` · gaia/references/sketch-based-authoring.md:102-104

**Narrowest true statement.** Narrowest true defect: at :101-102 the words "and then stops falling — 19% of the initial rate, forever", and the same pairing in the failure table at :380 ("reaches a steady flux, not a fixed point — 19% of the step-1 work at step 200"), misidentify a transient as the asymptote. Under the document's own inputs (257², curved σ=4 / 40 m crest on a hard mask, D=1, Δt=0.2) the per-step re-added height is still falling steeply through step 200 — 24.7% of the step-1 rate at step 100, 18.1% at step 200 — and settles at 4.19% of the step-1 rate, ~4.3× lower, only after ~2×10⁴ steps. It cannot be a fixed percentage in any case: the step-200 value is essentially domain-independent (19.5 / 18.4 / 18.1 / 18.0% at 65² / 129² / 257² / 513², because √(2DT)=8.9 cells has not yet found the boundary) while the settled value falls as ~1/ln(L/a) (12.84 / 7.27 / 4.17 / 2.37%). Everything else in the passage survives: "A per-step projection does not converge to a fixed point; it converges to a steady flux" is TRUE and exactly reproducible (100,000-step iterate 17.21 cell-metres/step against an exactly solved harmonic limit of 17.2093), so the finding's instruction to cut "it converges to a steady flux" as unmeasured is wrong; and :58-59 ("It also never settles; at step 200 it is still putting back 19% of what it put back on step 1") is already accurate and needs no edit, so the finding's "repeated at :58 … apply the same edit to :58" is a mis-sited blast radius. The finding's supporting equilibration estimate (~82,000 steps) is also ~4× high — I measure convergence to within 2% of the limit by ~2×10⁴ steps.

**Blast radius.** Three candidate sites, only two defective, all inside gaia/references/sketch-based-authoring.md. DEFECTIVE: :101-102 ("falls from 4,562 to 867 cell-metres over 200 steps and then stops falling — 19% of the initial rate, forever") and :380, the failure-table mechanism cell ("Per-step projection reaches a steady flux, not a fixed point — 19% of the step-1 work at step 200"), which pairs the transient number with the asymptote. NOT DEFECTIVE, contrary to the finding: :58-59 ("It also never settles; at step 200 it is still putting back 19% of what it put back on step 1") — a correct step-200 measurement plus "never settles", which is the right direction. Source register gaia/registers/pseudocode-execution.tsv:197 records no plateau either ("is still re-adding 867 -- 19% of that -- on step 200, so it never reaches a fixed point"), so the extrapolation was introduced at the document, not inherited. `grep -rn "steady flux\|4,562\|4562\|867\|stops falling"` over the corpus returns no other site of this claim: the other "stops falling" hits (mask-to-material.md:282, thermal-and-aeolian-erosion.md:136 and :403) are unrelated senses, and every "867" hit is Perlin's patent number 6,867,776 in obsolete/terrain-architect. One document only, two lines in it. Note also that the true magnitude is domain-dependent (4.2% at 257², 2.4% at 513²), so a correction must state the level as ~4% of the step-1 rate *at 257²* rather than substitute another bare percentage.

### sketch-based-authoring.md · `F2` · `unsupported` · gaia/references/sketch-based-authoring.md

**Narrowest true statement.** Narrower than the finder put it, and confined to one clause. At `gaia/references/sketch-based-authoring.md:214-215` the clause "**not the gap between the lines**" is false, and it is falsified by the document's own table 13 lines above (:197-201) as well as by independent computation. The rest of the sentence is sound: "the excursion grows with the distance to the clamped boundary" is true with the gap held fixed (−0.6155 → −1.2152 → −2.1027 at 65²/129²/257², s = 6), and the post-colon clause about line length is verified (17 → 63 columns moves the minimum only −0.5581 → −0.6278, a 12 % spread). What the plate excursion actually depends on is the gap measured *against* the distance to the boundary — neither variable alone. The strictly decisive evidence, which the finder did not run, is an equal-distance comparison: with the outer drawn line 19 rows from the first clamped ring in both cases, s = 6 (47² domain) gives min −0.4137 while s = 24 (65² domain) gives min −0.0899 — a 4.6× change in excursion at *identical* boundary distance. The lead-in at :211-212 ("a property of the 65² rig, not of six cells' separation") carries the same error and should be corrected in the same edit; the paragraph's conclusion at :218-219 ("not a number you can budget for") survives untouched, and is if anything strengthened. Severity label `unsupported` understates it: the claim is not merely unsupported, it is contradicted by data printed on the same page.

**Blast radius.** The exclusive clause "not the gap between the lines" appears at ONE SITE ONLY: gaia/references/sketch-based-authoring.md:214-215. `grep -rn "clamped boundary\|excursion"` and `grep -rn "0.616\|1.387\|2.103\|0.558\|0.628\|210%\|190%"` over the whole repo return only three in-document sites plus the audit file itself. The other two sites carry the same one-variable framing but state it truly, because they pin the gap:
- :341-342 (Crossovers): "the excursion grows with the distance to the clamped boundary — 39% above and 62% below **at six cells' separation** on §1's 65² rig, 190% and 210% on **the same pair** at 257²." True as written; a fix at :214 should still touch this bullet, since it leaves the gap's role invisible.
- :387 (failure table): "min −0.616 and max 1.387 on a 0-and-1 pair **six cells apart** on a 65² domain, and −2.10 and 2.90 on **the same pair** at 257² — it grows with the edit window." True as written.
No other document in the corpus repeats the claim or any of its magnitudes. The registers do not carry it either: gaia/registers/pseudocode-execution.tsv:199 records only the single-constraint rig (4.20 %/0.26 %, 16.5×, 92.0 %/79.9 %, −2.2e−05) and never the two-line gap or domain sweeps; gaia/AUDIT-2026-09-05.md:884 (X53) records the predecessor figure "31.7 % of the constraint range at 6-cell separation" — correctly conditioned on the separation — and no mechanism claim. So this is the corpus's characteristic one-end correction risk in reverse: the false clause is isolated at :214-215, and the two derived sites are already correctly scoped.

### sketch-based-authoring.md · `F3` · `stale` · gaia/references/sketch-based-authoring.md

**Narrowest true statement.** The failure-table row at gaia/references/sketch-based-authoring.md:377 states its symptom in RELIEF ("a 4-cell ridge keeps 18.8% of its relief in 200 steps") and offers as its widening cure the PEAK-derived threshold `σ ≥ √(2DT/3)` with no trace of the body's own qualification 242 lines above (:128-135: "That closed form is in PEAK amplitude... Treat the closed form as a lower bound on the width you need, not as the width that works, and check relief on your own field"). At exactly that threshold (σ = 5.16398 at D = 1, Δt = 0.2, 200 steps) peak retention is exactly 0.500, while relief retention — under every estimator I could construct that reproduces the document's own 18.8%-at-σ=4 datum — is 0.19–0.35, and the width needed for 50 % relief is 1.8–2.2× the printed threshold. The row's numbers are all correct and the "≥" form is itself a lower-bound statement; the defect is solely the missing metric qualification and the missing "check relief", i.e. an under-qualified quick-reference row, not a wrong number. Two elements of the finding's wording do not survive: (a) "stale — the correction landed in the body and never reached the failure table" is unsupported; git blame shows the caveat block (:128-136), the Crossovers bullet (:336-337) and the failure row (:377) all originate in the same commit ca7761c (2026-09-03), and registers/corrections.tsv holds no peak/relief repair for this document, so there was never a correction that failed to propagate — the inconsistency was authored in one pass; (b) "the second half of the fix ('check relief on your own field') is nowhere on the page they landed on" is literally false — it is on the same page at :135, true only of a reader who consults the failure table alone.

**Blast radius.** Two sites, both inside gaia/references/sketch-based-authoring.md; no other document or register repeats it. (1) The finding's site, :377. (2) The Crossovers bullet at :336-337 — "flips at `σ ≥ √(2DT/3)` for the diffusion term. At `D = 1`, `T = 40` that is 5.2 cells; below it the sim eats the feature" — same un-caveated threshold and arguably the stronger overclaim, since "below it the sim eats the feature" implies that above it the sim does not, which :133-134 denies. The finding names this in its fix paragraph but not in its site line. The third occurrence, the code block at :116, is NOT a defect site: the ⚠️ reconciliation follows 12 lines later. The "Use this" bullet at :54 is also clean — it prints 18.8 % relief and defers explicitly to "(§2 crossover)". Corpus greps run: `grep -rn "2DT/3|2 \* D \* T / 3|sqrt(2DT|√(2DT|half-life goes as|eats the feature" gaia/` -> only :116, :133, :336-337, :377 plus registers/pseudocode-execution.tsv:198 and the audit's own quotes; `grep -rn "18.8%" gaia/` -> :54, :84, :130, :377 plus an unrelated mip-coverage figure in mask-to-material.md:275. registers/pseudocode-execution.tsv:198 does not repeat the defect — it states the closed form purely in amplitude ("Amplitude after 200 steps 0.218 / 0.408 / 0.667 / 0.873, crossing 0.5 between sigma = 4 and 8 as the crossover sigma >= sqrt(2DT/3) = 5.16 cells requires") and never claims a relief result, so the peak/relief conflation does not propagate into the register.

### sketch-based-authoring.md · `F4` · `incomplete` · gaia/references/sketch-based-authoring.md

**Narrowest true statement.** gaia/references/sketch-based-authoring.md:162-163 prints the multigrid relaxation schedule as `5(l − i)` iterations on level `i` without defining `l` or stating which end of the hierarchy `i` counts from — neither symbol is defined anywhere in the document or the corpus — and in the same sentence calls it "the same schedule [orzan2008] uses", while the page's own source locator at :10 states orzan's schedule as "5i iterations at level i". The two printed forms are opposite functions of `i` and coincide only under the relabel j = l − i, i.e. only if the two papers index levels in opposite directions with l the maximum level index (levels 0..l); the page never states either convention. Under either "l = number of levels" reading the two are different schedules (l = 5: per-level {25,20,15,10,5} totalling 75 sweeps vs {0,5,10,15,20} totalling 50). So the equivalence is unsupported as printed and the schedule is not implementable from the page. That is the whole defect; the "zero relaxations on one level" mechanism the finder attaches to it does not hold.

**Blast radius.** One site only for the equivalence claim: gaia/references/sketch-based-authoring.md:163 is the sole occurrence of "same schedule" in the corpus (grep -rn "same schedule" --include=*.md; other hits are game-engine-guru/references/ECS_AND_DATA_ORIENTED.md:248 and JOB_SYSTEM_AND_FIBERS.md:27, both unrelated job-scheduling prose). Both magnitudes live in that one file too: `5 times l−i` at :9, `5i` at :10, `5(l − i)` at :163 — grep -rn for "5(l", "5 times l", "l − i", "5i iterations", "5i Jacobi" across /home/user/skills returns no other live site (only gaia/AUDIT-UNREFUTED-2026-09-09.md:316-320, the finding itself). gaia/references/papers-generation.md:53-56 and :376-377 record both papers and the borrowed solver but print no iteration schedule and no level convention. The only other corpus "multigrid" mention is obsolete/terrain-architect/references/13-climate-ecosystem.md:194, a code comment about Jacobi/multigrid boundary conditions with no schedule. So a fix at :163 must also touch the front-matter locators :9 and :10 in the same file — that is the only propagation risk, and it is the corpus's usual one-end-only correction trap.

### sky-and-weather-state.md · `SKY-1` · `wrong` · gaia/references/sky-and-weather-state.md:152-153

**Narrowest true statement.** sky-and-weather-state.md:152-153 attributes hillaire2020's 0.31 ms TOTAL RENDER TIME to the LUT rebuild ("the whole LUT set rebuilds in 0.31 ms"). The four LUT times this corpus transcribes from hillaire2020 Table 2 (0.01 + 0.05 + 0.04 + 0.07) sum to exactly 0.17 ms; the remaining 0.14 ms is the on-screen apply, which is not a LUT rebuild. The rebuild figure is therefore overstated by 1.824x, and the sentence's own comparison pairs a total-render figure against 250 ms, which the paper states for LUT updating alone. Narrowest fix: "the whole LUT set rebuilds in 0.17 ms, inside a 0.31 ms total that also carries the 0.14 ms on-screen apply". (Narrower than the finding in two respects: the 806x / 1470x ratios are the finder's illustration — no ratio is printed on the page — and the 0.45 ms double-count is one downstream consequence, not the defect itself.)

**Blast radius.** The "0.31 ms = LUT rebuild" misattribution is one site only: gaia/references/sky-and-weather-state.md:153. `grep -rn "0\.31" gaia/` returns 11 non-obsolete prose hits; I checked each.

One partial repeat, and it is the classic correction-landed-at-one-end pattern: gaia/references/atmosphere-and-aerial-perspective.md:411-412 - "**2020** [hillaire2020] - the tables themselves become cheap enough to rebuild in-frame: 0.31 ms at 1280x720 including all four updates, against 250 ms for the older bake." This repeats the same 0.31-vs-250 pairing (a total-render figure set against a LUT-update-only figure) in that document's own crossover section, 360 lines after its :47-49 "do the subtraction before you budget it". Its wording is defensible as written ("including all four updates" describes the total truthfully and never says the rebuild alone is 0.31), so it is a weaker instance, but a reader budgeting from that bullet lands in the same place. The same 250 ms is also carried at sky-and-weather-state.md:260 ("0.05 ms instead of 250 ms"), where 0.05 is a single Table 2 LUT row and the pairing is a different comparison (SKY-5's territory, not this one).

Correct uses of 0.31 as the total, needing no change: atmosphere-and-aerial-perspective.md:47, :52, :105, :151 and its locator at :9; papers-rendering.md:365 and :369; volumetric-clouds.md:17 and :270 (error-and-cost for the same configuration, where total render time is the right figure). gaia/references/index.md:58 and atmosphere's own description field already use 0.17 ms as the LUT figure, so the corrected number is the one the corpus indexes.

Unrelated 0.31 hits (different quantities, no bearing): coastal-erosion.md:120, impact-craters.md:155, heightfield-lod.md:236, and everything under obsolete/terrain-architect/.

### sky-and-weather-state.md · `SKY-2` · `stale` · gaia/references/sky-and-weather-state.md

**Narrowest true statement.** gaia/references/sky-and-weather-state.md:335 (audit cited :332) — the failure row "Clouds and blowing snow drift apart" prescribes, in BOTH its Mechanism and its Fix cell, the identification the body retracts by name 142 lines earlier. Fix cell: "Advect coverage with `driver-fields.md`'s wind field". Body :193: "It is *not* `driver-fields.md`'s wind field, which is a per-cell terrain-shelter magnitude over a 100–300 m search distance". Mechanism cell: "a private constant instead of the wind field" — same wrong identification, so the finder's own remedy (rewrite the Fix cell only) leaves half the row wrong; the correct contrast is a *private* constant versus the *shared* `prevailingWind` scene parameter, not constant-versus-field. Two subsidiary claims are real but weaker than the finder's wording: (1) the residue at :182 (not :183) does say "the coverage field should move with that field", but it is superseded in place by the ⚠️ block ten lines below it in the same section, so "un-corrected residue" overstates a superseded lead-in; (2) `windVec` at :329 (not :326) is an identifier inside a code example, ambiguous rather than wrong. Line numbers in the audit are all against a pre-5a38c9a snapshot: that commit inserted 3 lines at :201, shifting :332→:335 and :326→:329 (:183→:182 is a plain off-by-one).

**Blast radius.** The erroneous prescription is ONE SITE ONLY outside the audit file: gaia/references/sky-and-weather-state.md:335. `grep -rn "driver-fields.md\`'\''s wind field"` over the whole tree returns exactly :193 (the retraction), :335 (the defect), and AUDIT lines 371/373/375. The same wrong identification survives once more inside the same file as a superseded lead-in at :181-182 ("Gaia already produces one — `driver-fields.md` computes a wind field from the horizon sweep — so the coverage field should move with that field"). Every OTHER end that carries the underlying claim states it correctly and does NOT repeat the mis-identification: the source obsolete/terrain-renderer/references/10-lighting-shadows.md:275-276 says the scroll vector "is `13`/`15`'s wind VECTOR"; the bibliography ends sky-and-weather-state.md:15 and papers-rendering.md:397-398 both render it as "the cloud scroll vector being the wind vector"; the "Use this" section at :19-30 prescribes only "exactly one coverage field" and names no wind field at all. Magnitudes: "100–300 m" appears at driver-fields.md:82, :387, :429 and sky-and-weather-state.md:194 — all four consistent and correct, none of them defective. `prevailingWind` appears only in sky-and-weather-state.md (and an unrelated obsolete/terrain-architect/references/12-glacial-coastal.md). No register row logs this correction: `cut -f2 gaia/registers/corrections.tsv | sort | uniq -c` lists no sky-and-weather-state.md entry, though corrections.tsv:11 (X8) records the identical CLASS of defect in the sibling atmosphere-and-aerial-perspective.md — "Fix cell was ... the exact prescription the body at :94 withdraws".

### sky-and-weather-state.md · `SKY-3` · `stale` · gaia/references/sky-and-weather-state.md

**Narrowest true statement.** The Fix cell of the failure-table row now at gaia/references/sky-and-weather-state.md:337 ends "Decide whether the second-order brightening is worth a rebake, and schedule it", which prescribes exactly what the body at :247-252 retracts by name ("the right response is to cut it, not to schedule it ... Hold `ᾱ` constant at a representative value ... which is precisely the assessment that justifies cutting the edge rather than trying to schedule around it"). The correction was added to the body by commit d14d436 and that commit did not touch the table row, whose text has stood unchanged since 9efbf35 — a one-end correction. Only the Fix cell's final clause is defective: the Symptom cell and the Mechanism cell ("`ᾱ` ... sits inside Algorithm 4.1's iteration, so surface albedo is a bake input") and the "Expected, not a bug" opening are all consistent with :242-244 and :306 and should stand. The same file carries a second, un-corrected residue of the retracted framing at :254-255 ("it is the difference between a bake you can schedule and one that surprises you"), which the finding did not name.

**Blast radius.** One site only for the defective prescription, plus one un-flagged residue inside the same file. `grep -rniE "ᾱ|alpha-bar|ground albedo|surface albedo" gaia/ --include=*.md --include=*.tsv -l` returns exactly four files: the target, gaia/references/atmosphere-and-aerial-perspective.md, gaia/registers/source-findings.tsv, and the audit itself. The sibling's only albedo passage (atmosphere-and-aerial-perspective.md:167-172) names ground albedo as one of the medium changes that costs the 250 ms bake — consistent with both ends of the target and carrying no snow-line/cycle/scheduling prescription, so it does not repeat the claim and needs no correction from this finding. source-findings.tsv:43 logs the neighbouring sun-axis defect, not this one. `grep -rniE "rebake|snow line moves|schedule it" gaia/ --include=*.md --include=*.tsv` (excluding the audit) returns only sky-and-weather-state.md:248 (the body correction) and :337 (the stale row); `grep -c "schedule it"` on the target = 2, i.e. the retraction and the thing retracted. gaia/registers/corrections.tsv has no entry for this row, so no repair has been recorded. The magnitudes the failure scenario leans on are single-sited on this page and verified present: O(n⁶) Bruneton precompute at :44, 250 ms at :153 and :260.

### sky-and-weather-state.md · `SKY-4` · `stale` · gaia/references/sky-and-weather-state.md

**Narrowest true statement.** The document holds both halves of a retracted claim. At /home/user/skills/gaia/references/sky-and-weather-state.md:207 the analytic coverage form "buys three properties an integrated field cannot have", and the third of those, :217-221, is "**It does not force a whole-domain pass.** ... Advected coverage has the identical property [as driver-fields.md's unbounded advected precipitation], and an analytic one has neither problem." At :313-316 the same document retracts exactly that property: "an advected field's dependence over an interval `T` is `|W|·T` — **bounded, and therefore tileable with a halo that size** ... The analytic form still wins, on reversibility and farm-splittability, **not on tiling**." The three bullets at :211-221 map one-to-one onto that closing list (reversible slider / farm-splittable / tiling), so :316 enumerates two survivors where :207 promises three, and the third bullet is the residue the correction did not reach. The page cannot be read both ways; the corrective paragraph is the self-aware end (it opens by naming an earlier revision and saying "Both halves were wrong"), so :207 and :217-221 are the stale end.

The harm is constructible only for a reader who has chosen an **advected coverage buffer** — an option the page itself keeps live at :207-209 on cost grounds ("a cloud march pays N octaves of noise per sample where a buffer pays one fetch") — who then reads bullet three, concludes the dependence is unbounded, and follows the quoted precipitation remedy (whole-domain at coarse resolution, then upsample) for a node :313-314 says tiles with a |W|·T halo.

**Blast radius.** One site only for the claim itself. `grep -rn "whole-domain" --include=*.md --include=*.tsv .` returns exactly one occurrence of the coverage version, sky-and-weather-state.md:217 (plus :219 inside the quotation and :315 in the retraction); `grep -rn "advect" gaia/` shows no other document asserting that an advected coverage field cannot tile — volumetric-clouds.md (the contract's other end) mentions coverage only at :118 with no tiling claim, atmosphere-and-aerial-perspective.md and coverage.md:76 carry nothing on it, and the failure table at :323-338 has no row on tiling. `grep -rn "three propert\|farm-splittab\|reversibility and"` finds the count only at sky-and-weather-state.md:207 against :316. No row in gaia/registers/corrections.tsv covers sky-and-weather-state.md at all, so this correction is unregistered at both ends.

One adjacent end worth naming: driver-fields.md:365-368 is the source the stale bullet quotes, and it states the precipitation version flat and unqualified ("an advected precipitation field's does not [have a bounded search distance]"), with no |W|*T bound anywhere in that file. If the sky page adopts :313-316's bounded reading, driver-fields.md remains the unqualified end of the same idea for a neighbouring field. Magnitudes: 600 m (|W|*T at 10 m/s, 60 s) appears nowhere else in the corpus; 512^2 is the only tile figure in node-graph-runtime.md:336, and the "1-4 km" tile is the finder's, not the corpus's.

### sky-and-weather-state.md · `SKY-5` · `stale` · gaia/references/sky-and-weather-state.md

**Narrowest true statement.** Narrowest true defect: sky-and-weather-state.md:153-155 presents 250 ms and 5 s as one workload measured on two hardware generations ("the 2008 bake costs 250 ms on a 2016 GPU … the bake did not get slower"), while the corpus's own corrected reading of the same source — atmosphere-and-aerial-perspective.md:158-160, "`[Bru17b]` matters … It cites Bruneton's 2017 code release, not the 2008 paper, so the 250 ms measures that implementation on 2016-era hardware" — makes the 2016 figure a measurement of a different implementation. The key `[Bru17b]` occurs nowhere on this page (no body mention, no bibliography entry), and hillaire2020 §7, the figure's actual home, is outside this page's own hillaire2020 locator at :14 ("cited here ONLY for … 0.05 ms per Table 2 … Section 5.3"). That is a one-end-only propagation gap in an attribution, not a false number: hillaire2020 §7 does attribute the 250 ms to updating the Bruneton [BN08] model's LUTs on a 2016 GPU, so the sentence at :153 is under-qualified rather than untrue, and the fix is to add the key and section ("Bruneton's 2017 code release [Bru17b], measured by [hillaire2020] §7 at 250 ms on a 2016 GPU"), nothing more.

What fails in the finding as written: (a) "The hardware-progress conclusion is therefore drawn from two incommensurable artefacts, and 2008-vs-2016 GPU improvement is not what the 20x gap measures" — the page draws no hardware-progress conclusion and states no ratio; its stated conclusion is the negative "the bake did not get slower, the standard for what may sit off-frame got stricter", which an implementation swap cannot flip (it would have to make the 2008 paper's own bake cost >5 s on 2016 silicon). (b) "a 2017 reimplementation whose order count, resolutions and medium are not the 2008 paper's" is asserted with no support anywhere in this tree — the sibling's correction says only "that implementation on 2016-era hardware". (c) The second half of the proposed fix, "drop or requalify the 'the bake did not get slower' inference", is therefore unwarranted; only the citation half is.

**Blast radius.** Not one site only — four sites carry the 250 ms without [Bru17b], and exactly one carries it with:
1. gaia/references/sky-and-weather-state.md:153 (the finding's site) — "the 2008 bake costs 250 ms on a 2016 GPU".
2. gaia/references/sky-and-weather-state.md:260 — "that is precisely why it costs 0.05 ms instead of 250 ms", same implicit attribution of the 250 ms to the bake-every-sun-angle (2008) design. The finding names this site; it needs the same key.
3. gaia/references/atmosphere-and-aerial-perspective.md:9 — the hillaire2020 locator in the frontmatter of the very document that corrects this, quoting §7 as verbatim with the key deleted: "Updating all the LUTs using the code provided costs 250ms". The correcting document contradicts itself between its locator and its body.
4. gaia/references/papers-rendering.md:369-371 — the bibliography entry, introduced as "§7 verbatim", same deletion: "Updating all the LUTs using the code provided costs **250ms**".
5. gaia/registers/corrections.tsv:15 — refers in passing to "the 250 ms Bruneton bake" (a register note about a past state, lowest priority).
Only gaia/references/atmosphere-and-aerial-perspective.md:151-160 carries "[Bru17b]" inside the quotation and the correction that follows it. Sites 3 and 4 are the more serious end: both present the shortened text as verbatim, which is precisely the quotation-integrity defect source-findings.tsv:46 logs and whose adopted remedy ("a mechanical re-quote pass that diffs every marked-verbatim passage against its artefact") should have caught them and did not. Any fix that lands only at sky-and-weather-state.md:153 repeats the one-end pattern.
The other magnitude in the same sentence, the 5 s (sky-and-weather-state.md:146 and :154), is one document only — `grep -rn "5 seconds\|5 orders" --include=*.md --include=*.tsv .` returns no other gaia site, confirming the audit's separate SKY-6 note that the 2008 term of the comparison has no second end anywhere in the corpus.

### sky-and-weather-state.md · `SKY-6` · `stale` · gaia/references/sky-and-weather-state.md

**Narrowest true statement.** gaia/references/sky-and-weather-state.md:145-146 prints an unhedged, bolded wall-clock — "On 2008 hardware that bake was **5 seconds**" — and :154 re-presents it inside quotation marks as 2008's own "5 orders in 5 seconds", i.e. as the paper's own phrasing. No artefact in this tree states any precompute wall-clock for brunetonneyret2008. Three independent enumerations of the same §6 cost sentence omit it while recording its neighbours: this document's own locator at :10 ("section 6 ... for 125 fps at 1024x768 on an 8800 GTS with 0.4 ms and 2.6 ms term costs"), the corpus bibliography entry at papers-rendering.md:324-327 (same costs plus a secondary "25 fps with light shafts"), and the commit that introduced the sentence, 8655d0d, whose own verified-figures list names "the 8 MB packing, and the 125 fps on an 8800 GTS" — the other half of the very same sentence — and not the 5 s. The narrow defect is therefore citation hygiene, in two parts: (a) a P-tier wall-clock that no locator in the corpus declares and that a reader cannot check against anything; (b) quotation marks at :154 attributing a phrase to the 2008 paper that nothing in the tree shows it contains. NOT established, and where the finder overreached: (i) that the number is wrong or that a reader is misled in magnitude — from the page's own inputs (1,048,576 inscattering texels × 5 orders) an order-of-magnitude bake estimate on an 8800 GTS spans 0.6–11.8 s and straddles 5 s, so the "budgets a load-time bake and scales it forward" harm cannot be constructed; (ii) that this is "the failure mode the same document names in bold at :72-73" — that rule governs an RMSE from bruneton2017 Table 2 paired with a frame time from another paper, and the 5 s is neither an RMSE nor from Table 2 (the cross-paper pairing at :153-154 is SKY-5's subject); (iii) "unfalsifiable" is too strong — the source is declared READ IN FULL, so the figure has declared provenance; what it lacks is a locator declaring it and any second end in the corpus.

**Blast radius.** One site only. `grep -rn "5 seconds\|five seconds" gaia/references/ gaia/registers/ gaia/SKILL.md gaia/PLAN.md gaia/STATE.md` returns exactly two lines, both in the target file (:146 and :154); a repo-wide grep adds only unrelated hits (game-engine-guru/references/ASSET_PIPELINE_AND_COOKER.md:3, gauntlet-loop/references/cost-discipline.md:175, story-refinery). No other document states a 2008 precompute wall-clock in any form: the sibling's parallel 2008 crossover bullet, atmosphere-and-aerial-perspective.md:409-410, gives only "125 fps at 1024x768 on an 8800 GTS", and its brunetonneyret2008 locator at :10 says outright "The tables themselves and the precompute are sky-and-weather-state.md's subject, not this document's". So a fix here needs no propagation — the usual one-end-only risk runs the other way. The neighbouring magnitudes: "125 fps at 1024x768 / 8800 GTS" is stated at four sites (sky:146, sky:10, atmosphere:10, atmosphere:410, papers-rendering:326) and is locator-backed everywhere; "five orders" (the supported half of the :154 pairing) at sky:103 and papers-rendering:330; "250 ms" at sky:153, sky:257 and atmosphere:151-160 — that magnitude's defect is SKY-5's, not this one's. No corpus artefact (no PDF, no extracted text) of brunetonneyret2008 exists in the tree: `grep -rln "inria-00288758|Precomputed Atmospheric" .` returns only the three .md files that cite it.

### sky-and-weather-state.md · `SKY-8` · `unsupported` · gaia/references/sky-and-weather-state.md

**Narrowest true statement.** The Tier line at gaia/references/sky-and-weather-state.md:19-20 classifies "the cloud-shadow pass" as one of the bakes this page declares off-frame. No cloud-shadow bake is named anywhere in the document, and the page contradicts that classification three separate times: at :267-268 ("The instantaneous cloud shadow is a *rendering* term: it modulates sun visibility in the frame and nowhere else"), at :288-289 ("mean cloud fraction is a scalar scene parameter sitting beside the sun, **not** an edge from the cloud renderer into a bake"), and in the failure table at :331, where "An instantaneous cloud shadow multiplied into an authoring-time insolation bake" is the *mechanism of a defect*, not a prescription. The document's own inventory of graph-visible artefacts at :298-313 lists exactly two — the sky bake and the coverage field — and the cloud-shadow pass is neither. The defect is a regime misclassification only: the page prices no cloud-shadow pass, so no cost figure on it is wrong, and the harm is that the one line whose stated job (corrections.tsv:15 convention: "Every cost below is a per-frame cost ... unless it is named as a bake") is to separate in-frame from off-frame work puts a per-frame term on the off-frame side.

**Blast radius.** One site only. (i) All 38 Tier lines in the corpus (grep -rn "Tier: " --include=*.md gaia/): only sky-and-weather-state.md:19-20 carries a cloud-shadow clause. The two siblings with the same convention exclude different work — atmosphere-and-aerial-perspective.md:17-18 ("two load-time LUT bakes and the ambient probe's re-convolution cadence"), volumetric-clouds.md:23-24 ("the offline ones are the 2011 CPU render and the 34-hour path-traced reference"). (ii) grep -rni "cloud.shadow pass" over the whole tree returns sky-and-weather-state.md:20 and the audit file's own restatements (AUDIT-UNREFUTED-2026-09-09.md:403, :405, :407) — no third occurrence. (iii) Every other corpus site that touches the cloud shadow classes it as per-frame or leaves it unclassed, so none repeats the error: volumetric-clouds.md:23-24, :208-216, :301-302; papers-rendering.md:250, :271; atmosphere-and-aerial-perspective.md:335-337; coverage.md:76 and :92 (describe the cloud-shadow term with no regime claim). (iv) No register row records this clause: grep of gaia/registers/*.tsv finds corrections.tsv:15 (the atmosphere Tier line only) and source-findings.tsv:43-45 (sun-as-axis and the producer/consumer boundary), none of them this. The correction has landed at no end yet, so there is no one-ended correction to propagate.

### sky-and-weather-state.md · `SKY-9` · `unsupported` · gaia/references/sky-and-weather-state.md

**Narrowest true statement.** The defect is one clause at one site. `sky-and-weather-state.md:327`'s Fix cell attaches a provenance claim — hillaire2020's Table 1 ozone "coefficients and a tent-shaped profile, **crediting [bruneton2017]**" — that (a) no source in this corpus supports and (b) contradicts the same page's :126-128, "Of the five sources here only [preethamshirleysmits1999] models ozone", because [bruneton2017] is one of that five under BOTH available readings of "the other four sources here" (five-minus-Preetham = {bruneton2017, brunetonneyret2008, hosekwilkie2012, nishita1993}; six-P-minus-the-two-named = {brunetonneyret2008, hosekwilkie2012, preethamshirleysmits1999, nishita1993} — either way the cell asserts bruneton2017 supplies an ozone term the body says only Preetham has). Which half is the wrong one is narrower than the finder allowed: the corpus's only other read of that same table, `papers-rendering.md:348-350`, prints the row itself (σ_oa = 0.650, 1.881, 0.085·10⁻⁶ m⁻¹ plus a tent profile, quoted as "key to achieving sky-blue colors when the sun is at the horizon") and attributes it to hillaire2020 with **no** Bruneton credit, while independently re-asserting "Among the five *clear-sky model* sources it is the only one treating ozone". So "only Preetham" stands at two ends and the credit clause stands at one — it is the unsupported element. Separately and independently true: both :129 and :327 prescribe from hillaire2020 **Table 1**, which this document's locator at :14 does not declare — that locator says the source is "cited here ONLY for the contrast in what a sun move costs" and names the Fig. 4 caption, Table 2 and §5.3.

**Blast radius.** The "crediting [bruneton2017]" provenance clause: ONE SITE ONLY — gaia/references/sky-and-weather-state.md:327. It appears nowhere else in the repo (`grep -rn "crediting" ` and `grep -rn "Bru17"` across all top-level dirs return no second instance), and the corpus's other read of the same table contradicts it by omission.

The claim it collides with, "only Preetham of the five treats ozone", has FOUR sites, all mutually consistent: sky-and-weather-state.md:12 (locator, "the sole ozone model among these five"), :126-128 (body), :327's own Mechanism cell ("appears in [preethamshirleysmits1999] only on the direct beam"), and papers-rendering.md:346-348 ("Among the five *clear-sky model* sources it is the only one treating ozone"). Any repair must land at all four if the finder's alternative branch (change :127 to "Preetham and bruneton2017") is ever taken — this is the one-end-only risk in this finding.

hillaire2020's ozone content has TWO sites, and they disagree only on provenance: sky-and-weather-state.md:129 ("does carry ozone coefficients and a tent profile in its Table 1", no credit) and papers-rendering.md:348-350 (the actual row: σ_oa = 0.650, 1.881, 0.085·10⁻⁶ m⁻¹, tent profile, plus a direct quotation). The σ_oa triple appears at exactly one site (papers-rendering.md:349); the 0.650 hit in coastal-erosion.md:262 and registers/pseudocode-execution.tsv:204 is an unrelated coastal-diffusivity value.

The undeclared-Table-1 pattern repeats at the other end: papers-rendering.md's hillaire2020 entry (:360-380) declares §7 and Table 2 only, while its Table 1 ozone row is quoted from inside the preethamshirleysmits1999 entry. So a locator fix is owed at two sites, not one — sky-and-weather-state.md:14 and papers-rendering.md's hillaire2020 entry.

Consistent everywhere and needing no change: "brunetonneyret2008 contains no ozone" (sky-and-weather-state.md:10, :125-126, :327 Mechanism; papers-rendering.md:330).

### stratigraphy-and-lithology.md · `S1` · `wrong` · gaia/references/stratigraphy-and-lithology.md:164

**Narrowest true statement.** At gaia/references/stratigraphy-and-lithology.md:163-165 the generalisation `w = T / tan(angle between bedding and surface)` and `T ≥ 4·cellSize·tan(that angle)` is not the correct generalisation of the two cases it is built from. For a bedding dip δ and a surface slope σ in the same vertical plane (signed), the exact map-plane outcrop width of a bed of vertical span T is w = T/|tan δ − tan σ|; the page's form is off by exactly the factor (1 + tan δ·tan σ) on width, and by its reciprocal on the 4-cell gate, so it is exact only when tan δ·tan σ = 0 — i.e. only the σ = 0 and δ = 0 cases it was generalised from (plus the degenerate δ = σ, where both forms are undefined). Co-dipping bedding and surface make the gate too permissive, opposed tilts too conservative, and both regimes are reachable inside the page's own stated ranges (dip 0–30° at :98; hillsides 5–30° tabulated at :161-162): δ = 10°, σ = 30°, T = 150 m on 100 m cells passes the page's gate (145.6 m) while the true outcrop is 3.74 cells, below the page's own 4-cell aliasing threshold; ±30° opposed demands 692.8 m where 461.9 m suffices, 1.5× too conservative. The two `T/tan δ` uses elsewhere (:137/:154/:428) are the σ = 0 reduction and are correct as the page scopes them at :158; only the passage through "the angle between" at :163-165 is wrong.

**Blast radius.** One site only for the defective generalisation: gaia/references/stratigraphy-and-lithology.md:163-165 (the sentence spans those three lines; the finding cites :164). A corpus-wide grep for "angle between", "T / tan", "tan(that angle)", "cellSize·tan", "outcrop width" and "apparent dip" found no other file carrying an outcrop-width or aliasing-gate formula — the only other "angle between" hits are water-optics.md:150 and heightfield-lod.md:82, unrelated. Within the same file the dip-only form `T/tan δ` / `T ≥ 4·cellSize·tan δ` recurs at :137, :154, :295 and in the failure-table row at :428, but those are the σ = 0 reduction, which is exact and which the page explicitly scopes at :158 — they do NOT repeat this defect and need no correction under S1. Caveat for the fix landing at one end only: if :163-165 is rewritten to the |tan δ − tan σ| form, the failure-table row at :428 and the "Use this" gate wording still state the dip-only rule, so they should be re-read for consistency at the same time even though they are not themselves wrong. (Note :295's use of `T/tan δ` on a rig terrain that has hillsides is the subject of the separate finding S5, not S1.)

### stratigraphy-and-lithology.md · `S2` · `wrong` · gaia/references/stratigraphy-and-lithology.md:373

**Narrowest true statement.** At gaia/references/stratigraphy-and-lithology.md:373-374 the clause "The error is one-sided — it always over-resists" is false. The document's own sampler (:46-49) makes stratigraphic height s = h − U_cum fall monotonically at exactly the erosion rate, so in an alternating column every period crosses one hard→soft contact and one soft→hard contact, in equal numbers. Freezing K for the step gives signed error (discrete − exact) = (E_s − E_h)(Δt − d/E_start): negative at a hard→soft crossing (under-erodes; the case the sentence describes) and positive at a soft→hard crossing (over-erodes). At a soft→hard crossing the discrete step penetrates the resistant bed by exactly the authored contrast ratio E_s/E_h = 4× too far, for every offset d and every rate. The page's own failure row at :435 ("Thin beds erased entirely … the bed is skipped in one update") is that over-erosion case and is impossible under a one-sided over-resisting error. Only the mechanism clause fails: the measured Δt-dependence at :376-381 (conc 1.58→1.23, slope ratio 2.16→1.35) is untouched, as is the surrounding unconditional-stability argument at :358-369.

**Blast radius.** Three sites, all inside gaia/references/stratigraphy-and-lithology.md; no other file in the corpus carries the claim.

$ grep -rn "erodes the whole step\|whole step at the\|contact error\|over-resistance\|contact crossings" --include=*.md .
./gaia/references/stratigraphy-and-lithology.md:372-374  the prose claim ("The error is one-sided — it always over-resists")
./gaia/references/stratigraphy-and-lithology.md:429      failure table, Mechanism cell: "The coarse step was inflating it: one-sided over-resistance at contacts"
./gaia/references/stratigraphy-and-lithology.md:436      failure table, Mechanism cell: "One-sided contact error: a cell cutting through a contact erodes the whole step at the bed above's `K`", under the Symptom "Resistant beds look thicker than authored"
(the only other "one-sided" hits in the corpus are unrelated: shader-craft.md:448 and water-rendering.md:121 on a one-sided saturate clamp, gaia/PLAN.md:28/:162 restating those, and obsolete/terrain-architect/references/20-archetypes.md:123 on one-sided orographic precip.)

A fix landing at :373-374 only would leave :429 and :436 asserting the same false mechanism — this file's own pattern of a correction landing at one end. Note also that :436 is the site whose SYMPTOM depends on the one-sided reading, so it needs rewriting rather than a word swap; and :435, two rows above it, is the contradicting over-erosion case and is correct as written.

$ grep -rn "1\.0·10⁻³\|max(K·A\^m·S\^n)" --include=*.md . | grep -v AUDIT
./gaia/references/stratigraphy-and-lithology.md:387, :389, :390, :435 — the rate spread and the per-step-incision rule, all in the same file; nothing elsewhere depends on them.

### stratigraphy-and-lithology.md · `S3` · `wrong` · gaia/references/stratigraphy-and-lithology.md:292

**Narrowest true statement.** At gaia/references/stratigraphy-and-lithology.md:292-293 the superlative "the strongest scarp in the set" is false against the document's own results table, on the very metric the sentence cites. On `conc`, "Horizontal, 25 m beds, 16×" (:254) scores 2.29× against the dipped run's 1.85×; on `S_area`, "Horizontal, 100 m beds, 4×" (:255) scores 2.131 against 1.705 (:256). The dipped run is second, not first, on both scarp metrics. The document itself already crowns the other run at :270-271 — "the best packs more than twice the share of the trunk's descent into its resistant reaches" — and 2.29 is the only layered row above 2.0, while 1.85 is not. The narrowest true statement the table supports is scoped: among the 4× runs, Dip 10°/100 m has the highest `conc` (1.85×). A 4×-only reading of "the set" is foreclosed by the document's own parallel wording at :152-154 ("the weakest layered signal of any run in the set … and produce the strongest"), restated at :428 as "measured the weakest of any layered run", which fixes the comparison class as all layered runs.

Secondarily, and on the same lines, "dip helps" is one-sided for the exact pair the sentence compares (100 m beds, 4×, horizontal vs 10°): `conc` rises 1.45→1.85 (+27.59%) while `S_area` falls 2.131→1.705 (−19.99%), on the metric the same paragraph uses two lines later at :294 to name "the weakest". This half carries a real qualification the finder did not record: the ⚠️ at :260-264 flags all three metric columns as "the pre-correction record, not as settled", so the 20% figure is a pre-correction magnitude. It does not rescue the primary defect, because 1.85 and 2.29 come from the same frame, making the ranking comparison internally valid regardless of the frame's absolute accuracy.

The same superlative at :153-154 ("100 m beds at the same 10° … produce the strongest") fails identically.

Two finder nits that do not change the substance: (a) "the strongest on neither tabulated metric" — there are three tabulated metric columns, and on the third, `area_hard`, the dipped run is the table minimum (0.287), which is not a strength ranking, so "neither" is loose shorthand for the two scarp metrics; (b) the quote attributed to :272 actually sits at :270-271.

**Blast radius.** Two sites, both inside gaia/references/stratigraphy-and-lithology.md, and nowhere else in the corpus.

- :292-293 — "Dipping 100 m beds at 10° gives the strongest scarp in the set (`conc` 1.85× against 1.45× for the same beds horizontal) — dip *helps*." The primary site.
- :153-154 — "100 m beds at the same 10° give `w = 567.1 m` = **5.7 cells** and produce the **strongest**." The same superlative, same run, no metric named. A fix landing at :292 only would leave this end asserting it — this corpus's most-recorded defect shape.

No third site. `grep -rn "1\.85×|2\.29×|strongest scarp|Outcrop width dominates|dip \*helps\*"` across all *.md and *.tsv returns only :254, :256, :292, :293 plus the audit file. A corpus-wide grep for "strongest" in gaia/ returns no other scarp/dip superlative (remaining hits are driver-fields.md:82, water-closed-vs-open.md:146/148, layering-filters-and-masks.md:171, mask-to-material.md:213, wave-models.md:96/167, resolution-independence.md:225, water-rendering.md:304, volumetric-clouds.md:126, water-optics.md:76/308, thermal-and-aeolian-erosion.md:346/364 — all unrelated topics). tectonic-uplift.md:97 and coverage.md:54 repeat Gaia's "caprock, cuestas and mesas" mechanism sentence but make no strength comparison. The obsolete/ tree (terrain-architect 11-geological.md, 99-papers.md) discusses cuesta/hogback dip classes with no run-ranking claim.

Magnitudes involved, checked at every occurrence: 1.85× appears at :256 and :293 only; 2.29× at :254 only, plus an independent reproduction in gaia/registers/pseudocode-execution.tsv:187 ("trunk relief concentration 2.29x"); 2.131 and 1.705 at :255 and :256 only; 1.322/1.323 at :254, :258, :294. The related "weakest" half of the same paragraph has a third end at :428 in the failure table ("measured the weakest of any layered run") — that belongs to sibling finding S5, but it is the line that pins the comparison class for S3 and would need re-reading alongside any fix here.

Cross-end note: pseudocode-execution.tsv:187 records a "contrast 4 dipped 10 deg" run at S_area 1.005, conc 0.87× and calls it "the weakest of the set". Those figures match no row of the published table (the two dipped 10° rows are 1.705/1.85× and 1.322/1.21×), so the register and the document disagree about what the dipped 10° run measured. That divergence is outside S3's scope but is a second, unrecorded end worth a separate look.

### stratigraphy-and-lithology.md · `S4` · `wrong` · gaia/references/stratigraphy-and-lithology.md:279

**Narrowest true statement.** At `gaia/references/stratigraphy-and-lithology.md:279-280` the parenthetical "(the timestep table below, which drops it to 1.35 when `Δt` is halved)" misstates the refinement factor of the document's own table by 2×. The two numbers it cites, 2.16 and 1.35, are the `Δt = 2000` and `Δt = 500` cells of the "Channel slope ratio, resistant ÷ weak" row at :380 — a quartering of `Δt`, confirmed independently by the table header's own step counts (1000 → 4000 over the same 2 Myr). No `Δt = 1000` run exists anywhere in the file (the only step values that appear are 2000, 500 and 20 000), so nothing in the document measures a halved step. The word carries operational weight here because the same document issues "Halve `Δt`" as its prescribed convergence check at :396 and :429, and states at :394 that the measured requirement is `Δt ≤ 500` — i.e. two halvings, not one. Everything else in the sentence is sound: 2.16, 1.35 and 1.60 are correctly transcribed, and the restatement of the range at :426 ("measured 1.35–2.16× in slope") is correct because it attaches no refinement factor. Narrower than the finder in two respects: (a) the illustrative "a value of, say, 1.8" at `Δt = 1000` is not reproducible — no rig for this table is in the repo — so only the mislabel, not the intermediate value, is established; (b) the finder's proposed fix to :396 and :429 overreaches — "halve `Δt` and re-measure" is generic, correct refinement advice that makes no claim about reproducing the table, so only :279 needs the word changed to "quartered" or "at `Δt = 500`".

**Blast radius.** The "halved" mislabel is at one site only: gaia/references/stratigraphy-and-lithology.md:279-280. No other file repeats it.

Every other occurrence of the magnitudes is correct and carries no refinement-factor claim:
- :278 "It gives **2.16**" and :280 "**1.60** on `S_area`" — the values themselves, correct against :380 and the results table at :247.
- :380 the table row "| Channel slope ratio, resistant ÷ weak | 2.16 | **1.35** |" — the source of both numbers, correctly headed `Δt = 2000` / `Δt = 500`.
- :426 failure table "a 4× `K` contrast measured 1.35–2.16× in slope" — states the range only, attaches no timestep factor, so it is unaffected and needs no correction. (Worth flagging for the single-end-correction hazard: fixing :279 alone is correct here precisely because :426 makes no factor claim; do not "sync" :426 to a halving.)
- gaia/SKILL.md, STATE.md, PLAN.md: zero hits for 2.16, 1.35 or "halved".

Corpus-wide grep for the same shape of claim (a named quantity said to change "when Δt is halved"):
$ grep -rn "halved\|halving" --include=*.md . | grep -i "dt\|Δt\|step\|time"
Only unrelated and correct statements: gaia/references/simulation-time-budget.md:103 "halving the cell size quarters the timestep" (a correct dx² statement), shallow-water.md:161/:354, mask-operators.md:356, mesh-extraction.md:224, coastal-erosion.md:380, hydraulic-erosion.md:164, obsolete/terrain-architect/references/12-glacial-coastal.md:1729/:1856 ("Halve `Δx`, quarter `Δt`" — correctly paired). None mislabels a 4× step refinement as a halving.

The only other hits for the finding's text are the audit file itself (gaia/AUDIT-UNREFUTED-2026-09-09.md:457, :459, :483).

### stratigraphy-and-lithology.md · `S5` · `unsupported` · gaia/references/stratigraphy-and-lithology.md

**Narrowest true statement.** The causal clause at gaia/references/stratigraphy-and-lithology.md:294 — "Dipping 25 m beds at the same 10° gives the weakest (`S_area` 1.322), because their outcrop is 1.4 cells wide. It is not the dip that broke it; it is `T / tan δ` falling below the grid" — is not supported by the table it cites, on two counts that I reproduced. (1) The "Outcrop width" column prints the flat-surface w = T/tan δ, hence `∞` for every horizontal run (:252-255), a quantity the document's own :158-167 has already superseded with w = T/tan(angle between bedding and surface); on a rebuild of the document's own rig (128², 100 m cells, U = 5·10⁻⁴, K = 10⁻⁵, m = 0.5, n = 1, D = 0.02, Δt = 2000 × 1000 steps, 522 m relief against the document's 494 m) 25 m beds crop out at a median 0.803 cells horizontal against 0.770 cells at 10° dip — a 4% difference, not "∞ against 1.4" — with 84.3% of interior cells under the 1.418 cells the sentence blames. The tabulated 1.4 cells therefore describes neither the run it is attached to nor the contrast it is being used to draw. (2) The tabulated widths do not order the runs: the three 25 m / 4× runs whose measured widths agree to 4% (horizontal 0.803, 2° 0.805, 10° 0.770 cells) score S_area 1.597 / 1.408 / 1.322 and conc 1.58× / 0.92× / 1.21×, and the weakest layered run on conc (0.92×, the only one that fails to clear the controls decisively) is the run with the *widest* tabulated outcrop, 7.2 cells at 2°. No run in the document separates width from dip. The same attribution is stated at :152-154 and again in the failure table's Fix column at :428, so a fix at :294 alone leaves it standing twice.

**Blast radius.** One document, four sites — all inside gaia/references/stratigraphy-and-lithology.md, and a fix at :294 alone would leave the claim standing three times. :152-154 (the rule paragraph, same width-to-signal juxtaposition: "25 m beds at 10° ... 1.4 cells ... produce the weakest layered signal of any run in the set; 100 m beds ... 5.7 cells ... the strongest"); :248-258 (the "Outcrop width" column itself, `∞` on all four horizontal rows); :292-295 (the sentence under attack); :428 (failure-table row, which repeats the attribution as the justification for its Fix: "Outcrop width `T/tan δ` below the grid — 25 m beds at 10° are 1.4 cells on a 100 m grid, and measured the weakest of any layered run"). Related in the same file: :94 ("the binding constraint is the outcrop width, not the thickness"), which the measurement above also undercuts, and :158-167, which is the internal contradiction rather than a repeat. Outside this file: nothing repeats the claim or its magnitudes. `grep -rn "141\.8|567\.1|1\.4 cells|5\.7 cells|7\.2 cells|4·cellSize|Outcrop width|outcrop width"` over the corpus returns only this document plus registers/pseudocode-execution.tsv:206 (the sin->tan correction, which explicitly notes "The 1.4-cells headline figure survives either way (1.42 against 1.44 cells)") and an unrelated 141.8 in water-optics (Pope & Fry ratio, also at AUDIT-2026-09-05.md:278). registers/pseudocode-execution.tsv:187 carries a different measurement of the same run (S_area 1.005, conc 0.87x) asserted as "matching the outcrop-width argument"; AUDIT-2026-09-05.md:842 (X44) touches the neighbouring 1.323-vs-1.597 comparison, not this attribution. The obsolete/terrain-architect files mention outcrops only as landform vocabulary, with no width rule.

### stratigraphy-and-lithology.md · `S6` · `unsupported` · gaia/references/stratigraphy-and-lithology.md

**Narrowest true statement.** At gaia/references/stratigraphy-and-lithology.md:26 and :425 the DISCRETE implicit coefficient `f = K·Δt·A^m/dist` is printed with the locator [cordonnier2016] §3.1 eq. 1. In the paper itself (HAL hal-01262376, 10,771,411 B — the same artefact guard-proofs.tsv:335 records a verifier fetching), §3.1 "Geological Background" contains only the continuous law `dh(p)/dt = u(p) − k A(p)^m s(p)^n (1)`: zero occurrences of a timestep δt, of an inter-node distance ‖p_i−p_j‖, or of any implicit/discretised scheme. The printed expression is exactly the coefficient of §5 "Erosion" eq. (2), `h_i(t+δt) = [h_i(t) + δt(u_i + kA_i^m/‖p_i−p_j‖·h_j(t+δt))] / [1 + kA_i^m/‖p_i−p_j‖·δt]`, which sympy confirms is identical to stream-power.md:93-94's `f`-form under `f = K·Δt·A^m/dist`. The locator is misplaced at both sites; nothing else in the sentence fails — the physical claim ("`K` already reads per cell", "nothing downstream changes") is true and unaffected, so this is a citation-locator defect (severity `unsupported`), not a technical error. Two refinements to the finder's write-up, neither reducing it: (i) the generous reading, that §3.1 eq. 1 is cited for the claim rather than the formula, closes the escape rather than opening it — §3.1's own sentence is "The factor k is an erosion constant that depends on many factors, such as lithology…", i.e. eq. 1 writes k with NO point argument beside u(p), A(p), s(p), so §3.1 does not support "reads `K` per cell" either (it does support the document's frontmatter locator, "the term k that this document makes a field"); (ii) the proposed fix should cite §5 eq. (2) for the EXPRESSION only — the paper never prints the symbol `f` anywhere (0 occurrences of "f =" in the whole PDF); `f` is stream-power.md's own naming of the grouped coefficient.

**Blast radius.** Two sites, one document — gaia/references/stratigraphy-and-lithology.md:26 (body) and :425 (failure-table fix cell), exactly the pair the finding names. `grep -rn "f = K" --include=*.md --include=*.tsv .` returns only those two plus gaia/references/stream-power.md:93 (the pseudocode line itself, whose block is attributed at :78 and in frontmatter :10 to §5 eq. 2 — correct) and obsolete/terrain-architect/references/04-erosion-hydraulic.md:293 (attributed to "Braun & Willett (2013)", no cordonnier locator, and in obsolete/). Every other corpus site citing the implicit update points at §5 eq. 2 and is correct against the paper: stream-power.md:10 and :78, flow-routing.md:17, simulation-time-budget.md:13, resolution-independence.md:15, papers-flow.md:146. Every other site citing §3.1 uses it for the continuous law or for m/n — tectonic-uplift.md:9, resolution-independence.md:15 and :243, papers-generation.md:313, stream-power.md:11 and :41 — and all of those check out verbatim against §3.1 ("the ratio m/n is constrained by the shape of the stream profiles and is thought of being m/n ≈ 0.5 [WT99]. As in most geomorphological studies, we use n = 1 and m = 0.5"). So this is NOT a one-end correction risk in the usual sense — but note the two sites are :26 and :425, 400 lines apart, and a fix landing at the body only would leave the failure table (the surface a reader hits first) still pointing at §3.1; that is the corpus's own most-recorded defect shape (corrections.tsv:31). stratigraphy-and-lithology.md's frontmatter :9 also names only "§3.1 eq. 1 … §4.3 Lake Overflow", so applying the fix requires adding §5 eq. 2 to that locator too — a third edit site the finder did not mention.

### surface-and-scale-space.md · `F1` · `wrong` · gaia/references/surface-and-scale-space.md:40

**Narrowest true statement.** Narrowest true defect: :40, :101 and :110-111 compare the decimated and a-trous forms at equal LEVEL COUNT, which is not equal cutoff, and the page never states the level correspondence. At a = 0.4 the decimated analysis-and-synthesis applies the 5-tap kernel 2L times and the a-trous form L times, so the decimated low band carries exactly twice the variance (sigma ratio sqrt(2)). Measured, L=5: sigma 24.4696 vs 17.5186 cells, half-amplitude wavelength 133.1 vs 94.8 cells. So "35% cheaper in halo at L = 5" is the saving from a narrower filter, not from undecimation: an a-trous split matched to the decimated L=5 cutoff (dilations 1,2,4,8,16,16) has support radius exactly 94 against the decimated phase-rounded 96, i.e. 2.1%, not 35.4%. Because :414-421 anchors the user-facing cutoff to R(L), a reader who follows the warning at :37-42 and swaps forms at fixed L silently retunes the authored cutoff (R = 46 -> 30 cells at L=4; real half-amplitude cutoff 133 -> 95 cells at L=5) and the metres number printed in the UI goes stale.

WHAT FAILS in the finding, and it is the load-bearing half: the concrete failure scenario is INVERTED. The finder writes that after the swap "every feature between 377 m and 512 m leaves the low band, enters the residual, and is eaten by the surface operator -- the exact silhouette loss the document exists to prevent." The a-trous L=5 kernel is NARROWER (radius 62 vs 94, sigma 17.5 vs 24.5), so it smooths LESS and its low band RETAINS MORE at every wavelength. Measured low-band gain: 0.2468 -> 0.4953 at 377 m, 0.3770 -> 0.6137 at 450 m, 0.4721 -> 0.6870 at 512 m; the share handed to the residual FALLS by 21-25 percentage points across 377-532 m. Those features move INTO the silhouette, not out of it. Silhouette loss is the one harm the swap cannot cause; the real cost is the mirror image -- the operator is handed a narrower detail band, gets less mid-scale to rework, and the artist turns it up. The finder's "512 m" for the decimated cutoff is also loose: sinusoid-gain bisection puts it at 133.1 cells = 532 m (impulse-DFT gives 129.7).

**Blast radius.** Four sites, all inside gaia/.

1. gaia/references/surface-and-scale-space.md:40 — "it is 35% cheaper in halo at `L = 5`" (the prose claim).
2. gaia/references/surface-and-scale-space.md:101 — the table row "support radius, `L = 4` / `L = 5` | 46 / 94 px | **30 / 62 px**", the equal-L comparison itself. Both columns reproduce exactly on my rig, so the row is not wrong, only unlabelled as to cutoff.
3. gaia/references/surface-and-scale-space.md:110-111 — "`3·2⁵ = 96` px of halo against 62, so `(96−62)/96` = **35.4%**".
   (These three are the sites the finding's own header names. The finding did NOT flag a fourth:)
4. gaia/registers/pseudocode-execution.tsv:205 — the provenance row for the whole comparison. It repeats the same equal-L basis verbatim ("support radius 46/94 px decimated against 30/62 a-trous at L = 4/5") and its verdict cell still reads "the a-trous form needs **48% less halo at L = 5**" — the magnitude the document itself retracts at :111 ("an earlier version said 48%, which that table does not give"). So the register is both a repeat site for the equal-L basis and already STALE by 12.6 points against the document it backs. This is the one-end-only correction pattern the corpus records at pseudocode-execution.tsv:212 ("MY OWN CORRECTIONS KEEP LANDING IN ONE COLUMN AND NOT ITS SIBLING", which names "the a-trous storage figure survived in two places after being retracted").

No other file repeats it. `grep -rn "decimat"` across the corpus shows gaia/references/seamless-and-periodic.md:383 and :433 carry only the decimation-lattice phase rule, not the a-trous comparison; the obsolete/ hits are mesh-LOD decimation, a different subject. The finder's own magnitudes (sigma 24.47 / 17.52, radii 94 / 62 / 94) appear at one site only, this finding.

### surface-and-scale-space.md · `F10` · `incomplete` · gaia/references/surface-and-scale-space.md

**Narrowest true statement.** The failure-table row at `surface-and-scale-space.md:434` diagnoses the tile-seam quilt by a condition — "Halo is ≥ the support radius but **not a multiple of `2^L`**" — and prescribes a remedy — "Round the halo up to `3·2^L`" — that are both the aligned-grid special case of the rule the same document states generally at :296-299 and explicitly flags as insufficient at :302-306. On an offset tile grid the mechanism cell reads FALSE while the symptom is present, and the prescribed number is itself a failing halo. The string "origin" occurs nowhere in the failure table (:429-445), so a reader working from the table alone cannot reach the general condition `(tile_origin − halo) ≡ 0 (mod 2^L)`; the neighbouring row's "then round to the phase" (:435) names the phase without operationalising it. Reproduced at L=4, 64² tile, global origin 193: halo 48 (a multiple of 16, ≥ R=46) leaves 0.308295 m of max error against the whole-domain low band, unchanged at 64, 80, 96 and 112, while 49/65/81/97 are bit-exact 0.

NARROWING vs. the finder: (a) the Fix cell is not *wholly* the convenient form — it does append "fix the phase", so the reader is pointed at the phase, just given a number that does not fix it; the defect is that the only operational content in the row is the aligned-grid one. (b) The finder's proposed replacement mechanism, "(tile_origin − halo) is not ≡ 0 (mod 2^L)", drops the `halo ≥ R` half and must not be applied as written: at aligned origin 192 with halo 32, (192−32) mod 16 = 0 yet the error is 0.573471 m. The corrected cell needs BOTH conditions.

**Blast radius.** Not one site only — one further defective site in the same file, and one correct site elsewhere that must not be "fixed".

1. `gaia/references/surface-and-scale-space.md:426` ("Deeper is not safer" bullet): "Five levels on 512² tiles needs a **96 px halo**, so the build works a 704² region per tile — 47% of the pixels touched are halo." Same convenient form, stated as the required halo, with NO phase qualification of any kind. Reproduced failing on an offset grid (L=5, R=94, period 32, 64² tile at global origin 321): halo 96 → 0.252694 m, and identically 0.252694 m at 128 and 160; halos 97 and 129 → 0.000000. A correction landing only at :434 leaves this the remaining one-ended site.

2. `gaia/references/surface-and-scale-space.md:102` (à-trous vs decimated table): "halo needed | `3·2^L` **and** a phase rule | the radius, no phase rule". Prints the convenient number but explicitly says a phase rule also applies. Partial exposure, not a full repeat — worth a pointer, not a rewrite.

3. `gaia/references/surface-and-scale-space.md:17` (front matter): "a `3·2^L` halo that makes 47% of the pixels a five-level tiled build touches apron." A cost statement, not a prescription. Not a defect site.

4. `gaia/references/seamless-and-periodic.md:381-382` — the corpus's only other statement of the rule, and it is CORRECT and complete: "a *tiled* band split needs `halo ≥ 3·2^L − 2` **and** `(tile_origin − halo) ≡ 0 (mod 2^L)`, because the decimation starts at the sub-array's origin." Leave it alone; it is the model the :434 row should match.

5. Non-sites checked and cleared: `papers-masks-and-filtering.md:120` cites `R(L) = 3·2^L − 2` as a support radius against [paris2011]'s K, not as a halo prescription; `node-graph-runtime.md:437` gives generic support-radius halo advice (the "correct and not sufficient" claim :259-261 is aimed at); `seamless-and-periodic.md:433` is the periodic `N ≡ 0 (mod 2^L)` counterpart, a different quantity.

### surface-and-scale-space.md · `F2` · `wrong` · gaia/references/surface-and-scale-space.md:175-179

**Narrowest true statement.** The narrowest true defect is the MECHANISM sentence at :178 and the ENVELOPE it licenses at :177/:437, not the whole "property of your field" framing.

FALSE: ":178 Padding error is boundary error, so what it costs you is however much mass sits against the border." Holding the field fixed — the document's own full-width ramp, identical shape and identical mass against the border — and changing only the grid size from 256 to 257 moves reflect/edge from −11.22% to +34.10% and reflect/symmetric from −10.91% to +25.58% (swings of 45.3 and 36.5 points). That is larger than the entire field-to-field spread the page attributes it to: 13.60 points across four surfaces at N=256, 3.71 points at N=257.

The real driver is the parity of the L EXPAND target shapes ceil(N/2^k), k = 0..L−1, weighted toward the coarse levels where the 2-cell apron is a large fraction of the array: with the field held fixed, #odd=0 gives −12.58…−3.09% and #odd=5 gives +8.08…+34.10%.

UNDER-BOUND: the envelope "−15.0% to +2.2%" (:177) and the failure-table cell "Measured −15.0% to +2.2% at L = 5 depending on the field" (:437) are measured at 256², a size whose whole chain is even. On any grid whose chain goes odd — including the document's OWN worked field, 257² (:60, :196) — the positive excursion is 4x to 17x the printed +2.2% ceiling: 257² L=5 gives +34.10/+36.27/+35.96% (edge) and +25.58/+27.14/+26.93% (symmetric), and at the document's own L=4 gives +15.61/+16.72/+16.44% and +12.01/+12.83/+12.64%.

NOT IN DISPUTE (the finder does not attack it, and it is true): "and so is the sign" — at fixed N=256 reflect/edge measures +1.76% on smoothed noise against −11.22% on the ramp, so the sign genuinely is field-dependent.

WHERE THE FINDER OVERREACHED (why PARTIAL, not CONFIRMED):
1. Its proposed replacement law — "the magnitude is governed by whether the domain divides the decimation chain (N ≡ 0 mod 2^L)" — is not the law. On one fixed field at L=5, N = 248 (−1.88%), 254 (−9.07%) and 255 (−10.16%) are all ≢ 0 (mod 32) yet sit in the same negative regime as the N≡0 sizes (−12.58…−3.09%). N ≡ 0 (mod 2^L) is exactly the condition that makes every EXPAND target even, so it is SUFFICIENT for safety, not necessary, and it is not what "governs" the magnitude.
2. Its concrete failure — "a build on the canonical Burt grid — 1025² … measures +34%, an order of magnitude over" — is wrong by ~4x. At 1025² I measure +8.08/+8.17/+9.08% at L=5 and +3.81/+3.88/+4.28% at L=4. The excursion is an apron-fraction effect and decays roughly as 1/N: 257² → +34%, 513² → +16%, 1025² → +8%. The failure still constructs at 1025² (positive, ~4x the printed ceiling), but only 257² carries the +34%.
3. Two supporting figures do not reproduce: "under one point of field-to-field spread" at N=257 is 3.71 points on my four surfaces, and "stays in −0.79%…+2.84% at every N ≡ 0 (mod 32)" does not hold — the same fixed field at those N gives −12.58…−3.09%.

**Blast radius.** Two sites, both in gaia/references/surface-and-scale-space.md, and they must move together:
  - :175-179 — the body warning ("they are a property of your field", the −15.0% to +2.2% spread, and the "however much mass sits against the border" mechanism).
  - :437 — the failure-table cell "Measured −15.0% to +2.2% at `L = 5` depending on the field".

No other reference file in the corpus repeats it. Greps run:
  grep -rn "property of your field\|depending on the field\|property of the field" --include=*.md --include=*.tsv .
    -> surface-and-scale-space.md:175, :437; pseudocode-execution.tsv:183; the audit file itself. Nothing else.
  grep -rn "15.0% to +2.2%" ... -> surface-and-scale-space.md:177, :437; pseudocode-execution.tsv:183.
  grep -rn -- "15\.01|9\.94|10\.91|11\.22|2\.22%" ... -> only surface-and-scale-space.md:165-168 (the table those numbers come from) plus the register row. The single outside hit, atmosphere-and-aerial-perspective.md:243 ("2.22% vertically"), is froxel jitter and unrelated — and pseudocode-execution.tsv:214 already records that exact substring as a past false-positive regex collision.
  grep -rln "zero-interleaved|EXPAND|Σ(lo)" ... -> papers-masks-and-filtering.md:105 only defines Burt eq. (3)/(4); it carries no padding magnitudes.

gaia/registers/pseudocode-execution.tsv:183 is NOT a propagation site — it is the source of the correction, dated 2026-09-04, and it already says "The effect is a PARITY effect in N, not a property of the field -- holding the field fixed and varying only N, reflect/edge moves from +1.8% at 256 to +34.1% at 257". My rig reproduces that row's fBm figures to 0.1 pt (+28.31% symmetric / +37.81% edge at N=257 against its +28.4% / +37.9%). The document is dated 2026-09-03; the correction never landed in it. That register row's own numbers must stay consistent with whatever lands at :177 and :437.

Relevant to this corpus's most-recorded defect: corrections.tsv:152 records the 22%→42% fix on this same file at :336 precisely because a correction had landed at one end only. The same trap is live here — :177 and :437 are two ends of one claim, and :437 is the one a reader consults mid-symptom.

### surface-and-scale-space.md · `F3` · `wrong` · gaia/references/surface-and-scale-space.md:187-191

**Narrowest true statement.** At gaia/references/surface-and-scale-space.md:190-191 the parenthetical "(On an odd-length array `wrap` does invert; `EXPAND` does not make one.)" is false under the document's own definition of EXPAND. `expand(g, shapes[L-k-1])` (:50, :130) zero-interleaves into the target shape taken from the REDUCE chain, so on the document's own headline 257x257 field (:60, :196) the chain is 257->129->65->33->17->9 and every EXPAND target is odd (17, 33, 65, 129, 257); the interleaved array ends on a sample, and a two-cell left `wrap` apron on it is [0, sample] — exactly the inverted parity the same parenthetical concedes. Consequently "So `reflect` and `wrap` are the two structurally right pads" (:191) and the unqualified "`reflect` or `wrap`; at `L = 5` either is under 0.2%" in the :436 fix cell hold only when every EXPAND target is even (N ≡ 0 mod 2^L) — which the periodic rule at :334 forces but an open tile of arbitrary size does not. Measured (L=5, reflect REDUCE, my rig, which reproduces the document's own ramp column at 256² to 4 decimals): at 257² `wrap` EXPAND gives Σ(lo)/Σ(h)−1 of +24.32% (fBm), +25.96% (smoothed noise), +25.58% (full-width ramp) against `reflect`'s −0.007% / +0.006% / +0.000%; at 1025² it gives +5.87% (fBm) and +6.14% (ramp). What FAILS in the finder's write-up is the arithmetic of its concrete failure scenario, not its claim about the page: on the 1025² build it names, `wrap` costs about +6%, not "a quarter of the field's volume" (the ~25% figure is the 257² measurement — the error is boundary-confined and scales roughly as 1/N), and the sign is a GAIN in the low band, not a loss. Also confirmed as stated: Σ(lo)/Σ(h) cannot distinguish `wrap` from `symmetric` — the two are numerically identical in all 12 of my cases, so the document's `symmetric` figures at :165 are its unstated `wrap` figures.

**Blast radius.** One site for the false claim itself. `grep -rn "parity-correct|does not make one|\[40, 0\]|structurally right"` over the whole corpus returns only gaia/references/surface-and-scale-space.md:187-192 (and the audit file quoting it). The unqualified downstream reliance is in the same file: the :436 fix cell "`reflect` or `wrap`; at `L = 5` either is under 0.2%", which covers open tiles of any size (that cell's 0.2% bound is separately attacked by F6, and its "-15.0% for a zero-padded REDUCE" by F5, so :436 carries three findings). :437 is NOT a repeat — it already states the correct scoping, "`reflect` on an open tile, `wrap` on a periodic one". Outside this file the claim does not propagate: gaia/references/seamless-and-periodic.md:358-385 recommends `wrap` only for a periodic domain and carries the matching "period a multiple of 2^L" condition, which is exactly the regime in which `wrap` is parity-correct; `grep -rn "pad" gaia/references/ | grep -i wrap` outside these two files returns nothing, so no other document recommends a pad mode for a band split. The magnitudes: +24-26% at 257² appears nowhere in the corpus text (the register at pseudocode-execution.tsv:183 records the sibling odd-grid figures +28.4% symmetric / +37.9% edge / +34.1% edge-on-fixed-ramp, and notes the parity-in-N mechanism, but no `wrap` row exists in the document's padding table at :162-168 or anywhere else — the correction landed in the register on 2026-09-04 and never reached the page).

### surface-and-scale-space.md · `F4` · `wrong` · gaia/references/surface-and-scale-space.md:273

**Narrowest true statement.** At :273 the fallback "if you cannot [align the phase], size from `2^(L+2) − 4`" prescribes a halo congruent to −4 (mod 2^L). For L ≥ 3 it therefore satisfies the section's own phase condition `(tile_origin − halo) ≡ 0 (mod 2^L)` only when `tile_origin ≡ −4 (mod 2^L)`; on the normal aligned grid (`tile_origin ≡ 0`) it never does. It is thus strictly larger and strictly worse than the `3·2^L` the same section recommends 25 lines later: at aligned origin 256, 64² tiles, `3·2^L` measures 0.000000e+00 m at L = 3/4/5 while `2^(L+2) − 4` measures 1.38 / 1.85 / 1.99 m and costs 17% / 25% / 29% more halo. Nor can a bigger halo recover it: once halo ≥ R(L) the tile error is exactly invariant to halo size within a residue class (0.40603714525491341 m at halos 48, 64, 80, 96, 112 at origin 193 — identical to 17 significant digits), so what fixes a misaligned tile is a halo in the *right residue class*, not a larger one. At L = 2 the defect vanishes, since `2^(L+2) − 4 = 12 = 3·2^L`.

WHAT FAILS IN THE FINDER'S WORDING (hence PARTIAL, not CONFIRMED): "No halo fixes a misaligned phase" is false. A halo does fix it — one congruent to the origin mod 2^L. The document itself prints this at :302 ("bit-exact at halo **49** and **65**") and my rig reproduces it exactly (0.000000e+00 m at halos 49 and 65, origin 193). The same overreach is carried into the proposed fix ("no halo works — ... or align on (tile_origin − halo)"), which contradicts itself. The true statement is the narrower one: no *increase* in halo fixes it. Separately, "the fallback is inert" understates the defect — it is worse than the halo it replaces, not merely equal to it. The numbers 5.90 m / 6.98 m / 1.700560 m are field-specific and not reproducible (the finder's field is unstated); mine gives 1.393 / 1.719 / 0.406037 m on the same geometry, and the structure is identical.

**Blast radius.** ONE SITE ONLY for the defective prescription. `grep -rn "2^(L+2)" --include=*.md --include=*.tsv .` returns exactly two non-audit hits, both in this file: :272 (the measurement "the maximum over all `2^L` phases measures `2^(L+2) − 4` — 12, 28, 60, 124 px at `L = 2…5`" — correct, I reproduced it exactly) and :273 (the prescription — the defect). `grep -rn "if you cannot\|cannot align\|align the phase\|round to the phase\|up to the phase"` across the corpus returns :273 as the only occurrence of the fallback; the other gaia hits (:308, :435) carry the correct advice.

NEAREST RELATED SITE, endorsement not repetition: gaia/registers/pseudocode-execution.tsv:184 closes "R(L) is the retained-phase radius; the worst case over an unaligned impulse is 2^(L+2) - 4. The document now states both and **says which to size from**." The register blesses the practice at :273 without printing the fallback number as advice, and its own next row (:185) contains the measurement that refutes it (halo 60 wrong by 9.40 m at aligned origin 192, halos 48/64/80/96/112/128 all bit-exact). So a correction to :273 should also correct the closing note on register row 184 — this is the "correction landing at one end only" pattern.

SITES CARRYING THE CORRECT RULE (contradiction sites, would need no change): surface-and-scale-space.md :296-299 (rule box), :302-303 (origin-193 counter-example), :308, :435; gaia/references/seamless-and-periodic.md:382 ("band split needs `halo ≥ 3·2^L − 2` **and** `(tile_origin − halo) ≡ 0 (mod 2^L)`" — states the rule with no fallback number); gaia/references/papers-masks-and-filtering.md:120 and surface-and-scale-space.md:277-279 carry only `K = 3(2^(l0+2) − 1) = 2·R(l0+1) + 1`, a different quantity. gaia/references/node-graph-runtime.md:374 and :437 discuss halo = support radius generically and never mention the pyramid phase rule or the fallback.

MAGNITUDES: 60 px (L=4) and 124 px (L=5) appear only at :272-273 and tsv:184. The competing magnitudes 46/94 px (R(L)) and 24/48/96 px (3·2^L) appear at :17, :101-102, :268, :270, :296-299, :419, :434-435 and seamless-and-periodic.md:382 — all consistent, none affected by this correction.

### surface-and-scale-space.md · `F5` · `wrong` · gaia/references/surface-and-scale-space.md:436

**Narrowest true statement.** At gaia/references/surface-and-scale-space.md:436 the fix cell reads "`reflect` or `wrap`; at `L = 5` either is under 0.2% against −15.0% for a zero-padded `REDUCE`". The document's own padding table assigns −15.01% to the row `REDUCE`=`reflect` / `EXPAND`=zero (:167) — a zero-padded EXPAND — while the zero-padded-REDUCE row (:168, `REDUCE`=zero / `EXPAND`=`reflect`) measures −9.94%. The cell therefore attaches the −15.0% figure to the wrong operator, contradicting the table two hundred lines above and the section's own boldfaced thesis at :159-160 ("`REDUCE` and `EXPAND` pad different things … and the second is where the volume goes"). The narrowest fix is the finder's: "…against −15.0% for a zero-padded `EXPAND` (−9.9% for a zero-padded `REDUCE`)". (Scope note: the "−15.0% vs −9.94%" gap a reader would measure is specific to the 256² grid the table is stated on at :176; the mislabel itself is an internal contradiction independent of grid.)

**Blast radius.** One site only — gaia/references/surface-and-scale-space.md:436. `grep -rn -- "15\.0\|15\.01\|9\.94\|zero-padded\|zero pad"` over the repo returns no other place that attaches −15% to REDUCE. Specifically: gaia/registers/pseudocode-execution.tsv:183 records the pairs correctly in REDUCE/EXPAND order ("reflect/reflect -0.15%, reflect/symmetric +0.02%, reflect/edge +2.22%, reflect/zero -15.01%, zero/reflect -9.94%"); :177 and the adjacent failure row :437 quote the envelope "−15.0% to +2.2%" with no operator attribution (and :437's mechanism column correctly attributes the mechanism to EXPAND padding a zero-interleaved array); gaia/references/seamless-and-periodic.md:363 mentions zero padding asserting sea level one cell outside the field but attaches no magnitude and names no operator. No other gaia file mentions `REDUCE` at all.

### surface-and-scale-space.md · `F6` · `wrong` · gaia/references/surface-and-scale-space.md:436

**Narrowest true statement.** At gaia/references/surface-and-scale-space.md:436 the failure-table fix cell states an unqualified bound — "`reflect` or `wrap`; at `L = 5` either is under 0.2%" — on Σ(lo)/Σ(h) − 1, and the document's own padding table at :164 contradicts it for `reflect`: reflect REDUCE / reflect EXPAND measures −1.03% on the full-width ramp at L = 5, 256², about 5× the stated bound (independently reproduced: −1.0248% at 256², −0.2557% at 512²; the doc's own body at :178 also prints the ramp envelope as "−1.0% to −11.2%"). 0.2% is the smoothed-noise column figure (−0.15%) generalized to all fields. Two narrowings the finder did not state: (a) the violation is grid-size dependent — at 1024² the same ramp gives −0.0639%, under the bound — so the bound fails at the document's own measurement size and at 512², not universally; (b) the `wrap` half is unsupported rather than false — the document prints no Σ(lo)/Σ(h) number for `wrap` anywhere (grep: only the seam-step ratios at :322), and measured, `wrap` at both operators on N ≡ 0 (mod 2^L) conserves the sum exactly (+0.0000e+00), so it does satisfy "under 0.2%", while reflect REDUCE + wrap EXPAND gives −10.9129% on the ramp and wrap/wrap at N = 257 gives +25.58%.

**Blast radius.** One site only. `grep -rn "0\.2%" .` across the corpus returns the :436 cell plus four unrelated uses (atmosphere-and-aerial-perspective.md:12 and :214 and papers-rendering.md:388 — the fullscreen-triangle blog figure; water-rendering.md:213 — sigma_A truncation error). `grep -rn "under 0\.2\|either is under" .` matches only surface-and-scale-space.md:436 and the audit file's own quotation of it. The underlying correct figures are repeated in three places and are NOT wrong there: :164 (-1.03% table cell), :178 (ramp envelope "-1.0% to -11.2%"), and registers/pseudocode-execution.tsv:183 ("-1.0% to -11.2% (full-width ramp AND corner wedge, all negative)"). seamless-and-periodic.md:362-374 cites this document's padding recommendation but quotes only the seam-step table, no volume bound. So the correction lands at :436 alone — with the caveat that :436 is the same cell F5 corrects (the -15.0% REDUCE/EXPAND mislabel), so both edits must land together.

### surface-and-scale-space.md · `F7` · `stale` · gaia/references/surface-and-scale-space.md

**Narrowest true statement.** At gaia/references/surface-and-scale-space.md:409-410 the file name in the pointer is wrong and nothing else is. `tomasi1998` and `he2010` are not in `papers-generation.md` (0 occurrences of tomasi/he2010/bilateral/guided across its 56 entries); they are in `papers-masks-and-filtering.md` at :192 and :195, both at tier `P` — the tier, the two ids, and "neither was read for this document" are all correct as printed. The severity label `stale` is exact: papers-masks-and-filtering.md:64 states that the entries in its `## Terrain analysis and edge-aware filtering` section "came from `papers-generation.md`", and papers-generation.md's own front-matter description records "Terrain analysis and edge-aware filtering moved to papers-masks-and-filtering.md", so the pointer was once correct and was not updated when the split happened. One narrowing of the finder's failure scenario: a reader who opens papers-generation.md does find a forwarding note (its front-matter `description`, and index.md:86-87 lists the two files as siblings), so the next author is one hop from the entries rather than stranded — the sentence is false, the reader is not lost. Fix is the one-word one the finder proposes: `papers-generation.md` → `papers-masks-and-filtering.md`.

**Blast radius.** One site only. `grep -rn 'papers-generation' gaia/` returns non-audit prose hits in exactly five reference files, and only gaia/references/surface-and-scale-space.md:410 asserts that the filtering entries live there. The other four are correct: papers-masks-and-filtering.md:64 (records the move out of it), index.md:86 (bibliography table row, scope description only), papers-architecture.md:155 and papers-coastal-and-ice.md:122 (both cite papers-generation.md only as the model for a McGetchin citation form). The same two papers are cited elsewhere in the corpus without a wrong locator: gaia/references/terrain-analysis-masks.md front matter :16-17 lists he2010 and tomasi1998 at tier P with locators and no file pointer, and its body cites them at :251-252 — nothing to correct there. Registers carry no row repeating the claim (no hit for tomasi1998/he2010 in gaia/registers/). Note for the one-end-only failure mode: the fix is a single filename edit at :410 with no partner site to keep in sync, so there is no second end to miss.

### surface-and-scale-space.md · `F8` · `unsupported` · gaia/references/surface-and-scale-space.md

**Narrowest true statement.** At :419-420 the printed "roughly 370 m" is 2 x the 46-cell support radius (368 m), and support diameter is not a cutoff. The measured amplitude response of the L=4 low band at that wavelength is H = 0.710, so the low band still holds 71% of a 370 m feature and the residual gets 29% — "hands the operator everything narrower than roughly 370 m" misdescribes what the operator actually receives. For a complementary split (hi = h - lo, residual transfer 1 - H) the operationally meaningful boundary is H = 1 - H = 0.5, measured at 64.68 cells = 258.7 m by retained-phase impulse DFT and 66.5 cells = 266 m by phase-averaged sinusoid. For a shaped feature the gap is far larger than for a sinusoid: a 370 m FWHM Gaussian ridge keeps 95.5% of its peak height in the low band, a 300 m ridge 93.4%. The support-diameter rule the sentence teaches also does not generalise across levels: lambda(-3 dB)/2R runs 1.216 / 1.105 / 1.036 / 0.999 / 0.980 / 0.970 at L = 1..6, so it lands only at the one level the document happens to print. The same 370 m is repeated at :423, which the finding does not cite.

NOT sustained, and this is where the finder overreached: "258 m, not 370 m" is too absolute. 370 m is within 0.7% of the classical -3 dB (half-power) wavelength of the very same equivalent kernel — measured 91.90 cells = 367.6 m — so the printed number does match a named textbook convention, and the finding neither states that its own 258 m is the H = 0.5 (equal-split) convention nor says why -3 dB is the wrong one here. The reason it is wrong is derivable and worth putting in the fix rather than the bare number: the split is not power-complementary (at H = 0.7071 the low band keeps 50.4% of the power while the residual gets only (1-H)^2 = 8.4%, the remainder being the cross term), so a half-power cutoff describes nothing either band receives. Also not sustained: "90% of its mass inside 16 cells" — measured 82.3% within +/-16 cells; 90% is within +/-20. And the proposed fix's "128.0 at L=5" is 129.67 measured, while sigma ~ sqrt(0.6(4^L - 1)) runs 1.3% high (12.369 vs 12.209 measured at L=4, 24.775 vs 24.470 at L=5).

**Blast radius.** The metres-cutoff claim is confined to one file but TWO sites, and the finding cites only one of them. `grep -rn "370" --include=*.md --include=*.tsv .` returns, outside the audit file itself, only gaia/references/surface-and-scale-space.md:420 ("hands the operator everything narrower than roughly 370 m") and :423 ("protecting features narrower than 370 m protects things nobody will ever silhouette"). The finding's locator is :418-421, so :423 is an uncited repeat — and it states the OPPOSITE direction from :420 (:420 says narrower-than-370 goes to the operator, :423 says narrower-than-370 is protected), an internal inconsistency in the same bullet list that any fix landing at :420 alone would leave standing. This is exactly the corpus's most-recorded defect shape, already self-registered at registers/pseudocode-execution.tsv:212 ("MY OWN CORRECTIONS KEEP LANDING IN ONE COLUMN AND NOT ITS SIBLING").

`grep -rn "184 m"` outside the audit: only :419 here, plus impact-craters.md:155 (an unrelated crater-depth table cell). No other page in the corpus converts a support radius into a metres cutoff, and `grep -rn "narrower than\|wider than\|Detail Size"` finds no other site making this conversion.

The underlying R(L) = 3*2^L - 2 propagates widely but is NOT touched by this finding — every other site uses it as a halo radius, which is its correct use: surface-and-scale-space.md:17, :101-102, :270, :296-298, :434, :435; papers-masks-and-filtering.md:120 (K = 2*R(l0+1)+1); seamless-and-periodic.md:382; node-graph-runtime.md:374, :437; registers/pseudocode-execution.tsv:184 (which confirms R(L) as the support radius by impulse and says nothing about a cutoff). No register row anywhere derives a metres cutoff from R(L), so the 370 m has no provenance row at all — consistent with the `unsupported` label.

### surface-and-scale-space.md · `F9` · `unsupported` · gaia/references/surface-and-scale-space.md

**Narrowest true statement.** surface-and-scale-space.md prints two different series for one named quantity — the decimated low band's drift at 1/2/4/8 px shifts — and labels neither: 2.23 / 4.35 / 8.02 / 12.35 m at :103 (table row, no level, no field, no relief, no statistic) and 0.45 / 0.89 / 1.72 / 2.72 m at :340 (level implied L=4 by "returned to zero at the full period of 16", but no field, relief or statistic). The failure table at :445 hands the acting reader the smaller, corpus-unprovenanced number as "measured 2.72 m at an 8 px shift", while the symptom cell of that same row calls the drift "sub-metre". The 4.5×–5.0× gap is not a level effect (level moves the 8 px drift by 0–41% in my rig, never 4.5×) and cannot be a relief rescale of one run (drift is exactly homogeneous of degree 1 in the field, so a rescale holds the ratio constant, while the printed ratio falls 4.9556 → 4.5404). Narrower than the finder: the two series ARE reconcilable as peak |Δ| and mean |Δ| of a single run — my sweep produces a max/mean profile of 4.923/4.787/4.777/4.634 against the printed 4.956/4.888/4.663/4.540 — so the defect is the missing statistic-and-field label at both sites, not a demonstrably wrong number at either.

**Blast radius.** Three sites, all inside gaia/references/surface-and-scale-space.md, plus one register row. (1) :103 comparison-table row — 2.23/4.35/8.02/12.35 m, no level/field/relief/statistic. (2) :340 body — 0.45/0.89/1.72/2.72 m, level implied L=4, no field/relief/statistic; this line is the sole occurrence of that series in the entire corpus. (3) :445 failure-table row — reprints 2.72 m as "the" measured 8 px drift, and its own symptom cell contradicts it with "drifts sub-metre". (4) gaia/registers/pseudocode-execution.tsv:205 carries the larger series verbatim ("drifts 2.23 / 4.35 / 8.02 / 12.35 m decimated") and states no field, relief or level for it either, so the register cannot settle the conflict. Related but not a repeat: :39 ("the decimated pyramid drifts metres") is the qualitative form and is consistent with both series; it is the sentence that would go stale if the numbers were corrected at only one end. No other document repeats either series or the drift claim — grep for the number strings across the whole corpus and for "trous|undecimated" corpus-wide returns only these files (the book-writer hits are false positives on "monstrous"/"atrocity"), and gaia/SKILL.md, seamless-and-periodic.md and papers-masks-and-filtering.md mention the band split without any drift magnitude.

### terrain-analysis-masks.md · `TAM-2` · `wrong` · gaia/references/terrain-analysis-masks.md:176

**Narrowest true statement.** At terrain-analysis-masks.md:177-179, "**Hours is reachable, but only at a far heavier configuration**: … about a hundred times the work of the 4k × 16 case above" is contradicted by the document's own preceding sentence (:175-176). Holding grid, step count, algorithm and machine fixed at the document's own baseline — naive marching, 4096² × 1000 steps, one core — one hour is crossed at N ≈ 21 azimuths (1.34× the 16-azimuth work) at 10⁸ samples/s and at N ≈ 215 (13.4×) at 10⁹ samples/s. So ~100×, the only quantity the sentence supplies for "far heavier", overstates the work needed to reach one hour by 74× at the slow end of the document's own rate range and by 7.4× at the fast end; applied to that same baseline it yields 74.6 h or 7.5 h, never the "1–2 hours" the sentence points at. The slow end is the anchored one: the paragraph's own "the 'hours' end was an order of magnitude out" only coheres there (10 × 44.7 min = 7.5 h reads as "hours"; 10 × 4.47 min = 45 min does not), and at that end N = 32 — dozier2022's own timing-run count — already gives 1.49 h at 2.00× the work.

Two parts of the finding as written fail. (1) "no rate for either" is false for the CPU half: line 176 states "10⁹ to 10⁸ samples/s" for the baseline; only the GPU side is rateless. (2) "wrong by a factor of roughly 50" is the slow-endpoint figure alone (74× for one hour, 37× for two); at the fast endpoint of the document's own stated range the overstatement is 7.4×, which the finding never mentions. "the slow end the document itself chooses" also misdescribes a stated two-ended range as a choice, though the "order of magnitude out" clause does independently anchor it there.

**Blast radius.** The defective inference itself: ONE SITE ONLY — terrain-analysis-masks.md:177-179. `grep -rn "hundred times\|hundredfold\|46 Mcell\|580 direction\|4.5 to 45\|2.684"` over the whole checkout returns only that line plus the audit file, and one unrelated hit (sky-and-weather-state.md:212, "a slider nudged two hundred times an hour"). The 2.68·10¹¹ / "4.5 to 45 minutes" baseline likewise appears at exactly one place, terrain-analysis-masks.md:175.

The source magnitudes it leans on appear twice more, but WITHOUT the defective inference, so no correction is owed there: driver-fields.md:140 (table row "Per-point DDA ray march [stendardo2020] | GPU (CUDA) | ≈ 46 Mcell, **580 directions** | **1–2 hours per tile**") and driver-fields.md:431 (failure row "The occlusion bake takes hours | Per-cell, per-direction ray marching — 1–2 hours per tile on a GPU against ~2 s per azimuth for the sweep on CPU"). Neither states a work multiple or claims hours requires one.

THIS IS A ONE-END-ONLY CORRECTION, the pattern flagged as this corpus's most-recorded defect — but in the reverse direction from TAM-3's. The caution that would have prevented :178-179 already landed in driver-fields.md and never reached terrain-analysis-masks.md: driver-fields.md:142-145 ("Those two rows are not a like-for-like comparison, and reading them as one is a trap this document nearly set … The apparent thousandfold gap is the **direction count**, not the algorithm") and driver-fields.md:188-191 ("A number quoted without saying what consumes it is meaningless"). terrain-analysis-masks.md:178-179 performs precisely the cross-row reading driver-fields.md flags.

Within terrain-analysis-masks.md the defect does NOT propagate: the failure-table row at :277 ("AO bake is far slower than it needs to be at 4k | Naive per-cell marching | The horizon sweep …") carries no time figure, and the "Use this" section (:28-43) carries none either — so a fix at :177-179 is complete at that one site. No register row covers it: grepping gaia/registers/ for terrain-analysis-masks returns rows on the curvature coefficients (pseudocode-execution.tsv:180), the northness failure cell (pseudocode-execution.tsv:212) and the Dozier 1981 lineage (source-findings.tsv:38) only.

### terrain-analysis-masks.md · `TAM-3` · `stale` · gaia/references/terrain-analysis-masks.md

**Narrowest true statement.** terrain-analysis-masks.md:155 prints "`N = 8–16` azimuths" bare, while driver-fields.md:192-195 — a document this file names twice, at :167 and :178 — grades that same figure "this corpus's own practice, not a published one" and names terrain-analysis-masks.md as the file that states it without a citation. That grading landed in driver-fields.md on 2026-09-03 (commit 0fa6c48) and terrain-analysis-masks.md has been edited since (8490a4f, 2026-09-09) without picking it up, so the correction landed at one end only. It matters at this site because N is load-bearing: both printed figures, ":175 4.5 to 45 minutes" and ":179 about a hundred times the work", are computed at the range's upper end (N = 16); at N = 8 the same document would print 2.2–22 minutes and ~199×. The corpus has an explicit convention for carrying an uncited recommendation with its grade (registers/source-findings.tsv:42 "carried … as folklore, per the corpus convention"; driver-fields.md:398 "⚠️ No canonical source; standard practice is"; this file's own :37, :122, :166), so the omission is measured against the corpus's own standard, not taste. The defect is the missing folklore grade at :155, nothing more.

**Blast radius.** ACTIVE CORPUS (gaia/): the bare, ungraded "N = 8–16 azimuths" is ONE SITE ONLY — gaia/references/terrain-analysis-masks.md:155. The other two active occurrences already carry the grade and are therefore not repeats of the defect: gaia/references/driver-fields.md:150-151 ("The 8–16 is this corpus's own practice line, uncited") and :192-195 (the ⚠️ folklore block). gaia/references/heightfield-raymarching.md:174 "8–16 + log refine" is relief-mapping step counts [policarpo2005], a different quantity — not a repeat. The "2–5% of domain extent" figure occurs at gaia/references/terrain-analysis-masks.md:155 and :276 (failure table) and nowhere else in gaia; grep for maxDist across gaia/references returns only those five lines in that one file, so it is unsourced and unmeasured corpus-wide but has no correction landing at any other end.

OBSOLETE CORPUS (obsolete/, superseded): the exact bare claim is repeated in four prose sites, none carrying any grade — obsolete/terrain-architect/references/06-analysis-masks.md:216 ("Start at ~2–5% of the domain extent. `N = 8–16` azimuths is plenty for terrain") and :273, obsolete/terrain-architect/references/15-gpu-realtime.md:137 ("8–16 azimuths = 8–16 passes over the field"), obsolete/terrain-architect/reference-impl/HYPERREALISM.md:50 ("~8–16 azi"). :216 is also the only other site pairing 2–5% with 8–16. The predecessor register already recorded the figure as evidence-free: obsolete/terrain-architect/registers/numeric-claims.tsv rows 990-993, 1000-1001, 2771-2774 and 4601-4602 classify the 2, 5 and 8 literals as "trivial-literal:no-evidence / unguarded" (the 16 alone shows "guarded" against inputs.py::plateau). Unrelated "8-16" hits (vegetation-scatter byte budgets, contact-shadow steps, FDN delay lines, voxel bit packing) are coincidental string matches, not this claim.

### terrain-analysis-masks.md · `TAM-4` · `incomplete` · gaia/references/terrain-analysis-masks.md

**Narrowest true statement.** At gaia/references/terrain-analysis-masks.md:202, `heightSel(h, lo, hi, w) = smoothstep(lo-w, lo+w, h) * (1 - smoothstep(hi-w, hi+w, h))` is printed with no statement of what `lo`, `hi` and `w` mean geometrically and no relation between `w` and `hi − lo`; `w` appears only three times in the whole document (202, 203, 213) and is never given a value, a range, or a bound. Two consequences follow exactly and neither is on the page: (1) the mask attains 1 if and only if `hi − lo ≥ 2w` (at equality, only at the single midpoint); below that threshold its maximum is strictly under 1 — 0.711914 = 729/1024 for lo=2000, hi=2100, w=100, and 0.467300 = 30625/65536 for lo=2000, hi=2050, w=100, where the value at both authored numbers is 0.421875 = 27/64, not 0.5; (2) for every band the support is [lo−w, hi+w], and the value at `lo` and at `hi` is exactly 1/2 whenever `hi − lo ≥ w`, so a band authored in the world units the document emphatically demands ("Above 2000 m is 2000", :209) passes material `w` outside each authored edge and falls short of full coverage inside it. The document's own guard cannot detect case (1): the assertions at :219-221 are `Σ ≤ 1` at the fan-in and `Σ = 1` only after the base is emitted as its own channel, and both pass while the deficit is silently absorbed by the very clause that licenses it ("the shortfall is the bare base material"). The corpus's own execution run recorded the half-height/half-width fact (registers/pseudocode-execution.tsv:255, 2026-09-08) and it never reached the page. Two narrowing qualifications the finder did not make: the degradation just below the crossover is negligible, not a cliff — at hi−lo = 2w−2 the peak is 0.99996 and a visible shortfall needs w to be a large fraction of the band (w = 0.6·band gives peak 0.961, w = band gives 0.712) — so the practical failure regime is "w comparable to hi−lo", not "hi−lo < 2w" generally; and the finder's own (explicitly unverified) proposed-fix wording "the mask reaches 0.5 at lo and hi" is unqualified where it holds only for hi − lo ≥ w, a case its own 0.4219 counterexample contradicts. The finding body itself states both cases correctly.

**Blast radius.** Two sites. (1) The finding's own site, gaia/references/terrain-analysis-masks.md:202. (2) obsolete/terrain-architect/references/06-analysis-masks.md:356-359 repeats the identical formula and extends it to two more selectors with the same gap, and worse: it prints `heightSel(h, lo, hi, w) = smoothstep(lo-w, lo+w, h) * (1 - smoothstep(hi-w, hi+w, h))   # a height band`, plus `slopeSel(s, lo, hi, w)` and `curveSel(c, lo, hi, w)` spelled out in the same form, and its Soft-edges bullet says "Use `smoothstep` over a band `w`" -- calling w itself "a band", which actively invites the misreading that [lo, hi] are the band edges. No w/hi-lo relation, no half-height note, and its own failure list carries no row for it either. It sits under obsolete/ so it is out of the live corpus, but it is a second copy of the same defect and would inherit any correction. No other file defines these selectors: `grep -rn "heightSel|smoothstep\(lo|lo-w|half-height"` across the repo returns only those two documents plus gaia/registers/pseudocode-execution.tsv:255 (the execution record, which states the half-height/half-width fact correctly and is therefore the one site that already has it right) and the audit file itself. A correction landing on gaia/ only would leave the obsolete copy stating it as "a band w".

### tiled-streaming.md · `TS-1` · `incomplete` · references/tiled-streaming.md

**Narrowest true statement.** tiled-streaming.md names the tile's own header as the only home of `e(tile)` (:37, restated at :26 "each carrying its own baked geometric error" and at the :10 locator "stored with the chunk"), and that header sits inside the single per-tile blob it also mandates as "One IO request, one decode" (:117-118). It then requires the dominant term of the request priority, `sse_projected(tile)` (:84), to be recomputed every frame for `requested` tiles, a state it defines as "queued, no IO issued" (:58), and prescribes "Prioritise by projected error" as the fix for the cold-start row at :166 — the regime in which, by :75-76, only the pinned root levels are resident. The document names no resident structure (quadtree/tileset index or equivalent) holding `e` and the tile bounding volume before the payload arrives; a grep of the whole file for header|index|metadata|manifest|tree|tileset|catalog|quadtree returns only :14, :19, :26, :37, :39, :150, none of which is such a structure. So the dominant term of the prescribed priority function is unevaluable in exactly the state the function is prescribed to order. That much is confirmed. What is NOT entailed is the finder's specific consequence — "the priority product collapses to a constant across a level" and "does exactly 'everything requested at once at equal priority'". That holds only if the implementer substitutes a constant for the unevaluable term; a tile's XY footprint is derivable from its quadtree address under :34's constant-coverage-per-level rule, so a distance-proxy substitution is equally available and yields 256 distinct priorities across a 256-tile level, not a constant — i.e. "distance-only priority", which is a different failure the same page names six lines above at :91-92 (a starved mid-distance band), not the :166 equal-priority row.

**Blast radius.** The defective combination (per-tile error homed only in the single-IO tile blob + per-frame sse recompute for tiles with no IO issued) appears at ONE SITE ONLY: references/tiled-streaming.md. But it is asserted three times WITHIN that file, and a correction landing only at the cited :37 would leave two live: :26 "A quadtree of constant-size tiles, each carrying its own baked geometric error" (the "Use this" box) and the front-matter locator :10 "per-chunk geometric error stored with the chunk". The priority function itself is restated once outside the file, at registers/pseudocode-execution.tsv:258, verbatim ("priority = sse_projected(tile) * frustumFactor * predictFactor(cameraVelocity, tile)"), recorded NOT RUN with the independent observation that "sse_projected and predictFactor are named, not defined, in the fence, so there is nothing to evaluate" — related and corroborating, but it makes no storage claim, so it needs no correction. references/papers-rendering.md:123 describes ulrich2002 as "per-chunk maximum geometric error" with no storage-location claim. references/heightfield-lod.md:69-76 owns the rho = e*K/d projection but presupposes a resident region and makes no claim about where e lives for a non-resident tile; :175 ("opaque blobs") corroborates the gap rather than repeating the claim. Greps run: `grep -rniE "tile header|chunk header|tileset|geometricError|geometric error" references/ registers/`, `grep -rniE "sse_projected|priority *=|recompute priorit|prioritis|prioritiz" references/ registers/ *.md`, `grep -rniE "resident index|node list|metadata|manifest|out-of-core|separately loaded" references/heightfield-lod.md references/tiled-streaming.md references/gpu-driven-culling.md`.

### tiled-streaming.md · `TS-2` · `wrong` · references/tiled-streaming.md:84

**Narrowest true statement.** At tiled-streaming.md:86 the third factor's comment names an operation the printed code does not perform: `predictFactor` is multiplied in, while its comment says "take the max SSE" — a max over the prediction window, which is a replacement of the error term, not a multiplier on it. The block never says what `predictFactor` returns, and the three natural readings do not agree: (i) it returns the max SSE in px — the product is then sse_now x sse_max and orders the queue differently from the max the comment names; (ii) it returns a bounded boost (the semantics the predecessor document prints at obsolete/terrain-renderer/references/06-tiled-streaming.md:99, "boost tiles the camera is flying toward") — same disagreement; (iii) it returns sse_max/sse_now, in which case the product equals max(sse_now, sse_pred) * frustumFactor exactly (the finding's own proposed fix), but `sse_projected` cancels algebraically and the ":84 the dominant term" label on it is void. Under (i) and (ii) a tile the camera is closing on is deferred behind a mediocre steady tile: A at 2 px now / 12 px inside the window loses to B steady at 5 px (24 < 25) while the max rule ranks A first — constructible at 70 km/h closing speed under this corpus's own rho = e*K/d (e = 0.1 m, K = 935 px). The document defines `predictFactor` nowhere (full read of all 175 lines; the ancestor's disambiguating prose sentence "compute SSE from the *predicted* position too, take the max" was dropped in the rewrite).

**Blast radius.** Three sites; the live defect is at one.
1. /home/user/skills/gaia/references/tiled-streaming.md:84-86 — the finding site, and the ONLY place in the corpus where "take the max SSE" sits inside the multiplicative block.
2. /home/user/skills/obsolete/terrain-renderer/references/06-tiled-streaming.md:97-99 and :103-104 — superseded tree. Same product and the same "dominant term: error on screen NOW" comment, but the third factor reads "boost tiles the camera is flying toward" and the max rule is stated separately in prose ("compute SSE from the *predicted* position too, take the max"). It therefore does NOT carry this defect in the same form; it is the evidence that the gaia rewrite created it by folding the prose max into the factor's comment. If the fix lands only in gaia, this file keeps the older, clearer split — no correction needed there, but note the "dominant term" comment is common to both.
3. /home/user/skills/gaia/registers/pseudocode-execution.tsv:258 — repeats both claims verbatim as the fence's asserted content ("that screen-space error now is the dominant term ... and that prediction extrapolates 1-2 s and takes the max SSE") and records NOT RUN / NOT MEASURED, explicitly because "sse_projected and predictFactor are named, not defined, in the fence". The register propagates the ambiguity un-executed and must be updated with the document, or it will re-assert the old wording.
Magnitudes: "behind ~0.1" appears at gaia:85 and obsolete:98 only; "1-2 s" as a prediction window at gaia:86 and obsolete:103 only (other "1-2 s" hits in the corpus are auto-exposure and unrelated). The 20/25/80x figures exist only inside the audit file. No other document in the corpus states a streaming priority function at all.

### tiled-streaming.md · `TS-3` · `stale` · references/tiled-streaming.md

**Narrowest true statement.** The body's only queue guidance (:112-113) states it in the form the cited section explicitly labels as superseded — a target number of concurrent requests — while the section named in the document's own locator (:13) says the opposite in form and two orders of magnitude in size: "The title no longer needs to try and balance between outstanding work and queue depth. The title should submit all its outstanding requests," the drive "allowing the title to submit thousands of requests at once," and "create a queue with a capacity at least four times the largest number of requests created per frame." Neither the submit-everything rule nor the 4x capacity rule reaches the body, though the failure table at :170 names "queue overflow" as a cause of silently dropped requests and prescribes only "Count every drop." The three specifics the finding rests on all fail: "dozens" (>=24) is NOT the retired 12-16 rotational figure and lies above it, in the direction the source endorses; "NVMe wants dozens" is a floor contrasted in the same breath with "one outstanding read," not the cap the failure scenario requires; and the "800 / 33x too small" figure is not derivable as stated, because :121's 200 is tiles finishing DECODE in the document's explicitly pathological "canonical hitch," not requests CREATED per frame, and the source's 4x rule sizes queue capacity ("the maximum number of outstanding requests the queue can hold"), which is not the same quantity as a target in-flight depth. The defect is citation fidelity and understatement, not a false statement: "NVMe wants dozens of concurrent requests" is true of the hardware and is not contradicted by the source.

**Blast radius.** Two other sites, one of which repeats the defect.

1. REPEATS IT, more strongly — /home/user/skills/obsolete/terrain-renderer/references/06-tiled-streaming.md:132-133: "keep enough in flight to saturate NVMe (dozens of concurrent requests — NVMe queue depth is the point of NVMe). One-outstanding-read-at-a-time streamers run at HDD-era throughput on NVMe hardware." This is the predecessor of the gaia document; it carries the same "dozens" magnitude and, unlike gaia, has NO front-matter locator holding the submit-everything / 4x-capacity rule at all — its source line (:293) says only "Microsoft DirectStorage documentation: GPU decompression path, request batching, NVMe queue-depth guidance". If :112 is rewritten and this is not, it is exactly the one-end correction this corpus's own register flags (gaia/registers/pseudocode-execution.tsv:212). It is under obsolete/, so whether it is in scope for correction is a separate call.

2. NAMES THE SOURCE, CARRIES NO NUMBER — /home/user/skills/gaia/references/papers-rendering.md:135: "**directstorage** `F` — Microsoft. *DirectStorage* documentation. — Disk→GPU decompression, request batching, NVMe queue-depth guidance. Vendor documentation; drifts by release." No magnitude, so it does not repeat the defect and needs no change; noted because it is the bibliography end of the same citation.

Every magnitude checked across the whole repo (grep for "dozens", "queue depth", "queue capacity", "NVMe", "outstanding request", "four times", "12 to 16", "12-16", "65536", "50,000 IOPS", "thousands of requests", "submit all", "submit every", "in flight" over *.md and *.tsv): no other site prints a queue-depth figure for streaming IO. The 65,536 hits elsewhere are unrelated (R16 quantisation levels in obsolete/terrain-architect/references/08-output-contract.md:164/187, 256^2 grid node counts in gaia/references/river-networks.md:80/89 and the RN-3 audit entries, virtual-texture sizes in gaia/registers/pseudocode-execution.tsv:243). "four times" hits elsewhere are erosion droplet counts and unrelated ratios. Within gaia, the only sites naming this source are tiled-streaming.md:13, :113, :115 and papers-rendering.md:135.

### tiled-streaming.md · `TS-4` · `unsupported` · references/tiled-streaming.md

**Narrowest true statement.** The only artefact pointer this document gives for `[directstorage]` — the locator at `gaia/references/tiled-streaming.md:13`, "the Queue depth section of the GDK DirectStorage overview, its Decompression section, and Best Practices → Queue management" — resolves uniquely to https://learn.microsoft.com/en-us/gaming/gdk/docs/features/console/storage/directstorage/directstorage-overview, whose opening sentence is "This article provides an overview of the DirectStorage APIs for Xbox Series X|S consoles only", and that page contains neither of the two things :114-116 cites it for: it describes fixed-function decompression ("Decompression is handled with dedicated hardware", modes ZLibDecompress / BCPack / Swizzle into a title-supplied Destination buffer or XMemAllocatePhysicalPages page array), the string "GDeflate" appears zero times, the string "fallback" appears zero times, and "GPU" appears exactly three times, all three in the ID3D12Fence notification discussion. So the bullet's "GPU decompression … inflate there" and its "Keep the CPU fallback … two executors" are unsupported at the cited location. The substance is not wrong — it is correct for DirectStorage on Windows 1.1+ (GDeflate on compute, shipped 2022-11-07, 98 days after this page's ms.date of 2022-08-01), and the bibliography annotation at papers-rendering.md:135 ("Disk→GPU decompression") is right for that documentation family. The defect is confined to the locator: it names the one DirectStorage page in Microsoft's docs where this specific claim does not exist.

**Blast radius.** Four other sites, none of which repeats the defect itself (the console-page locator) — that appears at one site only, gaia/references/tiled-streaming.md:13.

1. gaia/references/papers-rendering.md:135 — the [directstorage] bibliography entry: "Microsoft. *DirectStorage* documentation. — Disk→GPU decompression, request batching, NVMe queue-depth guidance. Vendor documentation; drifts by release." Same claim, no platform, no artefact pointer. Not wrong (it is true of the Windows documentation), but it is the second gaia site of the "disk→GPU decompression" magnitude, and it is the corpus's only other pointer for this source id. A correction that lands on tiled-streaming.md:13 alone leaves this un-platformed annotation as the surviving pointer — the one-end-only pattern. Note also that these two files are the ONLY gaia references mentioning directstorage at all (grep -rln over gaia/references/).

2-3. obsolete/terrain-renderer/references/06-tiled-streaming.md:134-135 — the predecessor of this very document, repeating the claim in the same shape and further: "compressed tiles go disk → GPU memory and are decompressed on the GPU (GDeflate et al.), bypassing the CPU copy-and-inflate path … keep a CPU fallback path and treat the two as the same logical stage with different executors" — but sourced only as "**D** — Microsoft DirectStorage documentation" (:293), with obsolete/terrain-renderer/references/00-index.md:80 pointing at github.com/microsoft/DirectStorage. So the obsolete tree states the claim MORE precisely (it names GDeflate) and points at the right artefact; the GDK-console locator was introduced in the gaia rewrite. Obsolete tree, likely out of scope for a fix, but it shows the locator is a regression rather than inherited.

4. game-engine-guru carries the correct platform split three times and contradicts nothing here: references/ASSET_PIPELINE_AND_COOKER.md:201 ("DirectStorage on Windows PC supports GPU decompression with GDeflate … Xbox uses its platform I/O stack and dedicated decompression hardware rather than the PC GPU-GDeflate path"), references/CROSS_PLATFORM_AND_CONSOLE.md:186 ("use GDK DirectStorage with the console's dedicated decompression hardware and BCPack-aware content pipeline. Do not model this as the Windows PC GPU-GDeflate path"), and references/SOURCES_AND_VERSIONING.md:25-28, which cites the DirectStorage 1.4 Zstandard devblog for the desktop path and the Xbox GDK overview URL only under the label "Xbox GDK DirectStorage overview". The distinction gaia/references/tiled-streaming.md:114 misses is therefore already house knowledge elsewhere in this corpus, which makes the gaia locator the outlier.

Sibling finding TS-3 in the same audit cites the Best Practices → Queue management section of the same page; I reproduced that section too while resolving the locator ("The previous recommendation was to only maintain 12–16 asynchronous requests in flight at a time on a rotational drive" / "The title should submit all its outstanding requests"), so both TS-3 and TS-4 rest on the same page identity, which is now verified.

### tiled-streaming.md · `TS-5` · `unsupported` · references/tiled-streaming.md

**Narrowest true statement.** `/home/user/skills/gaia/references/tiled-streaming.md:37-38` — the monotonicity requirement itself is fine; only its stated consequence is wrong. Under this document's own hysteresis rule at :94-96 (refine at `sse > tau`, coarsen at `sse < tau·h`, applied to "the same tile"), a violation of `e(child) ≤ e(parent)` cannot produce a split/merge cycle: for one node the two predicates are mutually exclusive for any error data, and the dead band's width in camera distance is exactly `1/h`, in which `e(child)` does not appear. What does follow from a violation is (a) a silently non-conservative error bound — the top-down cut rests at a node passing `tau` while a descendant exceeds it — and (b) a multi-level jump when that node finally splits. Narrower than the finder's wording in one respect: oscillation is not merely "prevented by the dead band", it is not caused by a monotonicity violation under any refine/coarsen reading I could construct — under the cross-level reading a violation *suppresses* the cycle that monotone data produces.

**Blast radius.** Two sites, one live. (1) The audited line, /home/user/skills/gaia/references/tiled-streaming.md:37-38 — the only live-corpus statement of the contract ("e(child)" appears nowhere else under gaia/). (2) /home/user/skills/obsolete/terrain-renderer/references/06-tiled-streaming.md:39-40, the obsolete predecessor: "Monotonicity contract: `e(child) ≤ e(parent)` must hold in the baked data, or refinement can oscillate." Same claim, hedged with "can"; the gaia port hardened it to the flat "refinement oscillates otherwise", so a fix landing only on gaia leaves the hedged copy in the obsolete tree. Not repeats, but adjacent and worth checking together: gaia/references/heightfield-lod.md carries the same rho = e*K/d controller and tau (:71-76) and states NO monotonicity contract at all (the word "child" does not occur in the file) — so the live corpus's LOD owner is silent where the streaming document over-claims; and gaia/references/gpu-driven-culling.md:224 plus tiled-streaming.md:167 both attribute the flicker symptom to a missing hysteresis band, which is the correct owner and needs no change. Magnitudes 4 m / 9 m / 6 px / 0.8 / 13.5 px / 2.25x appear only inside the audit finding itself; h = 0.7-0.85 appears at tiled-streaming.md:95 and gpu-driven-culling.md:224 and is consistent at both.

### tiled-streaming.md · `TS-6` · `incomplete` · references/tiled-streaming.md

**Narrowest true statement.** tiled-streaming.md states a parent-residency requirement at :141-142 ("Morphing needs the parent's height at the child's vertices, which is one more reason parents stay resident") and nowhere enforces it: the eviction exclusion list at :104 covers only "tiles in the cut, pinned tiles, or tiles mid-upload"; the only pinning rule is ":75 The root levels are pinned"; the crossover budget paragraph at :42-45 does not carry the parent level; and the failure table (:163-175) has no row for a morph pop/crack caused by a missing parent. A parent released from the cut is by the document's own definitions `evictable` (:69-70, :73), and :75 ("if it was evicted, re-request it and keep drawing children") concedes parents do get evicted, so the gap is real rather than hypothetical. What FAILS in the finder's wording is the trigger: "Under memory pressure the hybrid evicts it" is too broad. Under the document's own priority function a parent of a cut tile is the HIGHEST-priority evictable in its neighbourhood — rho(parent) is in [h*tau, 2*tau] = 1.4-4.0 px at tau=2, h=0.7, against ~0.2 px for a driven-past tile at frustumFactor 0.1 (:85) and < h*tau = 1.4 px for any coarsened descendant — so the hybrid reaches it only once the rest of the evictable set is drained (the cold-start/teleport and max-traversal-speed regimes the document's own rows :166 and :175 name). Correspondingly "supplies no mechanism for it anywhere" is true only of STATED mechanisms: the priority ordering is an unstated, non-guaranteeing protection. The +25% budget figure is correct as an upper bound (atomic refinement per parent, :169, makes distinct parents exactly N/4 of the cut at the constant per-tile size of :34) but the instantaneously-needed subset is band-scoped, so the steady-state figure is nearer +5-9% of drawn-set bytes; the finder's "up to" hedges this correctly.

**Blast radius.** The unprotected-eviction claim itself is ONE SITE ONLY: the exclusion list "never touches tiles in the cut, pinned tiles, or tiles mid-upload" appears nowhere else — grep for "mid-upload|in the cut" across gaia/references/*.md returns only tiled-streaming.md:53, :69, :104. Likewise "pinned" as a residency rule appears only at tiled-streaming.md:75 and :104 (node-graph-runtime.md:439 "LRU by bytes with the viewed cone pinned" is an unrelated node-cache).

The underlying REQUIREMENT the exclusion list fails to honour is repeated at three further sites, all of which point enforcement back at the audited document:
1. gaia/references/heightfield-lod.md:212 — crack-contract cost cell: "both mip levels resident at the band (`tiled-streaming.md`)".
2. gaia/references/heightfield-lod.md:218-224 — "*Both* levels of every band resident is a residency requirement, not an optimisation you can defer", priced at 134 MB / 179 MB (R16 heightfield + mip pyramid) and 268 MB / 358 MB (with mipped RG8 normals), closing "`tiled-streaming.md` is what stops that ever being the resident set".
3. gaia/registers/corrections.tsv:75 — records the correction that added site 1, quoting tiled-streaming.md:139-140 (now :141-142) as the requirement and signing off "both ends now name it".
Related but distinct: heightfield-lod.md:116-147 (the sampleHeight(uv, nodeLod+1) mechanism and the one-registration condition) and its failure row :266; shader-craft.md:445 and virtual-texturing.md:172 concern feedback-bias eviction, not this claim.

MAGNITUDES: the "+25%" figure appears at no site in the corpus other than the finding itself (grep "25%|N/4|1/4" over tiled-streaming.md and heightfield-lod.md returns only heightfield-lod.md:149, an unrelated "quarter of a coarse texel"). The 15-30% band magnitude it depends on is repeated at tiled-streaming.md:11 (front-matter locator) and :141, heightfield-lod.md:9 (locator) and papers-rendering.md:126 — all consistent. So a fix that adds the parent level to the exclusion list and to the crossover budget at :42-45 lands at the enforcement end only; the cost end (heightfield-lod.md:218-224) is already correct and should be cross-referenced rather than re-derived, or the corpus gains a second, differently-derived price for the same bytes.

### virtual-texturing.md · `VT-1` · `stale` · gaia/references/virtual-texturing.md

**Narrowest true statement.** virtual-texturing.md:152-155 prescribes height-based blending unconditionally ("the largest visual return per line of code", no ⚠️) and gives [mishkinis2013] as its only route to the arithmetic, while correction X51 — which established that the form that article prints (`b_i = w_i + h_i`, `m = max_i(b_i) − depth`, `w'_i = max(b_i − m, 0)`) hands a nonzero share to a material of exactly zero weight in 4.28% of texels at Σw=1/depth 0.1 (7.63% at depth 0.2; 18.92%/26.49% at Σw=0.5), max share 1.0 — landed only on mask-to-material.md, which points at virtual-texturing.md six times (:18, :25, :69, :180, :288, :391) while virtual-texturing.md names no sibling but shader-craft.md (:104). The page's own mitigation at :158 ("renormalise weights after any quantization or filtering") does not reach it: at Σw normalised to exactly 1 the printed form still leaks 4.29% of texels at max share 1.0, because the fix is the weight-scaled bias `b_i = w_i·(1 + h_i)`, not the normalisation. The failure table (:167-179) has no row for a zero-weight material in the composite, where mask-to-material.md:378 has one. What is NOT true, and is the finder's overreach: virtual-texturing.md prints no formula, so "an implementer ... codes the printed form" is a two-step inference through an out-of-corpus citation, not something the page states; and "invisible in the near view" is false for this defect — that property belongs to the premultiplied-mip halo at :161-163, whereas the height leak is a wrong material painted at page resolution and is visible wherever the page is sampled. "Cached and permanent" does hold (:44 invalidates only when a composited input changes; the splat resolve does not).

**Blast radius.** Inside the audited corpus (gaia/), the same claim — the mishkinis2013 height blend endorsed with no zero-weight-leak caveat — appears at TWO further sites besides the finding site: (1) gaia/references/virtual-texturing.md:13, the front-matter locator "the height-based blend with a contrast term", which is the same unflagged pointer one line-class above :152; (2) gaia/references/papers-rendering.md:142, the bibliography entry — "Height-based splat blending: per-layer height maps let the more prominent material win the boundary instead of a linear cross-fade. Article, not peer-reviewed." — which grades the article F but carries neither the leak nor a pointer to the corrected form. gaia/references/mask-to-material.md is the corrected owner, not a repeat (:39 the scaled bias, :238-246 the warning, :378 the failure row). The leak MAGNITUDES (4.3/18.9/7.6/26.5, "the whole pixel") occur in gaia/ only at mask-to-material.md:240-245 and registers/corrections.tsv:26 — virtual-texturing.md carries none of them, which is exactly the one-ended landing.
Outside the audit scope but present in this repo, the additive form is PRINTED verbatim and unflagged twice: obsolete/terrain-renderer/references/07-materials-virtual-texturing.md:126-131 (`float fa = hA + wA, fb = hB + wB; float m = max(fa,fb) - contrast; ...`, endorsed again at :471) — under its stated `wA+wB=1` two-layer contract the leak is smaller but still nonzero (analytically contrast²/2, i.e. 0.5% at contrast 0.1, max share 0.5) — and obsolete/terrain-architect/references/08-output-contract.md:841-849 (`h_i = tile(materialHeight[i], tiledUV) + splat[i]`, credited to Mishkinis 2013), plus two derivative references at obsolete/terrain-renderer/references/13-snow-weather-surface-state.md:237 and 16-tool-viewports.md:30-32 that inherit `07`'s form without restating it.

### virtual-texturing.md · `VT-2` · `incomplete` · gaia/references/virtual-texturing.md

**Narrowest true statement.** The failure-table row at virtual-texturing.md:172 elevates a streaming-only signal to arbiter of a runtime-VT failure. In this document's own vocabulary "IO"/"disk" occurs only at :36, :43, :45 and :50 — all four assigning IO and transcode to streaming VT — while "Use this" (:25, :36) makes runtime VT the default. Yet the row is headlined "the IO queue says which of the two causes it is" and its fix dispatches on that queue ("Saturated: grow the pool… Quiet: subtract log2(feedbackScale)"), where "Saturated"/"Quiet" are queue adjectives that cannot attach to the eviction-age histogram named beside them. Under the runtime VT the document prescribes, the "IO saturated" arm is unreachable and "IO quiet" carries no information, so the row's designated arbiter is inert exactly where the document expects most readers to stand; the regime is never stated, and the corpus names no runtime-side substitute (grep for a page-render/compositor-queue metric across gaia/ returns nothing outside the audit file itself). The same regime-blindness sits in the neighbouring row :171, whose "budget the transcode burst" is streaming-only too. What does NOT hold is the finder's stronger framing: the row is not "a guard that cannot fail", because the same cell names eviction age in both branches ("eviction age under a couple of seconds" vs "eviction age long") and the fix's first sentence orders the eviction-age histogram read first — a signal that discriminates correctly in both regimes and that the document already states, complete and IO-free, at :131-134 ("Size by measured eviction age, not by cache-hit rate… pages living longer than that while the image stays blurry means the pool is not the constraint, and the mip the feedback pass is asking for is where to look"). The reported harm (a thrashing runtime-VT team takes the "Quiet" arm and multiplies its working set 16×) is therefore a misreading hazard created by the headline's hierarchy, not the row's determinate instruction: the reader must discard two of the three signals in the "Quiet" signature (long eviction age, live set ≈ feedbackScale⁻²) to reach it. The defect is the unqualified IO framing, not an unavoidable wrong dispatch.

**Blast radius.** Not one site only — the regime-blind IO signal appears at a second live site, and one obsolete ancestor.

LIVE, same claim: gaia/references/shader-craft.md:445 — "| Permanent blur with a quiet IO queue and long eviction ages | The feedback pass's own downscale was not subtracted… 6.25% of the areal density at quarter res | mip = computeMip(virtualUV) − log2(feedbackScale) [mittring2008] |". This restates "quiet IO queue" as a diagnostic signature for the same failure. It is weaker than :172 (it names only the feedback-bias arm, so it never claims IO decides between two causes) but it is more exposed: `grep -c "runtime VT|Runtime VT|streaming VT|Streaming VT|disk" gaia/references/shader-craft.md` returns 0 — that document names no VT regime anywhere, so its IO signal has no qualification available to the reader at all. A correction landing on virtual-texturing.md:172 alone would leave this end stating the same unqualified signal.

LIVE, same regime assignment (correct, and the basis of the finding): gaia/references/virtual-texturing.md:43 and :45.
LIVE, same streaming-only fix in a neighbouring row: gaia/references/virtual-texturing.md:171 ("budget the transcode burst"); transcode appears in the document only at :43, :45 and :171.

OBSOLETE tree (not a live site, but records that the defect is a rewrite regression): obsolete/terrain-renderer/references/07-materials-virtual-texturing.md:319 (same cost-centre table, "IO + transcode" for SVT vs "page render (full material graph)" for RVT), :372 ("budget the transcode pipeline for the burst"), :373 ("permanent blur plus IO saturation. Detect with eviction-age histograms" — detector is eviction age, so the ancestor does NOT commit the defect), :309 (eviction-age sizing rule).

MAGNITUDES, all re-derived and all consistent — no one-end correction needed here: 6.25% / feedbackScale^-2 / 1/16 at virtual-texturing.md:121, :122, :172; shader-craft.md:229, :230, :445; AUDIT-2026-09-05.md:1518, :1892. log2(feedbackScale)=2 at quarter res at virtual-texturing.md:119-120, :172; shader-craft.md:56, :227-229. Unrelated 6.25 hits (planetary-precision.md:30,34; resolution-independence.md; stream-power.md) are ULP and cell-size figures, not this magnitude.

UNRELATED, checked and excluded: game-engine-guru/references/ASSET_PIPELINE_AND_COOKER.md:67 uses "eviction age" for a build-cache DDC, not a VT pool. No entry in gaia/registers/corrections.tsv touches row :172.

### water-closed-vs-open.md · `F1-short-band-never-reflects` · `wrong` · /home/user/skills/gaia/references/water-closed-vs-open.md:116,120 (band table: "Short band (≲5 cm)" / "Reach | ... | Dies in ~2 m; never reflects")

**Narrowest true statement.** `gaia/references/water-closed-vs-open.md:120` states the short band "Dies in ~2 m; never reflects". The `~2 m` half is CORRECT (2.118 m at 3 cm, reproduced exactly from Lamb `tau = 1/(2*nu*k^2)` against the capillary-gravity group speed). The defect is confined to the absolute **"never reflects"**, which is false under the document's own damping law for two reasons the page itself supplies:

(1) **Band ceiling.** The `~2 m` is computed at 3 cm (`:113`) but labels a band the header defines as `≲5 cm` (`:116`). Reach scales steeply with wavelength: L(5 cm) = **5.66 m**, 2.7x the quoted figure. Across the page's own 8 m reference pool a 5 cm ripple arrives at the far wall at **24% amplitude**, and from a mid-pool source at **49%** — it reaches the wall and reflects.

(2) **Small bodies and near-wall sources.** The page's own enum (`:31`) includes `tank` and `basin`, and its gate table gives closed bodies "1–3 m of range" (`:148`). Against a wall 1 m away a 3 cm wave arrives at **62%** and a 5 cm wave at **84%**. The table's own Source cell (`:118`) says the short band is forced by "Wind, over the whole surface" — so a large fraction of its energy is born within one e-folding distance of a wall by construction.

The narrow error is a category slip: an e-folding distance shorter than the basin is treated as extinction. One e-folding leaves 37%. The long/short contrast at the far wall of the 8 m pool is **3.8x** (0.915 vs 0.243), not "rings many times" versus "never". The correct cell is roughly "Dies within ~2–6 m across the band; any reflected component is incoherent and adds no structure" — which also keeps `:120` from contradicting the page's own unqualified `:83` ("the domain edge is a **real wall**, and it reflects") and `:147` ("**Reflect.** The edge is a wall").

**Blast radius.** The absolute **"never reflects" is ONE SITE ONLY** — `gaia/references/water-closed-vs-open.md:120`. `grep -rni "never reflects\|never-reflect" .` over the whole corpus returns that line and nothing else; "short band" appears only at `:116` and `:194` of the same file.

The magnitudes it rests on are repeated, and are CORRECT at every site — no correction is owed at the far end:
- `gaia/references/wave-models.md:142-144` prints the same ladder (`16.5 cm -> ~90 m, ~345 s`; `3 cm -> ~2.1 m, ~11 s`; `5 mm -> ~0.14 m, ~0.3 s`). I reproduce 89.85 m / 344.8 s, 2.118 m / 11.40 s, 0.1418 m / 0.317 s. That page makes **no reflection claim for the short band** — its comment is only the long-band twin, "rings an 8 m basin many times over" — so it does not repeat the defect.
- The "within centimetres" magnitude at `water-closed-vs-open.md:197`, `wave-models.md:144` and `wave-models.md:370` refers to mm-scale ripple and checks out (L(5 mm) = 14.2 cm, L(1 mm) = 1.28 cm).
- `gaia/registers/pseudocode-execution.tsv:260` records the `c_min`/`lam_min` fence as RUN and SOUND, and notes wave-models.md prints the identical pair — my run agrees to the printed precision.

Adjacent, unresolved, and pointed at the OTHER end of the same paragraph: `gaia/AUDIT-2026-09-05.md:758` (**X30**, MED, **PLAUSIBLE**, never settled) argues the 90 m long-band figure uses Lamb's clean-surface rate and that an inextensible surface film raises damping ~20x, giving ~4.4 m. Note the interaction, because it is the one route that could have saved `:120`: under a 20x film rate `L(3 cm) = 0.11 m` and `L(5 cm) = 0.28 m`, and "never reflects" would become nearly true — but the same 20x destroys the neighbouring cell "Rings the basin many times" (90 m -> 4.4 m, half a pool length). The two cells cannot both stand under the film model, and under the model the page actually commits to and prints (Lamb clean-surface, `[lamb_damping]`), "never reflects" is the cell that fails. X30 and this finding should be resolved together on one damping model, and only `:120` needs the reflection wording changed.

### water-closed-vs-open.md · `F2-tau-formula-dimensionally-wrong` · `wrong` · /home/user/skills/gaia/references/water-closed-vs-open.md:11 (sources.lamb_damping locator: "eq. 9 the decay modulus tau = 1/2*nu*k^2 = lambda^2/8*pi^2*nu")

**Narrowest true statement.** The defect is confined to missing denominator parentheses in one transcribed citation string. In the `lamb_damping` source locator, `water-closed-vs-open.md:11`, the decay modulus is written `tau = 1/2*nu*k^2 = lambda^2/8*pi^2*nu`. Read with ASCII operator precedence the left side is `nu*k^2/2` (units 1/s — a rate, not a time) and the right side is `pi^2*lambda^2*nu/8` (units m^4/s — neither), and the two sides joined by `=` are not equal to each other. The quantity actually intended, `1/(2*nu*k^2) = lambda^2/(8*pi^2*nu)`, is exactly correct and correctly dimensioned (units s), and it is what the pages use: every magnitude derived from it reproduces to printed precision. No prose sentence, no equation a reader implements from, and no number on either page is wrong. The severity label `wrong` overstates this: it is a transcription/parenthesisation defect in a bibliographic locator with zero propagation, not a physics or arithmetic error.

**Blast radius.** Two sites carry the flawed string, verbatim and identically:
- `/home/user/skills/gaia/references/water-closed-vs-open.md:11` (the cited site)
- `/home/user/skills/gaia/references/wave-models.md:12` — verified by exact substring match: the fragment `tau = 1/2*nu*k^2 = lambda^2/8*pi^2*nu` is present in both lines (python substring test returned True for both). A correction landing only at `water-closed-vs-open.md` would leave `wave-models.md:12` carrying the identical unparenthesised form — exactly the one-end-only failure mode this corpus records most often.

NOT affected (checked, and each states the damping unambiguously and correctly):
- `papers-simulation.md:75` — carries the same `lamb_damping` source but writes only `α = 2νk²`. No parentheses needed, dimensionally sound.
- `wave-models.md:137` — `alpha = 2 nu k^2` with `nu ≈ 1e-6 m²/s`. Unambiguous.
- `water-closed-vs-open.md:112` — says only "viscous decay goes as `k^2`". No formula printed.
- `water-closed-vs-open.md:197` and `wave-models.md:370` — cite `[lamb_damping]` for the `lam_min`-is-not-a-shortest-wave point; no tau.

Every downstream magnitude was recomputed and is correct, so the blast radius of the *numbers* is nil:
- `water-closed-vs-open.md:113-114` "order 90 m at 16.5 cm — eleven lengths of an 8 m pool" → 89.85 m; 89.85/8 = 11.2. "only about 2 m at 3 cm" → 2.118 m.
- `wave-models.md:141-143` "16.5 cm -> ~90 m, ~345 s" → 89.85 m, 344.81 s. "3 cm -> ~2.1 m, ~11 s" → 2.118 m, 11.40 s. "5 mm -> ~0.14 m, ~0.3 s" → 0.142 m, 0.32 s.
- `wave-models.md:125` "Min GROUP speed 0.1776 at 4.35 cm" → 0.1776 m/s at 4.35 cm.
- `water-closed-vs-open.md:52-53` c_min = 0.2312 m/s, lam_min = 0.01712 m → both exact.

### water-closed-vs-open.md · `F3-lake-contradictory-swell-default` · `wrong` · /home/user/skills/gaia/references/water-closed-vs-open.md:65 vs :74 vs :143 vs :189 (and the derivability claim at :38-39)

**Narrowest true statement.** The defect is a single wrong class assignment at water-closed-vs-open.md:65-66, plus the two-question framing at :38-39 that forced it.

:65 assigns "large lake" to the **Open body** class, and the bullet's content is "A fetch-limited or fully-developed wind sea, plus **swell** generated by weather thousands of kilometres away. It never goes truly flat." Both halves are false for a lake of any size: no lake basin offers a 2000 km propagation path (longest: Tanganyika ~673 km, Baikal ~636 km, Superior ~560 km), and a lake does go truly flat — its wave field terminates at the far shore 17-40 h after the wind stops, whereas the ocean is continuously resupplied.

That assignment then collides with three sites that are each correct in themselves: :143's gate row gives every Open body residual swell **On**; :74 states "A lake has no distant storm feeding it, so its dead calm really is a mirror"; :189 lists "A mountain tarn heaves on a long swell" as a bug whose fix is "Gate swell to open bodies" — a fix that is self-defeating while :65 says a lake IS an open body. The enum at :31 has exactly one `lake` token and the table has exactly two columns, and :181 explicitly warns that geometry "cannot distinguish a reservoir from a lake", so no size-based sub-classification is available to an implementer to escape the collision.

The repair belongs at :65 and :38-39, NOT at :143 or :189. :38-39's "Everything in the gate table below follows from one of them [fetch and boundary]" is false at the residual-swell row: Lake Superior and the open ocean are in the same class on BOTH stated questions — fetch-unlimited (Superior's 560 km exceeds the 505 km needed for full development at 15 m/s) and a real-shore boundary (:86-87 puts a lake patch in the same "reflecting shore, fading offshore edge" case as a sea coast) — yet take opposite swell defaults. The row needs a third input the page never names: connectivity to a basin large enough to host remote storms. The ancestor document states that third input explicitly (obsolete/terrain-architect/references/03-flow-routing.md:~890, `SEA if waterSurface == globalSeaLevel and connectedToBoundaryOcean` — "an inland lake that happens to sit at sea level is a LAKE").

Severity `wrong` is correct. Scope note: this is a regression introduced at one site — the ancestor at obsolete/terrain-renderer/references/12-water-rendering.md:347-355 confines "thousands of kilometres" to "a glassy ocean" and then gates lakes off swell without exception; the gaia rewrite moved "large lake" into that bullet.

**Blast radius.** The erroneous claim (a lake in the open/swell class) is at ONE SITE ONLY: gaia/references/water-closed-vs-open.md:65. Every other site in the corpus states the opposite, correct rule, so nothing else needs repairing — but a fix landing only at :65 would leave the derivability framing untouched, which is its own half of the defect.

Eight sites stating the CORRECT contradicting rule (all in obsolete/, all consistent):
- obsolete/terrain-renderer/references/12-water-rendering.md:354 — "a lake has no distant storm feeding it, so it gets *no* residual swell — dead calm on a lake really is a mirror"
- obsolete/terrain-renderer/references/12-water-rendering.md:43 — "a lake gets **fetch-limited wind waves only** — no swell, no current (suppress the residual-swell component ... on lakes)"
- obsolete/terrain-architect/references/03-flow-routing.md:886 — "A lake gets wind waves only ... the sea gets swell, tide and nearshore circulation. Get the class wrong and you put ocean swell on a mountain tarn"
- obsolete/terrain-architect/references/03-flow-routing.md:927 — "with **no swell and no through-flow**"
- obsolete/terrain-architect/references/09-verification.md:362-363 — "Lake rendered with ocean swell ... lake=wind-waves-only via fetch"; "Mountain tarn has rolling swell | No fetch limit"
- obsolete/terrain-architect/references/00-index.md:274 — "fetch-limited **wind waves** ... not swell or current"
- obsolete/terrain-architect/references/08-output-contract.md:46 and 28-liquids.md:379 and 27-engine-data-handoff.md:245 — "lake gets wind waves only, river gets flow, sea gets swell + tide"

The DERIVABILITY claim ("fetch and boundary decide everything") repeats at 3 further sites and must be corrected with :38-39 or the corpus stays inconsistent:
- gaia/references/water-closed-vs-open.md:4 (frontmatter description) — "fetch and boundary decide the wave model, the sim edge, the budget and half the defaults"
- gaia/references/water-closed-vs-open.md:136 — "reasoned from fetch and boundary"; "The two questions it descends from are physics"
- gaia/references/index.md:50 — repeats the :4 description verbatim

Sibling gaia docs mentioning swell (wave-models.md:156,298,340; shallow-water.md:249,362; caustics.md:139; water-rendering.md:189) never assign swell to a lake — none affected.

Magnitudes checked for repetition: "thousands of kilometres" appears at exactly 2 sites — 12-water-rendering.md:348 (correctly scoped to "a glassy ocean") and water-closed-vs-open.md:66 (wrongly scoped to include "large lake"). "~15%" edge fade (:82,:147,:192), the Force 3/5/7 gates (:145,:171-173), and the 90 m / 2 m damping distances (:113-114) are internally consistent and are not part of this finding.

### water-closed-vs-open.md · `F4-enum-nine-values-table-two-columns` · `incomplete` · /home/user/skills/gaia/references/water-closed-vs-open.md:31 (nine-value enum) vs :140-152 (two-column gate table)

**Narrowest true statement.** `water-closed-vs-open.md:27` makes gating machinery off the authored nine-value `bodyType` enum (:31) the document's central instruction, but the gate table (:140-152) resolves only a two-way Open/Closed split and the document binds enum values to those columns in exactly two places — :65 "Open body (sea, large lake)" and :69 "Closed body (pool, tank, basin)". Four of the nine values (pond, canal, reservoir, river) are never assigned a column anywhere in the document; `canal` appears only in the enum declaration itself. A fifth, `lake`, is bound contradictorily: :65 bins it Open, whose :143 cell reads "Residual swell at zero wind: On", while :72-73 gives a lake the Closed cell's own words ("dead calm really is a mirror" against :143 Closed "Off — dead calm is a mirror"), so the failure-table fix at :189 "Gate swell to open bodies" reproduces the symptom in its own left column. :65's binning key ("large" lake) is additionally not expressible in the enum and is the size threshold :27-28 forbids. Narrower than a bare count mismatch: two columns are a legitimate axis — the defect is the missing and partly self-contradictory enum-to-column mapping, and the fact that `river` fits neither column (both choices reproduce defects listed in the document's own failure table at :191 and :192).

**Blast radius.** NOT one site only — five further sites, and one of them propagates a related mis-description.

1. `gaia/registers/pseudocode-execution.tsv:259` — repeats the nine-value enum verbatim and calls it "the nine body types the document's taxonomy admits", but records "NOT RUN -- the fence is a type declaration" / "NOT MEASURED". The nine-count is carried forward with the mapping never checked.

2. `gaia/references/papers-simulation.md:73` — describes `bodytype_doctrine` as "the gate table that flips nearly every water default **between natural and man-made bodies**". The table's actual columns are Open/Closed, not natural/man-made. This is exactly the crossing-axes error that refutation route (d) fails on (pond is natural but closed; a reservoir is man-made but can be open-fetch), recorded here as if it were the table's real axis. Second site, related defect.

3. `obsolete/terrain-architect/references/28-liquids.md:377-386` — the upstream doctrine, and it does NOT have this defect: it is explicitly structured as 7 natural values (`sea | lake | pond | river | stream | estuary | wetland`, "exhaustive for GENERATED water") plus a 5-value man-made extension (`pool | basin | tank | canal | reservoir`) = 12 total, and it hedges "**most** of the natural bands gate off". water-closed-vs-open.md:31 flattens this to 9, silently dropping `stream`, `estuary` and `wetland` (0, 0 and 0 enum mentions in the target document), and converts the hedged "most ... gate off" into a hard binary table.

4. `obsolete/terrain-renderer/references/12-water-rendering.md:43` — the named doctrine source, and it directly contradicts the target's :65+:143 combination: "a lake gets fetch-limited wind waves only — **no swell**, no current (suppress the residual-swell component ... on lakes)". Upstream also uses a THREE-way surface model ("sea gets swell + tide ...; a lake gets ... no swell ...; rivers get flow") that the two-column table collapses. This is the strongest external corroboration that binning `lake` Open is the error, not a reading of it.

5. The 7-value natural enum is repeated without the man-made extension at `obsolete/terrain-architect/references/27-engine-data-handoff.md:245` and `:348`, `08-output-contract.md:46`, `09-verification.md:362`, and `00-index.md:273`. These are consistent with each other and with (3); they are listed because any correction to the enum must land on all of them, and a fix applied only to `water-closed-vs-open.md:31` would leave the 9-vs-12 divergence in place — the one-end-only correction this corpus most often records.

---

## water-closed-vs-open.md — the last unread document

Audited for the first time: **7 findings raised**, 4 refuted-or-narrowed below; three
(`F5`, `F6`, `F7`) lost their refuters to a rate limit and remain **unverified**.

What its auditor could not settle, in its own words:

> Not checked: (1) Lamb, Hydrodynamics 6th ed. 1932 pp. 623-624 against the physical text - I verified only that the standard result tau = 1/(2*nu*k^2) matches the document's own description of eq. 7 and eq. 9, not the page numbers or article number. (2) Sibling corpus documents - this file names no other document by filename, so no cross-document contradiction could be tested. (3) Engine/rendering assertions that are empirical claims about real renderers and are not falsifiable from the text: \"screen-space reflection behaves unusually well\" on a small flat body (line 150), \"planar is a hero-body luxury\" (line 150), the depth ramp as \"the single strongest realism cue water has\" (line 148), and the aliasing-budget row (line 197). (4) \"one of the most-reported water artefacts\" (line 82) and \"the classic misclassification tell\" (line 72) - unfalsifiable as written, no counterexample available in the text. (5) The self-flagged unmeasured assumption at lines 104-106 (tiled-wall reflection coefficient) - the document already discloses it as unmeasured, so it is not a hidden defect. (6) Taste, prose and structure, per scope. Verified clean and reproducing from the document's own declared constants (sigma=0.0728, rho=1000, g=9.81, plus nu=1e-6 which the document does not declare): c_min = 0.231188 m/s vs printed 0.2312; lam_min = 0.017116 m vs printed 0.01712; the \"23.1 cm/s at 1.73 cm\" pair implies surface tensions 2.49% apart vs printed 2.5%; e-folding distance 89.85 m at 16.5 cm vs printed \"order 90 m\", and 89.85/8 = 11.2 vs printed \"eleven lengths of an 8 m pool\"; 2.12 m at 3 cm vs printed \"about 2 m\"; single-wave slope 2*pi*a/lambda and the amplitude-proportional-to-wavelength consequence (line 128-130) are correct; the three Beaufort knot ranges and the Force 3 quotation match the cited NOAA table exactly.
