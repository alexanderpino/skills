# Unrefuted audit findings — 2026-09-09

**STATUS: RAISED, NOT VERIFIED. Do not act on any line in this file without re-deriving it first.**

Twelve of the thirty-nine technique documents had never carried a single correction row —
no adversarial reader had ever opened them. Twelve were audited here, one hostile reader each,
instructed to re-derive every formula and recompute every printed number.

Every finding was to be attacked by an independent refuter prompted to kill it and to default
to *refuted* when uncertain. **Every one of those refuters died to a session rate limit before it
ran.** So this file is one half of a two-half process, and the missing half is the half that
decides what is true.

This corpus's own measured base rate is that roughly **one proposed defect in three is wrong**.
Applied to the 63 findings below, that predicts around twenty are mistaken. Which twenty is
exactly what the refutation pass would have told us. Until it runs, each finding is a lead.

`water-closed-vs-open.md` was never audited — its auditor died too. It remains the one
technique document with no adversarial read at all.

---

## VERIFIED BY HAND, 2026-09-09 — four of the sixty-three

The adversarial refutation pass could not run: a broken harness permission handler rejected
every file-access tool in all 64 workflow subagents (719 rejected calls; `Read`, `Bash`,
`Grep`, `Glob` each returning "the parameter ... is expected as `string` but provided as
`unknown`"). The agents never read a byte. The four below were therefore re-derived by the
orchestrator directly, and **only these four have any verification at all.** The other
fifty-nine remain exactly as raised.

| id | verdict | what was reproduced |
|---|---|---|
| `RN-4` | **CONFIRMED** | `river-networks.md:378` says `[genevaux2013]` and `[paris2023]` print the same rule "six years apart". 2023 − 2013 = **10**. |
| `ME-1` | **CONFIRMED** | `mesh-extraction.md:112` reports "3,453 interior edge contractions on the 41×41 grid". That grid has 41² = 1,681 vertices, 4·40 = 160 on the boundary, 39² = **1,521** interior. One interior contraction removes one interior vertex, so 3,453 is more than twice the available budget. |
| `TAM-1` | **CONFIRMED** | `terrain-analysis-masks.md:245` states `f − mean_ring_r(f) = (r²/4)·∇²f`. Measured on `z = 100 − (x²+y²)` at `r = 3` over 20,000 ring samples: centre 100.000, ring mean 91.000, difference **+9.000**, while `(r²/4)·∇²f = (9/4)(−4) = −9`. The identity is off by a sign; the correct form is `−(r²/4)·∇²f`. |
| `RN-3` | **CONFIRMED (arithmetic)** | `river-networks.md:82` tabulates `E − N + C` with an expected **0** against a measured **194,674**. Working back from the finding's stated inputs gives `C = 194,674 − 259,190 + 65,536 = 1,020`, exactly the perimeter-cell count of a 256² grid (4·256 − 4). Caveat: `E` and `N` are the finding's figures, not re-read from the page. |

`SKY-7` was attempted and is **not settleable from the cited lines alone** — the fp32 ULP
arithmetic reproduces exactly (2¹⁶·2⁻²³ = 7.8125 mm at 10⁵ m, confirmed against
`numpy.nextafter`), but `sky-and-weather-state.md:201-204` does not print the two magnitudes
the finding attacks, so the finding and the page have to be reconciled first.

---

## coastal-erosion.md — 6 raised, 0 refuted

`incomplete` 1  `unsupported` 3  `wrong` 2

### [CE-1] `wrong` — gaia/references/coastal-erosion.md:302

**Claim on the page.** "`wave-models.md` already owns the machinery you need to evaluate this — the travel-time/eikonal field whose iso-lines are refracted wavefronts... Do not rebuild it here; sample the shore-normal and the local wave direction out of it and feed `ψ`." (with :352 "Wave angle `ψ` | per-shoreline-cell | shore normal from the contour, wave direction from `wave-models.md`")

**Failure scenario as reported.** `ψ` in eq. (7)/(8) is the DEEPWATER angle — the document says so twice (:200-202 "breaking angles are small, because refraction turns waves toward the shore; the useful version is the deepwater recast"; :388 "The instability threshold is a deepwater angle, not a breaking angle"). The eikonal/travel-time field at wave-models.md:277-279 is propagated shoreward at `c(h)=sqrt(g·h)`, so its local direction is the REFRACTED angle. Snell (sinψ/c = const) caps it: with T = 10 s, c0 = gT/2π = 15.61 m/s, a sample at the document's own closure depth h* = 6 m has c = 7.36 m/s, so ψ ≤ arcsin(0.4715) = 28.13° for ANY deepwater angle up to 89°; at the breaking depth for H0 = 1 m (h = H/0.78 = 1.28 m) the cap is 13.00°; at the shoreline contour itself (h → 0) ψ → 0. The threshold is 42.392°, so a pipeline built exactly as :302 and :352 prescribe pins `μ > 0` at every cell forever: deepwater ψ0 = 60° arrives as 24.1° (μ ∝ cos²−1.2sin² = +0.632, near-maximum smoothing) precisely where the physics says a cape should grow. The result is both failure rows the document itself lists — :386 "Coastline is uniformly smooth, everywhere, always" and :388 "Capes and spits never appear at any setting" — produced by the document's own instruction.

**Proposed fix, unverified.** At :302 and in the :352 table row, say that `ψ = φ0 − θ` is built from the DEEPWATER wave direction (the eikonal solve's seed, or the wave-angle distribution) differenced against the local shore normal, and that the refracted direction sampled out of the travel-time field is the wrong input — it is the same error the :388 failure row names.

### [CE-3] `wrong` — gaia/references/coastal-erosion.md:164

**Claim on the page.** "**Nearly all of the answer is `h*`, and `h*` is a convention.**"

**Failure scenario as reported.** The document's own arithmetic ten lines later refutes it. R = S·(h*/A)^(3/2)/(h*+B): sweeping h* over the published 4–18 m gives 12.649 → 36.224 m, a 2.86× spread (:171, :173); moving A over Dean's two anchors at fixed h* = 8 m gives 21.466 → 7.589 m, a 2.83× spread (:175) — the same magnitude, which the document itself concedes as "Grain size does the same thing in the other direction". Over the A range the document tells you to expose ("give it a 0.05–0.25 range", :103) the spread is (0.25/0.05)^(3/2) = 11.18× — retreat 60.72 m at A = 0.05 against 5.43 m at A = 0.25, same S = 0.30 m, B = 2 m, h* = 8 m — i.e. 3.9× WIDER than the h* spread the sentence says dominates. Concretely: a builder who trusts the sentence hard-codes A = 0.10 and exposes h*; on a coarse-sand coast (A = 0.20, the document's own anchor) the tool reports 21.5 m where the document's own equation gives 7.6 m.

**Proposed fix, unverified.** Replace with the true statement: `R` is equally sensitive to `h*` and to `A` (2.86× against 2.83× over the published ranges, and 11.2× over the `A` range this document exposes); what makes `h*` special is that it is unmeasured, not that it carries most of the answer.

### [CE-4] `unsupported` — gaia/references/coastal-erosion.md:213

**Claim on the page.** "`wave-models.md` owns which height your spectrum produces — go and check before wiring this up."

**Failure scenario as reported.** wave-models.md (375 lines) contains no wave-height convention at all: grep for `significant`, `rms`, `r.m.s.`, `H_s`, `H_rms`, `H_1/3`, `root-mean` returns zero hits, and its spectrum section (:171-176) names Pierson–Moskowitz, JONSWAP and Phillips and says only "Drive them from wind speed and fetch, not from an amplitude dial" — it never says whether the field it produces is reported as H_s or H_rms, nor gives H_s = 4√m0. So the reader sent to check cannot resolve the very ambiguity this ⚠️ raises, and the 2.3× transport error the same paragraph warns about (and the :383 failure row "Transport rates out by ~2.3×") remains unguarded: they pick K2 = 0.34 or 0.15 by coin-flip and every alongshore transport rate is out by 0.34/0.15 = 2.27×.

**Proposed fix, unverified.** Either state the conversion in this document (a variance-spectrum field yields H_s = 4√m0 and H_rms = H_s/√2, so K2 = 0.15 for a spectral field reported as H_s), or drop the pointer — as written it is a locator into content that does not exist.

### [CE-5] `unsupported` — gaia/references/coastal-erosion.md:16

**Claim on the page.** "Everything else in this skill's erosion axis **roughens**. Stream power cuts valleys into a surface and sharpens the divides between them; thermal relaxation smooths, but only locally and to a repose angle. The coast is the corpus's one process whose governing equation is the **heat equation**"

**Failure scenario as reported.** stream-power.md:35 gives the corpus's fluvial governing equation as `∂h/∂t = U − K·A^m·S^n + D·∇²h`, and its section "## The diffusion term is not optional" (:139-148) states `D·∇²h` [culling1960] is what turns sharpened interfluves into hillslopes and that "the term belongs inside the same solver"; :261 calls it "not optional"; :273 lists "Knife-edge interfluves between channels | The diffusion term omitted" as a failure. papers-generation.md:323 describes culling1960 as "Hillslope transport as diffusion, `D·∇²h`", and sketch-based-authoring.md:145 writes it standalone as "`∂h/∂t = D∇²h`, the hillslope term `stream-power.md` keeps always on". That is a corpus process, in the erosion axis, whose governing equation is literally the heat equation, and whose job is to smooth. The document's hedge covers only thermal relaxation (slope-limited, correctly excluded) — it does not cover `D·∇²h`. Failure: a reader who accepts the frame builds the fluvial solve as pure stream power because "stream power sharpens" is the whole story here, and gets stream-power.md:281's symptom — relief and mean elevation climbing with every resolution increase, because `D·∇²h` supplies the only length scale in the model.

**Proposed fix, unverified.** Narrow the claim to what is true: the one-line model is the corpus's only process whose ENTIRE governing equation is a diffusion equation; `D·∇²h` inside the fluvial solve (stream-power.md:139-148) is the other heat-equation term in the erosion axis, and it smooths too.

### [CE-6] `unsupported` — gaia/references/coastal-erosion.md:119

**Claim on the page.** "At `x = 1 cm` with `A = 0.10` the local gradient is 0.31 — one in 3.2 — which is a cliff, not a beach face... The pure power law drawn straight into a heightfield puts a step at the shoreline, and it is the first thing anyone notices." (with the :379 failure row "A step or wall at the waterline")

**Failure scenario as reported.** 0.31 reproduces exactly ((2/3)·0.10·0.01^(−1/3) = 0.3094) but is evaluated 244× below the finest cell the document's own resolution table admits (:130, 10 km / 4096 = 2.44 m). A heightfield never samples the continuum singularity; what it draws is the first-cell mean slope A·Δx^(−1/3), which at the document's four listed cell sizes is 0.0743 (1 in 13.5), 0.0434 (1 in 23), 0.0275 (1 in 36) and 0.0202 (1 in 50) — the steepest of these, 4.2°, is gentler than a real coarse-sand beach face and is only 1.65× the profile's own mean slope of 1 in 22 quoted at :114. To make the first cell reach gradient 0.31 you need Δx = 3.4 cm, i.e. a 137 m domain across 4096 cells, which is nowhere in this document's 10–500 km regime. So the ⚠️ and the :379 failure row are inert where prescribed: a builder who splices a planar face over "the first few cells" is replacing a correct profile with an arbitrary one to fix a step that the grid never produced.

**Proposed fix, unverified.** Keep the analytic statement (the continuum slope diverges, and [dean1991] eq. (8) is the gravity-corrected form) but quote the number at a cell size this document actually targets — first-cell slope A·Δx^(−1/3), 1 in 13.5 at Δx = 2.44 m — and say that the splice is worth doing only below roughly a 5 cm cell, or restate the row as the land/sea slope discontinuity it really is.

### [CE-2] `incomplete` — gaia/references/coastal-erosion.md:387

**Claim on the page.** "Grid-scale noise on the shoreline explodes | Negative `μ` with no regularisation: backward diffusion amplifies the smallest `L` fastest | Cap the instability with wave shadowing, or clamp `ψ` below 42.392°" — and the document states no timestep bound anywhere for the diffusion solve at :190.

**Failure scenario as reported.** The document prescribes running `dy/dt = μ·d²y/dx²` explicitly on a grid and gives μ = 0.0539 m²/s and cell sizes (2.44 m at a 10 km domain, 12.2 m at 50 km) but never states the FTCS bound `Δt ≤ Δx²/(2μ)`. On its own finest listed grid that bound is Δt ≤ 55.3 s; at 50 km it is Δt ≤ 1383 s (23 min). The document's own e-folding table invites steps far larger — its shortest listed timescale is 1.3 hours and its longest 149 yr — so a natural Δt = 1 hour at Δx = 2.44 m gives r = μΔt/Δx² = 32.55 and a Nyquist amplification factor g = 1 − 4r = −129.2 per step (~1.2e21 after ten steps); Δt = 1 day gives r = 781, g = −3123. Even Δt = 1 minute is unstable (r = 0.542). The coastline blows up with `μ` STRICTLY POSITIVE, which is the symptom this row names while attributing it exclusively to negative μ, and the row's fix ("clamp `ψ` below 42.392°") does nothing about it. The sibling operator carries exactly this guard: stream-power.md:149-152 states `D·dt/Δx² ≤ 0.25`, `Δt ≤ Δx²/(4D)`, and keeps a 0.9 safety factor.

**Proposed fix, unverified.** State the von Neumann bound for the one-line solve (`Δt ≤ Δx²/(2μ)` in 1-D, with a safety factor), quote it at the document's own μ and cell sizes, and split the :387 failure row into the two distinct mechanisms — an over-long explicit step at positive μ, and unregularised backward diffusion at negative μ.

**What its auditor could not settle.** ARITHMETIC THAT DID REPRODUCE (so is not reported): A=0.067·w^0.44 inverted at both anchors (2.4848, 12.0073 cm/s; ratio 4.832; 20^0.44 = 3.7364); the whole W* table (253/465/1000/2415 and 89/164/354/854 m); mean slopes 1 in 22.36 and 1 in 134.16; the resolution table (2.441/12.207/48.83/122.07 m and 190/38/9.5/3.8 cells); the Bruun table (12.649/17.428/21.466/25.0/28.169/36.224 m), R/S = 71.554, 2.864× spread, 7.589 m at A=0.20, 7.155 m per decimetre; |μ| = 0.053886 m²/s from K2/D·T^0.2·H0^2.4 at ψ=0; the full e-folding table at both wave heights (1.31 h / 5.44 d / 1.49 yr / 148.96 yr, ×5.278); arctan(sqrt(5/6)) = 42.392046° by closed form AND by numerical maximisation of cos^1.2·sin (42.39205°), CERC breaking peak exactly 45.000°; −0.7426 at 20° and +0.6500 at 60°; unstable fraction 52.898%; 0.34/0.15 = 2.267 against 1.4^2.4 = 2.242; Sx angles 0.011459° and 0.114591° (matching driver-fields.md:381-383 verbatim); 25/5.8 = 4.31 and 25/2 = 12.5. Dimensional analysis of K2 (m^(3/5)·s^(−6/5)·s^(1/5)·m^(12/5)/m = m²/s) is correct. The sign warning at :260-269 is correct as written: μ = −(1/D)∂Qs/∂θ with ψ = φ0 − θ gives μ ∝ +∂Qs/∂ψ ∝ cos^0.2ψ·(cos²ψ − 1.2sin²ψ), positive at 20° and negative at 60°, so the printed bracket does carry −μ.

WHAT I COULD NOT SETTLE: (1) None of the four cited papers was reachable from this session, so every source claim was checked only for internal consistency and against the corpus's own bibliography entries in papers-coastal-and-ice.md:129-135, which agree with the document on all of: 504 profiles, n=2/3, eq. (8), the +224%/−68% SCOR range, Zhang et al. (2004), the 4 m/18 m closure depths, K2 = 0.34/0.15, the 35°–50° span, Γ = 0.02, and the landform list. (2) I therefore could not verify that ashton2006b eq. (8)'s prefactor is exactly K2/D with no extra numerical factor — |μ| = 0.0539 m²/s reproduces exactly only under that assumption — nor that eq. (10) prints the bracket in the order `(6/5)sin²−cos²`. (3) The document writes `W* = (h*/A)^(3/2)` and cites [dean1991], while its own frontmatter locator records eq. (12) as `W* = (H_b/(κ·A))^(3/2)`; the two agree only under the unstated substitution h* = H_b/κ. I did NOT report this, because that substitution is the standard closure-depth reading and I cannot open p. 59 to test whether Dean states it. (4) shadrick2022's 5.8 ± 4.0 / 5.9 ± 4.3 cm/yr and the 2–25 cm/yr range could not be checked against the paper. (5) No recorded run of any coastal operator exists anywhere in the registers — pseudocode-execution.tsv:190 records only closed-form re-derivation — so every behavioural claim in the document (e-folding times as observed outcomes, \"metre-scale wiggles erased within a day\", the cliff-notch/collapse coupling, the exposure-field claims) is arithmetic or argument, never measurement; I checked the arithmetic only. (6) I did not report the tension between the :387 fix \"clamp `ψ` below 42.392°\" (which suppresses the instability entirely) and the :388 failure row \"Capes and spits never appear at any setting\" — it is a real design conflict, but the clamp is offered as an emergency regularisation alongside wave shadowing and I could not make it produce a specific wrong number, so it is dropped rather than reported.

---

## impact-craters.md — 2 raised, 0 refuted

`unsupported` 1  `wrong` 1

### [IC-1] `wrong` — /home/user/skills/gaia/references/impact-craters.md:30-34, 272-273 (with 61-65, 121-124, 283)

**Claim on the page.** "choosing the simple or the complex branch by comparing the diameter against a transition diameter you scaled to your world's gravity — blending the two branches across 0.53–1.58 × `D*`" (:30-34); "| A world that is not Earth or Moon | Scale `D*` by `1/g` first, then everything else | The branch choice moves before any coefficient does" (:273); "On Earth's predicted `D* = 3.1 km` that is **1.7–5.0 km**" (:272).

**Failure scenario as reported.** Pike's two depth branches are lunar regressions that cross each other at a FIXED diameter — 0.196·D^1.010 = 1.044·D^0.301 at D = 10.5829 km (the document's own :78 figure). That crossing is a property of the fits and does not move with g. Below it the complex fit is the DEEPER of the two, so any world with D* < 10.58 km — i.e. any g > 30.8/10.583 = 2.91 m/s², which is Earth, Mars, Mercury and Venus, exactly the audience of §'The transition diameter is a property of the planet' — is instructed to evaluate the complex fit in a range where it is nonsense. Worked case, Earth, using the document's own D* = 3.14 km and its own blend band 1.65–4.96 km: a D = 5 km crater is fully on the complex branch, giving depth = 1.044·5^0.301 = 1.695 km, i.e. d/D = 1:2.95 — 1.70× DEEPER than the simple law's 0.996 km and deeper relative to diameter than any simple bowl (1:5). At the bottom of the same prescribed band, D = 1.7 km, the complex fit returns 1.225 km of depth, d/D = 1:1.39 — a crater deeper than it is wide is half as wide. This contradicts :121-124 ("A low-gravity moon is a world of deep bowls where Earth would have terraced, flat-floored, central-peaked basins") and inverts :283, which asserts the error can only run the other way ("Large craters are implausibly deep | Simple-branch depth extrapolated past the transition"). The same root cause bites the Moon less sharply but visibly: switching branches at D* = 19 km instead of at the fits' crossing puts a hard step in the size–depth curve, 0.196·19^1.010 = 3.835 km dropping to 1.044·19^0.301 = 2.533 km, a 1.51× discontinuity between a 18.9 km and a 19.1 km crater.

**Proposed fix, unverified.** Separate the two things the document currently conflates. `D*` scaled by 1/g decides whether a crater is morphologically complex (flat floor, terraces, central peak); it must not select which regression is evaluated. Select the depth/rim/width branch by each fit's own crossing — equivalently take the lower of the two branch values, min(0.196·D^1.010, 1.044·D^0.301) etc., which reproduces 10.583 / 21.273 / 30.352 km exactly and is continuous by construction — and state in §'The transition diameter…' and in the :273 crossover row that Pike's coefficients are lunar, that their crossings do NOT scale with gravity, and that a world with D* < 10.6 km has no published morphometry here, so the complex branch must not be used below 10.6 km. Add a failure-table row: "Small 'complex' craters on a high-gravity world come out deeper than simple ones | Complex branch selected by gravity-scaled D* below Pike's own 10.58 km crossing | Branch on the fit crossing, not on D*".

### [IC-2] `unsupported` — /home/user/skills/gaia/references/impact-craters.md:94-97

**Claim on the page.** "There is no single diameter at which a crater becomes complex, and the paper says so three separate times." … "his rim-flank-width fits at about 30.4 km — and of that last one he notes that \"visually the inflection seems closer to 20 km\". … [pike1977]"

**Failure scenario as reported.** The quoted sentence is attributed verbatim to Pike and is not carried by the document's own `pike1977` locator (:9), which is otherwise unusually complete: it quotes three strings verbatim ('at a crater diameter of about 10.6 km', 'at a diameter of about 21.3 km', 'the slope of Eq. (3) is now essentially 1.0…') and names p. 494 eqs. (8)–(9) for the rim-flank-width fits, but records no inflection remark and no '20 km'. The corpus's own read log is decisive against it: papers-generation.md:34-37 states that only "the two stated intersection diameters were then reproduced numerically" from the ADS scan — two, not three. The 30.4 km value is this corpus's own numerical solve of the two width coefficients (I reproduce 30.352 km), not a figure Pike prints, so the same sentence's count claim ("the paper says so three separate times") also exceeds what was read. Concrete failure: a reader following the corpus's provenance rule — every locator must carry the claim made against it — resolves :9 to p. 494 and finds neither the quotation nor a third stated crossing, so the sentence is unverifiable at its own citation and the ⚠️'s stated evidence count is one higher than the evidence. This is the open finding X45 (gaia/AUDIT-2026-09-05.md:843, status CHECKED; gaia/PLAN.md:339 still schedules it for Phase 3), and gaia/registers/corrections.tsv carries no row for impact-craters.md, so it has never been applied.

**Proposed fix, unverified.** Either extend the `pike1977` locator at :9 with the page and section where the 'visually the inflection seems closer to 20 km' sentence appears, or delete the quotation. Change "the paper says so three separate times" to "three separate pairs of fits cross at three different diameters, two of which the paper states (10.6 and 21.3 km); the 30.4 km crossing is solved here from eqs. (8)–(9)" — which is what :9 and papers-generation.md:34-37 actually support, and which loses nothing, since the abstract's 10–30 km band at :97-98 already carries the argument.

**What its auditor could not settle.** Everything numeric in the document reproduces from its own stated inputs; I re-derived each with python3 rather than by eye. Confirmed correct and NOT reported: all three fit intersections (10.5829 / 21.2726 / 30.3524 km vs the printed 10.583 / 21.273 / 30.4); the simple-branch d/D spread over [0.1,15] km (0.19154–0.20138, 5.14%) and rim/depth spread (0.18199–0.18567 against 0.036/0.196 = 0.18367); the complex rim/depth law (0.22605·D^0.098 → 0.2517 / 0.3017 / 0.3883 at 3 / 19 / 250 km); complex d/D of 1:7.8 at 20 km and 1:45.4 at 250 km, and 51.78 vs 5.50 km at 250 km; D*·g = 30.78 and all four table rows (8.30, 3.14, 110.0 km); every cell of the ejecta table (T = 9.83/16.76/28.58/57.88/98.70 m, rim = 17.83/36.00/72.70/184.1/371.8 m, ratios 0.551/0.466/0.393/0.314/0.266) and the 0.7468 km crossover, the 45–73% uplift share and the 1.81–3.77× overshoot; falloff at B=3 (1/8, 1/64, 1/1000) and B=2.8 (1/6.96, 1/48.5, 1/631); the full mass balance — 26.63%/37.09% to 4R below the plain, 21.72%/30.30% below the crest, 35.15%/48.97% to 100R, and the 1.226 convention factor; the SFD half-area diameters (harmonic means 0.01998 and 0.19802 km) and their log-range fractions (10.0%, 14.8%); the η=2 per-decade uniformity (dA ∝ D^(1−η) dD = D^−1); the dimensionlessness of 0.385 in n = 0.385 r^−2; the η-transfers-unchanged-between-r-and-D claim; and the band arithmetic 10/19 = 0.526, 30/19 = 1.579 with its Earth restatement 1.65–4.96 km ≈ \"1.7–5.0\" at 3.2×–9.6× D*.\n\nWhat I could not settle: (1) The primary sources themselves — pike1977 (Pergamon volume), austin2024, silber2017, minton2019 — were not opened here; I could only test each claim against the document's own declared locators and against papers-generation.md's read log, which is how IC-2 was reached. Whether Pike's p. 494 does or does not contain the '20 km' sentence needs the scan. (2) The diameter range over which austin2024's T = 0.14·R^0.77 was fitted is nowhere stated — neither in the :11 locator nor in papers-generation.md:369 — yet :149 asserts the ejecta table evaluates \"across the size range where each fit applies\" out to D = 10 km, a paper about \"small craters on the Moon\". The coefficient is consistent with the McGetchin 1973 form (0.14·R^0.74, metres) so the unit convention at :143 is sound, but I cannot confirm the fit is valid at R = 5 km and so cannot show the D = 5 km and D = 10 km rows are wrong. (3) Pike's smallest simple-crater diameters: the D = 0.5 km row and the 0.1 km end of the [0.1, 15] km ratio sweep may sit below his sample, unverifiable from the locator. (4) The product claims at :52-55 about Gaea's Crater/CraterField/Pockmarks nodes — no way to test what those nodes compute.\n\nDropped deliberately as unfalsifiable or as taste, so the next reader need not re-litigate them: the \"one landform … measured to three significant figures and published as a regression\" claim at :17-19 has an arguable counterexample in river-networks.md:11 (strahler1957's fitted b = 0.541, r_b = 3.52, stream-length b = 1.67), but no wrong number or wrong behaviour follows from it; \"wrong at both ends\" at :206 sits awkwardly against \"η ≈ 3 is the small end only\" two lines earlier, but the direction claim it supports (\"far too few large craters\") is correct — a single η = 3 against a shallow η = 1.8 middle branch under-produces r > 30 km craters by 15^1.2 ≈ 26× — and the phrasing admits a reading in which it is true; the Mars gravity printed as 3.71 at :114 against 3.72 quoted from silber2017 at :127 changes D* by 0.02 km and nothing rounds differently.

---

## mesh-extraction.md — 5 raised, 0 refuted

`unsupported` 1  `wrong` 4

### [ME-1] `wrong` — /home/user/skills/gaia/references/mesh-extraction.md:112

**Claim on the page.** "Over 3,453 interior edge contractions on the paraboloid, mean `sqrt(Δ)` was **0.0175** against a mean true vertical miss of **0.0031** — a ratio of **5.60**, ranging 4.17 to 6.94 across the contractions." — where "the paraboloid" is defined at :77 as "a 41×41 triangulated paraboloid".

**Failure scenario as reported.** A 41×41 grid has 41·41 = 1681 vertices, of which 4·40 = 160 are on the boundary and 39·39 = 1521 are interior. An edge contraction merges two vertices into one, so it removes exactly one vertex; an *interior* edge contraction (both endpoints interior) removes exactly one interior vertex. The run therefore admits at most 1,520 interior edge contractions, and at most 1,680 contractions of any kind. 3,453 is 2.27× the ceiling. I confirmed this by building an independent QEM rig on exactly this fixture and letting it contract every interior edge it could: it halts after exactly 1,520 contractions on 41×41 (and 3,480 on 61×61 — the smallest square grid that could support 3,453). So the printed 3,453, and with it the mean `sqrt(Δ)` 0.0175, the mean true miss 0.0031, the 5.60 ratio and the 4.17–6.94 range, cannot have come from the 41×41 fixture the document names two paragraphs earlier. A reader who reproduces the stated fixture gets a sample less than half the stated size and cannot check any of the five numbers.

**Proposed fix, unverified.** Name the grid the contraction run actually used, separately from the 41×41 grid used for the zero-initial-error check at :77 (a run of 3,453 interior contractions needs ≥ 61×61), or correct the count. Both ends of the fixture description have to agree, and whichever number changes, re-derive 0.0175 / 0.0031 / 5.60 / 4.17–6.94 from the run that is finally named. registers/pseudocode-execution.tsv:188 records the same 3453 as reproducing from `w3/qem.py`, so the register's row needs the grid size added at the same time.

### [ME-2] `wrong` — /home/user/skills/gaia/references/mesh-extraction.md:48

**Claim on the page.** "*Vertex clustering* — snap to a grid and merge; fast, and the error is unbounded and the output is unusable at the low end."

**Failure scenario as reported.** The document's own P-tier source says the opposite. [garland1997] §2.1, on Rossignac and Borrel: "A bounding box is placed around the original model and divided into a grid. Within each cell, the cell's vertices are clustered together into a single vertex... **However, while the size of the grid cells does provide a geometric error bound, the quality of the output is often quite low.** In addition, it is difficult to construct an approximation with a specific face count, since the number of faces is only indirectly determined by the specified grid dimensions." The bound is elementary: every vertex moves at most one cell diagonal, and every point of a triangle is an affine combination of its three vertices, so the Hausdorff displacement of the whole surface is at most that diagonal — pick a 0.5 m cell and no exported point is more than 0.87 m from the source. Concrete harm: an engineer choosing an export family reads "unbounded" and rejects the one method on the page whose geometric error is bounded by construction, in favour of a quadric threshold that this same document spends its longest section proving is *not* a geometric error and needs a separate measured `e` (:124-129) to be turned into one. The comparison inverts the property it is decided on.

**Proposed fix, unverified.** Replace "the error is unbounded" with what [garland1997] §2.1 actually charges vertex clustering with: the cell size does bound the geometric error, but output quality is low and the face count is only indirectly controlled by the grid dimensions, so you cannot hit a triangle budget. Keep the verdict, fix the reason.

### [ME-3] `wrong` — /home/user/skills/gaia/references/mesh-extraction.md:121

**Claim on the page.** "The direction holds everywhere (the ratio never drops below 1); only the magnitude was overreach." — the ratio being mean `sqrt(Δ)` over mean true vertical miss, measured at "6.91× at max slope 0.1, 5.59× at slope 1.0 ..., 4.09× at 2.0, 2.54× at 4.0 and 1.17× at 10.0" (:118-119).

**Failure scenario as reported.** The five measurements are monotone decreasing and stop one step above the crossing. Back out the implied accumulated plane count N from the document's own numbers using the exact relation for a locally planar region of slope s — a vertical deviation Δz sits at perpendicular distance Δz/√(1+s²) from every accumulated plane, so sqrt(Δ) = √N·Δz/√(1+s²) and the ratio is √N/√(1+s²): the five points give √N = 6.94, 7.91, 9.14, 10.47, 11.76, i.e. N ≈ 48, 63, 84, 110, 138. The ratio falls below 1 as soon as s > √(N−1) ≈ 7 to 12, and the document's own last sample (s = 10, ratio 1.17) is already inside that window. I built an independent QEM rig on the same fixture family (paraboloid on [-1,1]², 41×41, interior edge contractions, optimal-position collapse) and extended the sweep: ratio 8.06 at max slope 0.1, 7.16 at 1.0, 5.58 at 2.0, 3.53 at 4.0, 1.65 at 10.0 — same shape and order as the document, ~1.3× offset — then **0.863 at slope 20, 0.430 at 40, 0.179 at 100**. Worse, at the document's own steepest measured configuration (slope 10) the *per-contraction* minimum ratio in my rig is 0.662, so the ratio drops below 1 inside the measured range, not only past it, and the document reads this ratio per-contraction elsewhere ("ranging 4.17 to 6.94 across the contractions", :114). Concrete harm: a tool that exports `sqrt(Δ)` as `e` for a cliff chunk of slope 20 ships an `e` about 0.86× the true vertical error rather than 1.2–7× it; heightfield-lod.md's controller `rho = e·K/d` then refines *less* than the τ budget requires and the geometric error exceeds τ — an under-tessellation failure the failure table at :266 ("Runtime tessellates 1.2× to 7× more than the budget implies, worst on gentle ground") does not admit exists, so a reader instrumenting the metric will not look for it.

**Proposed fix, unverified.** Drop "everywhere" and "never drops below 1". State the bound instead: the ratio is √N/√(1+s²) for accumulated plane count N, so it exceeds 1 only while max slope stays below about √(N−1) — roughly 8–12 for the plane counts these five runs imply — and inverts on steeper ground. Either extend the sweep past slope 10 and print where it crosses, or say the crossing was not measured. Add the mirror row to the failure table at :266: on near-vertical ground `sqrt(Δ)` under-states `e` and the runtime under-tessellates.

### [ME-4] `wrong` — /home/user/skills/gaia/references/mesh-extraction.md:252

**Claim on the page.** "A chain that ships without it forces the runtime to guess, and every symptom in that document's failure table starts with a guessed `e`."

**Failure scenario as reported.** heightfield-lod.md's failure table (references/heightfield-lod.md:261-275) has 13 symptom rows. Exactly one of them involves a guessed `e`: :274, "Halving `tau` does not roughly quadruple triangles → Find the region whose `e` was guessed rather than measured". The other twelve have no dependence on `e` at all — :263 hairline cracks from `morphK` not reaching exactly 1.0; :264 single-pixel shimmer from a T-junction; :265 silhouette popping because height was sampled at the un-morphed position; :266 a crack surviving a correct `morphK` because the fine node reads mip L and its parent L+1; :267 lighting jumping because normals were not morphed with the same factor; :269 silhouette differing between depth prepass and base pass because each pass selected LOD independently; :270 shadow acne from selecting LOD at the light's distance; :271 dark seam lines because skirt curtains are visible to screen-space passes; :272 distant slopes sparkling from full-frequency normal mips; :275 vertex jitter beyond ~10 km from fp32 world-space maths. Concrete harm: an exporter author reads this line and concludes that writing a measured `e` into the metadata discharges the whole downstream failure list; they ship a chain with a correct `e` and no morph-constant sweep, and hit :263 and :266 — cracks at chunk borders — with no reason in this document to look at the morph at all. The document elsewhere insists the two concerns are disjoint ("Nothing here selects a level of detail, morphs between levels, or closes a crack at runtime", :26), which is exactly what makes the universal false.

**Proposed fix, unverified.** Narrow to what is true: "the one symptom in that document's failure table that is not a runtime bug — `Halving tau does not roughly quadruple triangles` — starts with a guessed `e`", or drop the quantifier entirely and say a guessed `e` is what that row's fix hunts for.

### [ME-5] `unsupported` — /home/user/skills/gaia/references/mesh-extraction.md:208

**Claim on the page.** "⚠️ **The first half of that is argued, not measured, and it is the one load-bearing comparison in this document with no number behind it.** Everything else here was run".

**Failure scenario as reported.** "What it beats" (:47-56) is four load-bearing comparisons — against vertex clustering, uniform decimation, marching cubes over an SDF, and exporting the full grid — carrying no number and no measurement, and it decides which family a tool implements, which is as load-bearing as a comparison gets on a page headed "Use this". Finding ME-2 shows the cost of trusting the claim: one of those unmeasured comparisons (vertex clustering's error is "unbounded") is contradicted by the document's own P-tier source, and it is unmarked because this ⚠️ tells the reader every comparison but one was run. The cluster-DAG row's floor claim at :243-244 ("those vertices survive to the coarsest level, so the chain floors out at the cluster-boundary vertex count however far you push the error threshold") is a second: a quantitative comparison between two build strategies, argued only, with the earlier draft's magnitude for it dropped rather than measured. So the count is at least three, not one, and "Everything else here was run" is false of the section immediately above the ⚠️ marker as well as of :47-56 and :235-258. Concrete harm: a reader auditing this page for unmeasured claims — which is exactly what the ⚠️ invites — checks only the greedy-versus-QEM crossover and passes the false vertex-clustering claim through.

**Proposed fix, unverified.** Scope the quantifier to the part of the document it is true of: "the one load-bearing comparison in the measured sections above with no number behind it". Then either measure or explicitly mark "What it beats" (:47-56) and the cluster-DAG floor claim (:243) as argued rather than run, so the ⚠️ stops vouching for them.

**What its auditor could not settle.** SCRIPTS ABSENT. registers/pseudocode-execution.tsv:188 says every QEM figure on this page reproduces from `w3/qem.py` and `w3/qem2.py`, but neither file is in this working tree (gaia/scripts/ holds only check.py, index.py, okf.py, requote.py, repudiation-prototype.py). So none of 2.8e-14, 1.8e-13, the `inf` condition number, 0.0175 / 0.0031 / 5.60 / 4.17-6.94, or the five slope ratios could be re-run against the original fixture. I built an independent QEM rig instead (paraboloid on [-1,1]^2, consistent-diagonal triangulation, unweighted plane quadrics, optimal-position collapse with endpoint/midpoint fallback, interior edges only) — it reproduces the SHAPE and order of magnitude of the slope sweep, not its exact values, because the domain, keep-fraction and definition of "true vertical miss" are mine. Finding ME-3 is stated so that it also stands on the document's own five printed numbers via the sqrt(N)/sqrt(1+s^2) relation, independent of my rig; finding ME-1 stands on arithmetic alone.

UNFALSIFIABLE AS PRINTED. The curved-patch boundary costs at :148 (2.2e-4 for the boundary collapse, 5.9e-4 for dragging the vertex a cell inward) name no curvature, cell size or amplitude, so there is no fixture to reproduce them from. The flat-patch 0.000e+00 pair at :143-144 is analytically exact and is correct.

SOURCES NOT OPENED. hoppe1996 (§3.1, §3.2 Geomorphs, §3.3, §3.5) was not fetched — the geomorph and vertex-split claims at :228-233 and in the table at :238-239 rest on the corpus bibliography only. karis2021 is a talk and the document itself states no slide is pinned for the cluster-DAG build half, so :240-246 could not be checked against a locator. garland1997 and garland1995 locators were ALL opened and ALL resolve: §5 eq. (2) with the a^2+b^2+c^2=1 normalisation, §4 for Q1+Q2 and eq. (1), §4.1's five numbered steps and the cost heap, §5.1 Geometric Interpretation with the parallel-plane degeneracy, §3.2's threshold t with t=0 giving plain edge contraction, §6's "Preserving Boundaries" paragraph (a bolded paragraph inside §6 Additional Details, not a numbered subsection) whose terrain-height-field sentence and perpendicular-constraint-plane construction are quoted verbatim and correctly; and garland1995's §3.1 Local Error Measure, §3.6 Conclusions from Importance Measure Experiments (quote verbatim, city/rolling-hills failure present), §4.4 Optimized Delaunay Greedy Insertion, §4.5.1 Combating Slivers, §5.3 Quality of the Approximations (m^-0.7 empirical against L2-optimal m^-1, and RMS steadier than maximum, both verbatim), plus the abstract's O((m+n) log m) against O(mn) and O(n log m + m^2) for "most previously published variants" and the million-point-in-under-a-minute figure. The "at most 3 times" quote at :88 is verbatim from §5 and the document's three-not-twice derivation at :89-92 is correct.

CHECKED AND CORRECT, recorded so a later reader need not redo them: cos^2(20 deg) = 0.88302 and the sqrt(n)*cos(20 deg) sequence 1.63 / 1.88 / 2.30 / 2.66 / 3.26 at n = 3, 4, 6, 8, 12 (:108-110); 2^(1/0.7) = 2.6918 -> "about 2.7x" (:194); the 100^2 = 10^4 rescaling factor (:132-133); ten floats for a symmetric 4x4 (:38, matches the paper's own wording); the singularity of eq. (1)'s matrix on a plane (rank-1 Q); and the document's 1.2x-7x range against its own 1.17 and 6.94 extremes.

NOT INVESTIGATED as a finding, flagged as loose rather than false: :136-137 attributes the non-resolution-independence of a quadric threshold to "valence and plane count changed", but refining a regular heightfield grid leaves every interior vertex at valence 6 in both grids — the mechanism is the accumulated plane count and the h^2 scaling of local deviation, not initial valence. The conclusion is correct, so I could not construct a failure from it.

INDUSTRY-PRACTICE CLAIMS not checkable here: "the default in every mesh toolchain" (:38), "usually halving triangle count per step" (:220), and the two export contracts at :254-258 (collision/render diagonal, simplify-before-baking-normals).

---

## planetary-precision.md — 3 raised, 0 refuted

`incomplete` 1  `unsupported` 2

### [PP-1] `unsupported` — /home/user/skills/gaia/references/planetary-precision.md:146 (citation) against :9 (locator)

**Claim on the page.** "**Horizon culling is the cheapest large cut you will ever add** [cozzi2011]." — while the frontmatter locator at :9 declares the whole of cozzi2011's usable range as `"ch. 5 Vertex Transform Precision — §5.3 rendering relative to eye on the CPU, §5.4 the GPU double-single form, §5.5 recommendations — and ch. 6 Depth Buffer Precision, §6.3 complementary depth buffering, §6.4 logarithmic depth"`.

**Failure scenario as reported.** The document makes three [cozzi2011] citations: camera-relative rendering (:43, = §5.3), GPU double-single (:80-81, = §5.4), and the horizon-culling test (:146). A reader who opens the declared locator range to verify the horizon pseudocode at :149-154 reads pp. 157-199 (ch. 5 Vertex Transform Precision, ch. 6 Depth Buffer Precision) and finds jittering, rendering-relative-to-eye, complementary depth buffering and logarithmic depth — and nothing whatsoever about horizon culling, occlusion cones, or occluder radii. The material is 190 pages later. Verified against the publisher's table of contents (https://www.gbv.de/dms/ilmenau/toc/654827850.PDF): ch. 5 runs pp. 157-180 with exactly the subsections the locator names (5.3 Rendering Relative to Eye Using the CPU p.169, 5.4 Rendering Relative to Eye Using the GPU p.171, 5.5 Recommendations p.177) and ch. 6 pp. 181-199 (6.3 Complementary Depth Buffering p.189, 6.4 Logarithmic Depth Buffer p.191) — so the two chapters the locator does name resolve perfectly, which is what makes the omission a silent one. Horizon culling is under §12.4 Culling, p. 390, in ch. 12 Massive-Terrain Rendering. The corpus already knows this: tiled-streaming.md:9 carries `{ id: cozzi2011, ... locator: "ch. 12 Massive-Terrain Rendering — §12.1 level of detail, §12.2 preprocessing, §12.3 out-of-core rendering, §12.4 culling" }`, and papers-rendering.md:131 lists "the horizon-culling test" among what cozzi2011 supplies. The source is the right one; this document's declared locator simply does not reach the claim it is attached to, so the citation at :146 cannot be checked from the coordinates given.

**Proposed fix, unverified.** Extend the cozzi2011 locator at :9 to name the third chapter it actually cites into — append "— and ch. 12 Massive-Terrain Rendering, §12.4 Culling (p. 390) for the horizon test", matching the range tiled-streaming.md:9 already points at.

### [PP-3] `unsupported` — /home/user/skills/gaia/references/planetary-precision.md:69-70, repeated at :11

**Claim on the page.** :69-70 "the evidence for it is Reed's direct simulation, where reversed-Z with a float depth buffer is **the only zero-error row** and erases the finite-versus-infinite far plane distinction entirely [reed2015]"; frontmatter locator :11 "the simulated indistinguishable-and-swap error table, **whose reversed-Z float32 row is the zero-error one**".

**Failure scenario as reported.** Reed's table (fetched from https://www.reedbeta.com/blog/depth-precision-visualized/, section "The Effects of Roundoff Error", near = 0.1, far = 10K, 10K linearly spaced depths) has seven rows across four columns {precomposed, separate} x {float32, int24}. Three rows are 0% indist / 0% swap in the float32 columns: "Unaltered Z values (control test)", "Reversed Z", and "Infinite + reversed-Z". The other four rows are 45/18, 45/18, 56/12, 59/10 precomposed. So a reader sent by the :11 locator to find "the zero-error row" finds three of them and cannot tell which is meant — and the first one they meet, the control test, is zero in all four columns and is not a depth mapping at all. The sentence at :69-70 also contradicts itself: "the only zero-error row" and "erases the finite-versus-infinite far plane distinction entirely" cannot both describe one row, because it is precisely the second clause that puts a second zero row (Infinite + reversed-Z) in the table. Reed's own text says only "Reversed-Z with a float depth buffer gives a zero error rate in this test" — he never writes "only", so the exclusivity is this document's strengthening, not the source's. The substantive conclusion the paragraph draws is right; the navigation instruction attached to it is not.

**Proposed fix, unverified.** At :69-70 write "the only zero-error depth *mapping* — both its finite and infinite far-plane rows"; at :11 write "whose two reversed-Z float32 rows are the table's only zero-error configurations, the unaltered-Z control row aside".

### [PP-2] `incomplete` — /home/user/skills/gaia/references/planetary-precision.md:146-147 and :152-153, against :158-159 and :175

**Claim on the page.** :146-147 "Scale space so the ellipsoid is the unit sphere, let `cv` be the camera in that space", with the test at :152-153 written as `dot(vt, vc) > dot(vc, vc) - 1` and `dot(vt, vc)^2 / dot(vt, vt) > dot(vc, vc) - 1`; then :158-159 "Use the patch's *maximum* height so peaks over the horizon survive, and the ellipsoid minus the deepest depression as the occluder radius, or valleys get culled while still visible"; and :175 "Valleys culled while still visible | Horizon test used the ellipsoid radius, not the deepest depression | Occluder radius = ellipsoid minus max depression".

**Failure scenario as reported.** The `1` in both comparisons at :152-153 *is* the occluder radius squared — it is the only place a radius enters the test — and :146-147 fixes it at the ellipsoid by construction. So the prescription at :158-159 and the fix column at :175 have no parameter to land in: an implementer who follows :146-147 literally has already hard-coded the exact bug those two lines name, and the document never says to change either the scaling or the constant. Concrete: scale by the WGS84 equatorial radius a = 6 378 137 m so the ellipsoid is the unit sphere; camera at 10 km altitude, |cv| = 1.001568; occludee a patch floor 430 m below the ellipsoid (Dead Sea shore — the Bentley Subglacial Trench at -2 555 m widens the band ~2.4x). Running :152-153 exactly as printed culls that patch over ground distances 349.2 km to 364.5 km — a 15.4 km-wide annulus around the horizon — while the occluder radius the document itself prescribes at :158-159, r = (a - 430)/a, does not cull it anywhere in that band. Sample points: at 356.2 km, 358.4 km and 361.8 km the printed test returns occluded = true and the prescribed test returns false. That is verbatim the symptom row :175 claims to have fixed. The corrected form is to replace both `- 1` with `- r^2`, r being the occluder radius in the scaled space; I checked that r-parameterised form against ray-sphere ground truth over 198 217 random (camera, target, r ∈ [0.5, 1]) configurations and it agrees exactly, 0 mismatches — as does the printed r = 1 form when the occluder really is the unit sphere (0 mismatches in 294 131 samples), so the pseudocode is correct mathematics fastened to the wrong radius.

**Proposed fix, unverified.** Either change :146-147 to "Scale space so the *occluder* is the unit sphere — the ellipsoid semi-axes minus the deepest depression, not the ellipsoid semi-axes", or keep the ellipsoid scaling and write the test with an explicit radius: `occluded = dot(vt,vc) > dot(vc,vc) - r*r && dot(vt,vc)^2 / dot(vt,vt) > dot(vc,vc) - r*r`, with a line saying r = 1 - maxDepression/a.

**What its auditor could not settle.** WHAT I VERIFIED AND FOUND CLEAN (so the next reader does not redo it): every row of the ULP table at :27-31 reproduces exactly from 2^(floor(log2 x)-23) — 1 km 6.1035e-5 m, 10 km 9.7656e-4 m, 100 km 7.8125e-3 m, 1000 km 6.25e-2 m, 6371 km 0.5 m; the 900 km / 6.25 cm and 2^20 m ≈ 1049 km / 12.5 cm claims at :33-34 are exact (2^20 = 1 048 576 m); the 12 mm bound figure at :36 is exact (1e5 x 1.2e-7); the "tight just above a power of two, up to 2x high just under the next" claim at :21-23 checks out (1.0067x at 2^e, 2.013x approaching 2^(e+1)); noise-and-warping.md:249's "~8 mm at 100 km" is this table's 7.8 mm, as :35 says. Geodetic-vs-geocentric max deviation is 0.1924 deg at latitude 45.10 (WGS84), matching ":119 about 0.19". Cube-sphere: gnomonic solid-angle ratio centre:corner = 5.196 ("roughly 5x"), tangent-adjusted u' = tan(u.pi/4) gives 1.299 centre:corner and 1.414 max:min over the face ("around 1.3-1.4x") — both exact. The horizon pseudocode at :149-154 is mathematically correct for a unit-sphere occluder (0 mismatches vs ray-sphere ground truth over 294 131 random configurations). The UV claim at :140-141 ("dies around level 23-24") and the failure row :174 ("quantizes around level 20+") are consistent, not contradictory: at u near 1 the ULP is 2^-24, so a level-24 tile spans exactly one representable value and a level-20 tile spans 16 — a degradation threshold and a death threshold, correctly different. The SV_DepthLessEqual / SV_DepthGreaterEqual pairing at :92-93 agrees verbatim with shader-craft.md:243-244, heightfield-raymarching.md:192-196 and corrections.tsv row 109 ([d3d11spec] §16.9.3.1), and every one of the four things :96-99 says shader-craft.md carries is actually there (:243-244 spec text, :253-256 why the wrong token is silent, :257 "Two ways to catch it, both cheap", :264-271 the undefined-behaviour half and the max-height-proxy precondition). All four upchurch2012 locators at :10 resolve against the Cornell PDF: §2 "Floating Point Analysis Methodology", §3.2 "Infinite Projection", §4.1 "Two-Step Transform", and §6 "Discussion and Conclusion" contains the quoted sentence verbatim — "Complementary Z, having a = n/(f-n) and b = fn/(f-n), will suffer the same arithmetic precision loss of 2e as 1/Z" — so the :66-68 attribution repair is sound. The hairy-ball invocation at :137 survives scrutiny: the cube corners carry pi/2 of angle deficit each, 8 x pi/2 = 4pi = 2.pi.chi by Gauss-Bonnet, so the edge rotations really are removable by per-edge tables and the corners really are where the irremovable holonomy sits.

WHAT I COULD NOT SETTLE: (1) Reed's table was read from the live reedbeta.com page on 2026-09-09, not from a 2015 archival snapshot — if the post was edited after publication, finding PP-3 is against the current text. (2) I confirmed §12.4 "Culling" p. 390 from the publisher's TOC (gbv.de) but did not open the book itself, so I did not confirm the sub-subsection title, the exact page of the horizon test, or that Cozzi's formulation is character-for-character the one printed at :149-154 — I established that pseudocode is correct independently rather than by comparison. (3) Three figures have no measurement recorded anywhere in this corpus and no citation attached: "Half of what gets reported as 'planet renderer bugs'" (:38-39), "this kills nearly half of what the frustum keeps" (:156, geometrically defensible for a level camera at surface level, since the horizon plane then passes essentially through the eye, but unmeasured), and "1/16 to 1/64 rate on consumer parts" (:82, consistent with published RDNA2 1/16 through Ada 1/64 but not checked against a vendor datasheet). None is falsifiable from the corpus, so none is reported. (4) The epiclwc locator at :12 was checked only through a summarising fetch of the UE5 Large World Coordinates page, which renders client-side; under the natural reading ("Niagara and Chaos are separately sectioned below" the Rendering → Shaders section) it resolves — Shaders does name LargeWorldCoordinates.ush and the new HLSL types, Niagara follows as a Rendering subsection and Chaos as the next top-level section — but I could not confirm the heading tree byte-for-byte. (5) OUT OF SCOPE BUT WORTH A LOOK BY WHOEVER AUDITS THAT FILE: gpu-driven-culling.md:62 budgets the horizon stage at "one dot product against the horizon plane/cone", while the test this document specifies needs dot(vt,vc) and dot(vt,vt) per chunk plus a square and a divide (dot(vc,vc) is loop-invariant per frame). The discrepancy lives in the sibling, not here, so I did not file it against planetary-precision.md.

---

## river-networks.md — 5 raised, 0 refuted

`unsupported` 1  `wrong` 4

### [RN-1] `wrong` — references/river-networks.md:42

**Claim on the page.** "**Discharge from drainage area**: `φ = 0.42·A^0.69`, `A` in m², `φ` in m³/s [genevaux2013], used unchanged by [peytavie2019]. Or take `A` straight from `flow-routing.md`'s accumulation." (restated in the source locator, :13, "with A in m² and φ in m³/s")

**Failure scenario as reported.** The quote is faithful — I fetched the paper (Purdue CGVLab PDF, p.143:5): "Let A [m2] be the watershed area. The mean flow φ of the river [m3s−1] is given by φ = 0.42·A 0.69." But the units are physically impossible, and this document *prescribes* the relation and then consumes its output two bullets later. Take a 10 km² headwater. `flow-routing.md:378` accumulates area in m² ("A[:] = cellArea # m²"), so A = 1×10⁷ and φ = 0.42·(10⁷)^0.69 = 2.84×10⁴ m³/s — roughly twelve times the mean discharge of the Rhine, for a catchment you can walk across. Fed into this document's own next bullet, `S_crit = 0.0125·Q^−0.44` gives 1.37×10⁻⁴, so every reach steeper than 0.014 % is authored braided: a 0.2 %-slope lowland creek comes out a braid. Read in km² the same relation gives φ = 2.06 m³/s, S_crit = 9.1×10⁻³, and the creek correctly meanders. The same φ into [peytavie2019]'s `a = φ/‖u‖` (:336) gives 2.8×10⁴ m² of water cross-section at 1 m/s instead of ~2 m² — a 3 km-wide channel on a 10 km² basin. At 1 km² the m² reading already gives 5,798 m³/s, so nothing on a normal map ever meanders.

**Proposed fix, unverified.** Keep the verbatim quote but flag the units as the source's error, the way the document flags Strahler's 0.541-vs-3.52 and candel's D50 unit. With `A` in km² the same Dunne & Leopold relation gives 0.42 m³/s at 1 km², 10.1 at 100 km², 241 at 10⁴ km² — the right order for bankfull — so state `φ = 0.42·A_km²^0.69`, or expose an anchor as the width section does at :119-123. Also note that Génevaux's φ is the *mean* flow while [leopoldwolman1957]'s line at :46 takes *bankfull*; the document silently equates them.

### [RN-2] `wrong` — references/river-networks.md:322

**Claim on the page.** "[genevaux2013] §7 prints `w(p) = (1 − d(p)²)² / r⁴` — a quartic bump with **no flat top**, reaching 1 only at the single point `d = 0`. So the case the warning described as safe is the paper's actual weight, and the channel bed does come out shallower than the profile asks for except directly on the centreline."

**Failure scenario as reported.** The transcription is exact — the paper (p.143:7) reads "w(p) = (1−d(p)2)2 / r4 if d(p)2 < r2 else w(p) = 0" — but the expression does not have the property the sentence asserts. w(0) = 1/r⁴, which equals 1 only when r = 1. Implement it literally with a 10 m support radius and the document's own operator `h_C = (1−w_B)h_A + w_B h_B`: on the centreline w = (1−0)²/10⁴ = 1×10⁻⁴, so a requested 4 m carve lands 0.4 mm deep; at d = 9.9 m, just inside the support, w = 0.941, so the bank is carved 3.76 m. The weight is a bathtub with its *minimum* on the centreline, not a bump with its maximum there, and "shallower … except directly on the centreline" is inverted precisely on the centreline. Worse, for r < 1 m the expression exceeds 1 — at r = 0.5 m, w(0) = 16 — so `h_C` is not a convex blend at all but the extrapolation −15·h_A + 16·h_B, and the sentence's opening premise ("That expression is a convex blend") fails outright. The source formula is dimensionally inconsistent: it subtracts a squared length from 1, and it is discontinuous at its own stated support boundary d² = r², where the paper sets w = 0 but the expression gives (1−r²)²/r⁴ (0.98 at r = 10).

**Proposed fix, unverified.** Keep the verbatim quote but say the source expression is broken and name the form that carries the properties the paragraph needs: `w(p) = (r² − d(p)²)²/r⁴ = (1 − (d/r)²)²`, which is 1 at d = 0, 0 at d = r (making §7's "else w(p) = 0" continuous), and is the same quartic `sketch-based-authoring.md:287` compares against smoothstep at the half-radius — its printed 0.0625 gap is (1−0.25)² − 0.5 = 0.5625 − 0.5, an identity that only holds for the normalised form. `sketch-based-authoring.md:292` already states this §7 weight as "`w(p) = 0` wherever `d(p)² ≥ r²`", which the expression printed here violates.

### [RN-3] `wrong` — references/river-networks.md:83

**Claim on the page.** D8-vs-MFD table row: "| undirected cycles, `E − N + C` | **0** | 194,674 |", against "| edges | 64,516 | 259,190 |" (:80) and "| `edges == N − roots` | true (65,536 − 1,020) |" (:81)

**Failure scenario as reported.** The row's own inputs force the component count. 194,674 = 259,190 − 65,536 + C ⟹ C = 1,020 — exactly the D8 root count the row above prints, and exactly the 4·256 − 4 = 1,020 perimeter cells of a 256² grid. But C in E − N + C is the number of connected components of the underlying undirected graph, not the number of sinks. Under D8 the two coincide (one root per tree, which is why the D8 column checks out: 64,516 − 65,536 + 1,020 = 0). Under MFD they cannot: the next row states that 63,983 of the 64,516 interior cells send to more than one receiver, and multi-receiver splitting is precisely what stitches adjacent D8 basins together, so on a single fractal dome every perimeter cell receives from the interior and the graph collapses to one weakly-connected component. With C = 1 the cyclomatic number is 259,190 − 65,536 + 1 = 193,655, so the printed 194,674 is 1,019 too high (0.53 %). The root count from the D8 run has been carried into the MFD column.

**Proposed fix, unverified.** Recompute C for the MFD column as an actual connected-component count rather than reusing the D8 root count, and print that figure (193,655 for C = 1). State C in the row so the arithmetic is checkable, as the D8 column already does. The document's argument — that MFD has cycles and D8 has none — is unaffected.

### [RN-4] `wrong` — references/river-networks.md:379

**Claim on the page.** "[genevaux2013] §6.1 and [paris2023] §6.1 print the same rule six years apart"

**Failure scenario as reported.** The two citation keys carry their years: genevaux2013 is Génevaux et al., ACM TOG 32(4), 2013 (`papers-flow.md:194`), and paris2023 is Paris et al., ACM TOG 42(6), 2023 (`papers-flow.md`, paris2023 entry). 2023 − 2013 = 10, not 6. A reader recomputing the interval from the document's own bibliography gets ten years. Six is the gap between genevaux2013 and peytavie2019, which is not one of the two works cited in this sentence and which carries no junction-angle rule (its §5.1 does junction height adjustment, not junction angle).

**Proposed fix, unverified.** "print the same rule ten years apart".

### [RN-5] `unsupported` — references/river-networks.md:94

**Claim on the page.** "`flow-routing.md` says exactly what you get if you try: \"thresholding an MFD field to get a network produces a smeared, braided mask\". *Smeared* is the operative word"

**Failure scenario as reported.** That sentence appears nowhere in `flow-routing.md`, and nowhere else in gaia/ — grep for "thresholding an MFD" returns only this line, and "smeared" in `flow-routing.md` returns only its failure-table row at `flow-routing.md:436`: "| River mask smeared and braided | MFD thresholded to make a network | Route D8 for the network |". A reader who follows the pointer to check a quotation presented with "says exactly" and quotation marks finds no such text; the locator does not resolve. The following clause then treats the invented wording as the sibling document's considered term. `scripts/requote.py`'s own header states the doctrine — a quotation that "does NOT appear in the artefact … is a misquotation and is the one verdict here that is a defect on its own" — but that tool matches quotations only against cited artefacts, so a fabricated quotation of a sibling document passes every guard in the corpus.

**Proposed fix, unverified.** Drop the quotation marks and cite the row: "`flow-routing.md`'s failure table has it as *River mask smeared and braided* where MFD is thresholded to make a network (`flow-routing.md`:436)" — or quote that row verbatim.

**What its auditor could not settle.** WHAT I RECOMPUTED AND WHAT REPRODUCED. Every printed number I could regenerate from the document's own inputs was recomputed in python3 and reproduced: the cfs→SI conversion 0.06·(0.3048³)^0.44 = 0.012504 → 0.0125; all eight cells of the planform table (0.0125/0.004538/0.001648/0.000598; 1.66/13.32/311.2/58304.8); Cottonwood 800 cfs = 22.65 m³/s, S_crit = 0.003167, with 0.0011 below and 0.004 above; 6.5·0.3048^(−0.1) = 7.32001 and the 11.20 % understatement; λ/w = 7.32/9.22/11.60/14.61; 0.5×0.69 = 0.345 and 10^0.155 = 1.4289, 1000^0.155 = 2.9174; (2/16)^(1/0.345) = 2.412×10⁻³; both Leopold triples summing to 1.00 and m/f = .85 / .25; the Strahler refit slope −0.547167 with 10^b = 3.52506 → 3.53, 10^0.541 = 3.4754, 10^0.547 = 3.5237, 3.52⁴ = 153.5 → 154; 139/200 = 69.5 %; m ∝ W^1.5S^0.5Q^−0.5 with 2^1.5 = 2.828, B_i = 1.914, 8× discharge to undo; A_xs ∝ Q^0.9 with 1000^0.05 = 1.4125 and 1000^0.1 = 1.995; √35.3147 = 5.9426; 1000^0.42 = 18.197; 1.32/2/10^0.345 → 10.1 %/27.0 %/121.3 %; 289/899 = 32.1 %, 4/71 = 5.6 %; 64,516 = 65,536 − 1,020 and the D8 cyclomatic 0. I also dimension-checked ω_pot = ρgQS/W_r (→ W/m², correct), φ = W_r/√Q (→ m^−0.5 s^0.5, correct), and candel eq. (10) (dimensionless in m, correct).

SOURCES I OPENED. genevaux2013 (Purdue CGVLab PDF) — verified the φ = 0.42·A^0.69 sentence with its [m2]/[m3s−1], the §7 weight w(p) = (1−d(p)²)²/r⁴ with its d²<r² support, h(p) = u_z(p)+δ(d(p)), h_C(A,B) = (1−w_B)h_A+w_B h_B, and "This asymmetric operator". leopoldwolman1957 (USGS PP 282-B) — verified the braid definition, the arbitrary 1.5 sinuosity, "a line described by the equation" on p. 60, λ = 6.5w^1.1 with "not a constant ratio but a power function having an exponent slightly larger than 1.0" on p. 59, fig. 46 on p. 59, the fig. 45B ratio quote on p. 58, and every Cottonwood Creek number including 0.049→0.060 foot and "the fortuitous occurrence of a patch of coarser gravel". leopold1953 (USGS PP 252) — verified p. 9 (b=.26/f=.40/m=.34 over 20 cross sections and the "vary widely from one cross section" quote), p. 16 (eqs. 1–3, b=0.5/f=0.4 downstream at mean annual discharge), p. 36 (the two triples side by side), p. 51 (the "important unsolved problem" quote). Eq. (1) of leopoldwolman1957 itself is a page image and did not extract; I take it as corroborated by the paper's own text that the two Cottonwood points fall on opposite sides of the line, which the document's SI conversion reproduces.

WHAT I COULD NOT SETTLE.
1. candel2021 is unreachable from here. edepot.wur.nl/532990 returned "Service temporarily unavailable" on three attempts (60 s, 180 s, 240 s, browser UA); journals.sagepub.com returned a Cloudflare interstitial to curl and HTTP 403 to WebFetch; dspace.library.uu.nl/handle/1874/420363 served a 567-byte stub. So ω_bm = 900·D50^0.42, ω_ia = 90·D50^0.42, the "D50, m" prose and the Fig. 2d/3c/6 axis labels, the 79 %-of-126 / κ = 0.48 and 96 % / κ = 0.95 scores, the 79 %-of-97 and 76 %-of-29 split, φ = 4.7 / 3.0, the 43 % for bar-free rivers, eqs. (10)–(11), and the Rakaia/Nqoga exemplars are all UNVERIFIED at source. The abstract (via Semantic Scholar) confirms the paper's framing but none of these figures. Related and unresolved: river-networks.md:172 says candel Table 1 scores the Leopold–Wolman line "against six other discriminators" (seven total) while papers-flow.md's candel2021 entry says the paper "Puts eight published channel-pattern discriminators on one dataset". One of the two is wrong; without the paper I cannot say which, and neither count propagates into a downstream number, so I have not reported it as a finding.
2. strahler1957 (AGU Transactions 38(6), paywalled) was not fetched. The counts 139/46/11/3/1, the printed b = 0.541 and r_b = 3.52, the fig. 4 length regression b = 1.67, and the Coates 4.0–5.1 / 2.8–4.9 ranges are unverified at source. The refit arithmetic reproduces exactly from the printed counts, and the internal inconsistency the document flags (10^0.541 = 3.475 ≠ 3.52) follows from the paper's own two printed figures whatever their provenance.
3. paris2023 and peytavie2019 were not opened. w_Γ ∝ a^0.5, w_Γ = 18.8·d_Γ^1.41, the Konsoer attribution, the three collision cases, the §5.1 monotonicity sentence, the unit-area template, a = φ/‖u‖, and the Rosgen-D minimum rule are all taken on the document's word.
4. The three harness scripts named in code spans — hydraulic_geometry.py, d8_is_a_tree.py, carve_monotone.py — are NOT in this repository (corrections.tsv:172 records exactly this as an open, unrepaired item across 12 documents). So I could not re-run the D8/MFD or carve-monotone measurements; I could only recompute the arithmetic that follows from their printed outputs, which is how RN-3 was found. The unreproducible-in-principle figures are: 259,190 MFD edges, 63,983 multi-receiver cells, 9,205 confluences, max in-degree 5, longest flow path 169 cells, and the carve table's 107.9 m / 138.6 m / 0.03 m / 4.6 m excavation figures and the 112 lifted samples. All of those are corroborated by an independent re-run recorded in registers/pseudocode-execution.tsv:194-195, which is not an independent check of the scripts' internal definitions.
5. Hooshyar et al. 2017 (the junction-angle attribution at :380) and Konsoer et al. 2013 (:140) are stated by the document itself as not opened; I did not open them either.

---

## sketch-based-authoring.md — 4 raised, 0 refuted

`incomplete` 1  `stale` 1  `unsupported` 1  `wrong` 1

### [F1] `wrong` — gaia/references/sketch-based-authoring.md:102-104

**Claim on the page.** "Measured, the height re-added per step falls from 4,562 to 867 cell-metres over 200 steps and then stops falling — 19% of the initial rate, forever. **A per-step projection does not converge to a fixed point; it converges to a steady flux.**" (repeated at :58 and in the failure table at :380, "Per-step projection reaches a steady flux, not a fixed point — 19% of the step-1 work at step 200")

**Failure scenario as reported.** A plateau is asserted at t = 40 in a problem whose equilibration time is ~4x10^2 times longer. At the document's own stated inputs (257^2 grid, D = 1, dt = 0.2, 200 steps, :79-80 and :138) the diffusion length is sqrt(2DT) = sqrt(80) = 8.9 cells, against ~128 cells to whatever fixes the far field; the time to reach a steady flux is ~L^2/D = 1.6x10^4, i.e. ~82,000 steps. Step 200 is therefore a transient, not an asymptote. Replicating the arm directly (257^2, Dirichlet outer boundary, curved sigma = 4 / 40 m crest re-imposed on a hard mask every explicit diffusion step, D = 1, dt = 0.2), the per-step re-added height is 7.4% of its step-1 value at step 200, 3.6% at step 1,000, 1.6% at step 10,000, against an exactly solved asymptote of 1.38% — still 5.4x above its own steady value at step 200 and still falling four decades later. Concretely: an engineer who budgets "19% of one diffusion step, forever" for a 10,000-step bake over-prices the projection by roughly 5x, and a reader who takes "stops falling" as evidence the operator has equilibrated will conclude, wrongly, that running longer cannot reduce the constraint's cost. The register row the sentence rests on records no plateau: registers/pseudocode-execution.tsv:197 says only "is still re-adding 867 -- 19% of that -- on step 200, so it never reaches a fixed point."

**Proposed fix, unverified.** Cut "and then stops falling", "forever" and "it converges to a steady flux" — none of them were measured. State what the run shows: the re-added height is still 19% of the step-1 value at step 200 and decays only logarithmically, so the projection keeps costing for the whole run and never reaches a fixed point. Apply the same edit to :58 and to the failure-table mechanism at :380.

### [F2] `unsupported` — gaia/references/sketch-based-authoring.md:213-214

**Claim on the page.** "What sets the excursion is the distance from the constraint to the clamped boundary, not the gap between the lines: the line's own length barely moves it, 17 columns to 63 taking the minimum only from −0.558 to −0.628."

**Failure scenario as reported.** The document's own table three lines above (:199-201) holds the domain fixed at 65^2 and varies only the gap: plate min −0.616 at s = 6, −0.332 at s = 10, −0.090 at s = 24. That is a 6.8x change in the excursion produced by the gap alone — larger than the 3.4x the same paragraph's domain sweep produces (−0.616 at 65^2 to −2.103 at 257^2 with s held at 6). I rebuilt the rig (65^2, 33-column drawn lines at values 0 and 1, 13-point clamped-plate biharmonic, two zeroed boundary rings) and it reproduces every printed entry: −0.6155/1.3870, −0.3317/1.1198, −0.0899/1.0200 at s = 6/10/24; 4.20% and 0.26% first-cell loss, 92.0% and 79.9% above 1%, min −0.000022 at :181-182; 100%/122% at 129^2 and 190%/210% at 257^2; −0.558 and −0.628 for the 17- and 63-column lines. On that same verified rig, holding the domain at 257^2 and varying only the gap gives min −2.103 (s = 6), −0.494 (s = 24), −0.220 (s = 48). So a tool author who takes the sentence literally sizes the plate overshoot from window size alone, predicts ~210% undershoot for any pair of contours on a 257^2 edit window, and rejects the biharmonic solver — while two contours 48 rows apart on that window actually undershoot by 22%, an order of magnitude less. The clause after the colon supports only the weaker claim it states (line *length* barely matters); it does not support the claim about the gap.

**Proposed fix, unverified.** Both variables set it, and what governs is their ratio: 65^2 with s = 6 gives 62% below, and 257^2 with s = 24 gives 49% below — the same gap-to-boundary ratio (~10.8) at four times the scale. Rewrite as "what sets the excursion is the gap measured against the distance to the clamped boundary, not either alone — the drawn line's own length barely moves it, 17 columns to 63 taking the minimum only from −0.558 to −0.628." The paragraph's conclusion ("not a number you can budget for") survives the correction unchanged.

### [F3] `stale` — gaia/references/sketch-based-authoring.md:377

**Claim on the page.** "| The drawn ridge is gone after the erosion pass | It was an initial condition and its width is under the diffusion length; a 4-cell ridge keeps 18.8% of its relief in 200 steps | Impose after, or widen it: half-life goes as σ², so `σ ≥ √(2DT/3)` |"

**Failure scenario as reported.** The row states its symptom in RELIEF and its cure in PEAK — the exact pairing the warning block at :128-136 was written to forbid ("That closed form is in PEAK amplitude, and the table above it reports RELIEF. They are not the same number... Treat the closed form as a **lower bound on the width you need**, not as the width that works, and check relief on your own field before trusting it"). The correction landed in the body and never reached the failure table. Concretely: a user whose sigma = 4 crest came out at 18.8% relief reads this row and widens to the printed threshold, sigma = sqrt(2*1*40/3) = 5.164 at D = 1, dt = 0.2, 200 steps. The closed form then returns exactly 5.164/sqrt(5.164^2 + 80) = 0.500 peak retention, which by the document's own next sentence (:134) is "appreciably less than half of what a viewer would call the ridge" — the ridge still reads as gone, the user re-files the same bug, and the second half of the fix ("check relief on your own field") is nowhere on the page they landed on.

**Proposed fix, unverified.** Carry the caveat into the row, e.g. fix column: "Impose after; widening only buys a floor — `σ ≥ √(2DT/3)` is a lower bound in PEAK amplitude and relief goes faster, so check relief on your own field (§2)." The Crossovers bullet at :336 has the same gap and can take the same clause.

### [F4] `incomplete` — gaia/references/sketch-based-authoring.md:162-163

**Claim on the page.** "Solved together as one over-constrained system by Jacobi relaxation inside a multigrid, at `5(l − i)` iterations on level `i` [hnaidi2010] §5.2 — the same schedule [orzan2008] uses for diffusion curves, which is where the method comes from."

**Failure scenario as reported.** Neither `l` nor the direction in which `i` counts is defined anywhere in the document (grep for "level" returns :162-163, :249, :297 and :385; none of them defines either symbol), and the printed formula evaluates to 5*(l−l) = 0 relaxations at whichever end of the hierarchy carries i = l — so as printed, one entire level of the multigrid is never smoothed and the reader cannot tell which. Worse, the document's own front matter prints the two schedules it calls "the same" as opposite functions of i: hnaidi2010 as "5 times l−i Jacobi iterations at multigrid level i" (:9) and orzan2008 as "5i iterations at level i" (:10). 5(l−i) decreases in i and 5i increases in i, so the equivalence holds only if the two papers index levels in opposite directions — which the document never says. An implementer who adopts the indexing that orzan2008's 5i implies (i = 0 finest, so that 5i puts the cheap sweeps on the coarse grids) and then applies the body's 5(l−i) runs 5l sweeps on the finest grid and zero on the coarsest, inverting the schedule on the one solve this document elsewhere measures as global (92% of the domain moved by one line, :223).

**Proposed fix, unverified.** Define the symbols on the page — `l` = number of levels, and state explicitly whether i = 0 is the finest or the coarsest — then either show that 5(l−i) and 5i are the same schedule under the two papers' opposite conventions, or drop "the same schedule [orzan2008] uses" and cite only hnaidi2010's form.

**What its auditor could not settle.** WHAT I DID REPRODUCE (so it is not in findings): the Gaussian-under-diffusion derivation A(t)/A0 = sigma/sqrt(sigma^2+2Dt) and t_half = 1.5 sigma^2/D, both correct and dimensionally sound; the whole half-life table at :121-126 (predicted 30/120/480/1920 steps and amplitudes 0.218/0.408/0.667/0.873 all reproduce exactly under an explicit 1-D diffusion run at D=1, dt=0.2, r=0.2, which is inside the 2-D explicit stability limit 0.25); the threshold 5.16 cells and 5.2 cells; the whole Laplace-vs-biharmonic section (4.20%/0.26%, 16.5x, 92.0%/79.9%, min −0.000022) reproduced to 3 dp on an independently assembled 65^2 rig with 33-column drawn lines; the entire two-line overshoot table (−0.616/1.387, −0.332/1.120, −0.090/1.020) and the membrane's [0,1] column; the domain growth 100%/122% at 129^2 and 190%/210% at 257^2 and the failure-table's −2.10/2.90; the line-length sweep −0.558 to −0.628; the 1-D clamped-beam figures 39%/63% at 65 nodes, 272%/298% at 257, and the \"within 0.014\" agreement (max deviation 0.0137); every ratio and percentage in the three-arm table (128x, 21%, 5%, 19%, nearly-doubled pits); the 0.0625 half-radius difference between the quartic and smoothstep; the 1.25 m/cell linear falloff and the 40 m/cell hard cut; the C1 property of (a^2−1)^2 at both ends.

COULD NOT SETTLE:
1. The three-arm comparison table (:82-88) — relief kept, interior pits, max |Δ²h| in the skirt. `constraint_solvers.py` is not in the tree (gaia/scripts/ holds only check.py, index.py, okf.py, repudiation-prototype.py, requote.py), and the terrain seed, the projection mask, the pit criterion and the definition of \"relief\" are not stated, so those five rows cannot be regenerated from the page. registers/pseudocode-execution.tsv:197 records them as re-running unmodified with the same values. My F1 finding rests on scale analysis and my own replication of the mechanism, not on disputing these five numbers.
2. The 0.002 m/cell entries at :279-280. The estimator for \"slope step at the seam\" is not stated. The analytic slope step of any C1 weight is exactly zero, so whatever appears in those cells is the residue of a particular finite-difference estimator — my own one-sided estimates on the same profile land between 3e-4 and 1.5e-1 depending on order. The document labels it \"the finite-difference floor of the measurement and not a real step\" and register row 199 records the same value for both weights, so I could not make it fail and did not report it.
3. Source content. None of the five PDFs (hnaidi2010, orzan2008, gain2009, genevaux2013, stava2008) were opened here — quotations, section numbers and paper figures were checked only against this document's own front matter and papers-generation.md, never against the artefacts. Two loose ends I could not close either way: (a) \"59 curves in 2.89 kB\" at :354 appears nowhere else in the corpus and is not among the items the hnaidi2010 §6 locator at :9 enumerates, though papers-generation.md:53-54 does record §6 and both tables as read; (b) :368 cites hnaidi2010 §2 and gain2009 §2 for the Zhou et al. positioning, while papers-generation.md:53-59 records only \"Sections 3 through 6\" and \"Section 4 and section 5\" as read for those two papers — but papers-generation.md:64 makes the identical §2 claim, and check.py reports that only 6% of citations in this corpus name a section at both ends, so a body section absent from the locator is the corpus norm rather than a declared defect. I left both out of findings.
4. Timings at :105-107 (110 / 532 / 26 us) differ from the register's 108.3 / 516.7 / 25.4 us. Both give 21% and 5%, and wall-clock timings from a re-run are not expected to reproduce, so this is not a defect.
5. Whether \"interactive\" in the Tier line at :18 is consistent with the only costs on the page (0.17-0.81 s for the multigrid solve, 2.3 s in the gain2009 locator). The corpus has no numeric definition of \"interactive\" that I could find, and check.py's budget metric compares the Tier line only against the `tags:` budget token, which agrees. Not falsifiable from here.

---

## sky-and-weather-state.md — 9 raised, 0 refuted

`stale` 5  `unsupported` 2  `wrong` 2

### [SKY-1] `wrong` — /home/user/skills/gaia/references/sky-and-weather-state.md:152-153

**Claim on the page.** "An O(1) multiple-scattering approximation is run *every frame* rather than baked, so the whole LUT set rebuilds in 0.31 ms where the 2008 bake costs 250 ms on a 2016 GPU."

**Failure scenario as reported.** 0.31 ms is hillaire2020 §7's TOTAL RENDER TIME, not the LUT rebuild. The sibling that owns the pass prints the decomposition: atmosphere-and-aerial-perspective.md:43-49 lists the four PC LUT times (0.01 + 0.05 + 0.04 + 0.07) and states "The four LUTs sum to **0.17 ms** ... The remaining **0.14 ms** is the on-screen apply", under the warning "do the subtraction before you budget it" — the exact subtraction this line omits. Input: an engineer budgeting the 2020 branch from this page takes 0.31 ms as the rebuild cost and then adds the on-screen apply separately from the sibling page (0.14 ms), reaching 0.45 ms for a pass the paper measures at 0.31 ms — a 45% over-budget, or, read the other way, a 1.82x overstatement of the rebuild half (0.31/0.17). It also breaks the very comparison the sentence is making: 250 ms is a LUTs-only figure ("Updating all the LUTs ... costs 250ms"), so the like-for-like ratio is 250/0.17 = 1470x, not 250/0.31 = 806x.

**Proposed fix, unverified.** Change to "the whole LUT set rebuilds in 0.17 ms (of a 0.31 ms total render time that includes the 0.14 ms on-screen apply)", and compare 0.17 ms against the 250 ms LUT-update figure, not 0.31 ms.

### [SKY-7] `wrong` — /home/user/skills/gaia/references/sky-and-weather-state.md:201-204

**Claim on the page.** "`prevailingWind*t` grows without limit, and in fp32 a noise coordinate stops resolving detail after roughly **10^5 metres** of accumulated offset - the sky visibly freezes after some tens of minutes of simulated time."

**Failure scenario as reported.** Both magnitudes are wrong, and they are wrong in different directions, so neither can be repaired by reading the other charitably. fp32 has a 24-bit significand: at an offset of 10^5 m the ULP is 2^16 * 2^-23 = 2^-7 = **7.8 mm** (verified: numpy nextafter on float32 at 1e5 gives 0.0078125). A cloud-coverage noise field's finest meaningful octave is metres at minimum, so 7.8 mm resolves it 128x over - nothing freezes and no detail is lost. The true freeze threshold, motion quantising when the per-frame increment |W|*dt falls below the ULP, is X = |W|*dt*2^23; the resulting TIME is independent of wind speed, t_freeze = dt*2^23 = 2^23/60 s = **38.8 hours** of simulated time at 60 fps (10 m/s: X = 1.4e6 m; 50 m/s: X = 7.0e6 m; both t = 38.8 h). The detail-smearing threshold is later still: the fp32 ULP first reaches 1 m at an offset of 2^23 = 8.4e6 m. So the stated distance is low by 14x-84x and the stated time low by ~60x. Even taking the document's own 10^5 m at face value, reaching it in "some tens of minutes" requires |W| = 56 m/s (a category-3 hurricane); at a normal 10 m/s prevailing wind it takes 167 minutes. Consequence: the prescription that follows ("Wrap the offset into the noise's period, or carry `t` in double") is presented as urgent for a session-length regime where it is inert, and a reader who trusts the stated threshold to size a wrap period will pick one ~2 orders of magnitude too small, wrapping the sky visibly on a tens-of-minutes cadence to avoid a defect that does not occur until day two.

**Proposed fix, unverified.** Replace with the derived threshold and its derivation: "in fp32 the offset quantises the per-frame motion once |W|*t*2^-23 exceeds |W|/framerate, i.e. after 2^23 frames - about 39 hours of simulated time at 60 fps, independently of wind speed; detail smears earlier only if the finest noise octave is under a metre, which happens past ~8.4e6 m." Keep the wrap/double-precision fix, sized to that threshold.

### [SKY-6] `unsupported` — /home/user/skills/gaia/references/sky-and-weather-state.md:145-146

**Claim on the page.** "On 2008 hardware that bake was **5 seconds** and the result ran at 125 fps at 1024x768" (reused at :154 as "2008's own '5 orders in 5 seconds'")

**Failure scenario as reported.** The figure is unsourced and appears nowhere else in the corpus. This document's own brunetonneyret2008 locator (:10) claims READ IN FULL and enumerates precisely what §6 supplies - "the three tables 64x256, 16x64 and 32x128x32x8 packed as 32x128x256 RGBA at 8 MB in half precision, and for 125 fps at 1024x768 on an 8800 GTS with 0.4 ms and 2.6 ms term costs" - with no precompute wall-clock. The corpus's own bibliography entry for the same section, papers-rendering.md:322-328, enumerates the same §6 costs and additionally records "25 fps with light shafts", still with no precompute time. `grep -rn '5 seconds' gaia/` returns only these two lines in this file. Input: a reader budgeting a load-time bake for a level-load screen takes 5 s as measured 2008 hardware and scales it forward; the number cannot be checked against any artefact, and the derived claim at :154-155 ("the bake did not get slower") is unfalsifiable because its 2008 term has no source. This is the failure mode the same document names in bold at :72-73 - a figure paired across papers and presented as one measurement.

**Proposed fix, unverified.** Either add the §6 sentence and page/section locator that states the precompute time to the brunetonneyret2008 locator at :10, or delete "that bake was **5 seconds**" at :146 and the "5 orders in 5 seconds" comparison at :154, leaving "5 orders" (which the locator does support) unpaired with a time.

### [SKY-9] `unsupported` — /home/user/skills/gaia/references/sky-and-weather-state.md:127

**Claim on the page.** "Of the five sources here **only** [preethamshirleysmits1999] models ozone" - and, at :324, "the other **four** sources here will not"

**Failure scenario as reported.** The document supplies its own counterexample inside a single failure-table cell. :324 reads: "Add an ozone absorption term - [hillaire2020] Table 1 supplies coefficients and a tent-shaped profile, **crediting [bruneton2017]**; the other four sources here will not." [bruneton2017] is one of those four. If hillaire2020's ozone coefficients and tent profile are credited to [bruneton2017], then [bruneton2017] states ozone coefficients and a profile - which is what "models ozone" means - so "only Preetham" is false. If instead the credit is to Bruneton's 2017 CODE RELEASE ([Bru17b], which the sibling atmosphere-and-aerial-perspective.md:157-160 keeps carefully distinct from the [Bru17] paper and which is not in this document's bibliography), then the citation key at :324 resolves to the wrong artefact. Either way one of the two sentences is wrong. Input: a reader with too-yellow sunsets who has already adopted the recommended Bruneton-class model (the 11.3 RMSE row this page tells them ships) reads :127-129 and :324, concludes the ozone term must be sourced from outside the five, and hand-adds one - on top of whatever ozone the model they built already has, double-counting the absorption. Separately, the ozone prescription rests on hillaire2020's **Table 1**, which is outside that source's declared locator at :14 (Fig. 4 caption, Table 2, and §5.3 only) - so the coefficients being prescribed come from a part of the source this document does not claim to have read.

**Proposed fix, unverified.** Settle which artefact Hillaire's Table 1 credits. If [bruneton2017] (the TVCG paper), change :127 to "of the five sources here, [preethamshirleysmits1999] and [bruneton2017] treat ozone" and :324 to "the other three". If [Bru17b] (the code release), write that key out at :324 and add it to the bibliography. Either way, extend the hillaire2020 locator at :14 to declare Table 1.

### [SKY-2] `stale` — /home/user/skills/gaia/references/sky-and-weather-state.md:332

**Claim on the page.** Failure-table row: "Clouds and blowing snow drift apart | The cloud scroll vector is a private constant instead of the wind field [tr_lighting_shadows] | **Advect coverage with `driver-fields.md`'s wind field**"

**Failure scenario as reported.** The body retracts this prescription by name at :192-199: "`prevailingWind` is a **scene parameter**, a single vector. It is *not* `driver-fields.md`'s wind field, which is a per-cell terrain-shelter magnitude over a 100-300 m search distance ... and an earlier revision of this document said 'advected by that field', which is not a thing the closed form can do." The correction landed in the body only; the failure table still prescribes the retracted thing, and the un-corrected residue also survives at :183 ("so the coverage field should move with that field and not with a private constant"). Input: a reader hits the symptom "clouds and blowing snow drift apart", goes to the failure table (the section read under pressure), and wires driver-fields.md's per-cell wind-shelter field into the cloud advection. Two wrong outcomes follow: (a) `coverage(p,t) = noise(p - prevailingWind*t, ...)` cannot consume a per-cell field at all — a scroll offset is one vector, so the reader must abandon the analytic form and go back to an advected buffer, re-acquiring exactly the irreversible time slider and farm-split divergence that rows :326-327 exist to prevent; (b) even if implemented, it advects a cloud deck kilometres up with a 100-300 m near-surface terrain-shelter magnitude, which the body says is "irrelevant" at cloud base. The failure table's own fix cell at :326 compounds it by naming `windVec` rather than `prevailingWind`, the distinction the body's correction turns on.

**Proposed fix, unverified.** Rewrite the :332 Fix cell to "Drive the coverage scroll from the same `prevailingWind` scene parameter that `driver-fields.md`'s wind field is derived from — not from that field itself", rename `windVec` to `prevailingWind` at :326, and delete or reword the residue at :183.

### [SKY-3] `stale` — /home/user/skills/gaia/references/sky-and-weather-state.md:334

**Claim on the page.** Failure-table row: "The sky bake re-runs when the snow line moves, and nobody knows why | ... | Expected, not a bug. Decide whether the second-order brightening is worth a rebake, and **schedule it**"

**Failure scenario as reported.** The body at :244-249 says the opposite in bold: "That is a coupling this corpus had not noticed - and the right response is to **cut it, not to schedule it** ... wiring `alpha-bar` back into the bake **closes a cycle**, which `node-graph-runtime.md` forbids outright. **Hold `alpha-bar` constant at a representative value.**" The correction landed at the body end only. Input: a reader whose sky bake re-runs on a snow-line move follows the table's fix, judges the rebake affordable, and schedules it — i.e. keeps the edge snow-cover -> alpha-bar -> sky bake. Since insolation depends on the sky and snow depends on insolation, the graph now contains the insolation -> snow -> alpha-bar -> sky -> insolation cycle; per node-graph-runtime.md that graph has no topological order, so the planner rejects it or the bake re-runs on every snow iteration at the O(n^6) cost the same page prices at 250 ms. The body's fix (hold alpha-bar constant) is the one that keeps the graph planar; the table prescribes the one that does not.

**Proposed fix, unverified.** Rewrite the :334 Fix cell to match :244-249: "Cut the edge, do not schedule it - wiring `alpha-bar` back into the bake closes a cycle `node-graph-runtime.md` forbids. Hold `alpha-bar` constant at a representative value."

### [SKY-4] `stale` — /home/user/skills/gaia/references/sky-and-weather-state.md:214-218

**Claim on the page.** "buys **three** properties an integrated field cannot have" ... "**It does not force a whole-domain pass.** ... Advected coverage has the identical property [unbounded dependence], and an analytic one has neither problem."

**Failure scenario as reported.** Contradicted by this document's own text 95 lines later, at :310-313: "an advected field's dependence over an interval `T` is `|W|*T` - **bounded, and therefore tileable with a halo that size** ... The analytic form still wins, on reversibility and farm-splittability, **not on tiling**." :218 asserts advected coverage inherits driver-fields.md's unbounded precipitation dependence; :311 explicitly calls that reading wrong ("Both halves were wrong"). Input: a reader planning the node graph for an orographic (genuinely integrated) coverage field reads the third bullet, concludes the dependence is unbounded, and follows driver-fields.md's precipitation remedy - whole-domain at a coarse resolution, then upsample. Per :310-312 that field tiles exactly, with a halo of |W|*T (a 10 m/s wind over a 60 s interval is a 600 m halo on a tile of typically 1-4 km), so the reader pays a whole-domain pass and an avoidable resolution loss for a node the same document says is tileable. The property count is also wrong as a consequence: with the third retracted at :313, two properties survive, not three.

**Proposed fix, unverified.** Delete the third bullet or restate it as "an advected field tiles with a |W|*T halo, which the analytic form avoids only when that halo grows large", and change "three properties" at :204 to "two properties" - matching :313's own summary ("reversibility and farm-splittability, not on tiling").

### [SKY-5] `stale` — /home/user/skills/gaia/references/sky-and-weather-state.md:153

**Claim on the page.** "the **2008 bake** costs 250 ms on a 2016 GPU" (repeated at :257, "why it costs 0.05 ms instead of 250 ms")

**Failure scenario as reported.** The 250 ms does not measure the 2008 paper. atmosphere-and-aerial-perspective.md:151-160 quotes hillaire2020 §7 verbatim - "Updating all the LUTs using the code provided **[Bru17b]** costs **250ms**" - and then flags exactly this: "⚠️ **`[Bru17b]` matters and an earlier revision deleted it from inside the quotation.** It cites Bruneton's **2017 code release**, not the 2008 paper, so the 250 ms measures that implementation on 2016-era hardware." The same defect is logged in gaia/registers/source-findings.tsv:46 ("[Bru17b] deleted from inside a 'verbatim' 250 ms quote (it cites a 2017 CODE RELEASE, not the 2008 paper)"). The correction landed in the sibling and never propagated here. Input: a reader accepts the document's derived inference at :154-155 - "Note that figure against 2008's own '5 orders in 5 seconds': the bake did not get slower, the standard for what may sit off-frame got stricter" - which is only valid if 5 s and 250 ms measure the same code on different hardware. They do not: one is (per this page) the 2008 paper, the other a 2017 reimplementation whose order count, resolutions and medium are not the 2008 paper's. The hardware-progress conclusion is therefore drawn from two incommensurable artefacts, and 2008-vs-2016 GPU improvement is not what the 20x gap measures.

**Proposed fix, unverified.** Write "Bruneton's 2017 code release [Bru17b], measured by [hillaire2020] §7 at 250 ms on a 2016 GPU", and drop or requalify the "the bake did not get slower" inference, which needs a like-for-like pair the corpus does not have.

### [SKY-8] `stale` — /home/user/skills/gaia/references/sky-and-weather-state.md:19-20

**Claim on the page.** Tier line: "Every render figure here is per-frame; the scattering tables **and the cloud-shadow pass** are named as bakes, and the 2020 row is that bake becoming a per-frame solve."

**Failure scenario as reported.** No cloud-shadow bake is named anywhere in the document, and the body says the opposite. :264-265: "The instantaneous cloud shadow is a *rendering* term: it modulates sun visibility in the frame and nowhere else." A grep of every occurrence of "bake" in the file (:145, :148, :152-159, :222, :238-258, :267-286, :300, :325-335) finds exactly two things named as bakes - the scattering tables, and a genuinely integrated coverage buffer (:220-222, "declare it global-ordered and accept that it is a bake") - never the cloud-shadow pass. Input: an engineer reading only the Tier line to partition the frame budget classifies the cloud-shadow pass as off-frame work and omits it from the per-frame budget; per :264-265 it is a per-frame sun-visibility modulation and must be budgeted in-frame. The Tier line is also the one line whose whole function, per the convention recorded in gaia/registers/corrections.tsv:15, is to tell a reader which costs are per-frame and which are not.

**Proposed fix, unverified.** Replace "the cloud-shadow pass" with the thing the document actually names as the second bake - "a genuinely integrated coverage buffer (:220-222)" - or drop the clause: "the scattering tables are named as a bake, and the 2020 row is that bake becoming a per-frame solve."

**What its auditor could not settle.** SOURCES NOT OBTAINABLE FROM THIS TREE (no PDFs are checked in; I did not fetch externally):

1. bruneton2017 Table 2's COST columns. The eight complexity cells printed at :38-45 (O(n^3)/O(n^6) precompute, O(n^2)/O(n^3)/O(n^4) memory, O(1)/O(n)/O(n^2) render) are unverified. The corpus's own bibliography entry, papers-rendering.md:300-317, reproduces the RMSE row and the caption footnote verbatim but does NOT reproduce the complexity cells, so there is no second end to compare against. The RMSE row itself (88.1 / 49.5 / 41.5 / 26.6 / 18.3 / 14.7 / 11.3 / 11.3) DOES match papers-rendering.md:305-307 exactly, as does the "O'Neal not O'Neil" spelling, the 09h30-13h30 window, the n/a 06h00 cell, and the per-single-sun-angle footnote for Haber and Nishita96. The derived arithmetic checks: 41.5/11.3 = 3.673 -> ":70's 3.7x" holds; the "understates Bruneton by a factor of n" reasoning at :63-65 is internally sound (Haber's O(n^6) per angle is O(n^7) for n angles against Bruneton's O(n^6)).

2. bruneton2017 Table 2's FEATURE columns. ":80-81's Table 2 marks it ground-viewpoint only, `no` for aerial perspective and `no` for sunrise/sunset" for Hosek is unverified. papers-rendering.md:313-314 confirms a separate `sunset sunrise` column exists but names no cell values.

3. Whether bruneton2017's own model includes ozone. This is the fact that decides which half of finding SKY-9 is the wrong half. I report the contradiction, not a verdict on which sentence to keep.

4. hillaire2020 Table 1's ozone coefficients and which key it credits ([Bru17] paper vs [Bru17b] code release). Bears on SKY-9. No corpus document mentions hillaire2020 ozone at all: `grep -rn ozone gaia/` returns only this document plus papers-rendering.md:330 and :347, both of which concern brunetonneyret2008 and preethamshirleysmits1999.

5. brunetonneyret2008 §6's precompute wall-clock. Bears on SKY-6. I established only that no artefact in this tree states it and that two independent enumerations of the same section (this document's own locator at :10, and papers-rendering.md:322-328) both omit it.

CHECKED AND CLEAN, so not reported: the 8 MB figure reproduces exactly (32*128*256 texels x 4 channels x 2 bytes = 8,388,608 B = 8.0 MiB); the 32x128x32x8 -> 32x128x256 packing is consistent with the 8-slice nu axis; the u_mus mapping (1-e^(-3mus-0.6))/(1-e^(-3.6)) maps mus in [-0.2,1] onto [0,1] and places u = 0.464 at mus = 0, so it does concentrate 46% of the axis on zenith angles 90-101.5 deg, confirming the "precision near 90 deg" claim at :231; the 680/550/440 nm triple and (5.8,13.5,33.1)e-6 m^-1 match papers-rendering.md:319-322; the driver-fields.md quotation at :216-218 is verbatim against driver-fields.md:366-368; the Winstral p = 0.38 and "Where wind moves snow, wind wins" quotations match driver-fields.md:218-223; the 100-300 m search distance matches driver-fields.md:82; the node-graph-runtime.md class quotation at :296-298 is verbatim against node-graph-runtime.md:423-424; the volumetric-clouds.md contract claim at :291-293 is true (volumetric-clouds.md:117-120 names it); `snow-and-weather-state` is correctly described as a separate topic (coverage.md:74, status `planned`).

NOT REPORTED, deliberately: (a) the "second-order brightening" magnitude at :248-251 is asserted twice with no measurement and no source and is the stated justification for cutting the alpha-bar graph edge, but I could not construct a number that makes the PRESCRIPTION fail - "hold alpha-bar constant" survives regardless, because the cycle is forbidden independently of the magnitude - so under the rules-of-evidence it is dropped. (b) The bare "125 fps at 1024x768" at :146 omits brunetonneyret2008 §6's "25 fps with light shafts", which papers-rendering.md:328 records and which source-findings.tsv:46 logs as a previously-caught quotation defect; I dropped it because the sibling's crossover bullet (atmosphere-and-aerial-perspective.md:410) omits it too, so this document is not the divergent end and the caveat does not change the conclusion the sentence draws. (c) "Haber renders in O(n^2), which is the one row not to choose for real time" at :70 survives - O(n) ray-marched rows are shippable in real time, so the superlative has no counterexample in the table.

---

## stratigraphy-and-lithology.md — 6 raised, 0 refuted

`unsupported` 2  `wrong` 4

### [S1] `wrong` — gaia/references/stratigraphy-and-lithology.md:164

**Claim on the page.** "So the general statement is that a bed's visibility is set by the angle between the bedding and the **surface**, not by the dip alone: `w = T / tan(angle between bedding and surface)`, and `T ≥ 4·cellSize·tan(that angle)`."

**Failure scenario as reported.** tan is not additive, so tan(σ−δ) ≠ tan σ − tan δ and the generalisation is false everywhere except the two special cases it was generalised from (σ=0 or δ=0). The exact map-plane outcrop width of a bed of vertical span T under a coplanar bedding dip δ and surface slope σ is w = T/|tan σ − tan δ|. Take bedding dipping 10° east under a hillside sloping 30° east — both inside the document's own stated ranges (dip 0–30° at :98; hillsides 5–30° at :161-162) — with T = 150 m on 100 m cells. The document's formula gives w = 150/tan20° = 412.1 m = 4.12 cells and its gate gives T ≥ 4·100·tan20° = 145.6 m, so T = 150 m PASSES the 4-cell threshold. The true width is 150/|tan30° − tan10°| = 374.0 m = 3.74 cells — below the document's own aliasing threshold. The gate certifies a bed that aliases. With the two tilts opposed (bedding 30° one way, surface 30° the other) the error reverses sign: the document's gate demands T ≥ 4·100·tan60° = 692.8 m where the true requirement is 4·100·(tan30°+tan30°) = 461.9 m, 1.5× too conservative.

**Proposed fix, unverified.** Replace both expressions with the coplanar form: `w = T / |tan δ − tan σ|` and `T ≥ 4·cellSize·|tan δ − tan σ|`, with δ and σ signed in the same vertical plane. The two cases the document derives correctly (flat surface, horizontal beds) are the σ=0 and δ=0 reductions of that one formula; only the passage through "the angle between" is wrong.

### [S2] `wrong` — gaia/references/stratigraphy-and-lithology.md:373

**Claim on the page.** "A cell that starts a step in a resistant bed erodes the **whole step** at the resistant `K`, even if it cut through the contact a tenth of the way in. The error is one-sided — it always over-resists — so a coarse `Δt` makes resistant beds look more resistant than they are"

**Failure scenario as reported.** The mirror case has the opposite sign and the document itself tabulates it. A cell that starts a step in a *weak* bed and cuts down through the contact into a resistant bed erodes the whole step at the weak K, removing far more than it should. With the rig's own numbers — 25 m alternating beds, 4× contrast, Δt = 2000 yr, and the document's own measured max(K·A^m·S^n) = 2.8·10⁻³ m/yr (:391) as the weak-bed rate — a cell sitting 0.5 m above a soft→hard contact removes 2.8e-3 × 2000 = 5.6 m in one step and ends 5.1 m inside the resistant bed. Exactly, it crosses the contact after 178.6 yr and then erodes at K_hard·Φ = 0.7·10⁻³ m/yr for the remaining 1821.4 yr, penetrating only 1.275 m. The discretised step cuts 4.0× too deep into the resistant bed: under-resistance, not over-resistance. The document's own failure row at :435 — "Thin beds erased entirely | Per-step incision comparable to bed thickness, so the bed is skipped in one update" — is precisely this error, and it cannot happen if the error "always over-resists". The measured direction at :376-381 (coarse Δt inflates conc and the slope ratio) survives; only the stated mechanism is false.

**Proposed fix, unverified.** Replace "The error is one-sided — it always over-resists" with the two-sided statement: hard→soft crossings under-erode and soft→hard crossings over-erode, and the net bias toward stronger scarps at coarse Δt is the measured result of :376-381, not a consequence of a one-sided error. Either drop the mechanism sentence or replace it with the residence-time argument that actually produces the bias.

### [S3] `wrong` — gaia/references/stratigraphy-and-lithology.md:292

**Claim on the page.** "Dipping 100 m beds at 10° gives the strongest scarp in the set (`conc` 1.85× against 1.45× for the same beds horizontal) — dip *helps*." (the same superlative at :154: 100 m beds at 10° "produce the **strongest**")

**Failure scenario as reported.** The run is the strongest on neither tabulated metric. The table 38 lines above gives conc 2.29× for "Horizontal, 25 m beds, 16×" (:254), above the dipped run's 1.85×, and the document itself calls that run the best at :272 ("the best packs **more than twice** the share of the trunk's descent into its resistant reaches"). On the other column, "Horizontal, 100 m beds, 4×" gives S_area 2.131 (:255) against 1.705 for the dipped run (:256). Worse for the prescription: for the exact pair the sentence compares — 100 m beds, 4×, horizontal vs 10° — dip raises conc 1.45→1.85 but drops S_area 2.131→1.705, a 20.0% loss on the metric the same paragraph uses two lines later (:294) to declare which run is "the weakest". An author who dips a 100 m column at 10° on this advice gets a 20% weaker map-wide slope contrast than leaving it horizontal, by the document's own table.

**Proposed fix, unverified.** Restrict the superlative to what the table supports: among the 4× runs, dip 10°/100 m has the highest `conc` (1.85×). Say that, and state that dip on this pair trades S_area for conc rather than "dip helps". Fix the same claim at :154.

### [S4] `wrong` — gaia/references/stratigraphy-and-lithology.md:279

**Claim on the page.** "It gives **2.16** on the channel slope ratio, resistant ÷ weak (the timestep table below, which drops it to 1.35 when `Δt` is halved)"

**Failure scenario as reported.** The table it points at (:376) is headed "`Δt = 2000`, 1000 steps | `Δt = 500`, 4000 steps" — a factor of 4, confirmed by its own step counts (2 Myr / 2000 = 1000; 2 Myr / 500 = 4000). No run at Δt = 1000 exists anywhere in the document, so nothing measures a halved step. An author following the prescription three sections later — "**Halve `Δt`**, see whether the scarp holds, believe the finer step" (:396), repeated in the failure table at :429 — goes 2000 → 1000, measures a slope ratio somewhere between 2.16 and 1.35, and reads this sentence as saying a halved step should have landed on 1.35; a value of, say, 1.8 then looks like non-convergence when it is exactly what a 2× refinement should give on the way to the 4× value. The "24.8 m on 494 m of relief — 5.0%" RMS on the same table is likewise a 2000-vs-500 (4×) difference, not a halving.

**Proposed fix, unverified.** At :279 say "quartered" or "at `Δt = 500`". At :396 and :429, either state the check as "quarter `Δt`" or add the missing `Δt = 1000` run that would license "halve".

### [S5] `unsupported` — gaia/references/stratigraphy-and-lithology.md:294

**Claim on the page.** "Dipping 25 m beds at the same 10° gives the weakest (`S_area` 1.322), because their outcrop is 1.4 cells wide. It is not the dip that broke it; it is `T / tan δ` falling below the grid." — resting on the "Outcrop width" column, which prints `∞` for every horizontal run (:252-255).

**Failure scenario as reported.** The document's own §:158-167 withdraws the flat-surface reading it then uses: horizontal beds "do not escape it", and 25 m beds crop out at 1.42 cells on a 10° hillside, 0.69 on 20°, 0.43 on 30°. The rig's slopes follow from its own stated inputs (U = 5·10⁻⁴, K = 10⁻⁵, m = 0.5, n = 1, 100 m cells): S = U/(K·√A) gives S = 0.500 at 1 cell of drainage (26.6°), 0.354 at 2 cells (19.5°), 0.250 at 4 cells (14.0°) — so over the majority of a D8 network the horizontal 25 m beds outcrop at 0.5–1.0 cell, *narrower* than the 1.4 cells blamed here. Yet "Horizontal, 25 m beds, 16×" scores conc 2.29×, the highest in the table, and "Horizontal, 25 m beds, 4×" scores 1.58×, against the dipped 1.4-cell run's 1.21×. The converse also fails: "Dip 2°, 25 m beds" has the widest dipped outcrop in the set, 7.2 cells (5× the `T ≥ 4·cellSize·tan δ` gate), and the lowest conc of any layered run, 0.92× — inside the uniform-`K` control band of 0.86–0.89×, i.e. no scarp at all. Outcrop width as tabulated predicts neither the strongest runs nor the weakest, so the causal attribution has no run behind it: no pair in the table varies width alone.

**Proposed fix, unverified.** Either replace the `∞` entries with the rig's actual bedding-vs-surface widths (which §:158-167 already knows how to compute) so the column describes the runs, or withdraw the causal clause: state that the 25 m/10° run is lowest on `S_area` (1.322, against 1.323 for the 16× run) without claiming outcrop width is why, and note that the 2° run at 7.2 cells is the counterexample.

### [S6] `unsupported` — gaia/references/stratigraphy-and-lithology.md:26

**Claim on the page.** "Stream power's `f = K·Δt·A^m/dist` [cordonnier2016] §3.1 eq. 1 already reads `K` per cell" — repeated verbatim in the failure table at :425.

**Failure scenario as reported.** This file's own frontmatter (:9) states what §3.1 eq. 1 contains: "dh(p)/dt = u(p) - k*A(p)^m*s(p)^n" — a continuous PDE with no `f`, no `Δt` and no `dist` in it. The expression cited is the discrete coefficient from the implicit update: `stream-power.md`, which owns the solver, prints `f = K * Δt * pow(A[i], m) / dist[i]` at :93 and attributes that block in its own frontmatter (:10) to "cordonnier2016 … section 5, eq. 2, the implicit update h_i(t+dt) = (h_i(t) + dt*u_i + f*h_j(t+dt))/(1 + f)". A reader who opens §3.1 eq. 1 to verify that `f` reads `K` per cell — which is this document's whole opening argument that nothing downstream changes — finds no `f` there and no discretisation at all. The locator does not resolve to the claim, at both occurrences.

**Proposed fix, unverified.** Cite [cordonnier2016] §5 eq. 2 for the `f = K·Δt·A^m/dist` expression at :26 and :425, and keep §3.1 eq. 1 for the law itself and for "the term k that this document makes a field", as the frontmatter locator already describes it.

**What its auditor could not settle.** SOURCES. None of the six primary papers were opened here (cordonnier2016, benes2001, mitchell2021, forte2016, barnhart2018, stava2008). So the quoted strings could not be checked against the papers themselves — benes2001's "where k is the number of the layers — this is usually much smaller", MAX_LEVEL = 10, "a layer with zero density" for voids (:205, which the frontmatter locator does not cover), barnhart2018's "More memory efficient but does not record the transient dynamics", mitchell2021 Table 1's -45 to +15 degree dip range, and the eq. (6)/(12) numbering. I checked them only against this repo's own locators. Note papers-generation.md:353 records that mitchell2021 "deliberately does not run n = 1", so the "at n = 1 is exactly K·A^m" reduction at :171-172 is arithmetic on eq. (6) rather than a reported result; the document presents it as a reduction, so I did not report it.

THE SYNTHETIC BUTTE (:313-328). The initial face angle is never stated, so the "1.998 → 1.996 km" null result cannot be reproduced. With the stated per-bed limits (resistant 60 deg, weak 32 deg), a 100 m weak bed relaxing from 60 to 32 widens its outcrop from 57.7 m to 160.0 m — 102 m, or 2 cells at 50 m — so a face genuinely "at 60 deg" (:318) is NOT converged for the weak beds, and the single-limit explanation given for the null result does not cover the two-limit experiment that produced it. Whether 2 m is nonetheless consistent depends on where "scarp radius" is measured (a crest measurement on a resistant cap already at its own limit would legitimately not move), and that is not defined. I could not make it fail either way, so I did not report it.

RIG OUTPUTS. The raw runs behind the results table, the "four independent rebuilds" (:283-286, :305-307) and the max(K·A^m·S^n) spread (:390-393) are not in the repo, so 1.11-1.13 / 1.25-1.66 for S_area, 0.728-0.736 / 0.860-0.869 for area_hard, and 1.0e-3 to 2.8e-3 m/yr could not be reproduced. Everything derivable from them does check out: dt_max 1786-5000 yr against the printed "1800 and 4900"; coefficients 0.020-0.056 and their 3.6x/10x ratios against 0.2; 24.8/494 = 5.02%; the geometric-mean control at 1e-5; U*T/period = 20 columns and 40 contacts; all outcrop widths (141.8, 567.1, 716 m), all hillside widths (2.86/1.42/0.69/0.43 cells), 1/cos at 2/10/30/60 deg (0.06%/1.5%/15%/2x), tan(30 rad) = -6.405, and 4096^2 x 10 x 2 x 4 B = 1.34 GB. The corrected area_hard is also self-consistent: 1/(1 + (K_h/K_s)(S_h/S_s)) gives 0.649 at the document's 2.16 slope ratio and 0.748 at 1.35, bracketing the reported 0.73.

DROPPED AS TOO WEAK TO DEFEND. (a) ":316-317 'the per-bed angles are indistinguishable from one angle' — the per-bed area_hard (48.91%) sits 0.55 pp outside the single-angle control band (49.46-49.62%, width 0.16 pp), about 3x the control scatter; with N=3 controls I could not decide between a real overclaim and loose wording for 'negligible'. (b) :94 'Bed thickness ... 25-200 m measured below' — only 25 m and 100 m beds appear in any table, so 200 m is measured nowhere, but whether 'measured below' attaches to the range or to the row is ambiguous. (c) :36/:128/:206 describe the sample as costing 'one floor' where the code uses fmod, which :128's own parenthetical disclaims — an fmod is a floor plus a multiply and subtract, so the cost statement is loose rather than wrong.

---

## surface-and-scale-space.md — 10 raised, 0 refuted

`incomplete` 1  `stale` 1  `unsupported` 2  `wrong` 6

### [F1] `wrong` — gaia/references/surface-and-scale-space.md:40 (also :101, :110)

**Claim on the page.** "**Tiled — the Gaea-class case — use the undecimated (à-trous) form**: it is exactly shift-invariant where the decimated pyramid drifts metres, it is 35% cheaper in halo at `L = 5`"; table row "support radius, `L = 4` / `L = 5` | 46 / 94 px | **30 / 62 px**"; "`3·2⁵ = 96` px of halo against 62, so `(96−62)/96` = **35.4%**"

**Failure scenario as reported.** The two forms are compared at equal level count, not at equal cutoff, so they are not the same filter. The decimated analysis-and-synthesis applies the 5-tap kernel 2L times (L reduces at scales 1,2,…,2^(L−1) plus L expands at the same scales); the à-trous form as the document defines it at :96 ("`L` dilated separable convolutions in place") applies it L times. Variance therefore differs by exactly 2, i.e. sigma by sqrt(2). Measured by impulse (a = 0.4, reflect, 1-D, 8193 samples): decimated L=5 gives sigma 24.47 cells, 50%-amplitude wavelength 128.0 cells, support radius 94 (reproducing the document's 94); à-trous L=5 gives sigma 17.52 cells, 50%-amplitude wavelength 94.2 cells, support radius 62 (reproducing the document's 62). At the document's own 4 m/cell (:420) an engineer who follows `## Use this` and swaps a decimated L=5 split for an à-trous L=5 split on a tiled build moves the protected band from 512 m to 377 m: every feature between 377 m and 512 m leaves the low band, enters the residual, and is eaten by the surface operator — the exact silhouette loss the document exists to prevent. The 35.4% is bought by smoothing less, not by decimation. At matched cutoff the saving disappears: an à-trous with dilations 1,2,4,8,16,16 measures sigma 23.18, 50%-amplitude wavelength 124.1 cells (against the decimated 128.0) and support radius 94, i.e. a halo of 94 against the decimated 96 — 2%, not 35%.

**Proposed fix, unverified.** State the level correspondence: an à-trous split needs roughly L + 0.5 levels' worth of dilation to match a decimated L-level low band (sigma ratio sqrt(2)). Either compare at matched cutoff — decimated L=5, support 94, against à-trous dilations 1,2,4,8,16,16, support 94, and drop the 35% claim — or keep 62 px and state plainly that the à-trous L=5 low band cuts at 94 cells where the decimated cuts at 128, so switching forms retunes the silhouette band.

### [F2] `wrong` — gaia/references/surface-and-scale-space.md:175-179 (propagated to :437)

**Claim on the page.** "⚠️ **The magnitudes are not a constant of the method — they are a property of your field, and so is the sign.** … Padding error is boundary error, so what it costs you is however much mass sits against the border." Failure table: "Measured −15.0% to +2.2% at `L = 5` depending on the field"

**Failure scenario as reported.** The dominant variable is the domain size mod 2^L, not the field. On a rig that reproduces the document's own ramp column exactly (reflect/symmetric −10.91%, reflect/edge −11.22% at 256², L=5), holding the full-width ramp fixed — identical shape, identical mass against the border — and changing only N moves reflect/edge from −11.22% at N=256 to **+34.10% at N=257**, a 45-point swing with no change to the field. Conversely, at fixed N=257 three unrelated surfaces (fBm, smoothed noise, full-width ramp) give +33.22% / +33.76% / +34.10% — under one point of field-to-field spread. Sweeping N at L=5, reflect/edge over three fBm fields stays in −0.79%…+2.84% at every N ≡ 0 (mod 32) (224, 256, 288, 320, 512) and runs +22.8%…+39.7% at N = 257, 258, 260, 264. Concrete failure: a build on the canonical Burt grid — 1025², or the document's own 257² test field (:60, :196), where Burt's EXPAND takes (M+1) to (2M+1) so every pyramid level is odd — with `edge` padding on EXPAND reads the printed envelope "−15.0% to +2.2%" as the worst case and measures +34%, an order of magnitude over. The corpus register already recorded this on 2026-09-04 (registers/pseudocode-execution.tsv:183: "THIS REJECTION WAS ITSELF TOO NARROW … at N = 257 the same configuration gives +28.4% (symmetric) and +37.9% (edge) … The effect is a PARITY effect in N, not a property of the field"); the correction never landed in the document.

**Proposed fix, unverified.** Replace the field attribution with the measured one: the magnitude is governed by whether the domain divides the decimation chain (N ≡ 0 mod 2^L); the field moves it by a few points, N moves it by tens. State that the table is measured at 256², a size that divides 2^5, and that the same pairs at 257² cost +25% (symmetric) and +34% (edge). Bound the failure-table cell by the odd-grid number, not the 256² one.

### [F3] `wrong` — gaia/references/surface-and-scale-space.md:187-191 (relied on at :436-437)

**Claim on the page.** "⚠️ **`wrap` is parity-correct too** … on the even-length arrays `EXPAND` actually produces, a two-cell `wrap` apron measures `[40, 0]` … (On an odd-length array `wrap` does invert; `EXPAND` does not make one.) So `reflect` and `wrap` are the two structurally right pads"

**Failure scenario as reported.** EXPAND does make odd-length arrays, and does so in the document's own primary configuration. Burt & Adelson define EXPAND as taking an (M+1)×(N+1) array to a (2M+1)×(2N+1) one, so every level of a canonical pyramid is odd; the document's own worked field is 257² (:60, :196), whose chain is 257→129→65→33→17, and expanding 129 to the 257 target shape the pseudocode at :50 passes produces a zero-interleaved array of length 257 that **ends on a sample**, not a zero. A two-cell left `wrap` apron on that array is [0, g128] — zero then sample — the inverted parity the parenthetical concedes. Measured, L=5, reflect REDUCE: at N=256 `wrap` EXPAND gives +1.26% (fBm) / +0.22% (smoothed noise) / −10.91% (ramp); at N=257 the same three give **+24.93% / +25.34% / +25.58%** against `reflect`'s +0.04% / +0.07% / −0.00%. Concrete failure: a reader on a 1025² open-tile build hits the failure row at :436, whose fix cell offers "`reflect` or `wrap`" as equals for a non-periodic domain, picks `wrap`, and loses a quarter of the field's volume out of the low band while the round-trip test stays bit-exact. (Note also that Σ(lo)/Σ(h) cannot distinguish `wrap` from `symmetric` at all — for a symmetric kernel both conserve each convolution's sum exactly, and the two columns are numerically identical in all 18 cases measured — so the document's `symmetric` figures at :165 are also its unstated `wrap` figures.)

**Proposed fix, unverified.** Delete "`EXPAND` does not make one" — it does, on every odd grid, which is Burt's canonical case and this document's own test field. Qualify the recommendation: `wrap` is parity-correct only when the expanded array is even at every level, i.e. when N ≡ 0 (mod 2^L) — which the periodic rule at :334 already forces — and is parity-inverting otherwise. Drop `wrap` from the fix cell at :436, which covers open tiles of any size.

### [F4] `wrong` — gaia/references/surface-and-scale-space.md:273

**Claim on the page.** "Size from `R(L)` only if you also align the phase, as below; if you cannot, size from `2^(L+2) − 4`."

**Failure scenario as reported.** No halo fixes a misaligned phase, so the fallback is inert in exactly the regime it is prescribed for. The tile's decimation lattice starts at the sub-array's origin, so a misaligned tile computes a different function, not a truncated one. Measured (L=4, R=46, 2^(L+2)−4 = 60, 64×64 interior tiles against a whole-domain reference on a 1024² field): halo 60 leaves 5.90 m of error at aligned origin 192 and 6.98 m at origin 193. Worse, the error is *constant in halo* once halo ≥ R and depends only on (origin − halo) mod 16: at origin 193, halos 48, 64, 80 and 96 all give exactly 1.700560 m. The document's own phase table at :290 already prints 2.33 m at halo 60, and its own paragraph at :303 already prints 2.57 m at halo 80 — both ≥ 60 — and the register (registers/pseudocode-execution.tsv:185) prints 9.40 m at halo 60. An engineer with a build region they cannot align (the crop-region preview the document names at :305) sizes the halo at 60 px for L=4 per this sentence, pays 30% more apron than at halo 46, and still ships the quilt.

**Proposed fix, unverified.** Replace the fallback. `2^(L+2) − 4` is the dependency radius for an impulse of unknown phase, not a halo that buys tile-exactness. If you cannot align the sub-array origin, no halo works — take the à-trous form, or align on (tile_origin − halo) as :296-306 already says. Say that explicitly here rather than offering a number that reads as a cure.

### [F5] `wrong` — gaia/references/surface-and-scale-space.md:436

**Claim on the page.** "…against −15.0% for a zero-padded `REDUCE`"

**Failure scenario as reported.** −15.01% is the `reflect` REDUCE / `zero` EXPAND row of the document's own table at :167. The zero-padded **REDUCE** row is the next one down, :168, and measures −9.94%. The failure table hands the −15% to the wrong operator, and does so against the section's own thesis at :159-160 ("**`REDUCE` and `EXPAND` pad different things** … and the second is where the volume goes"). Concrete failure: a reader whose REDUCE zero-pads and whose EXPAND reflect-pads reads this row, expects −15%, measures −9.94%, and concludes the diagnosis does not fit; a reader who instead reads it as "REDUCE padding is the expensive one" fixes REDUCE's pad on a build whose −15.01% is coming entirely from EXPAND, and the loss does not move.

**Proposed fix, unverified.** "…against −15.0% for a zero-padded `EXPAND` (−9.9% for a zero-padded `REDUCE`)".

### [F6] `wrong` — gaia/references/surface-and-scale-space.md:436

**Claim on the page.** "`reflect` or `wrap`; at `L = 5` either is under 0.2%"

**Failure scenario as reported.** The document's own reflect/reflect row at :164 measures −1.03% on a full-width ramp — five times the stated bound — and my rig reproduces it (−1.02% at 256², L=5, and −0.26% at 512²). And `wrap` has no `Σ(lo)/Σ(h)` measurement anywhere in the document: the padding table at :162-168 has no `wrap` row, and the only `wrap` numbers printed (:322) are wrap-seam step ratios, a different quantity. Concrete failure: a studio with a coastal-shelf or tilted-plate terrain (a full-width ramp, the very surface the table's fourth column measures) uses `reflect` at L=5, asserts the documented "under 0.2%" as a CI gate on Σ(lo)/Σ(h), and the gate fires on a correct build at −1.03%.

**Proposed fix, unverified.** Quote the bound the table supports: reflect/reflect measured −0.15% on smoothed noise and −1.03% on a full-width ramp at L=5, so "under about 1% on the surfaces measured". Drop `wrap` from the bound, or measure it — and see F3 for why it must not be offered for an odd-N open tile at all.

### [F8] `unsupported` — gaia/references/surface-and-scale-space.md:418-421

**Claim on the page.** "The low band's kernel has the measured radius `R(L) = 3·2^L − 2` cells, so at 4 m/cell a four-level split smooths over a 46-cell (184 m) radius and hands the operator everything narrower than roughly 370 m. State that number, in metres, in the UI"

**Failure scenario as reported.** The number is derived from the support diameter (2 × 184 m), but support is not the smoothing scale. Measured by impulse through the L=4 low band (a = 0.4): the equivalent kernel has sigma 12.21 cells (49 m at 4 m/cell), 90% of its mass inside 16 cells, and its amplitude response crosses 0.5 at a wavelength of 64.5 cells = **258 m**, not 370 m. At the printed 370 m the low band still retains 0.71 of the amplitude, so the residual — and therefore the surface operator — sees only 29% of it. Concrete failure: the UI is labelled "protects everything wider than 370 m"; an artist who wants a 300 m ridge reworked by a roughening operator finds it about 40% reworked (low-band gain 0.60 at 300 m) and turns the operator up until it eats the 600 m features too, which is the failure the section is trying to prevent. The support radius is the right number for the halo (it is what :266-268 measures) and the wrong one for the cutoff.

**Proposed fix, unverified.** Derive the metres number from the equivalent kernel's cutoff, not its support: at a = 0.4 the L-level low band has sigma ≈ sqrt(0.6(4^L − 1)) cells and a half-amplitude wavelength of about 4·2^L cells (64.5 measured at L=4, 128.0 at L=5), so at 4 m/cell a four-level split hands the operator everything narrower than about 260 m. Keep R(L) where it belongs, in the halo bullet.

### [F9] `unsupported` — gaia/references/surface-and-scale-space.md:103 against :340 and :445

**Claim on the page.** Table: "low band under a 1/2/4/8 px shift | 2.23 / 4.35 / 8.02 / 12.35 m". Body: "the decimated pyramid drifted 0.45, 0.89, 1.72 and 2.72 m and only returned to zero at the full period of 16." Failure table: "measured 2.72 m at an 8 px shift"

**Failure scenario as reported.** Two series for one named quantity, 4.5×–5.0× apart, and neither line states the field, the relief or (for the table row) the level. The gap is not explained by level: measured on 512² fBm at 1000 m relief, the decimated drift at 1/2/4/8 px is 0.93/1.75/3.35/4.41 m at L=4 and 0.70/1.35/2.60/4.90 m at L=5 — L=5 is 11% above L=4 at an 8 px shift, not 4.5×. Nor is it a clean relief rescale: the ratio between the two printed series drifts from 4.96 at 1 px to 4.54 at 8 px. Only the table row has provenance (registers/pseudocode-execution.tsv:205 prints 2.23/4.35/8.02/12.35 verbatim); the 0.45/0.89/1.72/2.72 series appears at exactly one place in the whole corpus, this line, yet it is the one the failure table at :445 hands the reader and the one :342 credits with driving the à-trous recommendation. Concrete failure: an engineer sets an acceptance threshold of 3 m for "how far can an unaligned edit move the low band" from the failure table's 2.72 m and ships a build the comparison table says drifts 12.35 m at the same shift.

**Proposed fix, unverified.** State the field and relief on both lines, or delete one. The shape reproduces on any field — monotone rise to the half-period, exactly 0.00 at 2^L, and exactly 0.00 at every shift for à-trous (verified) — so quote the shape as the finding and one series, labelled, as its illustration.

### [F7] `stale` — gaia/references/surface-and-scale-space.md:410

**Claim on the page.** "Gaia's bibliography already carries the bilateral filter (`tomasi1998`) and the guided filter (`he2010`) at `P` in `papers-generation.md`"

**Failure scenario as reported.** The locator does not resolve. `grep -ni 'tomasi|he2010|bilateral|guided' gaia/references/papers-generation.md` returns nothing. Both entries live in `papers-masks-and-filtering.md` — tomasi1998 at :192 and he2010 at :195 — which is also where this document's own `burt1983` (:96) and `paris2011` (:110) sit. The next author, whom this sentence exists to point somewhere ("they are named so the next author knows where to start"), opens papers-generation.md and finds neither entry.

**Proposed fix, unverified.** Change `papers-generation.md` to `papers-masks-and-filtering.md`. (The tier is right: both are `P` there, and he2010 is marked `[not-opened]`, consistent with "Neither was read for this document".)

### [F10] `incomplete` — gaia/references/surface-and-scale-space.md:434

**Claim on the page.** Failure table: "Halo is ≥ the support radius but not a multiple of `2^L` … | Round the halo up to `3·2^L`; do not blend the seam, fix the phase"

**Failure scenario as reported.** Both the mechanism and the fix are the convenient form of the rule, which :302-306 flags as false for an offset tile grid — and the failure table is where a reader acts without reading the body. At L=4 with tiles at global origin 193, halo 48 is a multiple of 2^L and ≥ R and still wrong: the document prints 2.57 m at :303 and my rig measures 1.700560 m, unchanged at halos 64, 80 and 96. So a crop-region preview, a user-defined build region, or any tile grid whose origins are not multiples of 2^L follows this row exactly, rounds 46 up to 48, and the quilt is still there — with the reader now believing the phase has been fixed.

**Proposed fix, unverified.** Carry the general rule into the cell: mechanism "(tile_origin − halo) is not ≡ 0 (mod 2^L)"; fix "round the halo up until (tile_origin − halo) ≡ 0 (mod 2^L) — `3·2^L` only when tile origins are themselves multiples of 2^L".

**What its auditor could not settle.** Verified and CLEAN (recomputed, not skimmed), so the absence of findings on these is a result, not a gap: kernel derivation w = [0.25−a/2, 0.25, a, 0.25, 0.25−a/2] from normalization + symmetry + equal contribution a+2c=2b (:128-131), and its unit sum for any a; R(L) = 3·2^L − 2 measured by impulse on the retained phase (4/10/22/46/94 at L=1..5, exact); the max-phase radius 2^(L+2) − 4 (12/28/60/124, exact, and derivable as R(L)=2R(L−1)+4); K = 3(2^(l0+2) − 1) = 2·R(l0+1) + 1 (exact identity); the phase rule halo ≥ R and (origin − halo) ≡ 0 (mod 2^L), reproduced bit-exact on my own rig including the origin-193 case; every mod-16 cell of the phase table at :289; 3·2^L as the smallest halo satisfying both for L ≥ 2; 4^-4 − 1 = −99.609% (:68); the identity A − B = lowband(f(h)) + f(hi) − f(h) and its exact 20 m affine row (:239, :247); the operator table's internal consistency — all four rows and the correction row are consistent with one field of mean 509.76 m and 1000 m relief, and the 1.5·r row's +0.0025% is exactly half the split floor, as it must be; the −0.0050% correction floor against the −5.0e-05 reflect/reflect figure at L=4 (:197) and the 2.5e-02 m residual mean; every "% of relief" column at :208-214 and :238-243; the 11.6× at :199; 47% apron from 704²/512²; 35.4% from (96−62)/96; 1.48× from 0.142/0.096; N ≡ 0 (mod 2^L) cells at :336; the wrap/reflect/symmetric/edge apron examples [40,0]/[20,0]/[0,10]/[10,10] (:190) against numpy.pad semantics; à-trous exact shift-invariance (0.00 at every shift tested). Burt eq. (2)/(3)/(4) attributions are self-consistent and match my recollection of the paper's numbering.

COULD NOT SETTLE: (1) The erosion/talus table at :349-354 — thermal ΔVol 0.000e+00, 178.15 m low-band change, the −23.59% export case and 546.76 m — has no register row and states no grid size, talus angle, iteration scheme or relief, so it cannot be regenerated; only its 17.8%-of-relief arithmetic checks out. (2) The five Gaea node rows at :380-385 rest on F-tier vendor pages not reachable from this checkout; the interpretive claim "`Deflate` … `Amount` is the cutoff level, not a strength" is wider than the quoted sentence beside it, but I cannot open the source to convict it. (3) The [burt1983] Fig. 3 quotation at :143 ("small negative lobes") against the failure table's "trimodal" at :438 — the paper is not in the tree. The kernel taps at a = 0.6 are [−0.05, 0.25, 0.6, 0.25, −0.05], so negative lobes are real and the physical claim holds; I could not check the quoted wording or which word the caption uses. (4) ":72 "check that its gain is 4"" is ambiguous and I dropped it rather than report it: Σ(expand(g)) = 4Σ(g) is true, but a correct expand's amplitude/DC gain is 1 (feed a constant, get the constant), so an implementer who tests gain the obvious way sees 1 and could "restore" a spurious factor 4. I could not settle which the sentence means. (5) ":147 "machine precision at all three values (1.8e-15, 8.9e-16, 3.6e-15 m)"" — the paragraph names four values of a (0.5, 0.4, 0.3, 0.6), so which three the triple covers is not stated; 1.8e-15 matches the a = 0.4 figure at :63, which is the only consistency check available. (6) Wall times at :105 state no hardware. (7) The corner-wedge claim at :177 ("on a full-width ramp and on a corner wedge the same pairs all run negative") — my wedges ran positive at 256² (+34% clip, +11% plane, +18% cone), but "corner wedge" is not defined precisely enough to convict, so I did not report it; the ramp column, which is defined, reproduced exactly.

---

## terrain-analysis-masks.md — 4 raised, 0 refuted

`incomplete` 1  `stale` 1  `wrong` 2

### [TAM-1] `wrong` — /home/user/skills/gaia/references/terrain-analysis-masks.md:245

**Claim on the page.** "TPI is a band-pass at a chosen radius, and it degenerates to the Laplacian only as `r → 0`: `f − mean_ring_r(f) = (r²/4)·∇²f + O(r⁴)`."

**Failure scenario as reported.** The sign is inverted. For any smooth f, the mean over a circle of radius r is mean_ring_r(f) = f + (r²/4)∇²f + O(r⁴) (the linear term integrates to zero; ⟨x²⟩=⟨y²⟩=r²/2, ⟨xy⟩=0), so f − mean_ring_r(f) = −(r²/4)∇²f + O(r⁴). Concrete instance: a symmetric peak z = 100 − (x²+y²), ring radius r = 3. Ring mean = 100 − 9 = 91, so TPI = z(0,0) − 91 = +9 — positive, which is what TPI means (the cell stands above its neighbourhood). The document's identity gives (r²/4)·∇²z = (9/4)·(−4) = −9. Verified numerically at r = 0.1/0.2/0.5 with a 200k-point ring quadrature: TPI = +0.01/+0.04/+0.25 against the document's −0.01/−0.04/−0.25. This is not a convention question — line 115 defines this document's Laplacian node as (Z2+Z4+Z6+Z8−4·Z5)/L², the ordinary positive ∇², and line 112 uses that same ∇² in `profile + plan = ∇²z` (which I re-derived and confirmed: 2(D+E) = Zxx+Zyy). A reader who follows line 245 to substitute a Laplacian node for a small-radius TPI, or to port a TPI threshold onto the Laplacian node the document recommends two paragraphs earlier for "convexity", gets ridges classified as valleys — precisely the failure-table row at line 274 ("Curvature mask selects ridges where it should select valleys").

**Proposed fix, unverified.** Print the identity as `f − mean_ring_r(f) = −(r²/4)·∇²f + O(r⁴)` and change "degenerates to the Laplacian" to "degenerates to the *negated* Laplacian", so the r→0 limit still classifies a peak positive the way TPI does. If the intent is to keep the sentence sign-free, state it as |TPI| → (r²/4)|∇²f| with the sign opposite to ∇²f.

### [TAM-2] `wrong` — /home/user/skills/gaia/references/terrain-analysis-masks.md:176

**Claim on the page.** "**Hours is reachable, but only at a far heavier configuration**: `driver-fields.md` prices a sourced per-point DDA bake at 1–2 hours per tile on a GPU for 46 Mcell across **580** directions — about a hundred times the work of the 4k × 16 case above."

**Failure scenario as reported.** The sentence two lines earlier prices the 4k × 16 case at "4.5 to 45 minutes on one core at 10⁹ to 10⁸ samples/s" (2.684·10¹¹ samples — I reproduce both endpoints exactly: 268.4 s and 2684 s). At the slow end the document itself chooses, 10⁸ samples/s, that case is already 44.7 minutes, so reaching one hour needs 3.60·10¹¹ samples = 1.34× the work, not ~100×. Hold the grid, the step count and the machine fixed at 4096² × 1000 steps on one core at 10⁸ samples/s and raise only the direction count: N = 22 gives 1.03 h, and N = 32 — dozier2022's own timing-run direction count, recorded at driver-fields.md:190-191 as one of the two figures that *are* sourced — gives 5.369·10¹¹ samples = 1.49 h at 2.00× the work. Even reading "hours" generously as five hours needs only 6.7× (N ≈ 107). So the stated threshold for reaching hours is wrong by a factor of roughly 50, and the counterexample sits at a direction count the corpus already sources. Separately, the ~100× ratio itself (46·10⁶ × 580 / (4096² × 16) = 99.4, arithmetically correct) bridges a GPU job and a one-core CPU baseline with no rate for either, which is the exact error the next sentence forbids: "State the cell count, the direction count and the machine, or the figure means nothing."

**Proposed fix, unverified.** Replace "only at a far heavier configuration … about a hundred times the work" with the crossover the document's own arithmetic gives: at 10⁸ samples/s on one core, 4096² × 1000 steps crosses one hour at about 21 azimuths and two hours at about 43 — i.e. hours is reachable on the same 4k grid at 1.3–2.7× the 16-azimuth work, below dozier2022's own 32-azimuth run. Keep the driver-fields.md 46 Mcell × 580 figure as a separate, GPU-hardware data point and say so, rather than as the work multiple that makes hours possible.

### [TAM-3] `stale` — /home/user/skills/gaia/references/terrain-analysis-masks.md:155

**Claim on the page.** "Start at 2–5% of the domain extent, `N = 8–16` azimuths — the horizon varies smoothly."

**Failure scenario as reported.** driver-fields.md — a document this file names twice, at lines 167 and 178 — carries a correction pinned to this exact line that never landed here. driver-fields.md:192-195: "⚠️ **The 8–16 figure is this corpus's own practice, not a published one.** It is stated in `terrain-analysis-masks.md` without a citation … It is a reasonable default and it is graded as folklore, not borrowed authority — which matters because the crossover below leans on it." And driver-fields.md:150-151: "only the 580 is sourced. The 8–16 is this corpus's own practice line, uncited; see the direction-count note below before resting a budget on it." driver-fields.md:190-191 records that the direction counts any source in this corpus actually publishes are 32–64 for a view factor [dozier2022] (whose own timing run used 32) and 137 then 580 for [stendardo2020]. terrain-analysis-masks.md prints 8–16 bare, in a document that elsewhere flags every one of its uncited claims — line 37 ("no survey is being cited for that, and none exists here"), line 122 ("neither the block above nor any of them is canonical"), line 166 ("named rather than cited"). Concrete consequence: this uncited N is load-bearing here — line 175's "4.5 to 45 minutes" baseline and line 179's "about a hundred times the work" are both computed from N = 16 (see TAM-2, which is what happens when the budget rests on it). A reader who takes line 155 as grounded at face value, unlike a reader of driver-fields.md, budgets at 16 azimuths where the corpus's lowest sourced count is 32. The `2–5% of domain extent` in the same sentence, repeated in the failure table at line 275, has no source or measurement anywhere in the corpus either and carries no flag at all.

**Proposed fix, unverified.** Mark 8–16 as this corpus's own practice rather than a sourced figure, matching driver-fields.md:192-195, and name the sourced counts (32–64 [dozier2022], 137/580 [stendardo2020]) beside it. Give `2–5%` the same treatment at line 155 and in the failure-table row at line 275.

### [TAM-4] `incomplete` — /home/user/skills/gaia/references/terrain-analysis-masks.md:202

**Claim on the page.** "heightSel(h, lo, hi, w) = smoothstep(lo-w, lo+w, h) * (1 - smoothstep(hi-w, hi+w, h))", offered under "Soft edges, always" with no stated relation between `w` and `hi − lo`.

**Failure scenario as reported.** `lo` and `hi` are half-height points, not band edges, and the two smoothsteps overlap once hi − lo is not large against 2w — a regime the document never bounds. Transcribed and evaluated over 1.1M samples: lo=2000, hi=2100, w=100 peaks at **0.7119** (at h = 2050) and is nonzero from 1900 to 2200; lo=2000, hi=2050, w=100 peaks at **0.4673** and reads only 0.4219 at both authored edges. So an author who asks for a 100 m rock band at 2000–2100 m with a 100 m soft edge gets a mask that never exceeds 71% anywhere, and the priority stack at line 218 then leaks 29% bare base material through the middle of the band. The document's own prescribed guard cannot catch it: the assertion at line 219 is `Σ ≤ 1` at the fan-in and `Σ = 1` only "at the point where you close the stack by emitting the base as its own channel" — both pass, because the shortfall is legitimately absorbed as base, and line 219 already licenses that as "the shortfall is the bare base material". Even in the well-behaved case (lo=1800, hi=2200, w=100) the mask is 0.5 at both authored numbers and its support runs 1700–2300, so a threshold authored as "rock from 1800 to 2200" paints rock 100 m below and above. The corpus's own execution run recorded exactly this (registers/pseudocode-execution.tsv:255, run 2026-09-08: "`lo` and `hi` are half-height points, so a band authored as [lo, hi] passes material outside it and stops short inside it, by w at each end") and it was never written into the document.

**Proposed fix, unverified.** State beside the fence that `lo` and `hi` are half-height points and `w` a half-width — the mask reaches 0.5 at `lo` and `hi` and has support [lo−w, hi+w] — and that `hi − lo ≥ 2w` is required for the band to reach full coverage, since below it the mask peaks under 1 and the stack silently emits base material. A failure-table row ("a material band never reaches full coverage" / "2w wider than the band" / "widen the band or narrow w") would carry it to the reader who only reads the table.

**What its auditor could not settle.** SOURCES I COULD NOT OPEN. zevenbergen1987, horn1981, beven1979 and he2010 are all marked NOT OPENED in this file's own front matter and are paywalled; I did not reach them either. In particular I could not settle whether the profile/plan expressions at lines 83-84 match Zevenbergen & Thorne's own published SIGNS — the front matter concedes only that the equation NUMBERING is unchecked, while claiming the expressions "are the standard published form", and registers/source-findings.tsv:17 already carries this as OPEN. I re-derived the five coefficients independently and they are correct as an interpolation (D=Zxx/2, E=Zyy/2, F=Zxy, G=Zx, H=Zy at the window centre, all verified numerically against a 3x3 stencil), so the MATHEMATICS is sound whatever ZT printed. timonen2010 §3/§5 and bavoil2008 slides 12/16/23/24 were not re-opened; the pop condition h(v2)d(v1) >= h(v1)d(v2) and the sin h − sin t weighting were checked only for internal consistency against the front-matter locators. The weiss2001 poster (Fig. 2a/2b/2c/4a, the "inherently scale-dependent phenomenon" quote and the Yosemite illustration at lines 247-249) was not opened; 62 and 67 cells at 30 m do give 1.86 and 2.01 km, which reproduces.

NOT REPRODUCIBLE HERE. The spatialEco figure "+0.995" at line 111 needs R and the spatialEco package, neither available in this environment; this document's own "−1.000" on z = −0.05x² − 0.5y² at (1,0) I re-derived and it is exact. I could not check whether every mainstream GIS makes a diverging spur positive (line 106) beyond ArcGIS and GRASS — I confirmed both: the ArcMap Curvature (Spatial Analyst) page attaches "A positive value indicates the surface is upwardly convex at that cell" to PLAN curvature (so the quote at line 107 is correctly attributed, and my initial suspicion that it was the general-curvature sentence was wrong and is dropped), and GRASS r.slope.aspect states "Convex form values are positive and concave form values are negative" for both pcurvature and tcurvature — which also independently confirms line 121's claim that ArcGIS and GRASS disagree on the sign of PROFILE curvature. SAGA, Houdini, Whitebox and RichDEM were not checked.

CHECKED AND CLEAN, recorded so a later reader does not repeat it. Every printed number I could regenerate did regenerate: the worked example at line 124 (profile −0.002700, plan −0.037000 at L = 0.001, 0.1, 1.0 and 10.0, and the signs invert on the positive surface); profile + plan = ∇²z to 10 digits on a non-quadratic test surface; plan/√p = −1/r for any radially symmetric hill, derived in closed form; the Gaussian-hill figures, which the document does NOT parameterise but which I recovered — z = exp(−r²/2), r = 1.3 gives error ratios 3.979 / 3.995 / 3.999 over L = 0.2→0.025 and 2.36 / 3.50 / 4.70 significant digits at L = 0.4 / 0.1 / 0.025, matching lines 93 and 98 exactly; the cos² integral (analytically the correct cosine-weighted visibility, 1 at θ=0); the HBAO comparison (1−sin30 = 0.500 vs cos²30 = 0.750, 0.134 vs 0.250, and the maximum divergence is at exactly 30°, which is what "diverge most where terrain lives" claims); 4096²×16×1000 = 2.684·10¹¹ and 4.47/44.7 minutes; the 46 Mcell × 580 ratio of 99.4; the TWI tan bias (0.076 at 25°, 0.443 at 45°, printed as 0.08–0.44); the cylinder plan ≡ 0 and paraboloid profile = plan degeneracies at line 127; and the "Use this" section, the body and all 14 failure-table rows agree with each other, including the northness/dzdy pairing at line 270, which I re-derived in both conventions. The "exact to twelve digits on any quadratic surface" claim I spot-checked at L = 0.001 to 10 (exact to printed precision) but did not sweep for a worst case over all quadratics and all L. I also considered and DROPPED `max(closing(h,r) − h, 0)` at line 238 as a guard that cannot fail — grayscale closing is provably extensive for any flat structuring element containing the origin, so the clamp is never active (confirmed: min(closing − h) = 0.0 at r = 1, 3, 7 on random data) — but I cannot make it produce a wrong number or a wrong behaviour, so by this corpus's rules of evidence it is not a finding.

---

## tiled-streaming.md — 6 raised, 0 refuted

`incomplete` 2  `stale` 1  `unsupported` 2  `wrong` 1

### [TS-2] `wrong` — references/tiled-streaming.md:84

**Claim on the page.** ```
priority = sse_projected(tile)                  // error on screen NOW — the dominant term
         * frustumFactor                        // in-frustum 1.0; behind ~0.1, never 0
         * predictFactor(cameraVelocity, tile)  // extrapolate 1-2 s, take the max SSE
```

**Failure scenario as reported.** The block cannot be read consistently under either meaning of `predictFactor`, and one of the two readings inverts the queue order. (a) Literal reading, the one registers/pseudocode-execution.tsv:258 records ("prediction extrapolates 1-2 s and takes the max SSE"): `predictFactor` returns an SSE, so `priority` has units of px x px = px^2, and the two error terms multiply. Tile A is at sse 0.5 px now and reaches 40 px within the 1-2 s window (camera flying at it); tile B sits at 5 px now and 5 px later. priority(A) = 0.5*40 = 20, priority(B) = 5*5 = 25, so B is streamed first, although A is about to present an 80x larger error. Taking the max instead gives A = 40, B = 5 — the opposite order, and the order prediction exists to produce. (b) Dimensionless reading, `predictFactor` = sse_future/sse_now: then priority = sse_future * frustumFactor and `sse_projected` cancels algebraically, so it is not "the dominant term", it is not a term at all. Each reading falsifies one of the block's own two comments.

**Proposed fix, unverified.** Write the error term once and take the maximum over the prediction window rather than multiplying by it: `priority = max(sse_projected(tile), sse_predicted(tile, cameraVelocity, 1-2 s)) * frustumFactor`, and drop the "dominant term" comment, which is meaningless in a product where every factor has identical leverage.

### [TS-4] `unsupported` — references/tiled-streaming.md:114

**Claim on the page.** "**GPU decompression** — compressed tiles go disk -> GPU memory and inflate there, bypassing the CPU copy-and-inflate path [directstorage]", cited via the locator at :13 to "its Decompression section" of "the GDK DirectStorage overview".

**Failure scenario as reported.** I fetched the named page. Its first line reads "This article provides an overview of the DirectStorage APIs for Xbox Series X|S consoles only. Please see DirectStorage on Desktop for DirectStorage on Desktop details." Its Decompression sections describe FIXED-FUNCTION hardware, not the GPU: "Decompression is handled with dedicated hardware", with modes `ZLibDecompress` / `BCPack` / `Swizzle` writing into a title-supplied destination buffer, plus `SetStagingBufferSize` (default 32 MiB) for a system staging buffer. The word GPU appears on that page only in connection with `ID3D12Fence` notification. GPU decompression (GDeflate on compute) is a DirectStorage-on-Windows 1.1 feature documented elsewhere, and the cited page is dated 2022-08-01, before it shipped. Concretely: a reader follows the locator to implement "disk -> GPU memory and inflate there" plus the "CPU fallback ... two executors" of :116, finds no GPU-decompression API at the cited location and no CPU/GPU executor split (the console path has a dedicated silicon block with no CPU fallback to design around), and builds to the wrong API surface for the platform they are actually on.

**Proposed fix, unverified.** Split the locator: keep the GDK overview for Queue depth and Queue management, and point the Decompression half at the Windows/desktop DirectStorage documentation for GPU decompression (GDeflate) with its CPU fallback. Alternatively restate :114 as "hardware decompression" if the console page is the intended referent — but then :116's two-executor CPU fallback has no source.

### [TS-5] `unsupported` — references/tiled-streaming.md:38

**Claim on the page.** "with `e(child) <= e(parent)` enforced in the data — refinement oscillates otherwise."

**Failure scenario as reported.** The stated consequence does not follow from this document's own refine/coarsen rule at :94 ("Refine at `sse > tau`, coarsen at `sse < tau*h`" with h < 1). For any node the split predicate sse > tau and the merge predicate sse < tau*h are mutually exclusive whatever the error data holds, so no monotonicity violation can produce a split/merge cycle. Worked counterexample with a violation in place: e(P) = 4 m, e(C) = 9 m, K/d = 1 px/m, tau = 6 px, h = 0.8 (tau*h = 4.8). Split test on P: 4 > 6 false. Merge test on P: 4 < 4.8 true. The cut sits at P and never moves — stable, not oscillating — while C carries sse 9 px, 50% over the tolerance the whole scheme is refined against (:20, "the same screen-space error currency"). As the camera closes, P first splits at K/d = 3/2, where sse(C) = 13.5 px = 2.25 x tau, so the cut skips a level in one step. The real consequences of a violation are a silently non-conservative error bound and a two-level pop; oscillation is what the dead band at :94 prevents, and it prevents it with or without monotonicity.

**Proposed fix, unverified.** Replace "refinement oscillates otherwise" with the consequence that actually follows: the top-down cut stops at a node that passes tau while its descendants exceed it, so the screen-space error guarantee is not conservative, and refinement skips levels instead of stepping one level at a time.

### [TS-3] `stale` — references/tiled-streaming.md:112

**Claim on the page.** "**Async IO with real queue depth.** NVMe wants dozens of concurrent requests. A streamer with one outstanding read runs at HDD-era throughput on NVMe hardware [directstorage]." — against this document's own locator at :13, "Best Practices -> Queue management — submit every outstanding request, and size the queue at four times the per-frame request count".

**Failure scenario as reported.** I fetched the cited page (GDK DirectStorage Overview). Its Best Practices -> Queue management section says verbatim: "The recommendation with rotation disks was to have a queue size between 12 and 16" and "The recommendation for DirectStorage is to not buffer requests, but enqueue requests as soon as they're created. The entire system is a pipeline, and title buffering will create bubbles in the pipeline, which can seriously hurt performance", then "create a queue with a capacity at least four times the largest number of requests created per frame". So "dozens" is exactly the ROTATIONAL-disk figure the cited source retired, printed in a sentence whose second clause is about not running at HDD-era rates. The correct rule reached the front-matter locator and never reached the body. Concretely: an engineer implements "NVMe wants dozens" as a target depth and throttles the streamer to ~24 in flight — the "title buffering" the source names as a pipeline bubble. With this document's own per-frame figure of 200 tiles (:121) the cited rule gives a queue capacity of 800; 24 is 33x too small.

**Proposed fix, unverified.** Replace "NVMe wants dozens of concurrent requests" with the source's actual guidance: submit every outstanding request as soon as it is created and never throttle to a target depth; size the queue at four times the largest per-frame request count (800 at the 200-tiles-per-frame figure at :121). 12-16 is the rotational-disk number.

### [TS-1] `incomplete` — references/tiled-streaming.md:37

**Claim on the page.** "2. **`e(tile)` in the tile header**, computed at bake time [ulrich2002]" — against :58 "**requested** — queued, no IO issued", :84 "priority = sse_projected(tile)", :90 "Recompute priorities every frame for `requested` tiles", and :166 "Cold start: everything requested at once at equal priority | ... Prioritise by projected error".

**Failure scenario as reported.** The document places the per-tile baked error inside the tile's own header, i.e. inside the blob it also prescribes as "One blob per tile ... One IO request" (:118). But `sse_projected(tile)` needs `e(tile)` and the tile's bounding volume (heightfield-lod.md:71-74: rho = e*K/d), and the document requires that value to be recomputed every frame for `requested` tiles, which by its own state machine have had NO IO issued. Concretely: teleport into a cold region. Only the pinned root levels are resident (:76), so no candidate tile's header is in memory. `sse_projected` is unevaluable for every candidate, the priority product collapses to a constant across a level, and the streamer does exactly "everything requested at once at equal priority" — the failure at :166 that this very priority function is prescribed to fix. The prescription is inert in the regime it is prescribed for. Nothing in the document (grep for header/index/metadata/manifest/tree returns only :37, :39, :150) names a resident structure that could supply the error before the payload arrives; cozzi2011's own lineage puts geometricError and boundingVolume in a separately-fetched tileset index for precisely this reason.

**Proposed fix, unverified.** State in part 2 that the per-tile error and bounding volume are replicated in a small resident index (the quadtree/tileset metadata) loaded ahead of any tile payload, and that the copy in the tile header is a validation redundancy — the LOD and priority decisions read the index, never the blob.

### [TS-6] `incomplete` — references/tiled-streaming.md:104

**Claim on the page.** "The hybrid evicts the lowest priority *evictable* tile, breaking ties by last-used frame, and never touches tiles in the cut, pinned tiles, or tiles mid-upload" — against :141-142, "Morphing needs the parent's height at the child's vertices, which is one more reason parents stay resident."

**Failure scenario as reported.** Once four children are renderable and the parent is released from the cut (:73), the parent is by definition `evictable` — ":70 out of the cut, still in memory. This is the cache." It is not in the cut, it is not pinned (only "the root levels are pinned", :76), and it is not mid-upload, so the exclusion list gives it no protection. Under memory pressure the hybrid evicts it. Next frame a child inside the morph band ("the last 15-30% of each LOD range", :141) has no parent heights: its outer-band vertices no longer land on the parent's surface, so the cross-level edge contract restated at :139 breaks and a crack or a hard pop appears — the exact defect the morph exists to remove, and the failure table has no row for it. The document states the residency requirement in one section and supplies no mechanism for it anywhere: not in the exclusion list, not in the pinning rule, and not in the "one spreadsheet" of budget arithmetic at :44-45, which it changes by up to +25% (the parent level holds 1/4 as many tiles as the level below it, at the constant per-tile size mandated by :34).

**Proposed fix, unverified.** Add the parents of tiles in the cut to the eviction exclusion list at :104 (or pin the level immediately above the cut), and say in the crossover paragraph at :42-45 that the budget must carry that parent level — up to +25% of the drawn set's bytes at constant tile size.

**What its auditor could not settle.** SOURCES NOT OPENED. (1) losasso2004 §3 — the locator at :12 claims the section is titled \"Clipmap regions\" and that \"the render region is the hollowed frame between active_region of l and of l+1\", which additionally assumes higher level index = finer. hhoppe.com/geomclipmap.pdf fetched as 1.5 MB of binary with no extractable text layer, so neither the section title, the render-region definition, nor the index convention was verified. heightfield-lod.md:12 and papers-rendering.md:124 independently place toroidal access and nested rings at §3, which is consistent but is not the same claim. (2) cozzi2011 ch. 12 §12.1-12.4 (:9) — textbook, not opened here; the chapter title and section list match the corpus bibliography but no artefact was read. (3) ulrich2002 (:10) — SIGGRAPH course notes, not opened; whether the per-chunk error is stored with the chunk or in a separately loaded tree index is exactly the point TS-1 turns on, and I could not settle it from the source. (4) andersson2007 §5.2.4 / §5.4.1 (:14) — course talk, not opened, and papers-rendering.md:134 carries no section numbers to cross-check against, so those two § locators are unresolved. (5) strugar2009 15-30% (:11, :141) — not re-read from the whitepaper; taken from registers/source-findings.tsv:31, which records the verbatim sentence and the unnumbered-headings caveat, and it agrees with heightfield-lod.md:165-166 and papers-rendering.md:126.\n\nUNMEASURED BUDGET LINES, reported here rather than as findings because I could not make any of them fail. No measurement exists anywhere in the corpus for: the 8-32 MB/frame upload band at 60 Hz (:119; = 480 MB/s to 1.92 GB/s, internally consistent and conflicting with no sibling figure — greps for MB/frame, MB/s, GB/s and staging across all 47 reference documents return no competing number); the 200-tiles-in-one-frame canonical hitch (:121); the ~0.1 behind-camera frustum factor and the 1-2 s prediction window (:85-86); and the 15-30% band's application \"over the outer band of a tile\" (:140) rather than over a distance range, which is a category difference from CDLOD's per-vertex distance morph but which the same sentence then states correctly as \"each LOD range\", so it self-corrects. h ~ 0.7-0.85 (:95) agrees exactly with gpu-driven-culling.md:224. registers/pseudocode-execution.tsv:257-258 records both fenced blocks in this document as NOT RUN / NOT MEASURED, so there is no prior execution to compare against.\n\nALSO NOTED, NOT REPORTED: the section heading at :79 promises \"why FIFO fails\" and FIFO appears nowhere in the section, whose comparisons are pure LRU, pure priority, a fixed ring cut and zone load-on-cross. I judged this structural rather than a defect I can make fail, so it is not in findings."

---

## virtual-texturing.md — 3 raised, 0 refuted

`incomplete` 1  `stale` 1  `unsupported` 1

### [VT-3] `unsupported` — /home/user/skills/gaia/references/virtual-texturing.md:116

**Claim on the page.** "**And subtract its own downscale from the mip it asks for.** … Request `mip = computeMip(virtualUV) − log2(feedbackScale)`" — presented inside a bullet whose only citation is "[barrett2008]" (:114), with no provenance flag.

**Failure scenario as reported.** shader-craft.md:200 names this document as the owner of the number ("Both numbers are `virtual-texturing.md`'s and are restated here rather than re-derived"), and shader-craft.md:228 flags the same rule "⚠️ **Derived here, not sourced:** [mittring2008] §2.3.6.4 gives [barrett2008]'s tile-id pre-pass and its read-back latency, with no reduced-resolution buffer and no bias term, so it cannot carry this." registers/source-findings.tsv:33 independently confirms that §2.3.6.4 reading from the extracted chapter text. The flag landed on the restatement and not on the owner. A reader auditing the provenance of the corpus's own prescription opens virtual-texturing.md, sees the rule under the bullet's single [barrett2008] citation, follows it via papers-rendering.md:141, and finds no reduced-resolution buffer and no bias term — the exact misattribution source-findings.tsv:33 already recorded for the neighbouring mittring2008 feedback-pass citation. The omission is conspicuous because this same document flags its two other unsourced claims explicitly: the aniso cap at :77-79 ("stated here from practice, not from a source") and the scale constant at :86 ("derived here").

**Proposed fix, unverified.** Carry shader-craft.md:228's flag at the owning end: mark the bias term "derived here, not sourced" and state that [mittring2008] §2.3.6.4 and [barrett2008] give the tile-id pre-pass and its latency but neither a reduced-resolution feedback buffer nor a bias term.

### [VT-1] `stale` — /home/user/skills/gaia/references/virtual-texturing.md:152

**Claim on the page.** "**Height-based blending, not a linear weight lerp** [mishkinis2013]. Give each layer a height map, and let the more prominent material win the boundary with a contrast term" — under the section "What still has to be right inside the page", opened at :150 by "Caching a bad resolve caches it faithfully."

**Failure scenario as reported.** Correction X51 (registers/corrections.tsv, agent-m2m / agent-verify-m2m) established by exact quadrature that the recipe as [mishkinis2013] prints it — `b_i = w_i + h_i`, `m = max_i(b_i) − depth`, `w'_i = max(b_i − m, 0)` — gives a nonzero share to a material whose weight is exactly zero in 4.28% of texels at Σw = 1, depth 0.1 (7.63% at depth 0.2; 18.92%/26.49% at Σw = 0.5), max share 1.0000 (whole pixel). The fix — normalise first, then `b_i = w_i·(1 + h_i)` — landed on mask-to-material.md:39 only. virtual-texturing.md still prescribes the technique with [mishkinis2013] as its only pointer, no caveat, and no reference to mask-to-material.md anywhere in the file (the only sibling it names is shader-craft.md at :104), while mask-to-material.md:25 declares the two-way boundary and points here six times. Concretely: an implementer building the page compositor from :152 codes the printed form, and by this document's own premise at :150 and :161-163 the leak is box-filtered into the page's mip chain and cached — permanent, invisible in the near view, and the failure table has no row for a zero-weight material appearing in the composite.

**Proposed fix, unverified.** Add the caveat and the route in the bullet: the recipe as [mishkinis2013] prints it lets a zero-weight material win on height alone (4.28% of texels at Σw = 1, depth 0.1); normalise the weights first and scale the bias by them, `b_i = w_i·(1 + h_i)` — `mask-to-material.md` owns the form and the measurement.

### [VT-2] `incomplete` — /home/user/skills/gaia/references/virtual-texturing.md:172

**Claim on the page.** Failure-table row: "Permanent blur — **and the IO queue says which of the two causes it is**" … "**Thrash**: … IO saturated … **Or the feedback bias**: … IO quiet, eviction age long" … Fix: "Read the eviction-age histogram and the IO queue before growing anything. Saturated: grow the pool or bias mips … **Quiet: subtract `log2(feedbackScale)` from the requested mip**".

**Failure scenario as reported.** The document's own comparison table at :43-45 assigns runtime VT the page producer "GPU compositor" and the cost centre "Page render (the full material graph)", reserving "IO and transcode" to streaming VT — and "Use this" (:25, :36) makes runtime VT the default, crossing over to streaming only under a stated gate. So on the architecture this document prescribes there is no IO stage, and the named discriminator can only ever read "Quiet": it is a guard that cannot fail. A runtime-VT team with permanent blur from genuine thrash (working set exceeds the pool, GPU page-render queue saturated) reads the IO queue, finds it quiet because no IO path exists, follows the "Quiet" arm, and subtracts `log2(feedbackScale)` from the requested mip — which makes requests finer, multiplies the live working set by `feedbackScale²` (16× at quarter res, by this document's own :122 and :172 arithmetic) and worsens the thrash it was meant to diagnose. The "IO saturated" arm of the thrash branch is unreachable in the same regime. Row :171's "budget the transcode burst" is inert for the same reason.

**Proposed fix, unverified.** Name the discriminator per regime: under runtime VT it is the page-render queue and the compositor's per-frame page budget (the cost centre :45 assigns it), under streaming VT the IO/transcode queue. Or lead the row with eviction age — the only signal in it that discriminates in both regimes — and demote the IO queue to the streaming case.

**What its auditor could not settle.** PRIMARY SOURCES NOT OPENED. I made no network fetch, so the five source locators in the front matter were checked only against the corpus's own recorded extractions. mittring2008 §2.3.2.1/§2.3.2.2/§2.3.2.7/§2.3.6.4/§2.3.6.5 are corroborated by registers/source-findings.tsv:33, which quotes the §2.3.6.4 opening sentence verbatim from the fetched PDF and confirms the §2.3.x subsection tree — so I treated that locator as resolved. tanner1998's locator "§2, the nested toroidal clipmap stack" has no corpus record of anyone opening the 1998 paper (papers-rendering.md:139 gives no section number), so the section number is unverified. epicrvt is undated engine documentation the bibliography itself flags for per-release re-verification; :27-29 correctly restates that flag, but I cannot confirm the current Unreal docs still describe page composition and invalidation as claimed. mishkinis2013's "contrast term" wording — the article uses "depth"; mask-to-material.md:189 uses both — I could not settle against the article.

NUMBERS I COULD NOT FALSIFY, AND WHY. (a) :130-131 "A screen shows at most `screenPixels × overdraw × anisoSlack` unique texels; at 4K that is a few thousand pages of 128²". 3840×2160 = 8,294,400 px; /16384 = 506.25 pages at overdraw × anisoSlack = 1. Reaching "a few thousand" needs an unstated multiplier of roughly 4-10, which is ordinary for those two factors (and partial page coverage pushes the page count up further), so the figure reproduces under reasonable inputs and I dropped it. (b) :114-115 "Latency is 1-3 frames minimum" — no source, no measurement in registers/, but no failure I can construct. (c) :76 "clamp max aniso, typically 4-8×" — explicitly flagged in-document as unsourced; with the 4-texel DXT border prescribed two sentences earlier, 8× is at the edge of what the border supports, but the range is stated as practice and I could not make it fail.

ONE AMBIGUITY I COULD NOT SETTLE. :161-163 and row :175 prescribe premultiplied-space mip generation for the page composite ("Box-filtering weights and albedo independently averages through zero-weight texels"). That presupposes the page stores separable weight and albedo channels. The cache-boundary table at :58-61 says a page holds the "Static splat/ID resolve" — i.e. the resolved result — in which case there is no weight channel to premultiply and the prescription is inert. The document never states which layout a page uses, so I could not decide whether this is a correct rule or an inert one, and did not report it.

NOT RE-DERIVED FROM SCRATCH. mask-to-material.md's X51 leak percentages (4.28% / 18.92% / 7.63% / 26.49% by exact quadrature) are taken from registers/corrections.tsv as recorded, including the verifier's refutation of the implementer's own 5.1/19.3/8.7/27.2 figures. I did not re-run the quadrature; finding VT-1 does not depend on the exact percentage, only on the leak being nonzero and unflagged here.

---
