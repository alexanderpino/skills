# PROOFS FOR THE ASSERTIONS LISTED BELOW. Not "every assertion in check.py" -- an earlier
# version of this header claimed that and was false: seven assertions had no row at all.
# Claiming coverage you do not have is the defect this whole skill is built against, so the
# scope of this file is now stated as what it is.
#
# Re-executed 2026-09-02 against the CURRENT tree, in an isolated copy under /tmp holding only
# flow-routing.md, papers-flow.md, index.md and coverage.md -- so that other authors' in-flight
# documents could not colour a result. Every mutation was applied to a SUBJECT (a document, the
# bibliography, the coverage map or the index), never to the guard, and reverted afterwards.
#
# WHY THE PREVIOUS VERSION OF THIS FILE WAS WRONG, recorded rather than quietly replaced:
#   - it claimed "every assertion"; seven had no row (no bibliography file, missing `type`,
#     bad `status`, `okf_version` outside the index, a `sources` entry with no `id`, a declared
#     source never cited in the body, and the duplication check)
#   - one row's mutation did not produce the output recorded beside it (+250 filler lines was
#     said to give 602 lines; the document is 102 lines, so it gave 352 and did not trip the cap)
#   - rows recorded before a rename still named `papers.md`, a file that no longer exists
#   - the locator row claimed "a citation a reader cannot check" and proved only that a KEY was
#     present -- `locator: ""` passed
#   - the front-matter row claimed the subset was enforced, proved for inline maps only, while
#     inline lists carried the identical unfixed bug
# All of these were found by an adversarial review, not by me.
#
# ── RE-EXECUTED 2026-09-06, PHASE 0 ──────────────────────────────────────────────────────────
# Every row below carrying a NUMBER was re-run against the current tree in an isolated copy
# under /tmp, and the numbers replaced with what the run printed. What moved, and why it is
# recorded rather than quietly corrected:
#   - row `per-document line cap` said "+400 filler lines (918 total)" against a 411-line
#     document. Both halves were wrong: the mutation writes `'\nfiller\n'*400`, which is 800
#     lines, not 400, and 411 + 800 is 1211, not 918. The register was enshrining a number
#     nobody had re-run -- the same defect its own header records for the previous version.
#   - the coverage-target row's line number moved 77 -> 88 as coverage.md grew.
#   - every REPORTED metric moved with the corpus: locators 24/133 -> 170/241, unread
#     22/232 -> 26/256, approximation 4/36 -> 7/37, propagation 15/245 -> 16/256, reach
#     18/193 -> 17/193, recommendation-first 19/20 -> 36/37.
#   - "69 of 196 entries wrap onto a continuation line" was stale twice over; the measured
#     figure is 88 of 214, which is what check_propagation's docstring already said. Two
#     places in the guard disagreed about the same count and neither was re-run.
#   - requote's ENUMERATION was re-run and moved: 119 -> 120 checkable quotations, 68 -> 71
#     carrying no resolvable citation id, plus exclusion counts the tool did not print at all
#     when the row was written (394 spans under the 40-character floor, 0 code/URL spans, 425
#     lines inside fences). Its LOCATION SPLIT -- 6 located, 1 not found, 112 UNFETCHED --
#     could NOT be re-run: that needs the 42-PDF artefact cache, which this session does not
#     have. It is left with the date it was taken and marked NOT RE-RUN rather than
#     re-asserted. A number nobody re-measured is exactly what this block is fixing.
# Two rows were also DOWNGRADED to the literal mutation each covers -- see them below.
assertion	mutation	expected	observed	verdict
bibliography exists	delete papers-flow.md	red	references/papers*.md: no bibliography file exists	PROVEN
OKF `type` present	delete the `type:` line	red	references/flow-routing.md: no `type` -- the one always-required OKF key	PROVEN
`status` is a legal value	status: draft -> status: probably	red	references/flow-routing.md: status `probably` is not one of ['deprecated', 'draft', 'stable']	PROVEN
`okf_version` only in the bundle root	add okf_version to a document	red	references/flow-routing.md: `okf_version` belongs in the bundle root only	PROVEN
a source entry has an id	remove `id:` from one sources row	red	references/flow-routing.md: a `sources:` entry has no `id`	PROVEN
declared source is cited in the body	replace [planchon2002] in prose with plain text	red	references/flow-routing.md: `sources:` declares `planchon2002`, never cited in the body	PROVEN
a document cites SOMETHING	delete the whole `sources:` block	red	references/flow-routing.md: no `sources:`. A document that cites nothing cannot be checked at all	PROVEN -- this was the guard's largest hole: with no sources, NOTHING was checked and a document of pure invention passed
a body marker resolves	[barnes2014] -> [vonneumann1946]	red	references/flow-routing.md: body cites [vonneumann1946], which exists in NO bibliography. A citation to nothing is a fabrication, not a typo.	PROVEN -- previously guarded by `if mid in bib`, so a fabricated citation was silently DROPPED
declared tier matches the bibliography	freeman1991 tier: P -> tier: N	red	references/flow-routing.md: declares `freeman1991` as tier `N`, but papers grades it `P`	PROVEN -- the `tier:` field was decorative before this; it was never compared
locator is usable, not merely present	locator: "eq. 2, exponent p = 1.1" -> locator: ""	red	references/flow-routing.md: `freeman1991` has no usable `locator`	PROVEN
per-document line cap	append `'\nfiller\n'*400` to flow-routing.md -- 800 lines, not 400	red	references/flow-routing.md: 1211 lines, over the 450 cap -- it is two topics	PROVEN, re-run 2026-09-06. The previous version of this row said "400 filler lines (918 total)" against a 411-line document; the mutation writes 800 lines and 411+800 is 1211. Both halves were wrong and neither had been re-run. The cap is 450 for a document and 600 for a bibliography (BIB_MAX_LINES): a bibliography is a LIST, not a topic, so "it is two topics" does not apply to it -- it was exempt altogether by two conditions that could never fire, and papers-rendering.md reached 411 lines with no ceiling at all
index.md is generated, not written	append a fake row to index.md by hand	red	index.md is out of date. Run `python3 gaia/scripts/index.py`.	PROVEN -- and note this only fires because check.py now RUNS index.py --check; it was defined but never called, so the documented command went green on a stale index
coverage: every document is claimed	delete the flow-routing row from coverage.md	red	references/flow-routing.md exists but no coverage.md topic claims it -- the map is stale, or the document was never planned	PROVEN
coverage: a covered topic's file exists	add a `covered` row pointing at ghost.md	red	coverage.md:88: `ghost` points at ghost.md, which does not exist	PROVEN, re-run 2026-09-06 (the line moved 77 -> 88 as coverage.md grew). ADDING a row is the right mutation and RE-POINTING an existing one is not: re-pointing also unclaims a real document and breaks its `routes_to`, so three functions report it and the row stays red with check_coverage deleted -- which is how it was vacuous in CI
front matter: no nested inline map	a: { b: { c: d } }	Unparseable	nested map or list inside an inline map is outside the subset	PROVEN
front matter: no nested inline LIST	tags: [a, [b, c]]	Unparseable	nested list or map inside an inline list is outside the subset	PROVEN -- this bug survived the first fix, which patched maps and left the identical hole in lists; it yielded ['a', '[b', 'c]']
front matter: lists must be closed	tags: [a, b	Unparseable	`[a, b` is not a closed inline list	PROVEN -- previously ACCEPTED, silently dropping the last element
front matter: no duplicate keys	two `sources:` blocks	Unparseable	duplicate key `sources`; the first would be discarded	PROVEN -- previously the second silently won, REMOVING half a document's attribution
a U+2028 inside a quoted front-matter value is REJECTED	put a U+2028 inside a quoted value	Unparseable	front matter contains U+2028, a separator that is invisible in a diff but splits lines	PROVEN, and DOWNGRADED 2026-09-06 to what the mutation actually shows. It said "no invisible line separators" and "a quoted value could inject a key or close the front matter early" -- the first over-claims (five separators are rejected, this run exercised one), and the second is the MOTIVE, not the result: the mutation demonstrates rejection, never an injection. The injection is reachable in principle -- str.splitlines() breaks on U+2028 where a diff and a reader do not -- but nobody has built it, so it is stated as the reason for the rule and not as a proven exploit
DECOY reword prose	"Every drainage network" -> "Practically every drainage network"	green	exit 0	GREEN
DECOY background entry	append an entry marked [background]	green	exit 0	GREEN
DECOY reorder sources	swap two rows in sources:	green	exit 0	GREEN
DECOY CRLF line endings	rewrite a document with \r\n	green: real newlines only, CR stripped	parsed identically	GREEN
NOT PROVEN	duplication / merge-candidate check (check.py check_duplication)	-	no mutation written for it; it is unproven, not known-vacuous	OPEN
NOT PROVEN	`verified:` staleness via sources_digest	-	the mechanism is new and untested by mutation; a human has not yet stamped any document, so there is nothing to invalidate	OPEN
NOT PROVEN	locator QUALITY (check.py locator_quality)	-	reported as a metric, never enforced: 170/241 (71%) of the FOLLOWABLE locators name a section, equation or page, with a further 15 declaring `no artefact:` and excluded from the ratio (re-measured 2026-09-06; it read 24/133 (18%) when this row was written, and the grounding work since is most of the difference). The rest are topic paraphrases like "the fill algorithm", which is an entire paper. A reader cannot follow those, and the guard cannot tell them from "eq. 7". Enforcing the remaining 71 today would redden them at once and teach everyone to ignore the guard, so it is a visible ratio that has to go up.	OPEN
NOT PROVEN	cross-skill path resolution	-	four documents pointed at a phantom water-physics/ (since corrected to terrain-renderer/)... which does not exist on this branch. check.py resolves citation IDS, not filesystem paths outside gaia/, so a dangling provenance pointer is invisible to it. Being fixed in prose; the guard gap remains.	OPEN
#
# ── RE-EXECUTION, 2026-09-02, AFTER THE REVIEW WAVE ─────────────────────────────────────────
#
# The rows above were proven against a tree that has since changed substantially: four critic
# reviews and four fix agents rewrote parts of every document, and a guard proven against an
# old tree is not known to bite against the current one. Eight mutations were re-applied to a
# full copy of the skill under /tmp (never to the working tree, never to the guard itself),
# each reverted before the next:
#
#   M1  citation key in a body marker changed to a non-existent id
#         -> RED, and BIDIRECTIONALLY: "body cites [guardado2005], which exists in NO
#            bibliography" AND "`sources:` declares `guardado2004`, never cited in the body"
#   M2  every body marker for a declared source deleted (orphaning it)
#         -> RED: "`sources:` declares `bornwolf_optics`, never cited in the body"
#   M3  a locator emptied to ""
#         -> RED: "`shah2007` has no usable `locator`". This is the assertion an earlier
#            version of this register got WRONG -- it proved only that a KEY was present.
#   M4  400 filler lines appended
#         -> RED: "958 lines, over the 450 cap -- it is two topics"
#   M5  the generated index hand-edited (one heading)
#         -> RED via `index.py --check`, exit 1
#   M6  a `covered` topic re-pointed at a document that does not exist
#         -> RED, and BIDIRECTIONALLY: the dangling arrow AND the now-unclaimed document
#   M7  `status: finished`, not in the legal set
#         -> RED: "status `finished` is not one of ['deprecated', 'draft', 'stable']"
#   M8  `okf_version` added to a document that is not the bundle root
#         -> RED: "`okf_version` belongs in the bundle root only"
#
#   DECOY  prose reworded throughout one document, no citation touched (a heading rewritten
#          and every occurrence of "the bed" changed to "the pool floor")
#         -> GREEN, as required. A guard that fires on rewording would be untrustworthy in the
#            opposite direction, and this corpus is being reworded constantly.
#
# Eight of eight bite; the decoy does not. The OPEN rows above are unchanged -- in particular
# locator QUALITY (27/135 sharp) is still reported and not enforced, and M3 proves only that an
# EMPTY locator is caught, not that a topic-paraphrase locator is. Those are different claims
# and this register does not conflate them.
#
# ── NEW ASSERTION, 2026-09-02: check_recommendation() ───────────────────────────────────────
#
# This closes the last unimplemented item on the approved plan's verification list. The plan
# asked for it checked BY SHAPE -- "flag any document where a named alternative gets its own
# heading" -- and that version is unbuildable without false positives, because a real
# time-budget crossover legitimately gives alternatives their own space. A guard that cries
# wolf on the corpus's best documents is trained out of usefulness in a week, so it was not
# built that way, and this note records the refusal rather than leaving the item looking done.
#
# What is enforced is the other half of the same doctrine, which is unambiguous: a content
# document must NAME AN APPROACH TO IMPLEMENT, in a `## Use this` section. A document without
# one is a survey, and this skill's entire claim over a literature review is that it
# recommends.
#
#   MUTATION  `## Use this` renamed to `## Some notes` in tiled-streaming.md
#     -> RED: "no `## Use this` section -- it surveys rather than recommends, or the
#        recommendation is buried"
#   DECOY 1   a DIFFERENT heading renamed ("## How this fails..." -> "## Failure modes")
#     -> GREEN, as required
#   DECOY 2   the `## Use this` section MOVED to sit after a later section
#     -> GREEN, and the reported count fell 19 -> 18
#
# Decoy 2 is the deliberate design. Whether the recommendation comes FIRST is REPORTED, not
# enforced: caustics.md legitimately defines the phenomenon before recommending a tier, and
# hard-failing it would be the guard dictating prose order. But a corpus drifting toward
# explain-then-maybe-recommend is the slide into a survey, so the number is visible and has to
# be argued for rather than sliding quietly. 36 of 37 state it first (re-measured 2026-09-06;
# 19 of 20 when this block was written).
#
# All three run in CI on every push (.github/workflows/gaia.yml, the `bites` job), so this is
# a standing check rather than a one-afternoon proof like the rows above it were.
#
# ── A REPORTED METRIC THAT WAS ITSELF WRONG, 2026-09-02 ─────────────────────────────────────
#
# locator_quality() is REPORTED, not enforced, and it had no mutation row because there is no
# pass/fail to mutate. That is exactly how it went unchecked. Its pattern matched the BARE
# WORDS "equation", "section", "algorithm", "table", "figure", "page" and "chapter" -- so
# "the fill algorithm" and "the thin-elastic-plate equation", both pure topic paraphrases,
# counted as sharp. The metric measured vocabulary, not followability, and it was inflated by
# precisely the locators it existed to find.
#
# Found by an agent doing the locator work, not by this register: it noticed that a locator it
# had deliberately declined to sharpen was already being scored as sharp.
#
# The pattern now requires a designator -- a number after the word, or a section mark. A bare
# § still counts when it introduces a NAME ("§Computation of Fn(x)"), because a named section
# is followable where an English word is not. Checked both ways on a fixture set:
#
#   vague  "the fill algorithm"                            (was: SHARP)
#   vague  "the thin-elastic-plate equation"               (was: SHARP)
#   vague  "the area-slope channel-initiation threshold"
#   SHARP  "eq. 2, exponent p = 1.1"
#   SHARP  "§3, the 8-neighbour steepest-descent rule"
#   SHARP  "§Computation of Fn(x)"
#   SHARP  "p. 682"        SHARP  "Figure 2, p. 30"        SHARP  "§2.3 eq. 6"
#
# Reported ratio before the fix: 70/137 (51%). After: 62/137 (45%). The earlier 27/135 figure
# quoted throughout this register's history was overstated for the same reason.
#
# THE GENERAL LESSON, which is why this is written down rather than quietly patched: a metric
# that only ever prints a number is never seen to fail, so nothing forces it to be right. The
# enforced assertions above all have mutation rows because failing is what they do. Any future
# REPORTED number needs a fixture set like the one above, or it is decoration.
#
# ── NEW ASSERTION, 2026-09-02: check_no_artefact() ──────────────────────────────────────────
#
# The locator metric now has three categories, not two. Some citations rest on classical
# results, standard analysis, or a convention this repository recommends -- things with no
# citable artefact at all. Nine were named when this block was written; the count re-measured
# 2026-09-06 is 15 citations against 13 tagged entries (airy_coastal, bodytype_doctrine,
# braid_flow_split, capillary_gravity, constraint_timing, explicit_diffusion_limit,
# fluid_authority, iop_split, periodic_lattice_practice, seam_fake_practice,
# steadystate_discharge, strat_authoring, subcell_channel). Their locators can NEVER become
# precise, so counting them in the denominator made the metric look permanently unfinished and
# quietly implied 100% was the target. It is not: they are excluded, so the ceiling of the
# ratio AS NOW DEFINED is 241/241, and the figure that used to be quoted as the ceiling --
# 128/137 -- was the ceiling of the OLD denominator, which no longer exists.
#
# Those locators now declare it themselves, opening `no artefact:`, and are excluded from the
# ratio. THE AUTHOR declares it; no script infers it from bibliography prose.
#
# WHY THIS NEEDED A GUARD OF ITS OWN. An exclusion is a way to make a gap disappear. Paste
# `no artefact:` onto a real paper's locator and the denominator shrinks and the percentage
# RISES -- this is the only mutation in the suite that IMPROVES a reported number, which is
# precisely what makes it dangerous. So the claim is checked against the bibliography, where a
# source's nature is already recorded: the marker is only accepted if the entry itself says the
# source has no canonical, citable or external artefact. Two independent places must agree.
#
#   MUTATION  `no artefact:` prepended to braun2013's locator in stream-power.md
#     -> RED: "`braun2013` is marked `no artefact:`, which excludes it from the locator ratio,
#        but its bibliography entry does not say it lacks a canonical source. Either the marker
#        is wrong, or the entry needs to say so -- do not shrink the denominator by assertion"
#     -> and the number it was trying to buy: 68/128 (53%) became 68/127 (54%), with the
#        no-artefact count going 9 to 10. The guard is what stands between that and a green run.
#
# Seven fixtures were added to --selftest for the marker itself, including three that must NOT
# be swallowed by it -- two real locators that merely mention the word "artefact" in passing,
# and the bare paraphrase form the marker replaces. Letting any of those through would delete a
# genuine gap from the denominator silently. 23/23 locator fixtures and 7/7 marker fixtures
# (re-measured 2026-09-06; 20/20 when written -- three source-code locator fixtures were added).
#
# Runs in CI (.github/workflows/gaia.yml, `bites`).
#
# ── check_no_artefact() REWRITTEN, 2026-09-03: a reviewer broke the previous one ─────────────
#
# The version recorded above regex-matched the bibliography entry's PROSE for "no canonical
# source", and `bibliography()` only captures an entry's FIRST PHYSICAL LINE. A reviewing agent
# demonstrated the bypass: `nuke_request` has a real, openable artefact (Foundry's NDK docs) and
# says "No canonical paper" on its third line, so marking it `no artefact:` failed -- but move
# those four words onto line 1 and the identical edit passed GREEN, excluding a citable source
# from the locator denominator and moving the reported ratio 53% -> 54%.
#
# That is the only mutation in the suite that IMPROVES a reported number, which is what makes it
# worth attacking, and the old docstring's defence -- "two independent places have to agree" --
# was one author's prose checked against the same author's prose by a four-word regex.
#
# The exclusion is now a STRUCTURED DECLARATION, not an inference. `_ENTRY` gained an explicit
# `[no-artefact]` tag alongside the existing `[background]`, and the check runs BOTH ways:
#
#   MUTATION A  `no artefact:` prepended to braun2013's locator (entry not tagged)
#     -> RED: "marked `no artefact:` ... but its bibliography entry in papers-generation.md is
#        not tagged [no-artefact]"
#   MUTATION B  the reviewer's exact bypass -- "No canonical paper." moved to line 1 of
#               nuke_request's entry AND its locator opened with `no artefact:`
#     -> RED. Under the old implementation this passed green and bought 53% -> 54%.
#   MUTATION C  airy_coastal, which IS tagged [no-artefact], given a real locator in
#               wave-models.md ("§4 the dispersion relation, ...")
#     -> RED: "tagged [no-artefact] in papers-simulation.md, but this document gives it a
#        locator that does not open with `no artefact:`. A source cannot have no artefact here
#        and an openable one there"
#   DECOY     the legitimate no-artefact citations, untouched
#     -> GREEN. 9 exclusions and a ratio of 85/159 when this was written; 15 exclusions and
#        170/241 re-measured 2026-09-06.
#
# All three mutations run in CI (.github/workflows/gaia.yml, `bites`), which carries 33 mutation
# rows and 4 rows that must stay green (re-counted 2026-09-06; eleven and two when written).
#
# THE LESSON, which generalises past this check: a guard that reads PROSE is not checking a
# second source, it is re-reading the first one through a regex. Where a claim must be
# corroborated, corroborate it against a structured field somebody had to write on purpose.
#
# ── PHASE 0, 2026-09-06: THE SIX HOLES, AND `bites` REBUILT SO A ROW CANNOT LIE ──────────────
#
# Every row below was run in an isolated copy of the skill under /tmp. Each was proven in BOTH
# directions, which is the change that matters: red WITH the guard, and green with the one rule
# it names stubbed out. `red` on its own says only that something objected. Two rows in this
# register's CI job were red for a reason other than the guard they named, and both would have
# stayed red with that guard deleted:
#   - `illegal status value` was red because the generated index carries the status column and
#     went stale, not because `finished` is outside the vocabulary. Fixed by regenerating the
#     index inside the mutation, before the assertion.
#   - `coverage points at a document that does not exist` RE-POINTED an existing arrow, which
#     also unclaims a real document and orphans its `routes_to`, so three functions reported it.
#     Fixed by ADDING a row that points at ghost.md, which leaves exactly one rule to fire.
# Both were re-run under the new harness before the fix, and both were reported as vacuous by
# it -- that is the evidence the harness works, and it is why every row now carries the message
# it must match and the rule that must be stubbed.
the index stamp normaliser is anchored to ONE line	inject a `}`-free block between `at: ` and the closing `}` of index.md's generated stamp	`index.py --check` red	red -- "index.md is out of date". With the pre-fix pattern restored (`[^,]` and `[^}]`, which match NEWLINES) the same file reports "index.md is current", exit 0	PROVEN both ways. The unanchored pattern ran from `at: ` to the next `}` ANYWHERE IN THE FILE and collapsed everything between them to `<stamp>` on both sides, so an index announcing every document human-verified passed --check green with check.py green beside it. A forged `verified:` forges one document; this forged the verification state of the whole corpus in one edit
a front-matter fence closes only at column 0	insert an indented `  ---` as the last line inside caustics.md's front matter	Unparseable	red -- "<path>/references/caustics.md:14: indented `---` inside the front matter. A fence closes only at column 0, and this parser will not guess whether this was meant as text or as the close". With `rstrip()` returned to `strip()` and the pre-scan disabled, the run is GREEN and the front matter has silently closed early	PROVEN both ways. This is the shape the parser's own docstring promises never happens: keys written visibly inside the fences vanish into the body without a word. The subset does not guess which of the two readings the author meant, because the two readings put different keys in the front matter
a duplicate key inside an inline map raises	`{ id: shah2007, tier: F, tier: P }` in a `sources:` row	Unparseable	red -- "duplicate key `tier` in an inline map; the first would be discarded". With the check stubbed the row resolves to P, agrees with the bibliography's P, and the run is GREEN	PROVEN both ways, and the green half is the finding: the document graded the source F on the page, the parser read P, and the tier-agreement guard compared P against P and passed. `:177` already raised on a duplicate at top level; this is the same rule one level down, which is the sibling-fixed-alone shape okf.py already records for `_inline_list` against `_inline_map`
an unknown key in a `sources:` row is rejected	`tier:` -> `teir:` on one row	red	red -- "references/caustics.md: `shah2007` carries unknown `sources:` key(s) `teir`; only `id`, `locator`, `tier` are read". With the check stubbed the run is GREEN and the row is UNGRADED	PROVEN both ways. A misspelled `tier:` is worse than an absent one: the agreement check reads `if "tier" in s`, so the row loses its comparison while still LOOKING graded on the page. `Tier:`, `teir:` and `locater:` all passed in silence before this
an ABSENT `status:` is a failure, not a default	delete the `status: draft` line from caustics.md	red	red -- "no `status:` -- required, not defaulted". With `fm.get("status", "draft")` restored in check.py AND in index.py -- the repair the plan rejected -- the run is GREEN	PROVEN both ways, and the green half is exactly why the key is required rather than defaulted. Three sites used to disagree about a missing key: check.py validated it as `stable`, the "stable needs `verified:`" rule read None and never fired, and index.py PRINTED stable -- so deleting one line advertised a draft as human-checked, past both guards. Defaulting to `draft` fixes the display and makes the mutation a NO-OP: a draft with the line deleted is still a draft, so nothing could ever be seen going red
the failure table is STRUCTURE, not a heading spelling	rename `## How this fails, and what it looks like` to `## Failure modes` in tiled-streaming.md	red	red -- "0 heading(s) start `## How this fails` or `## When it fails`; exactly one is required"	PROVEN both ways. This used to be a DECOY that had to stay green, and it cannot be both: `covers_body` digests `## Use this` and the failure table, so a digest keyed on a heading spelling can be EMPTIED by renaming the heading, with nothing going red. The decoy is retired and re-pointed at `## The residency state machine`, an ordinary section, which stays green
a stamp scopes the CLAIMS, not only the citations	stamp caustics.md with valid `covers`/`covers_body`, then reword one sentence inside `## Use this`	red	red -- "`verified:` is stale: the CLAIMS changed -- `covers_body` says `2a662f982a25`, `## Use this` and the failure table now digest to `d9c85c69826e`. That happened after human:ci-fixture read it, so the verification no longer covers the document"	PROVEN both ways, with the stamp written by the fixture because NO document in the corpus carries one yet -- otherwise these six rules are unreachable by mutation, which is how an assertion defined and never invoked survives. Reproduced end to end before `covers_body` existed: stamp a document, replace EVERY prose sentence in its body with an invention, and the guard exits 0 while the index prints **checked**
a stamp scopes the CITATIONS as well	the same stamp, then re-point one locator	red	red -- "`verified:` is stale: the SOURCES changed"	PROVEN both ways. The message names WHICH HALF moved, which is the whole point: "the digest changed" sends a reader to re-read a document when all that happened was a new citation, or the reverse
a stamp must carry BOTH digests	delete `covers_body:` from the stamp, leaving `covers:`	red	red -- "`verified:` is missing `covers_body`. A stamp scopes to BOTH halves of what was read"	PROVEN both ways
`verified:` names a human, not the word `human:`	`by: "human: "` -- seven characters, naming nobody	red	red -- "`verified:` needs a named `human:<id>` actor. `human: ` names nobody". With the pre-fix rule restored (`len(who) > len("human:")`, which is 7 > 6) the run is GREEN	PROVEN both ways. What has to be non-empty is the ID, not the string
`verified:` present REQUIRES `status: stable`	drop a stamped document back to `status: draft`	red	red -- "`verified:` on a `draft` document. The index prints **checked** for any document carrying a stamp, so this advertises a verification the status line denies"	PROVEN both ways. Before this, a draft carrying a stamp read as **checked** in the index while its own status line said otherwise
the OKF conformance block runs on APPARATUS too	delete `type: Bibliography` from papers-flow.md	red	red -- "references/papers-flow.md: no `type` -- the one always-required OKF key"	PROVEN both ways. `if path in skip: continue` at the top of the loop exempted the bibliographies, the index and the coverage map from the FORMAT contract as well as from the citation checks, so a bibliography could lose its `type:`, take an illegal `status:`, grow an `okf_version:` or carry a `verified:` naming nobody with nothing looking. papers-flow.md is the subject because its tags name the same axis its `type:` does, so deleting the key does NOT restage the index -- every other bibliography would have gone red through a stale index instead, which is the vacuity this whole block is about. ⚠️ THE EXEMPTION IS NOW EXACTLY TWO THINGS, both about CLAIMS: the `sources:` requirement and the body-marker cross-check. Running the marker check over apparatus reddens a CLEAN corpus -- measured, one hit: papers-flow.md:146 ends the braun2013 entry with the literal `[background]`, which `_MARKER` reads as a citation to an id in no bibliography. The literal exemption the audit proposed would have turned the corpus red on prose about a tag
requote's `--selftest` actually calls `check()`	insert `return [], 0, 0, 0, 0, {}` at the top of `check()`	`--selftest` red	red -- the fixture corpus reports 0 MATCH, 0 ALTERED, 0 UNFETCHED against a want of 2/1/1	PROVEN. The corpus path used to be hard-coded inside `check()`, so `--selftest` exercised `normalise` and two string fixtures and never called the function at all: gutting it left the selftest green. A `docs=` parameter and a fixture corpus written to a tmpdir per run fixed it. The fixture is not committed to references/ on purpose -- a document nobody planned is a document check.py would correctly fail
"nothing checked" is not a pass	run requote against a cache holding only README.txt	non-zero exit	red -- "requote: FAIL -- a cache was given and NOT ONE quotation was located or refuted. Every checkable quotation was UNFETCHED or unresolvable, so this run establishes nothing about the corpus."	PROVEN both ways. The tool printed that sentence and returned 0, so its own prose contradicted the exit status CI reads. It also now exits non-zero on any ALTERED, and PRINTS the three exclusion counts -- spans under the 40-character floor, code and URL spans, and lines inside fences -- so the denominator the ratio is NOT taken over is on screen rather than silent
NOT PROVEN	the `bites` harness itself, in the direction that matters	-	the two-pass design was validated by re-running the two known-vacuous rows through it: both were reported as staying red with the guard they named stubbed. That is evidence the harness detects a vacuous row. It is NOT evidence that every row's stub is the ONLY rule that could catch its mutation -- a mutation caught by two rules the stub happens to cover would look identical from here.	OPEN
an ALTERED verdict exits non-zero	point --cache at a directory whose one artefact text does not contain the sentence bilodeau2014 is quoted for	non-zero exit	red -- "ALTERED references/atmosphere-and-aerial-perspective.md:12 [bilodeau2014]" twice, then "requote: FAIL -- 2 quotation(s) do not appear in their artefact.", exit 1. With `if altered:` stubbed to `if False:` the identical run exits 0	PROVEN both ways, 2026-09-06. ALTERED is the one verdict in this tool that is a defect on its own -- MATCH needs a human to read the continuation and UNFETCHED establishes nothing -- so it is the one that must reach CI's exit status. ⚠️ WHAT THIS DOES NOT SHOW: the two findings here are MANUFACTURED by a junk cache, not real misquotations. It proves the exit path, not that the corpus is clean; the corpus direction needs the artefact cache
the dead `note` key in check_propagation is REMOVED, not filled -- and the reason is a measurement, not an oversight	build it: populate `note` from each entry's continuation lines and compare `ref + note` against every citing locator	if the repair is right, more comparable pairs and no new false positives	BUILT AND RE-MEASURED 2026-09-06. Comparable pairs rise 16/256 -> 48/256 and it reports SEVEN corpus-wide hits, every one of them correct prose: mei2007's entry names §3 while three documents cite §3.2, §3.2.1, §3.2.2 and §4 (a PARENT section, read as disjoint by a token compare); stava2008's entry names §4 for the pipe cross-section while two documents cite §5, §7 and §8 for other claims; hillaire2020's entry names §7 for the cost figures while two documents cite §5.3. Seven for seven false	REJECTED, AND THE KEY DELETED RATHER THAN LEFT DEAD. `_scan` never built the key, so `f"{entry['ref']} {entry.get('note', '')}"` was always half empty string -- a dead expression that LOOKED like it read the whole entry. The choice recorded here is: delete the use, keep the reach at 16/256 deliberately, and say so in the docstring. It is the identical false-positive shape this register already records for the body-prose direction -- a document legitimately cites different sections of one paper for different claims, and an entry need not enumerate them all
the guard's error text names a file that EXISTS	cite an id absent from every bibliography	red, naming where to look	red -- "cites `nonexistent_paper`, absent from every references/papers*.md (papers-architecture.md, papers-coastal-and-ice.md, papers-flow.md, papers-generation.md, papers-masks-and-filtering.md, papers-rendering.md, papers-simulation.md)"	PROVEN. The message said "absent from papers.md" for a whole rename -- a file that has not existed since the bibliography was split into seven family files. This register's own header records rows naming `papers.md` as a known post-rename defect, so the register was corrected and the guard's error text was not, inside the tool that polices exactly that shape. The glob is now a single constant, `PAPERS_GLOB`, read by both `paper_files()` and the message, so a future split cannot stale it again
source-code line ranges count as a locator	score the lague_erosion locator, which names Erosion.cs plus four line ranges	SHARP -- a file and a line range is more followable than most section numbers	was scored VAGUE before the fix; SHARP after, and the corpus ratio moved 64% to 68% with two locators reclassified (lague_erosion and barnes2014's 'Algorithm 3 line 1')	PROVEN -- fixtures 'Erosion.cs lines 47-128' and 'line 124, the speed update' assert SHARP, and 'the droplet loop and brush weights in the published source' still asserts vague
unread sources are counted, not hidden	add and remove the NOT OPENED marker on a locator	the `unread` line moves by one	26/256 (10%) of citations declare the source was never opened (re-measured 2026-09-06; 22/232 when this row was written); --selftest asserts 6 fixtures, including that `no artefact:` and `Abstract only` are NOT counted as unread, and that a lower-case `not opened` counts	PROVEN -- but note what it is: it counts a DECLARATION, and its only guarantee is that a writer who declines to declare is making a claim in prose the guard will not repeat. It cannot detect an undeclared unread source, and nothing can
source-code line ranges stay a locator	replace the `\blines?\s+\d` alternative in LOCATOR_PRECISE with one that never matches	--selftest red	red -- the two source-code fixtures reclassify as vague	PROVEN, run locally before it was added to CI
the not-opened marker stays wired	replace LOCATOR_NOT_OPENED with a tuple that matches nothing	--selftest red	red -- the two NOT OPENED / NO LOCATOR fixtures stop counting as unread	PROVEN, run locally before it was added to CI
headings are structural, not decoration	re-inject the exact splice that shipped: swallow a section's heading into a sentence and leave the fragment as a `## ` line	red	red -- references/driver-fields.md:327: heading has an unbalanced backtick. The duplicate-name rule catches the same corruption independently	PROVEN against a copy of the tree. Every other check stayed green on the corrupted file: citations resolved, the cap held, front matter parsed, `## Use this` was still first. A document could be spliced in half and ship
a document's axis tag and its coverage section agree	re-tag sea-ice.md `generation` while coverage.md files it under `## Simulation`	red	red -- references/sea-ice.md:75: tagged `generation` ... but coverage.md files it under `simulation`. A second case, removing the axis tag entirely, is also caught	PROVEN against a copy. Three documents had drifted this way and NOTHING could see it: check_coverage matches topics to files and never reads the headings those topics sit under
`--check` sees a hand-written line that BEGINS `generated:`	insert `generated: FABRICATED -- 99 documents, all human-verified` under the index heading	red	red -- previously reported `index.md is current`, exit 0, because the staleness filter DELETED every line starting with that prefix from both sides instead of normalising the stamp	PROVEN, and DOWNGRADED 2026-09-06 to the one injection shape it covers. It was filed as "not blind to a hand-written `generated:` line", which reads as a general claim about the filter, and the general claim was FALSE while this row said PROVEN: normalising the stamp closed this shape and left a second one open, because `[^,]` and `[^}]` match NEWLINES, so the pattern ran from `at: ` to the next `}` anywhere in the file. An index whose heading was followed by a `}`-free block still reported `index.md is current`, exit 0. That needed the anchored pattern and a row of its own -- see `the index stamp normaliser is anchored to ONE line`. One row saying PROVEN over two exploits is how the second one survived
an apostrophe does not merge list items	parse `tags: [don't, generation, won't]`	three items	one item -- the apostrophe opened a quoted span and swallowed both commas; `[rock's, sand's, generation]` gave two items instead of three	PROVEN and FIXED. Only the double quote opens a span now. No document in the corpus currently has an apostrophe in a list, so this was latent -- and the first tag is the axis, so it would have silently refiled a document
check_not_opened actually runs	remove a `[not-opened]` tag while the citing locator still declares NOT OPENED	red	red	PROVEN. Gutting the function to `return []` did NOT prove this -- on a clean corpus a check that finds nothing is indistinguishable from a check that does nothing, and all three new guards passed that way
check_headings actually runs	duplicate `## Use this` at the same level in caustics.md	red	red	PROVEN, replacing an earlier gutting mutation that was vacuous for the same reason
check_axis_agreement actually runs	tag sea-ice.md `generation` while coverage.md files it under Simulation	red	red	PROVEN
a trailing [not-opened] is not swallowed	write the tag at the END of an entry, where the two sibling tags go	--selftest red if the regex drops it	two fixtures added; the regex now accepts both positions and the parser ORs them	PROVEN. 88 of 214 entries wrap onto a continuation line (re-measured 2026-09-06; the row said 69 of 196 and check_propagation's docstring said 88 of 214 -- two places in the guard disagreeing about one count, neither re-run), so for those the visual end of the entry is a line the opener regex never reads -- exactly where a hand would put the tag
approximation 7/37 documents (19%) state BOTH an error and a cost -- re-measured 2026-09-06; 4/36 when this row was written, and 10 give an error with no cost, 3 a cost with no error, 17 neither	NOT PROVEN	REPORTED METRIC, not a check. Counts documents whose body contains BOTH an error (a +/- figure, "within N%", "N% too low", "max relative error") and a cost (a number bound to ms, us, MB, GB, KB, TB, MiB, GiB, KiB, fps, "bytes per cell", or budget-tier language). An earlier version counted cost alone, which measured the wrong unit -- cost is half of a pair. Selftest carries 13 cost fixtures and 11 error fixtures. Both patterns require a NUMBER. The design decisions they pin: wall-clock prose ("about 2 minutes at 16 azimuths", "an e-folding time of 1.3 hours") must NOT count, because it is indistinguishable by pattern from a physical timescale -- coastal-erosion.md's 1.3 hours is a shoreline, not a bake; bare budget-tier phrases ("too expensive per frame", "synthesise triangles per frame", "choose a budget tier") must NOT count, because they are subject matter and not measurements; and on the error side a negation ("we did not compute an RMSE"), a bare acronym ("MAE") and a qualitative phrase ("near-constant relative error") must NOT count. THIS METRIC READ 10/36 FOR HALF A DAY BECAUSE THE COST PATTERN MATCHED THOSE BARE PHRASES -- six of the ten in the `both` bucket had no numeric cost at all. It is recorded here rather than quietly fixed, because a metric that counts prose is the exact failure it was built to detect.	It counts a UNIT, not a claim. A document could print "32 MB" in an unrelated aside and score; a document could give a careful asymptotic cost in cells and not. It is a floor on how many documents discuss cost at all. It also cannot tell a measured cost from a quoted one.	OPEN
propagation: both ends of a citation must name overlapping sections	PROVEN by CI mutation	Moves the fiorio1996 locator in mask-operators.md from section 1 to section 9 at the DOCUMENT end only, leaving the bibliography entry saying section 1. No other check in the file sees it: the id still resolves, the locator is still non-empty and still counts as sharp. Selftest carries 7 fixtures pinning the token extractor -- the seventh is the text of a WRAPPED entry's continuation line, which tokenises perfectly and is deliberately not read; it pins that the reach is a choice, not an inability to parse, including the one that decided its design -- "the paper `hnaidi2010` section 4.2 borrows from" must yield NOTHING, because that section belongs to a different paper named in the prose. Without that filter the guard's single corpus-wide hit was exactly that false positive.	IT SEES ONLY 6% OF CITATIONS. 16 of 256 pairs name a section at both ends (re-measured 2026-09-06; 15 of 245 when written); the other 240 name one at one end or neither and cannot be cross-checked at all. It reports that ratio for exactly this reason. It also only fires on DISJOINT sets -- overlap passes, because an entry legitimately describes more of a paper than any one document cites -- and it reads sections only, never page numbers.	PROVEN
# NEGATIVE RESULT, recorded rather than shipped: a body-prose direction for the propagation guard
# was built and measured and does NOT work. Comparing section tokens in body prose against the
# front-matter locator for the same id finds ~50 comparable pairs (against 15 for the
# frontmatter-vs-bibliography direction), but its only corpus-wide hit is a FALSE POSITIVE:
# noise-and-warping.md cites perlin2002 section 3 in its front matter for the quintic fade and
# section 2 in its body for where the second derivative is written down. Both are correct. A
# document legitimately cites different sections of one paper for different claims, and a locator
# need not enumerate them all, so an intersection test fires on correct prose.
#
# A second limitation, found the same way and worth stating because it defeated a REAL defect:
# terrain-analysis-masks.md's body attributed a claim to timonen2010 section 4 while its own front
# matter said "Section 4 ... carries neither claim". The guard could not see it, because the
# locator NAMES section 4 in order to exclude it, and an intersection test is satisfied by a
# mention. A locator containing a negative statement is invisible to this instrument. That case was
# found by a critic and fixed by hand.
a subject critic's self-marked CONFIRMED is not evidence; an adversarial verifier must re-derive each finding	ran one subject critic over terrain-analysis-masks.md, then a separate adversarial verifier briefed to DISPROVE each of its 16 findings against the artefacts	if critic confidence tracked correctness, the verifier would confirm all 16	Refuted: F1 (Timonen's real-time result does not refute a bake recommendation whose stated criterion is baseline length, and the paper's own section 9 proposes the 4k offline use); F2 as filed (its opening -- no accuracy figure anywhere in the file -- is false, and the same critic verifies three of those figures elsewhere in its own report; the COST half stands); F15; F16; and items 1 and 2 of F11. Two further findings, F3 and F4, were already fixed on disk before the critic finished, so were STALE rather than defects. Net: 9 of 16 filed findings were actionable as stated. Applying on the critic's word would have reversed a correct recommendation and deleted a true warning. The verifier also caught the critic UNDERSTATING one defect and getting one rendering analysis backwards while still reaching the right fix -- so a verifier that only says yes or no is not enough; it has to re-derive.	GUARD JUSTIFIED. 9 of 16 filed findings were actionable as stated. Applying on the critic's word would have reversed a correct recommendation and deleted a true warning. Verification is not optional at this refutation rate.
the 'content lives only in prose' defect -- found BY HAND in five consecutive documents -- can be caught by a lexical check on section headings	built check_section_reach(): for each ## section other than Use this and the failure table, test whether ANY content word of its heading appears in the union of those two. Then ran it against hydraulic-erosion.md at 634ca6f, the exact pre-fix version whose grain-classes section (31% of the body, carrying armouring, downstream fining, per-class capacity and per-class repose) reached neither end.	the motivating case is reported as unreachable	IT REPORTS NOTHING. The heading is 'Grain classes: one capacity, one talus angle, and why that is not enough', and the words 'capacity' and 'angle' both already appear in that document's failure table -- in rows belonging to the PIPE section. Weighting by corpus-wide document frequency does not rescue it: 'capacity' is rare across the corpus and still present in this document's own table. Tried both rules; both pass the defect.	GUARD DOES NOT HOLD FOR ITS MOTIVATING CASE, and is shipped saying so in its docstring and in its reported line. Lexical overlap cannot separate 'the table covers this section' from 'the table happens to use this word elsewhere' -- the distinction is semantic, so no stronger WORD rule fixes it; a different instrument would be needed. Kept for the strictly weaker condition it can see -- a section with no lexical contact with either end at all -- which flags 17 of 193 (re-measured 2026-09-06; 18 of 193 when written). NOTE: the first two cases cited as real when this row was written -- 'The three-media rule' and 'The 42 degree switch' -- were BOTH false positives, and fixing them took the count from 21 to 18. The first was covered by a failure row saying 'Three media attenuating the same path', missed because the hyphen made one token where the row has two; the second is named in Use this and three failure rows, missed because the distinctive term is a NUMBER and the pattern required a leading letter. The two examples reached for first as evidence the guard worked were both wrong, which is itself the measurement. What remains is mostly generic navigational headings (Submission, The ladder, Where this sits in the pipeline, The handoff) for which a failure row would be meaningless. It does NOT discharge the hand review, and this row exists so nobody rebuilds it and believes it works.
a correction written by the lead is about as likely to carry a new defect as the claim it replaces, and adversarial verification is what catches it	three lead corrections this session, each checked by a verifier reading the commit itself: the MFD underflow fix in flow-routing.md, the P-tier downgrade reasoning, and the cordonnier2016 locator propagation in papers-flow.md and flow-routing.md	that re-deriving a critic's finding before applying it is sufficient	IT IS NOT. (1) The MFD fix shipped 'exactly p >= 22, a property of the double, not of your terrain' -- generalised from a fixed eps that was not the document's own ulp-scaled algorithm, contradicting the same document 110 lines earlier; it also said water was DESTROYED after measuring only edge delivery, when auditing every terminus shows it conserved and stranded; and its one-line fix divided by zero on real pits. (2) The cordonnier2016 propagation DELETED A CORRECT LOCATOR: the retraction was about which section carries the ATTRIBUTION, and section 5 is still the right locator for the scheme and eq. 2, which stream-power.md says in the same passage the retraction sits in. It also asserted the artefact was unfetchable; that is true from this session -- three curls, 12507-byte bot challenge each time -- but a verifier fetched 10,771,411 bytes, so the claim was scoped too widely.	THE VERIFIER LAYER IS LOad-BEARING FOR THE LEAD, NOT ONLY FOR CRITICS. All three were caught by an adversarial verifier reading the commit, not by the lead re-reading it. The rate is roughly one defective correction per three applied, which is indistinguishable from the critic refutation rate. Corrections must be verified on the same terms as findings.
the corpus's worst recurring defect -- a quotation cut at the clause that reverses it -- can be caught mechanically	built gaia/scripts/requote.py: extract every quotation of 40+ chars with its citation id, locate it in the cached artefact, and print the artefact's NEXT 240 characters. Ran it over the whole corpus against a 42-PDF cache.	the tool flags cuts	IT DOES NOT FLAG THEM AND CANNOT. A cut quotation is PRESENT in its artefact -- that is what makes it a cut rather than a misquotation -- so no text search can distinguish it from an honest quotation. What the tool can do is SURFACE THE CONTINUATION so a reader sees what was dropped; the judgement stays human. Corpus enumeration re-run 2026-09-06: 120 checkable quotations, 71 carrying no resolvable citation id, and -- new, because the tool now prints what it drops -- 394 quoted spans under the 40-character floor, 0 code or URL spans, 425 lines inside fenced blocks. The LOCATION SPLIT is NOT re-run and keeps its 2026-09-03 date: 6 located, 1 not found, 112 UNFETCHED, about 5% of the corpus's quotations. It needs the 42-PDF artefact cache, which the 2026-09-06 session did not have, and a number nobody re-measured is what the header of this file exists to stop. What WAS re-run is the fixture suite, 16/16, which now includes a fixture CORPUS on disk: `check()` gained a `docs=` parameter so `--selftest` calls the function itself rather than only its helpers -- before that, gutting `check()` to `return [], 0, 0, 0, 0` left the selftest green, an assertion defined and never invoked. `main()` also exits non-zero when a cache is supplied and `match + altered == 0` (nothing checked is not a pass) and when anything is ALTERED, and it prints the three exclusion counts -- quotations under the 40-character floor, code and URL spans, and lines inside fences -- so the denominator the ratio is NOT taken over is on screen.	GUARD SHIPS WITH ITS LIMITS STATED, and it caught three defects in ITSELF before catching any in the corpus. (1) Front-matter locators are YAML double-quoted strings, so the first version read every LOCATOR as a verbatim quotation and reported 19 ALTERED -- a description read as a quotation, which is the same 'right content, wrong coordinate' shape the corpus records. (2) The apostrophe in a possessive opened a quotation: it matched "paper's own axis pair is DOMAIN and RANGE ... search it for" as quoted text. (3) pdfminer breaks words across column wraps -- barnes2014 extracts as 'works by inse rting' and 'flooding a nd' -- so two of its four surviving findings were the reader's artefact, not the corpus's defect; fixed with a whitespace-stripped fallback and an index map so continuations still slice from the original. Net real finding in the corpus: one ASCII rendering presented inside quote marks as verbatim (bavoil2008's W(r) = 1 - r squared), now marked a transcription. The 112 UNFETCHED is the honest headline: this instrument is bounded by artefact access, not by its logic.
a corpus that records its own errors will crowd its recommendations out with revision history	counted every warning block inside every  section, and separately those phrased as self-errata (an earlier version / used to read / an intermediate revision), across all 38 documents	self-errata have accumulated in the recommendation sections, where a reader goes for the answer	MEASURED, AND THE WORRY IS LOCALISED RATHER THAN SYSTEMIC. Fifteen documents carry a warning block inside ; THIRTEEN of them carry zero self-errata there. Only three instances exist corpus-wide: driver-fields.md (1, and it is the FIRST thing in the section) and surface-and-scale-space.md (2, both added by the lead today). Corpus-wide the self-erratum share of ALL warning blocks is modest -- worst is surface-and-scale-space at 4 of 13, most documents under 20%.	FINDING NARROWED FROM A PROCESS RISK TO THREE SITES. An architect reported 7 of 22 warning blocks in driver-fields.md as revision history; a strict pattern gives 3 of 22, so the count is contested, but its sharper claim -- that  OPENS with an erratum -- is confirmed for that file. Fixed the two in surface-and-scale-space by REORDERING rather than deleting: the actionable claim leads, the erratum trails in the same sentence. The record is kept, because recording its own errors is this corpus's doctrine; what changes is that a reader opening the recommendation section now reads a recommendation first. driver-fields.md left alone pending its verifier.
