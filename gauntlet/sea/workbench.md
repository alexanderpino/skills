# Gauntlet workbench

Updated 2026-08-20T20:23:23+00:00 — generated by `gauntlet.py board`. Do not edit by hand.

- Wave **15 of 26**
- Cost so far: ~34 subagent calls
- Target score: 9/10 (per-dimension: physics=9, prose=8, visual=9) | WIP limit: 3 lane(s)
- Next wave funds: bathymetry, wave-field, optics (waiting: chapter)

## Active

| lane / dimension | rounds | score | trend | open gap | last evidence |
|---|---|---|---|---|---|
| bathymetry / physics | 4 | 6/9 | score 8→6, flat | B10 FAILS ON THE BED THE RENDERER SHIPS. Fit log d on log s over the cleanest far-field band of run_bay()'s evolved bed (89 rows, rms log-residual 0.0018): Dean exponent 0.6113 +/- 0.0043 against the bar's 2/3 +/- 0.005 -- 11x the tolerance. plan_ramp ALONE returns 0.66667 with sd 0.00000 because it IS the analytic generator, so scoring B10 there is the degenerate row the key's own tolerance rule forbids. Cause: the max() join with the coastal loop's planed bench inside bay_bed, worth up to 0.35 m against the pure ramp in that band. | gauntlet/sea/bar/physics/answer-key.md |
| bathymetry / prose | 0 | — | too few bar rounds to read a trend | — | — |
| bathymetry / visual | 1 | 3/9 | too few bar rounds to read a trend | The land carries no relief, no material variation and no rock. High-frequency standard deviation 0.006/0.032/0.008 of 255 over a patch holding 51.10% of the frame -- exactly zero texture. No cliff, no headland, no wave-cut bench, no exposed rock, no offshore reef, against sections H1, J and L which require all of them. The renderer HAS a rock material gated on slope, but the bed in view never crosses the threshold so it never fires. Also: the wet/dry sand ladder runs BACKWARDS (seaward 194,182,169 brighter than landward 183,169,148) with no edge across 48 px, against H3 which calls that waterline one of the strongest tonal edges in the reference set. NOT CLOSEABLE IN THIS LANE: needs the bed generator to produce slopes crossing the rock threshold and relief above the waterline. | gauntlet/sea/evidence/s10-bathy-frame.png |
| chapter / physics | 1 | 8/9 | too few bar rounds to read a trend | The key's own largest hole, named by the scout rather than papered over: the cos^2s(theta/2) SPREADING PARAMETERISATION has no threshold. Mitsuyasu 1975 and Hasselmann 1980 are attribution-only, neither paper is in this container, so S4 checks the PRESENCE of alongshore structure and explicitly cannot fail a field that is short-crested by the wrong AMOUNT. Eight further items are marked OPEN rather than defaulted. | gauntlet/sea/bar/physics/answer-key.md |
| chapter / prose | 1 | 8/8 | too few bar rounds to read a trend | Six figures cover the two water chapters. THE OTHER TWENTY CHAPTERS STILL SHIP ZERO. The figure generator, the determinism harness and the --selftest convention now exist and are the expensive part; extending them to 01-11 and 13-19 is cheaper per figure than this round was, and the same measured gap applies there -- a reader of chapter 19 on fluid simulation still cannot see anything. Deliberately not chased this round under the prune ruling. | terrain-renderer/references/figures/trapped-series.png |
| chapter / visual | 0 | — | too few bar rounds to read a trend | — | — |
| optics / physics | 4 | 8/9 | score 8→8, flat [deciding track] | A2 UNMET, AND IT IS ONE LINE. atmosphere.solar_position computes air mass with no horizon guard: at the shipped site for 2026-08-10 22:30 WEST it returns elevation -20.872 deg and air mass nan, raising only a RuntimeWarning. The critic multiplied that into exp(-TAU_R*m) over a 4x4x3 radiance field: FULLY NaN, NOTHING RAISED. It survives in production only by luck -- SHOOT is a hard-coded daytime tuple. The key's requirement is exact: a path that can feed a below-horizon illuminant into an air-mass extinction must be shown to REJECT it, not to survive it. Fix: one rejection on el_app <= 0 inside solar_position. | gauntlet/sea/bar/physics/answer-key.md |
| optics / prose | 0 | — | too few bar rounds to read a trend | — | — |
| optics / visual | 1 | 3/9 | too few bar rounds to read a trend | The render draws the ENSEMBLE MEAN of the glint distribution and never the samples. The path clips at 255 across 144 px with an interior standard deviation of 1.0-2.6 grey levels, then falls 255->118 over 45 px with not one non-monotone step. A photographed path is granular at every scale: single facets clip and dark water stays between them. The SAME missing stochastic surface flattens the water body -- alongshore high-frequency sd 0.20-0.30 grey levels, a painted gradient rather than a sea. ONE FIX SERVES BOTH: give the surface a small-scale slope realisation, not only its Cox-Munk second moment. | gauntlet/sea/evidence/s5-glitter-render.png |
| wave-field / physics | 4 | 7/9 | score 8→7, flat [deciding track] | SECTION S HAS A REAL IMPLEMENTATION AND ZERO GUARDS, and several beach.py docstrings ASSERT THE OPPOSITE -- 'the suite checks that by quadrature at eight values of s', 'the suite measures both numbers at five lattice sizes'. validate.py and validate_beach.py contain ZERO occurrences of any section S function. Those claims are false on this tree. Separately: section K is not implemented at all (7 ERROR rows, no Kelvin wake in the tree), and section D is verified but NEVER RENDERED -- beach_render.py does not import beach_diffract, so 0 pixels of either hero frame carry a Sommerfeld edge. | gauntlet/sea/bar/physics/answer-key.md |
| wave-field / prose | 0 | — | too few bar rounds to read a trend | — | — |
| wave-field / visual | 1 | 3/9 | too few bar rounds to read a trend | The surf zone is a luminance ramp, not foam. One continuous soft grey band; the cross-shore profile is a single smooth hump (111->166 DN over 18 rows, then flat sand). No second maximum, no bubble texture, no bright breaking line, no alongshore break-up. Section C requires three visually different whites; the render shows one soft grey. Both edges are smooth curves with zero cusp, scallop or foam-finger structure at any scale -- the bar's overriding rule says the tell is usually the foam edge, and here it is an airbrush gradient. NOT CLOSEABLE IN THIS LANE: the surf-line COUNT needs a bar system in the depth field, below this lane. | gauntlet/sea/evidence/s10-wavefield-frame-J.png |

## Parked (unfunded, gap still open)

_none_

## Retired

_none_

## Recent rounds

| wave.round | lane / dim | mode | winner | margin | score | note |
|---|---|---|---|---|---|---|
| 11.1 | optics / visual | rubric | other | decisive | 3 | The render draws the ENSEMBLE MEAN of the glint distribution and never the samples. The path clips at 255 across 144 px with an interior standard deviation of 1.0-2.6 grey levels, then falls 255->118 over 45 px with not one non-monotone step. A photographed path is granular at every scale: single facets clip and dark water stays between them. The SAME missing stochastic surface flattens the water body -- alongshore high-frequency sd 0.20-0.30 grey levels, a painted gradient rather than a sea. ONE FIX SERVES BOTH: give the surface a small-scale slope realisation, not only its Cox-Munk second moment. |
| 12.1 | bathymetry / physics | rubric | ours | clear | 8 | The 45.4% coastal plateau is still one declared albedo and its high-frequency sd did not move. It is a missing PROCESS, not a constant -- no weathering coefficient reaches it at slopes ~1e-4. It is an emerged MARINE TERRACE, which is terrain-architect chapter 12's own sea-level-history loop. Named and measured, not built. It is the largest single thing in frame. |
| 12.2 | optics / physics | rubric | ours | clear | 8 | The builder landed the slope realisation (cb20a6d) and died on a session limit mid-diagnosis of the K-framing sea/sky seam, where the sea reads 20% brighter than the sky it reflects at grazing while the J-framings close to 2-4 levels. Unfinished, not failed: the seam diagnosis and the clip fraction carry into the next round. |
| 12.2 | wave-field / physics | rubric | ours | clear | 8 | The builder landed two foam defects (ee3c17b) and died on a session limit before reaching the DIRECTIONAL SPECTRUM. The sea is still a 1-D profile extruded alongshore -- alongshore residual 5.2 DN against 31.7 DN across the crests, a ratio of 0.16, which the critic called corrugated roofing. If the file assumes zero directional spread, that is the whole finding. |
| 13.3 | optics / physics | rubric | ours | decisive | 8 | No s13 visual evidence exists: the builder died on a session limit before the figure step, so this round's proof is in the commit and the suite and NOT in a picture. The seam fix is verified scene-linear (+19.8% -> -2.0%, horizon_seam 0.1481 -> 0.0113) but nobody has looked at the horizon at 1:1. Owner ruling stands: every wave produces visual evidence. |
| 13.3 | wave-field / physics | rubric | ours | decisive | 8 | The builder reported THREE REAL FAILURES IN ITS OWN NEW ROWS and died mid-diagnosis. Whatever landed is green (413 pass / 0 FAIL verified in one foreground run) but the three failures were never explained, and an unexplained failure that stopped failing is the worst kind. Re-open it: find what those rows were asserting and why they failed. Also unreached: the directional spreading itself, since the round was spent on the phase field's missing alongshore half. |
| 13.2 | bathymetry / physics | rubric | ours | clear | 8 | The plateau is now an output of a sea-level history -- tread at 14.154 m, alongshore level sd 0.017 m, an old cliff line at x = 836 m -- but NOBODY HAS SEEN IT. No s13 figure exists and the plateau's high-frequency sd, the number the critic scored 3/10 on, was never re-measured after the change. A terrace that is correct in the elevation histogram and still one flat albedo in the frame has closed the physics gap and not the visual one. |
| 14.1 | chapter / physics | rubric | ours | clear | 8 | The key's own largest hole, named by the scout rather than papered over: the cos^2s(theta/2) SPREADING PARAMETERISATION has no threshold. Mitsuyasu 1975 and Hasselmann 1980 are attribution-only, neither paper is in this container, so S4 checks the PRESENCE of alongshore structure and explicitly cannot fail a field that is short-crested by the wrong AMOUNT. Eight further items are marked OPEN rather than defaulted. |
| 14.2 | chapter / prose | rubric | ours | clear | 8 | Six figures cover the two water chapters. THE OTHER TWENTY CHAPTERS STILL SHIP ZERO. The figure generator, the determinism harness and the --selftest convention now exist and are the expensive part; extending them to 01-11 and 13-19 is cheaper per figure than this round was, and the same measured gap applies there -- a reader of chapter 19 on fluid simulation still cannot see anything. Deliberately not chased this round under the prune ruling. |
| 15.3 | bathymetry / physics | rubric | other | clear | 6 | B10 FAILS ON THE BED THE RENDERER SHIPS. Fit log d on log s over the cleanest far-field band of run_bay()'s evolved bed (89 rows, rms log-residual 0.0018): Dean exponent 0.6113 +/- 0.0043 against the bar's 2/3 +/- 0.005 -- 11x the tolerance. plan_ramp ALONE returns 0.66667 with sd 0.00000 because it IS the analytic generator, so scoring B10 there is the degenerate row the key's own tolerance rule forbids. Cause: the max() join with the coastal loop's planed bench inside bay_bed, worth up to 0.35 m against the pure ramp in that band. |
| 15.4 | wave-field / physics | rubric | other | clear | 7 | SECTION S HAS A REAL IMPLEMENTATION AND ZERO GUARDS, and several beach.py docstrings ASSERT THE OPPOSITE -- 'the suite checks that by quadrature at eight values of s', 'the suite measures both numbers at five lattice sizes'. validate.py and validate_beach.py contain ZERO occurrences of any section S function. Those claims are false on this tree. Separately: section K is not implemented at all (7 ERROR rows, no Kelvin wake in the tree), and section D is verified but NEVER RENDERED -- beach_render.py does not import beach_diffract, so 0 pixels of either hero frame carry a Sommerfeld edge. |
| 15.4 | optics / physics | rubric | other | thin | 8 | A2 UNMET, AND IT IS ONE LINE. atmosphere.solar_position computes air mass with no horizon guard: at the shipped site for 2026-08-10 22:30 WEST it returns elevation -20.872 deg and air mass nan, raising only a RuntimeWarning. The critic multiplied that into exp(-TAU_R*m) over a 4x4x3 radiance field: FULLY NaN, NOTHING RAISED. It survives in production only by luck -- SHOOT is a hard-coded daytime tuple. The key's requirement is exact: a path that can feed a below-horizon illuminant into an air-mass extinction must be shown to REJECT it, not to survive it. Fix: one rejection on el_app <= 0 inside solar_position. |
